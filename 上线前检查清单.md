# 上线前检查清单

> 依据：项目全阶段交付总结报告 · 第六节 遗留事项与建议
> 适用：竹香酒全站生产部署（Docker Compose 形态）
> 用法：逐项执行并在 [ ] 内打勾，全部完成后方可上线
> **执行进度**：2026-08-30 本地冒烟已执行第一、二节（strict 认证链 + LLM 轨真实生效 + 降级兜底），第三节 Docker 联调因本地沙箱限制未执行（CI 已兜底，上线前需人工跑通）

---

## 一、认证模式切换（AUTH_MODE=strict）

> 背景：开发期默认 `compat` 兼容模式（X-Role/X-Member-Id 旧头仍可用），生产必须封死旧通道，JWT 为唯一合法凭证。

- [x] 1.1 生产 `.env` 中显式设置 `AUTH_MODE=strict`（确认无遗漏，docker-compose 经 env_file 注入生效）✅ 2026-08-30 本地以 strict 模式启动验证
- [x] 1.2 冒烟验证未登录拦截：`curl -i http://localhost:8000/api/knowledge/entries` 预期 **401**（无头拒绝）✅ 实测 401
- [x] 1.3 冒烟验证旧兼容头失效：带 `X-Role: admin` 头访问管理端点，预期 **401**（strict 下兼容头不再放行）✅ 实测 401
- [x] 1.4 登录页可用：浏览器打开 `login.html`，会员/管理员双 Tab 登录均成功换取 token ✅ API 实测：13800000001(member)/13800000002(admin) 均 200 换 JWT 双令牌
- [x] 1.5 Bearer 全链路：登录后访问五个管理页（knowledge-dashboard / knowledge-sources / ai-learning-dashboard / ai-learning-detail / ai-learning-report），数据加载正常无 401 ✅ API 实测：admin Bearer 访问 /api/knowledge/entries 200；member token 访问返回 403（角色检查生效，鉴权链路通）
- [x] 1.6 伪造 token 拒绝：篡改 token 任意字符访问受保护端点，预期 **401**（签名校验生效）✅ 实测 401
- [ ] 1.7 转人工跳转安全：登录页 redirect 参数回跳正常，`javascript:` 伪协议 / `//host` / 协议前缀均被拒绝（API 级已验证，浏览器 UI 层上线前人工点验一次）

**回归基线**：本地已过 strict 模式 7 项测试（无头 401 / 仅 X-Role 401 / 登录白名单 / Bearer 200 / 伪造 401 / auth.js 衔接 / 防跳转），上线抽验 1.2-1.5 即可。

---

## 二、LLM 轨生产配置（默认 off）

> 背景：RAG 合成 / 多模态理解 / 抓取清洗 / Embedding / Rerank 五轨依赖智谱 API，未配置时自动降级 rule 轨（功能可用但非智能形态）。生产需显式启用。

- [x] 2.1 生产 `.env` 配置 `LLM_API_KEY=<智谱Key>`（经 env_file 注入，不落代码库；`.gitignore` 已覆盖 `.env`）✅ Key 已配置于用户级环境变量（生产 .env 待部署时写入）
- [x] 2.2 设置 `LLM_ENABLED=true` 启用总开关 ✅ 实测 on
- [ ] 2.3 按需开启各轨（默认均 off，逐项决策）：
  - [ ] `KNOWLEDGE_EMBEDDING=on`（语义向量检索）
  - [ ] `KNOWLEDGE_MEDIA_LLM=on`（GLM-4V 图片/视频理解）
  - [ ] `KNOWLEDGE_CRAWL_LLM=on`（抓取智能清洗）
  - [ ] `KNOWLEDGE_RERANK=on`（检索重排）
  - [x] `KNOWLEDGE_CHAT_LLM`（chat 合成轨，按需）✅ 实测 on
- [x] 2.4 启动日志确认：容器启动时 `llm_client.log_feature_status()` 输出 key 配置状态/总开关/各轨生效或回退，核对无静默降级（密钥不输出，只看状态行）✅ 实测输出完整状态面板：key 已配置/总开关 on/RAG 合成 on/其余四轨 off(回退 rule)
- [x] 2.5 冒烟验证 LLM 轨真实生效：`POST /api/knowledge/ask` 带 `"provider": "llm"`，返回自然语言答案 + `[n]` 引用标注（非模板融合文本）✅ 实测（seed-brand 播种 3 条后）：mode=synthesized，自然语言合成答案带 [1] 标注 + citations 溯源（entryId/similarity=0.2917/source）+ 动态置信度
- [x] 2.6 验证降级兜底：临时改错 `LLM_API_KEY` 重启，确认 ask 自动回退 rule 轨仍可用（不 500）✅ 实测：错误 Key 下 HTTP 200，answer 自动回退 rule 轨模板文本（"关于「…」, 为您整理以下信息…"），引用与置信度正常

**成本提示**：五轨开启后按智谱计费，建议先开 Embedding + Chat，观察后再开其余三轨。

---

## 三、Docker 容器本地联调（CI 兜底 → 亲测）

> 背景：开发期本地 Docker Desktop 未运行，容器内行为一直由 CI docker job 兜底验证。上线前必须本地完整跑一次。
> **2026-08-30 执行情况**：沙箱环境限制了 Docker Desktop 日志文件写入（AppData\Local\Docker\log），daemon 无法在助手沙箱内启动（两次尝试共等待 3 分钟+均失败）。CI docker-build job 已绿兜底；以下 8 项需在用户终端手动执行（预计 Docker Desktop 正常启动后逐项跑通）。

- [ ] 3.1 启动 Docker Desktop，确认 WSL2 内存配置 8GB 生效（`C:\Users\ccvcc\.wslconfig`，UTF-8 BOM + CRLF）
- [ ] 3.2 `docker compose config` 校验配置合法（env_file 缺失不阻断：required: false）
- [ ] 3.3 `docker compose up -d` 启动全栈，等待 healthcheck 通过：
  - [ ] backend 健康探测 `curl -f http://localhost:8000/api/decision/health` 返回 200（interval=10s / timeout=5s / retries=5 / start_period=30s）
  - [ ] Redis 容器 ping 正常
- [ ] 3.4 容器内执行 seed：`docker-compose exec backend py scripts/seed_redis.py`，确认供应链 19 域 + 普通 + 财务域全部写入（verify 输出无缺项）
- [ ] 3.5 容器内存监控：观察 backend 容器内存稳定在限额内（mem_limit=1g / memswap=2g / reservation=512m，uvicorn 单 worker + no-access-log）
- [ ] 3.6 冒烟一条完整业务链：login 换 JWT → checkout 下单（库存扣减 + 订单落库 + 分润）→ agent-shipping 认领/结算，Redis 模式下数据一致
- [ ] 3.7 Prometheus 抓取验证：`GET /metrics` 返回 Prometheus 文本格式（HELP/TYPE 完整），接入既有监控栈
- [ ] 3.8 重启持久化验证：`docker compose restart` 后数据仍在（Redis 持久化生效）

---

## 四、Git 推送通道（低优先级，运维项）

> 背景：本地 SSH key（id_ed25519，2026-08-22 重生成）公钥未注册 GitHub，origin 已永久固定 HTTPS + Classic PAT。当前通道可用，仅影响后续运维习惯。

- [x] 4.1 确认当前 PAT 未过期且具备 repo contents 读写权限（`git push` 冒烟一次）✅ 2026-08-30 多次推送成功（最近 d8fcb52/4a31685）
- [ ] 4.2 （可选）如需切换 SSH：将 `~/.ssh/id_ed25519.pub` 注册到 GitHub 账号后，`git remote set-url origin git@github.com:ccvccp/zhuxiang-jiu.git`
- [ ] 4.3 （可选）PAT 到期前在 GitHub Settings → Developer settings → Tokens 轮换，轮换后同步更新本地 credential manager

---

## 五、上线后观察项（附）

- [ ] `/metrics` 中 LLM 调用 error 计数是否持续增长（智谱服务异常感知）
- [ ] RAG 缓存命中率（layer=embedding/result × hit=yes/no）是否合理（过低考虑调 TTL）
- [ ] 缺口队列（gap queue）增长速度——过快说明知识覆盖不足，触发教学/抓取补充
- [ ] 质量调度器 6 小时周期运行日志正常（淘汰/自动过审无异常堆积）

---

**门禁标准**：第一~三节全部勾选完成方可上线；第四节为运维可选项；第五节为上线后一周内每日巡检。
