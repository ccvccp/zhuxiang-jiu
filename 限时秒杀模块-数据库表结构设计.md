# 竹奕酒官网 · 限时秒杀模块数据库表结构设计

> 版本: v1.0 | 日期: 2026-08-22 | 分支: feature/flash-sale
> 架构: 4 层(core/repositories/services/routes) | 双模式存储(内存/Redis) | 锁: core.locks.get_lock

## 一、模块概述

限时限量特价酒抢购:管理端创建场次→添加秒杀商品→发布,用户端在场次进行中抢购下单。
核心保障:库存扣减防超卖、每人限购、待支付订单幂等、取消/超时回补库存、防刷(注册时长/等级)。

## 二、表结构

### 1. flash_sessions 秒杀场次

| 字段 | 类型 | 说明 |
|---|---|---|
| sessionId | string PK | 场次号 FS+日期+序号(如 FS20260822001) |
| name | string | 场次名称(如"晚 8 点整点秒杀") |
| startTime | string | 开始时间 ISO8601(UTC) |
| endTime | string | 结束时间 ISO8601(UTC) |
| status | string | 存储状态: draft(草稿)/published(已发布)/cancelled(已取消) |
| itemOrder | int | 商品数量(冗余计数) |
| createdBy | string | 创建人(admin) |
| createdAt / updatedAt | string | 时间戳 |

**运行时状态(不落库,查询时由时间推导)**: `not_started`(未开始)/`in_progress`(进行中)/`ended`(已结束)。
推导规则: now < startTime → not_started; now > endTime → ended; 其间 → in_progress。draft/cancelled 直接透传。

### 2. flash_items 秒杀商品

| 字段 | 类型 | 说明 |
|---|---|---|
| itemId | string PK | 秒杀商品号 FI+日期+序号 |
| sessionId | string FK | 所属场次 |
| productId | string FK | 产品ID(引用产品模块) |
| productName | string | 产品名称快照(展示用) |
| originalPrice | float | 原价快照(展示对比) |
| flashPrice | float | 秒杀价(必须 < 原价) |
| flashStock | int | 秒杀库存总量 |
| soldCount | int | 已售数量(锁内扣减/回补) |
| limitPerMember | int | 每人限购数量(整个场次累计) |
| status | string | active / removed(下架) |
| createdAt / updatedAt | string | 时间戳 |

约束: (sessionId, productId) 唯一; flashPrice > 0 且 < 产品售价; flashStock ≥ 1; limitPerMember ≥ 1。

### 3. flash_orders 秒杀订单

| 字段 | 类型 | 说明 |
|---|---|---|
| orderNo | string PK | 订单号 FO+日期+序号 |
| memberId | int | 会员ID |
| sessionId / itemId / productId | string | 冗余关联(快照) |
| quantity | int | 购买数量 |
| unitPrice | float | 成交单价(秒杀价快照) |
| totalAmount | float | 成交总额 |
| status | string | pending_payment(待支付)/paid(已支付)/cancelled(已取消) |
| cancelReason | string | 取消原因(主动取消/超时取消/场次取消) |
| paidAt / cancelledAt | string | 状态变更时间 |
| createdAt / updatedAt | string | 时间戳 |

**幂等与限购判定**: 同一会员同一秒杀商品仅允许存在一张非取消订单(pending/paid);限购按 (pending+paid) 订单数量合计判定,cancelled 不计入且回补库存。

### 4. flash_settings 参数配置(单例,管理端可改)

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| enabled | bool | true | 秒杀总开关 |
| minRegisterHours | int | 0 | 抢购需注册满 N 小时(0=不限制) |
| minMemberLevel | int | 0 | 抢购需会员等级 ≥ N(0=不限制) |
| orderExpireMinutes | int | 15 | 待支付订单超时分钟数 |
| maxQuantityPerOrder | int | 5 | 单笔订单最大数量 |
| updatedAt / updatedBy | string | — | 审计 |

## 三、并发与锁设计(对齐 core.locks 约定)

| 操作 | 锁键 | 保护范围 |
|---|---|---|
| 抢购下单 | `flash:item:{itemId}` | 库存判定+扣减、限购判定、幂等判定(读-改-写全临界区) |
| 取消/超时回补 | `flash:item:{itemId}` | 库存回补与扣减互斥 |
| 支付 | 无锁 | 纯状态流转,查后置(pending→paid),重复支付→409 |

Redis 模式下锁键实际为 `lock:flash:item:{itemId}`(由锁工厂统一加前缀,见 core/locks.py)。

## 四、状态机

```
场次: draft ──publish──> published(运行时: not_started→in_progress→ended)
         └──────cancel──────┘
  cancelled(published→cancelled 时, 其 pending 订单全部取消并回补库存)

订单: pending_payment ──pay──> paid(终态)
           └──cancel(主动/超时/场次取消)──> cancelled(终态, 库存回补)

库存: flashStock 固定; soldCount ∈ [0, flashStock]
      下单 sold += qty(锁内); 取消 sold -= qty(锁内)
```

## 五、API 端点(15 个,静态路径先于动态路径声明)

| 端点 | 方法 | 鉴权 | 说明 |
|---|---|---|---|
| /api/flash/sessions | GET | 公开 | 场次列表(附运行时状态) |
| /api/flash/sessions/{sessionId} | GET | 公开 | 场次详情+商品(剩余库存/进度) |
| /api/flash/order | POST | 会员 | 抢购下单(幂等+限购+锁内扣减) |
| /api/flash/my/orders | GET | 会员 | 我的秒杀订单 |
| /api/flash/orders/{orderNo} | GET | 会员/管理员 | 订单详情 |
| /api/flash/orders/{orderNo}/pay | POST | 会员(本人) | 模拟支付成功 |
| /api/flash/orders/{orderNo}/cancel | POST | 会员(本人)/管理员 | 主动取消(回补库存) |
| /api/flash/admin/sessions | POST | 管理员 | 创建场次(草稿) |
| /api/flash/admin/sessions/{sessionId}/items | POST | 管理员 | 添加秒杀商品 |
| /api/flash/admin/sessions/{sessionId}/publish | POST | 管理员 | 发布场次 |
| /api/flash/admin/sessions/{sessionId}/cancel | POST | 管理员 | 取消场次(联动取消+回补) |
| /api/flash/admin/settings | GET/POST | 管理员 | 参数查询/修改 |
| /api/flash/admin/stats | GET | 管理员 | 场次销售统计 |
| /api/flash/admin/orders/expire-cancel | POST | 管理员 | 批量取消超时订单(回补) |

鉴权兼容: X-Member-Id/X-Role 头(compat)或 JWT Bearer(中间件注入,效果一致)。
异常映射: KeyError→404, ValueError→409(项目统一约定)。

## 六、防刷与幂等清单

1. 模块总开关 enabled 关闭 → 一律 409
2. 会员注册时长 < minRegisterHours → 409(防小号批量抢购)
3. 会员等级 < minMemberLevel → 409
4. 会员被禁用(status=0)→ 409
5. 场次非进行中(未开始/已结束/草稿/已取消)→ 409
6. 单笔数量 > maxQuantityPerOrder → 409
7. 已有待支付订单(同会员+同商品)→ 409(幂等防重复下单)
8. 累计购买(pending+paid) + 本次 > limitPerMember → 409
9. 剩余库存不足 → 409
10. 重复支付/取消已终态订单 → 409
