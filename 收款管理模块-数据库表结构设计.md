# 收款管理模块 · 数据库表结构设计

> 适配项目双模式存储(内存 dict + Redis Hash),对齐 wallet/finance 模块风格。
> 重点设计:**幂等性**(支付回调/退款回调重复推送)与**状态机**(支付/退款/付款/对账全生命周期)。

## 一、设计原则

### 1.1 双模式存储对齐
- **内存模式**(`STORE_MODE=asyncio`)：dict 主信息 + 索引 Set
- **Redis 模式**(`STORE_MODE=redis`)：Hash(主信息,支持 HINCRBYFLOAT) + String(JSON)(灵活结构) + Set(索引)
- 与 wallet_repository.py / finance_repository.py 风格一致

### 1.2 幂等性设计(核心)
支付渠道(微信/支付宝)可能重复推送回调,必须保证幂等：

| 场景 | 幂等键 | 实现方式 |
|------|------|------|
| 支付回调 | `channel_trade_no` + `pay_no` | 回调时先检查支付单状态,若已 `paid` 直接返回成功 |
| 退款回调 | `refund_no` + `channel_refund_no` | 回调时先检查退款单状态,若已 `refunded` 直接返回成功 |
| 创建支付 | `order_id` + `order_type` | 同一订单同一类型只能有一个未关闭的支付单 |
| 创建退款 | `pay_no` + 退款金额 | 部分退款累计不超过原支付金额 |

**技术实现**：
- 内存模式：`if pay_no in _callbacks_done: return` 去重 Set
- Redis 模式：`SETNX payment:callback:{channel_trade_no} 1` 原子加锁,过期 24h

### 1.3 状态机设计(核心)

#### 支付订单状态机
```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   pending ──→ paying ──→ paid ──→ refunding ──→ refunded   │
│      │           │        │                                  │
│      │           │        └──→ closed(超时关闭)              │
│      │           └──→ failed(支付失败,可重试到 paying)      │
│      └──→ closed(主动取消)                                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 退款状态机
```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   pending ──→ auditing ──→ approved ──→ refunded            │
│      │           │                                          │
│      │           └──→ rejected(审核拒绝)                     │
│      └──→ cancelled(用户撤回)                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 付款状态机(平台对外付款,如钱包提现打款)
```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   pending ──→ auditing ──→ approved ──→ paying ──→ paid      │
│      │           │              │                            │
│      │           └──→ rejected  └──→ failed(打款失败,可重试) │
│      └──→ cancelled                                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 对账状态机
```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   pending ──→ matched(一致)                                  │
│      │                                                      │
│      └──→ diff(差异) ──→ investigating(调查中) ──→ resolved │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 二、核心表清单(5 张表)

| 优先级 | 表名 | 用途 | 主键 | Redis Key 模式 |
|:---:|------|------|------|------|
| P0 | `payment_orders` | 支付订单 | `pay_no` | Hash `payment:order:{pay_no}` |
| P0 | `payment_refunds` | 退款记录 | `refund_no` | String(JSON) `payment:refund:{refund_no}` |
| P0 | `payment_payouts` | 平台付款 | `payout_no` | String(JSON) `payment:payout:{payout_no}` |
| P1 | `payment_reconciliation` | 对账记录 | `recon_date` + `channel` | String(JSON) `payment:recon:{date}:{channel}` |
| P1 | `payment_channels` | 渠道配置 | `channel_code` | Hash `payment:channel:{code}` |

### 编号规则
| 实体 | 前缀 | 格式 | 示例 |
|------|:---:|------|------|
| 支付单号 | `PAY` | `PAY{YYYYMMDD}{seq:04d}` | `PAY202608210001` |
| 退款单号 | `RF` | `RF{YYYYMMDD}{seq:04d}` | `RF202608210001` |
| 付款单号 | `PO` | `PO{YYYYMMDD}{seq:04d}` | `PO202608210001` |
| 对账批次 | `RECON` | `RECON{YYYYMMDD}{channel}` | `RECON20260821WX` |

## 三、表结构详细设计

### 3.1 payment_orders(支付订单表 · P0)

**业务定位**：记录每一笔支付订单的全生命周期(创建→支付→回调→关闭/退款)

**内存键**：
- `_mock_store["payment_orders"]` — 主信息 dict（key=pay_no）
- `_mock_store["_payment_order_seq"]` — 序号计数器
- `_mock_store["_payment_order_index:{user_id}"]` — 用户支付单索引 Set
- `_mock_store["_payment_order_index:{order_id}"]` — 订单支付单索引 Set（幂等校验用）

**Redis Key**：
- `payment:order:{pay_no}` — Hash(主信息)
- `payment:order:seq` — String(INCR 序号)
- `payment:order:index:user:{userId}` — Set(用户支付单索引)
- `payment:order:index:order:{orderId}` — Set(订单支付单索引)
- `payment:callback:lock:{channelTradeNo}` — String(回调幂等锁, TTL 24h)

**字段设计**：

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| payNo | str | 支付单号(主键) | `PAY202608210001` |
| orderId | str | 关联订单号 | `ORD202608210001` |
| orderType | str | 订单类型: retail/groupbuy/custom/wallet_deposit | `retail` |
| userId | str | 用户/会员 ID | `9001` |
| payChannel | str | 支付渠道: wechat/alipay/unionpay/bank/aggregate | `alipay` |
| payMethod | str | 支付方式: native/jsapi/h5/page/transfer | `jsapi` |
| totalAmount | float | 订单总金额 | `1000.00` |
| discountAmount | float | 优惠抵扣 | `100.00` |
| pointsAmount | float | 积分抵扣 | `50.00` |
| actualAmount | float | 实付金额 | `850.00` |
| channelTradeNo | str | 渠道交易号(渠道返回) | `202608212200149231...` |
| status | str | 状态机: pending/paying/paid/failed/closed/refunding/refunded | `paid` |
| expireTime | str | 支付超时时间(ISO8601) | `2026-08-21T20:30:00+08:00` |
| payTime | str | 支付成功时间 | `2026-08-21T20:15:30+08:00` |
| callbackTime | str | 回调接收时间 | `2026-08-21T20:15:31+08:00` |
| callbackContent | str | 回调原始数据(JSON) | `{"trade_status":"TRADE_SUCCESS",...}` |
| sceneType | str | 场景: order_pay/wallet_deposit/agent_purchase | `order_pay` |
| failReason | str | 失败原因(失败/关闭时填充) | `USER_CANCEL` / `TIMEOUT` |
| createdAt | str | 创建时间 | `2026-08-21T20:15:00+08:00` |
| updatedAt | str | 更新时间 | `2026-08-21T20:15:31+08:00` |

**幂等性约束**：
- `channelTradeNo` 唯一(渠道交易号,回调时由渠道返回)
- 同一 `orderId` + `orderType` 只能有一个非关闭状态的支付单
- 回调时：`if status == "paid": return success`(已支付直接返回成功)

**状态流转规则**：
| 当前状态 | 允许的下一状态 | 触发动作 |
|------|------|------|
| pending | paying / closed | 创建支付请求 / 主动取消 |
| paying | paid / failed / closed | 收到成功回调 / 失败回调 / 超时关闭 |
| paid | refunding | 发起退款 |
| refunding | refunded | 退款完成 |
| failed | paying | 重新发起支付 |
| closed | (终态) | - |
| refunded | (终态) | - |

---

### 3.2 payment_refunds(退款记录表 · P0)

**业务定位**：退款申请审核与原路退款执行记录

**内存键**：
- `_mock_store["payment_refunds"]` — 主信息 dict
- `_mock_store["_payment_refund_seq"]` — 序号计数器
- `_mock_store["_payment_refund_index:{pay_no}"]` — 支付单关联的退款单索引
- `_mock_store["_payment_refund_pending"]` — 待审核退款单集合

**Redis Key**：
- `payment:refund:{refundNo}` — String(JSON)
- `payment:refund:seq` — String(INCR)
- `payment:refund:index:pay:{payNo}` — Set(支付单关联的退款单)
- `payment:refund:pending` — Set(待审核退款单集合)
- `payment:refund:callback:lock:{channelRefundNo}` — String(退款回调幂等锁, TTL 24h)

**字段设计**：

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| refundNo | str | 退款单号(主键) | `RF202608210001` |
| payNo | str | 关联支付单号 | `PAY202608210001` |
| orderId | str | 关联订单号 | `ORD202608210001` |
| userId | str | 用户 ID | `9001` |
| refundType | str | 退款类型: full/partial | `partial` |
| refundAmount | float | 退款金额 | `500.00` |
| refundedAmount | float | 已退款金额(部分退款累计) | `300.00` |
| refundReason | str | 退款原因 | `商品质量问题` |
| refundChannel | str | 退款渠道(同原支付渠道) | `alipay` |
| channelRefundNo | str | 渠道退款号(渠道返回) | `202608212200149231...` |
| status | str | 状态机: pending/auditing/approved/rejected/refunded/cancelled | `refunded` |
| auditor | str | 审核人 | `admin` |
| auditRemark | str | 审核备注 | `同意退款` |
| auditTime | str | 审核时间 | `2026-08-21T21:00:00+08:00` |
| refundTime | str | 退款到账时间 | `2026-08-21T21:05:00+08:00` |
| callbackContent | str | 退款回调原始数据 | `{"refund_status":"REFUND_SUCCESS",...}` |
| createdAt | str | 创建时间 | `2026-08-21T20:50:00+08:00` |

**幂等性约束**：
- `channelRefundNo` 唯一(渠道退款号)
- 部分退款累计金额不超过原支付金额：`sum(refundAmount) <= payment_orders.actualAmount`
- 退款回调时：`if status == "refunded": return success`

---

### 3.3 payment_payouts(平台付款表 · P0)

**业务定位**：平台对外付款记录(供应商货款/物流费/老酒回收款/推广佣金/钱包提现打款)

**与钱包模块关联**：钱包 `withdraw` 审核通过后,创建付款记录,打款成功后更新钱包提现状态为 `paid`

**内存键**：
- `_mock_store["payment_payouts"]` — 主信息 dict
- `_mock_store["_payment_payout_seq"]` — 序号计数器
- `_mock_store["_payment_payout_pending"]` — 待审核付款集合
- `_mock_store["_payment_payout_index:{payee_account}"]` — 按收款账号索引

**Redis Key**：
- `payment:payout:{payoutNo}` — String(JSON)
- `payment:payout:seq` — String(INCR)
- `payment:payout:pending` — Set(待审核付款集合)
- `payment:payout:index:payee:{accountHash}` — Set(收款账号索引)

**字段设计**：

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| payoutNo | str | 付款单号(主键) | `PO202608210001` |
| payoutType | str | 付款类型: supplier/logistics/recycle/commission/wallet_withdraw/salary | `wallet_withdraw` |
| sourceId | str | 来源单号(关联业务单据) | `WD202608210001`(钱包提现单号) |
| payeeName | str | 收款人名称 | `张三` |
| payeeAccount | str | 收款账号 | `6222000112345678` |
| payeeBank | str | 收款银行 | `工商银行` |
| payeePhone | str | 收款人手机号 | `13800009001` |
| amount | float | 付款金额 | `1000.00` |
| taxAmount | float | 代扣税费 | `0.00` |
| actualAmount | float | 实付金额 | `1000.00` |
| payChannel | str | 付款渠道: bank_transfer/alipay_transfer/wechat_transfer | `bank_transfer` |
| channelPayoutNo | str | 渠道付款流水号 | `BK202608210001` |
| status | str | 状态机: pending/auditing/approved/paying/paid/failed/rejected/cancelled | `paid` |
| auditor | str | 审核人 | `admin` |
| auditRemark | str | 审核备注 | `同意打款` |
| auditTime | str | 审核时间 | |
| payTime | str | 付款时间 | |
| failReason | str | 失败原因 | `BANK_ACCOUNT_INVALID` |
| voucherUrl | str | 付款凭证 URL | `https://...` |
| retryCount | int | 重试次数(打款失败可重试,上限 3) | `0` |
| createdAt | str | 创建时间 | |

**幂等性约束**：
- `sourceId` + `payoutType` 唯一(同一来源单据同一类型只能创建一个付款单)
- 打款失败可重试,但 `retryCount` 上限 3 次,超过自动转 `rejected`

---

### 3.4 payment_reconciliation(对账记录表 · P1)

**业务定位**：日终对账,平台流水与渠道账单三向核对

**内存键**：
- `_mock_store["payment_reconciliation"]` — 主信息 dict（key=`{recon_date}:{channel}`）
- `_mock_store["_payment_recon_diff"]` — 差异待处理集合

**Redis Key**：
- `payment:recon:{date}:{channel}` — String(JSON)
- `payment:recon:diff` — Set(差异待处理集合)

**字段设计**：

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| reconDate | str | 对账日期(YYYY-MM-DD) | `2026-08-21` |
| channel | str | 渠道: wechat/alipay/unionpay/bank | `alipay` |
| totalOrders | int | 系统支付笔数 | `150` |
| totalAmount | float | 系统支付金额 | `158000.00` |
| channelOrders | int | 渠道账单笔数 | `148` |
| channelAmount | float | 渠道账单金额 | `156000.00` |
| feeAmount | float | 渠道手续费 | `472.00` |
| refundOrders | int | 退款笔数 | `5` |
| refundAmount | float | 退款金额 | `3500.00` |
| netAmount | float | 实收净额 = totalAmount - refundAmount - feeAmount | `154028.00` |
| matched | bool | 是否一致 | `false` |
| status | str | 状态机: pending/matched/diff/investigating/resolved | `diff` |
| diffDesc | str | 差异说明 | `2笔订单渠道无记录,1笔渠道有平台无` |
| diffDetails | str | 差异明细(JSON 数组) | `[{"type":"missing_in_channel","payNo":"PAY..."}]` |
| resolvedBy | str | 处理人 | `admin` |
| resolvedTime | str | 处理时间 | |
| createdAt | str | 创建时间 | |

---

### 3.5 payment_channels(渠道配置表 · P1)

**业务定位**：支付渠道配置(费率/限额/开关/优先级),支持动态切换渠道

**内存键**：
- `_mock_store["payment_channels"]` — 主信息 dict（key=channel_code）

**Redis Key**：
- `payment:channel:{code}` — Hash(渠道配置)

**字段设计**：

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| channelCode | str | 渠道编码(主键): wechat/alipay/unionpay/bank/aggregate | `alipay` |
| channelName | str | 渠道名称 | `支付宝` |
| channelType | str | 渠道类型: third_party/bank/aggregate | `third_party` |
| enabled | bool | 是否启用 | `true` |
| priority | int | 优先级(数字越小越高) | `1` |
| feeRate | float | 手续费率 | `0.006` |
| feeCap | float | 手续费封顶(0 表示不封顶) | `0` |
| minAmount | float | 最小支付金额 | `0.01` |
| maxAmount | float | 最大支付金额(0 表示不限) | `50000.00` |
| dailyLimit | float | 日累计限额(0 表示不限) | `500000.00` |
| supportRefund | bool | 是否支持原路退款 | `true` |
| supportPartialRefund | bool | 是否支持部分退款 | `true` |
| config | str | 渠道配置(JSON: appId/mchId/publicKey 等) | `{"appId":"2021...",...}` |
| sceneTypes | str | 支持场景(JSON 数组): order_pay/wallet_deposit/agent_purchase | `["order_pay","wallet_deposit"]` |
| createdAt | str | 创建时间 | |
| updatedAt | str | 更新时间 | |

## 四、与已有模块的关联

### 4.1 订单管理(order)
- **关联点**：订单支付时创建支付单,`sceneType=order_pay`,`orderId=订单号`
- **回调成功**：更新订单状态 `PENDING → PAID`
- **退款**：订单售后退款时创建退款单,退款成功更新订单状态为 `REFUNDED`

### 4.2 钱包盈利(wallet)
- **充值**：钱包充值时创建支付单,`sceneType=wallet_deposit`,`orderId=钱包交易号`
- **提现打款**：钱包提现审核通过后创建付款单,`payoutType=wallet_withdraw`,`sourceId=提现单号`
- **打款成功**：回调更新钱包提现状态为 `paid`

### 4.3 代理商管理(agent)
- **进货付款**：代理商进货时创建支付单,`sceneType=agent_purchase`,`orderId=进货单号`
- **返利打款**：代理商返利提现创建付款单,`payoutType=wallet_withdraw`(复用钱包提现流程)

### 4.4 财务管理(finance)
- **支付凭证**：支付成功后自动生成收款凭证
- **退款凭证**：退款成功后自动生成付款凭证
- **对账流水**：日终对账结果同步到财务流水表

## 五、并发安全与锁键设计

| 场景 | 锁键 | 说明 |
|------|------|------|
| 创建支付单 | `payment:order:{orderId}` | 防止同一订单重复创建支付单 |
| 支付回调处理 | `payment:callback:{channelTradeNo}` | 防止回调重复处理(幂等锁) |
| 发起退款 | `payment:refund:{payNo}` | 防止同一支付单并发退款 |
| 退款回调处理 | `payment:refund:callback:{channelRefundNo}` | 防止退款回调重复处理 |
| 付款打款 | `payment:payout:{payoutNo}` | 防止重复打款 |
| 对账 | `payment:recon:lock:{date}:{channel}` | 防止重复对账 |

## 六、实现优先级

| 优先级 | 实现内容 | 预估方法数 |
|:---:|------|:---:|
| P0 | payment_orders(创建/查询/状态流转/回调幂等) | 12 |
| P0 | payment_refunds(创建/审核/回调) | 8 |
| P0 | payment_payouts(创建/审核/打款/回调) | 8 |
| P1 | payment_reconciliation(对账/差异处理) | 6 |
| P1 | payment_channels(配置管理) | 5 |
| **合计** | - | **39** |
