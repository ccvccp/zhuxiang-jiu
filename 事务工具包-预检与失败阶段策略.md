# 事务工具包 · 预检策略与失败阶段(failedStage)说明

> 适用范围:`zhuxiang-jiu/js/toolkit/transaction-template.js` 及所有基于它的事务服务
> (agent-upgrade-service.js / checkout-service.js / inventory-service.js)
> 维护日期:2026-08-18

---

## 1. 背景:为什么要整理这份文档

在把「库存扣减逻辑」迁移到工具包(新建 `inventory-service.js`)时,回归测试 **TC2(库存不足回滚)** 出现如下失败:

```
断言失败: TC2 失败阶段应为阶段3 (期望 "阶段3-库存扣减", 实际 undefined)
```

根因:`deduct` 的 **preflight(预检)里预先判断了库存是否充足**,库存不足时直接走
`return { abort: true }` **中止**流程。而 `TransactionTemplate` 的 `failedStage`
**只在"事务内阶段抛错"路径才会设置**——预检中止路径不产生 `failedStage`、也不触发回滚。

修复方式:把「库存充足性 / 商品存在性」这类**业务校验从 preflight 移到事务内的阶段**
(由共享核心 `applyDeduct` 在阶段3 执行并抛错),让失败走完整的 `BEGIN → 抛错 → ROLLBACK`
路径,`failedStage` 即可正确标记为「阶段3-库存扣减」。

本文档把这套约定固化下来,避免后续新增事务服务时重蹈覆辙。

---

## 2. failedStage 的三种流程语义(源码事实)

来源:`transaction-template.js` 的 `run()` 方法。一次事务有 **三种结局**,返回结构不同:

| 流程结局 | 触发条件 | 返回字段 | 是否回滚 | failedStage |
|----------|----------|----------|----------|-------------|
| **success(成功)** | 所有阶段跑完、preflight 通过 | `{success:true, ctx, logs}` | 否 | 无 |
| **aborted(预检中止)** | preflight 返回 `{abort:true}` | `{success:false, aborted:true, ...preResult, logs}` | **否**(还没开事务) | **无** |
| **failed(阶段抛错)** | 某个 stage.action 抛错 | `{success:false, error, failedStage, executedStages, ctx, logs}` | **是**(adapter.rollback) | **有** |

`failedStage` 的取值优先级(见 `transaction-template.js` catch 块):

```js
const failedAt = _currentStage || this.logger.lastStage() || '未知阶段';
```

- `_currentStage`:抛错时正在执行的 stage 名(最精确,首选)
- `lastStage()`:基于日志推断的最后一个阶段(退化方案,可能被 adapter 内部 info 日志干扰)
- `'未知阶段'`:兜底

> **关键结论**:`failedStage` 是「事务内失败」的产物。预检中止(aborted)不会有它。
> 所以:**凡是希望失败时能定位到阶段 + 看到回滚日志的业务校验,都不能放在 preflight。**

---

## 3. 预检(preflight)策略约定

### 3.1 preflight 只做"结构 / 参数校验"

preflight 的定位是 **事务前只读、无副作用、可中止的快速校验**。允许放:

- 入参非空(items 非空、清单不为空)
- 入参结构合法(每行有 `id`、`qty` 为正数、枚举值合法)
- 只读的纯计算(如 `calculateOrderPrice` 算价、AI 风险评估),其结果后续阶段要用
- 会员/资源查找(只读定位,不判断业务可行性)

### 3.2 业务校验放进"事务内的阶段"

以下校验 **必须放在事务内的某个 stage** 中执行(失败即抛错 → 触发回滚 + 标记 failedStage):

- 库存充足性(`stock < qty`)
- 商品存在性
- 优惠券有效 / 未使用
- 积分是否足够
- 余额是否足够
- 任何「业务规则是否满足」的判断

### 3.3 为什么不放在 preflight(反面教材)

把业务校验放进 preflight 会出现:

1. **无 failedStage** → 上层拿到 `{success:false, aborted:true}`,无法定位是哪一步失败
2. **无回滚日志** → `tx_log` 里只有 preflight 的痕迹,没有 `BEGIN/ROLLBACK`,排查困难
3. **与委托方不一致** → checkout 阶段4 委托 `applyDeduct`,后者在事务内抛错才能让
   checkout 的 `failedStage='阶段4-库存扣减'`;如果库存校验在 inventory 自己的 preflight,
   走的是 abort,checkout 那条链路根本调不到 inventory 的 deduct(它调的是 applyDeduct)

> 一句话口诀:**"结构校验进 preflight,业务校验进阶段"。**

---

## 4. 三模块现状对照表

| 服务 | preflight 校验内容 | 业务校验所在阶段 | 失败时 failedStage 示例 |
|------|-------------------|-----------------|----------------------|
| `agent-upgrade-service.js` | AI 风险评估(只读,拒绝则 abort) | 阶段2 目标等级校验(事务内) | `阶段2-开启事务` |
| `checkout-service.js` | 购物车非空 + `calculateOrderPrice` 算价 + 查会员 | 阶段4 库存(委托 applyDeduct)、阶段5 优惠券、阶段6 积分 | `阶段4-库存扣减` / `阶段5-优惠券核销` |
| `inventory-service.js` (deduct) | items 非空 + id 存在 + qty>0(纯结构) | 阶段3 `applyDeduct`(库存+商品存在性) | `阶段3-库存扣减` |
| `inventory-service.js` (restock) | items 非空 + id 存在 + qty>0(纯结构) | 阶段3 `applyRestock`(商品存在性) | `阶段3-库存回补` |

参考实现:
- `zhuxiang-jiu/js/inventory-service.js` 的 `deduct` preflight(第 ~315 行)
- `zhuxiang-jiu/js/checkout-service.js` 的 preflight(第 ~163 行)与阶段4 委托(第 ~251 行)

---

## 5. 共享核心 + 委托模式(避免嵌套事务)

库存逻辑统一收敛到 `inventory-service.js` 的 **共享核心**:

```
applyDeduct(dbRef, items, logger, opts)
  └─ 在「调用方事务」内执行:校验→扣减→写流水→查预警
     不开自己的 BEGIN/COMMIT,失败抛错由外层捕获并回滚
```

- `checkout-service` 阶段4 → 调用 `InventoryService.applyDeduct(dbRef, ctx.items, ctx.logger, ...)`
- `inventory-service.deduct` 独立入口 → 用自己的 TransactionTemplate 包一层 `applyDeduct`

> **禁止**:在 `applyDeduct` 内部再 `new TransactionTemplate` 开子事务,会造成嵌套事务、
> 快照互相覆盖、回滚语义错乱。共享核心永远是"无事务边界的原子操作单元"。

---

## 6. 回归测试约束(写失败用例时务必遵守)

基于快照回滚的固有语义,失败用例的断言要这样写:

### 6.1 失败路径(期望 failedStage + 回滚)

```js
assertEqual(r.success, false, '应失败');
assert(r.error.includes('库存不足'), '错误信息');
assertEqual(r.failedStage, '阶段3-库存扣减', '失败阶段');   // ← 必须有
const txTypes = db.tx_log.map(t => t.type);
assertIncludes(txTypes, 'ROLLBACK', '应有 ROLLBACK');
assertEqual(db.inventory_logs.length, 0, '流水不持久化(原子性)');
```

### 6.2 BEGIN 不留存于回滚(易踩坑)

`begin()` 的实现是:**先拍快照,再把 BEGIN 推入 live db**。因此回滚用快照覆盖 live db 时,
BEGIN 会被丢弃——**回滚后的 `tx_log` 只含 ROLLBACK,不含 BEGIN**。

```js
// ✅ 正确:回滚后只断言 ROLLBACK
assertIncludes(txTypes, 'ROLLBACK', '应有 ROLLBACK');
// ❌ 错误:回滚后断言 BEGIN 会失败(BEGIN 不在快照里)
// assertIncludes(txTypes, 'BEGIN', ...);
```

成功路径才同时有 `BEGIN + COMMIT`。

> 此行为与 `agent-upgrade-regression-test.js` TC4、`checkout-regression-test.js` TC3/TC4、
> `inventory-regression-test.js` TC2 保持一致,勿改动。

---

## 7. 维护清单(新增事务服务时逐项核对)

新增一个基于工具包的事务服务时,确认以下各项:

- [ ] preflight 只做结构/参数校验,**不含**库存/余额/券有效等业务判断
- [ ] 业务校验放在某个 stage 的 action 里,失败用 `throw new Error(...)`(不要 `return {abort}`)
- [ ] 共享核心(供其它服务委托的函数)**不**自己开 TransactionTemplate,只做原子操作
- [ ] 独立入口(deduct/restock/submit...)用 TransactionTemplate 包一层共享核心
- [ ] 失败用例断言 `failedStage` + `ROLLBACK` + 数据库原子性(表数量/状态恢复)
- [ ] 失败用例 **只断言 ROLLBACK**,不断言 BEGIN(快照回滚语义)
- [ ] 成功用例断言 `BEGIN + COMMIT` + 数据库持久化
- [ ] 若供上层委托,确认上层 `failedStage` 能正确定位到调用方阶段
  (如 checkout 阶段4 委托 applyDeduct,失败时 `failedStage='阶段4-库存扣减'`)

---

## 8. 相关文件索引

| 文件 | 作用 |
|------|------|
| `zhuxiang-jiu/js/toolkit/transaction-template.js` | 事务编排框架(BEGIN/COMMIT/ROLLBACK/preflight/failedStage) |
| `zhuxiang-jiu/js/toolkit/upgrade-logger.js` | 结构化日志(事务ID/计时器/executedStages/lastStage) |
| `zhuxiang-jiu/js/inventory-service.js` | 库存扣减/回补服务(applyDeduct 共享核心 + deduct/restock) |
| `zhuxiang-jiu/js/checkout-service.js` | 订单结算服务(阶段4 委托 applyDeduct) |
| `zhuxiang-jiu/js/agent-upgrade-service.js` | 代理商升级服务(preflight=AI风险评估) |
| `zhuxiang-jiu/js/inventory-regression-test.js` | 库存回归测试(TC1~TC4) |
| `zhuxiang-jiu/js/checkout-regression-test.js` | 订单回归测试(TC1~TC4) |
| `zhuxiang-jiu/js/agent-upgrade-regression-test.js` | 代理商升级回归测试 |
