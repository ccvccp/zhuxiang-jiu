# AI智能管理模块设计文档

> 版本：v1.2　日期：2026-08-29　定位：全站角色经济中枢（承接33号权限AI智能管理模块，向上统辖角色全生命周期）
>
> v1.2 变更：P0+P1 决策与实现状态全量同步——决策项闭环报告交叉引用（§12新增）；数据模型补 role_clawbacks 负账表与口径字段（§6）；API 从设计稿18端点更新为实际25端点（§7）；闭环验证证据落档（§10）。
>
> v1.1 变更：D-7/D-8/D-9 三项决策全部闭环（见附录），服务分润参数定为初版基线。
>
> 核心理念：**一次注册、按需认领、权责利对等、服务即收益、行为入信用、AI统筹调度**

---

## 1. 模块定位

### 1.1 要解决的问题

当前系统各模块"各自管角色、各自算分润"，缺少一个统一的中枢：

| 现状问题 | 具体表现 | 本模块对策 |
|---|---|---|
| 角色获取方式分散 | auth双角色、admin后台角色、perm申请审批、credit按roleType给起始分，四套并存 | 统一为"注册认领制"，认领入口唯一 |
| 权责利不对等 | 有权限无收益挂钩、有收益无责任约束（责任书与分润协议分离） | 权责利三合一数字契约 |
| 服务不变现 | 人工客服接AI转接的单，干好干坏收入一样（现固定分配客服ID=1，无绩效挂钩） | 服务分润闭环：满意度挂钩销售额分润 |
| 分润口径不一 | venue按差价利润60/20/20结算；用户层面的分润原则是"实际销售价格×60%为产品获利池" | 统一分润总账，旧口径作为决策项对齐 |
| 信用两套并存 | 竹信分（用户维度）与权责信用分（岗位维度）互不联通 | 信用行为总线：行为统一入账，总分+岗位子分 |
| AI转人工断链 | chat转人工不建工单（ticket的source=ai预留未打通），派单硬编码 | AI服务调度中枢接管转接路由 |

### 1.2 一句话定位

**AI智能管理模块 = 全站角色的"经济操作系统"**：会员付款产生实际销售价格，其中60%作为产品获利池；所有角色（生产工人、人工客服、代理商、推广员、合作商、管理者……）围绕"产品从生产到交付到服务"的全过程提供各自的履约服务，从获利池中按贡献细分收益；每一次履约行为同时进入信用分体系，信用分反过来决定接单优先级和分润系数——形成"认领→履约→评价→分润→信用→更多收益"的自增强闭环。

### 1.3 核心概念模型

```
                     会员付款购买（实际销售价格 P）
                              │
                    产品获利池 = P × 60%
                              │ 细分（按贡献角色）
      ┌──────────┬──────────┼──────────┬──────────┬──────────┐
   会员回馈     生产与质量    服务过程      渠道合作     推广引流    平台留存
 （返利/积分）（工人计件+质量）（客服/物流）（代理/网店/酒店）（推广员/达人）
      │           │           │           │           │          │
      └───────────┴─────┬─────┴───────────┴───────────┴──────────┘
                        ▼
              信用行为总线（一切履约行为入信用分）
                        ▼
              竹信分（用户总分）+ 权责信用分（岗位子分）
                        ▼
        信用分反哺：接单优先级 ↑ / 分润系数 ↑ / 先享后付额度 ↑
```

---

## 2. 成熟模型对标与创新

### 2.1 对标模型

| 成熟模型 | 借鉴点 | 本模块应用 |
|---|---|---|
| 海尔"人单合一" | 每个员工直接面对订单，收入与订单价值挂钩 | 生产工人按关联订单计件分润，而非固定工资 |
| 滴滴/Uber派单引擎 | AI派单+服务分动态调节接单权重 | 客服派单算法：信用分+技能+负载+历史满意度 |
| Airbnb超赞房东 | 高满意度获得标识→更多订单→更高收益 | 满意度≥4星才触发分润，连续高满意度升接单权重 |
| 淘宝客服绩效体系（响应时长/转化率/满意度） | 过程指标与结果指标并重 | SLA时效系数×满意度系数双重调节分润 |
| 阿里云RAM/SAP GRC | 权限申请-审批-审计-回收全生命周期 | 复用33号perm模块的逐级审批与责任书机制 |
| 商业保险精算定价 | 信用即费率 | 竹信分等级直接作为分润乘数（信用即货币） |
| 供销社/合作社按交易量返利 | 先按贡献记账、期末按累计返利 | 季度结算与兑换机制（复用信用模块4.x） |

### 2.2 创新点（相对成熟模型的差异化）

1. **服务分润闭环（业内首创）**：传统客服拿固定工资或按单提成；本设计让人工客服从"AI转人工服务所关联的订单实际销售额"中分润——AI把无法解决的问题转给人，人解决了且用户满意，人从这笔交易的销售分润池中获得收益。AI与人不是替代关系，而是"AI筛选+人收口"的协作分润关系。
2. **信用即货币**：竹信分不只是额度工具，直接作为分润乘数与派单权重，让"权责利对等"从口号变成可计算的公式。
3. **权责利三合一数字契约**：责任书（责）+分润协议（利）+权限包（权）签一次、版本化、可审计、到期复审。
4. **AI双向中枢**：AI既是派单调度者（把服务需求路由给最优角色），也是监管者（满意度预测、异常分润检测、信用异动预警）。

---

## 3. 角色权责利矩阵（角色全景）

围绕"会员购买"这一主链路，全站角色定义如下（认领条件均为"注册认领制"——先注册成为会员，再认领具体角色，而非平台分配）：

| 角色 | 认领条件（权） | 核心责任（责） | 收益来源（利） | 信用规则 |
|---|---|---|---|---|
| 会员 | 注册+18周岁实名合规 | 按时付款、真实评价、维护账号安全 | 实物+购物体验+贴心服务；消费返利1%、积分1.5竹叶/¥1 | 履约加分、恶意差评/薅羊毛扣分 |
| 生产工人 | 技能认证+岗位认领+签契约 | 按工艺标准完成生产环节；生命码扫码留痕（质量可追溯） | 计件分润：关联订单售价×环节分润率×质量系数 | 质量事故扣分+分润追回，优质批次加分 |
| 人工客服 | 实名+培训考核+服务责任书 | SLA响应（紧急立即/高2h/中4h/低24h）；解决率；满意度 | 基础接单积分+满意度分润（详见5.3）；AI转接单优先派给高信用者 | 满意度≥4星加分，超时/投诉/升级扣分 |
| 代理商 | 认领+区域授权+保证金 | 区域销售目标、渠道秩序 | 超额累进返利（15%/25%/30%边际）+区域分润+品鉴酒 | 完成目标加分，窜货/乱价扣分 |
| 市级网店 | 认领+店铺资质 | 月销目标、服务标准 | 三档进货折扣（0.70/0.80/0.90）+分润 | 达标档位联动信用 |
| 酒店酒吧会所合作商 | 认领+铺货协议 | 陈列、动销、结算配合 | 差价利润分润（本站60%/代理20%/酒店20%口径） | 结算违约扣分 |
| 推广员/达人 | 认领推广码 | 真实推广、不刷量 | 订单金额×等级佣金（5%-15%推广员；10%-20%达人） | 自购不计佣；刷量封禁 |
| 仓储/物流角色 | 认领+岗位契约 | 时效配送、破损率 | 过程服务分润（P1阶段接入） | 超时/破损扣分 |
| 管理者（运营/财务/审计等） | 33号perm逐级审批+责任书 | 岗位职责+SoD职责分离 | 权责信用分月度考核奖惩（奖金入钱包/积分） | 权责信用分月度考核，自动奖惩 |

> 原则：每个角色"利"的大小与"责"的轻重、"权"的大小严格对等；收益全部来自自己在主链路上的履约贡献，没有"不劳而获"的角色。

---

## 4. 六大子系统设计

### 4.1 子系统一：角色注册认领中心

**流程**：浏览角色目录 → 提交认领申请（AI预审：资质/信用门槛/配额） → 逐级审批（复用perm模块审批链） → 签署三合一契约 → 试用期（30天） → 转正 / 拒绝。

- **角色目录**（role_catalog）：每个角色维护认领条件、区域/总量配额、考核标准、分润协议模板版本，由超管维护。
- **试用期规则**：分润减半、信用记录双倍权重——防止"认领套利"（认领后不履约白拿收益）。
- **退出机制**：主动退出（结算未入账分润）/ 违约清退（扣信用、追回分润）/ 到期未复审自动冻结。

### 4.2 子系统二：权责利契约引擎

契约 = 权限包（映射perm_nodes 28权限点）+ 责任条款（SLA/质量标准/SoD矩阵）+ 分润协议（基数/费率/系数）+ 信用条款（加减分规则），四要素版本化绑定。

- 契约签署时**同步生成perm授权实例与责任书**，实现"签一份契约=同时获得权限、责任、收益资格"。
- 契约到期前15天提醒复审；复审结果联动信用分。

### 4.3 子系统三：AI服务调度中枢（修复现状断链）

当前chat模块转人工固定分配客服ID=1且不创建工单。本中枢接管所有"服务需求→最优角色"的路由：

**派单算法**：

```
调度得分 = 竹信分×40% + 技能匹配度×25% + 当前负载（忙闲比）×20% + 近30天满意度×15%
```

- **转接联动（P0必做）**：chat会话触发转人工时——①自动创建`source=ai`工单；②调用本中枢派单（替代硬编码）；③会话、工单、订单三方关联（分润依据链）。
- **抢单模式（P2已实现）**：无可用客服时工单留在待分配池（`GET /api/role/grab/tickets`），客服可抢单（`POST /api/role/grab/ticket`，先到先得）：锁内防双抢、资格校验（有效契约+负账未冻结）、抢单留痕（派单记录 mode=grab）——认领制在微观层面的体现。
- **服务-订单关联**：工单/会话可绑定订单号，作为"本次服务内容的销售额"的数据基础。

### 4.4 子系统四：分润结算引擎

- **结算触发**：订单完成且售后期（7天）结束 / 用户确认满意度后即时结算（二选一，按契约配置）。
- **统一分润总账**：所有模块的佣金/返利/分润统一记入profit_ledger（来源模块、基数、费率、系数、状态），计算层仍由各业务模块（agent/venue/traffic/wallet）负责，本模块负责**记账与对账统一**。
- **防套利风控**：同一客服对同一用户的月度分润笔数上限、服务频次异常检测（AI）、自买自导检测（会员-客服同人）、分润金额异常阈值告警。
- **入账**：结算完成后调用钱包模块入账（复用现有钱包账户体系）。

### 4.5 子系统五：信用行为总线

- 各模块发布标准化行为事件（eventId/userId/role/behavior_code/delta/来源模块），总线统一落库并分发至：
  - **竹信分引擎**（credit模块）：计算用户总分——总维度；
  - **权责信用分**（perm模块）：计算岗位子分——岗位维度。
- **总-分结构**：竹信分=人的整体信用（决定分润系数、先享后付），权责信用分=某岗位的履职信用（决定该岗位接单权重、岗位奖惩）。两套体系不合并数据结构，通过总线保持同源同步。

### 4.6 子系统六：AI监管大脑

> ✅ **P2 已实现（2026-08-29）**，端点 `/api/role/ai-brain/*`（3扫描+预警列表+处置）：

- **满意度预测**（`scan/satisfaction`）：扫描进行中人工会话，规则引擎评分（负向情绪词×20 + 未解决×15 + 低置信回复占比×30 + 长会话+10，≥60 触发干预预警），风险分回写会话 `emotionScore`（激活原空壳字段）；
- **异常分润检测**（`scan/anomaly`）：三类模式——金额离群（leave-one-out：单笔>同客服其余笔均值×5 且>¥30）、同人高频（同客服-同会员当月≥5笔）、顶格频发（当月触顶流水≥3笔）；
- **信用异动预警**（`scan/credit-drop`）：7天窗口内**负向流水**累计≥100（下滑量语义，正向加分不抵消）→ 契约自动置 suspended 冻结接单 + 预警待人工复核（复核后 `activate` 恢复）；
- 预警统一落 `role_alerts` 表（type/severity/subject/detail/status），处置 `open→resolved` 留痕；
- 与24号合规合法智能监控模块联动（敏感词、违规行为）——P2 复用其词库于情绪检测。

---

## 5. 分润设计（核心规则）

### 5.1 总原则

**产品获利池 = 实际销售价格 × 60%**，全站所有角色的分润均在此池内细分，不新增额外成本项。

> ✅ **决策项 D-7（已确认，2026-08-29）**：采用**双轨口径**——①面向合作商的既有协议（venue/agent结算）维持"差价利润60/20/20"旧口径，保证合同连续性；②面向过程服务角色（客服/工人/推广/会员回馈）采用"实际销售价格×60%"新口径子池；③统一分润总账（profit_ledger）按计算基数分轨道记账（`basis=diff_profit` / `basis=sale_price`），代理商返利以月度进货额为基数、单独记 `basis=purchase_amount` 轨道，对账时分轨道核算，互不混淆。
>
> ✅ **P1 落地（2026-08-29）**：venue 结算回写平台/合作商/代理三条 diff_profit 流水（代理 userId 取自铺货记录）；agent 返利回写 purchase_amount 流水（T0 未达门槛零返利不产生噪音流水）；traffic 佣金回写 sale_price 流水；三模块记账均为旁路钩子（局部导入+失败隔离，记账失败不阻断原结算）；风控汇总按口径动态统计三轨道分布。

### 5.2 获利池细分结构（建议初版，全部可配置）

| 子池 | 占获利池比例 | 承接角色 | 对应现有能力 |
|---|---|---|---|
| 渠道合作分润 | 25% | 代理商/网店/酒店 | venue/agent模块（现有口径并行） |
| 生产与质量分润 | 15% | 生产工人（按环节拆分） | 生命码/溯源模块联动（P1） |
| 服务过程分润 | 15% | 人工客服、物流仓储 | 本模块新增（P0先做客服） |
| 推广引流佣金 | 10% | 推广员/达人 | traffic模块（现有5%-20%费率映射） |
| 会员回馈 | 10% | 消费返利/积分 | 钱包1%返利+积分1.5竹叶/¥1（现有） |
| 平台留存 | 25% | 平台运营/管理团队 | 支撑权责信用分考核奖金池 |

### 5.3 服务分润公式（用户示例场景：AI转人工客服）

> ✅ **决策项 D-8（已确认，2026-08-29）**：基础分润率 **1%**；满意度系数**断崖式**；**满意度确认后即时结算**；**单笔封顶¥50、月度封顶¥3000**。

**触发条件**：AI会话转人工 → 客服接单 → 用户确认解决并评分 → 满意度≥4星 且 工单关联已完成订单 → 即时结算入钱包。

```
客服本次分润 = min( 关联订单实际销售价格 × 1%
             × 满意度系数（5星=2.0 / 4星=1.2 / ≤3星=0）
             × 信用系数（按竹信分当前分数区间即时映射: L5=1.3 / L4=1.15 / L3=1.0 / L2=0.8 / L1=0）
             × 时效系数（SLA内=1.2 / 超时=0.5 / 被升级=0）, 50 )
月度累计封顶：¥3000（超出部分滚入季度考核奖励池）
```

> 实现说明：信用系数按 `level_from_score(当前竹信分)` **即时映射**，而非账户 `creditLevel` 字段——后者是 v8.0 持续天数评估结果（用于先享后付等长期权益），若直接使用会导致新升高分用户分润恒为0（等级尚未走完持续天数）。"分数即货币"原则下，分数变动即时反映到分润系数。

示例：订单实际售价¥1200，5星，L4，SLA内 → 1200×1%×2.0×1.15×1.2 = ¥33.12。

**结算与追回规则**：
- 结算时点：用户满意度确认后**即时结算**入钱包（ledger状态直接settled）；
- 退款追回：已结算分润的订单发生退款时，从该客服下月分润中**等额扣回**（不足部分记负账，累计超¥500冻结接单）；
- 封顶意义：防大额订单服务分润畸高（¥10万订单满系数理论¥3312 → 封顶¥50），防套利动机。

**对等约束（责的对价）**：
- ≤3星：0分润+信用-5分；
- 超时未响应：分润减半+信用-10分；
- 工单被升级（48h规则触发）：分润取消；
- 恶意刷满意度（AI检测）：取消分润+追回历史+清退角色。

### 5.4 生产工人分润公式（P1）

```
工人环节分润 = 关联订单实际销售价格 × 环节分润率（按工序配置，合计≤15%子池）
            × 质量系数（抽检合格1.0 / 优质1.2 / 质量事故0并追回）
```

质量追溯依托生命码扫码留痕（哪个环节、哪个工人、何时），质量事故可精确追责到人——"责"可执行的根基。

> 实现说明（2026-08-29）：
> - **环节分润率**：7工段配置合计 15%（BREW 3% / STOR 1.5% / BLEND 3% / FILL 2.5% / PACK 2% / WARE 1.5% / OUT 1.5%），常量 `STAGE_PROFIT_RATES`；
> - **工人取数**：批次须 `released`（7工段完成+瓶码绑定）方可结算；每工段取**最后一条 pass 打卡**的 `memberId`（`trace_stage_punches` 链式哈希留痕，责任粒度为工段级）；
> - **订单关联**：P1 为结算端声明式关联（显式传批次号+订单实售价）；P2 收尾已实现**自动关联**——生命码激活时携带 orderId 落码留痕（`activate_life_code` 新增 order_id 参数），`settle-auto`/`preview-auto` 按批次聚合已激活生命码的关联订单（去重）取实售价合计自动结算；
> - **幂等**：批次维度一次结算（ledgerNo=WRK-{batchNo}-{stageCode} 前缀幂等）；
> - **入账**：钱包奖励余额（未开通→pending 可重试）+ 总账 sale_price 轨道；
> - **信用对价**：premium 全员 +5 / accident 全员 -15 且零分润（按工人去重，非按工段）；pass 中性无事件；
> - **预演**：`preview` 只读计算，不写账不入钱包。

---

## 6. 数据模型（新表）

| 表 | 关键字段 | 说明 |
|---|---|---|
| role_catalog | roleCode/roleName/claimConditions/creditThreshold/quota/profitDesc/dutyTerms/status | 角色目录（7角色种子） |
| role_claims | claimId/userId/roleCode/aiPrecheck(passed/reasons/bambooScore)/status(pending/approved/rejected)/reviewer | 认领申请（AI预审：信用门槛/配额/重复拦截） |
| role_contracts | contractNo/userId/roleCode/status(probation/active/suspended/terminated)/dutyTerms/profitTerms/profitTemplateVersion/signedAt/probationEndsAt/expireAt | 权责利三合一契约实例（条款版本快照） |
| service_dispatch_records | dispatchId/sessionId/ticketNo/assigneeId/scoreDetail(信用/技能/负载/满意度四维)/mode(dispatch/grab) | 派单记录（分润依据链） |
| profit_ledger | ledgerNo/ticketNo/refNo/roleCode/userId/**basis(sale_price/diff_profit/purchase_amount)**/base/rate/coefficients/amount/payable/clawbackDeducted/deferredAmount/status(pending/settled/reversed)/sourceModule | 统一分润总账（D-7 三轨道） |
| credit_events | eventId/userId/roleCode/behaviorCode/delta/sourceModule/refId/ts | 信用行为总线事件（分发竹信分引擎） |
| **role_clawbacks** | userId:roleCode → 负账余额 | 退款追回负账（D-8.1：抵扣+冻结接单） |

所有表沿用现有存储模式（内存dict/Redis Hash，键前缀`zhuxiang:`，写操作加`lock:`分布式锁：派单`role:dispatch:{sessionId}`、结算`role:settle:{ticketNo}`、工人结算`role:worker-settle:{batchNo}`、签约`role:sign:{claimId}`）。

---

## 7. API设计（已实现 32 个端点）

**用户端（X-Member-Id，8个）**：
- `GET /api/role/catalog` 角色目录（含认领条件与收益说明）
- `POST /api/role/claim` 提交认领申请（AI预审）
- `GET /api/role/my/claims` 我的认领申请
- `POST /api/role/contracts/sign` 签署契约（三合一，签约即试用）
- `GET /api/role/my/contracts` 我的契约列表（含试用期到期标记）
- `POST /api/role/my/contracts/{id}/terminate` 主动退出
- `GET /api/role/my/earnings` 我的分润流水（含系数明细）
- `GET /api/role/my/credit-events` 我的信用行为记录

**管理端（X-Role: admin，14个）**：
- `POST /api/role/admin/catalog` 维护角色目录
- `GET /api/role/admin/claims` + `POST /api/role/admin/claims/{id}/review` 认领审批
- `GET /api/role/admin/contracts` + `POST /api/role/admin/contracts/{id}/action`（activate/suspend/terminate）
- `GET /api/role/admin/ledger` 分润总账查询（按用户/角色/**口径**/状态筛选）
- `GET /api/role/admin/risk-summary` AI风控汇总（总账/契约/负账/溢出池/口径分布）
- `POST /api/role/service-profit/reverse` 退款追回（D-8.1）
- `POST /api/role/service-profit/retry` pending流水重试入账
- `POST /api/role/admin/probation-sweep` 试用期满自动转正扫描（D-8.4）
- `POST /api/role/ledger/record` 统一记账（外部模块回写，幂等；P1）
- `POST /api/role/worker-profit/settle` 工人分润结算（P1，批次幂等）
- `GET /api/role/worker-profit/preview/{batch_no}` 工人分润预演（P1，只读）

**客服/内部（X-Role: admin/cs_staff，7个）**：
- `POST /api/role/service-profit/settle` 工单满意度确认后即时结算（幂等）
- `GET /api/role/service-profit/clawback/{user_id}` 负账余额与冻结状态查询
- `POST /api/role/dispatch` 派单调度入口（chat转人工内部调用）
- `POST /api/role/credit/event` 信用行为事件发布（服务间）
- `GET /api/role/grab/tickets` 待抢单工单池（P2）
- `POST /api/role/grab/ticket` 客服抢单（锁内防双抢，P2）

**AI监管大脑（X-Role: admin，5个，P2）**：
- `POST /api/role/ai-brain/scan/satisfaction` 满意度风险扫描（emotionScore激活）
- `POST /api/role/ai-brain/scan/anomaly` 异常分润检测（金额离群/同人高频/顶格频发）
- `POST /api/role/ai-brain/scan/credit-drop` 信用异动扫描（负向累计≥100→冻结+预警）
- `GET /api/role/ai-brain/alerts` 预警列表（类型/状态筛选）
- `POST /api/role/ai-brain/alerts/{id}/resolve` 预警处置（open→resolved）

**季度结算（X-Role: admin，1个，P2）**：
- `POST /api/role/quarterly-joint-settle` 季度联合结算（溢出池合并发放，幂等）

> 端点规划差异说明：设计稿的 `contracts/{id}/renew`（续约）与 `admin/settle/run`（手动结算批次）未在 P0/P1 实现——续约由到期复审流程覆盖（P2），结算批次由 ticket 确认钩子与三模块旁路记账替代；实际新增了设计稿未列的 reverse/retry/clawback/probation-sweep/worker-profit 等治理端点。

---

## 8. 模块合并统筹（是否合并其他模块）

**结论：物理上新建、逻辑上统辖；只合并"角色认领生命周期"这一层，其余模块保持独立、以总线对接。**

| 候选模块 | 决策 | 理由 |
|---|---|---|
| 权限AI智能管理（33号perm） | **部分吸收**：认领审批复用其逐级审批链；责任书并入三合一契约 | perm的权限树/SoD/审计已稳定运行（有完整测试），重复建设成本高；本模块在其上叠加"利"的维度 |
| 信用管理（竹信分） | **不合并**，总线对接 | 信用分计算引擎（0-1000、5级、先享后付、季度结算）刚完成v8.0且测试完备；总线只做事件分发 |
| AI智能聊天及人工聊天 | **不合并**，改造对接 | 必改三点：①转人工自动建`source=ai`工单 ②派单改调本模块调度中枢（去掉硬编码ID=1） ③满意度回传信用总线 |
| 客服管理（工单） | **不合并**，对接 | 工单状态机/SLA/满意度已完备，作为服务分润的数据源 |
| 钱包盈利 | **不合并**，入账对接 | 分润结算结果写入钱包账户，复用现有余额/流水/提现体系 |
| 代理商/酒店/网店/推广（traffic） | **不合并**，总账统一 | 各自结算口径有合同连续性（D-4已决策"以文档为准"），只统一记账与对账 |
| 后台管理 | **不合并** | 本模块管理端页面挂入现有后台框架 |

**风险提示**：前端`js/profit-sharing.js`与后端`agent_service.py`的代理商返利阶梯曾存在不一致（前端：25万门槛含20%档；后端：20万门槛15%/25%/30%纯边际累进）。✅ **决策项 D-9（已确认，2026-08-29）**：**后端为准+前端同步改造**——设计文档v2.1已按后端修订（T0-T3边际累进），P0阶段更新`profit-sharing.js`的`AGENT_REBATE`配置为T0-T3口径（20万门槛，15%/25%/30%边际累进），前端仅作展示层，不再自行维护规则常量。

---

## 9. 前端设计

- **role-hub 角色中心**（taro-app）：角色市场（目录+收益试算器）、我的契约、收益看板（分润流水+系数明细+信用趋势图）；
- **客服工作台增强**：接单队列（派单/抢单）、本次服务预估分润实时显示、满意度与信用分看板；
- **管理后台**：角色目录管理、认领审批、分润总账对账、AI风控大盘。

## 10. 测试与验收（已执行）

**测试入口**：`backend/test_role_routes.py`（asyncio 内存模式，LOCK_MODE/STORE_MODE=asyncio）

| 验收项 | 结果 | 说明 |
|---|---|---|
| 核心闭环E2E | ✅ | 会员下单→AI会话→"转人工"→自动建source=ai工单→派单高信用客服→服务→5星确认→分润¥33.12入钱包→信用+5→下次派单权重变化（全链路断言） |
| 对等性测试 | ✅ | 2星零分润+信用-5；超时分润减半；被升级分润取消 |
| 防套利测试 | ✅ | 同人自导拒绝；负账超¥500冻结派单；单笔/月度双封顶 |
| 并发测试 | ✅ | 派单锁`role:dispatch:{sessionId}`；结算幂等（ledgerNo去重）；工人结算批次幂等（前缀判重） |
| P1总账接入 | ✅ | venue三角色流水/agent进货额轨道/traffic佣金轨道/重复记账幂等/三轨道分布汇总 |
| P1工人分润 | ✅ | 预演只读、7工段份额、premium×1.2、accident零分润、未放行拒绝 |
| 回归验证 | ✅ | chat 56 / ticket 38 / credit 117 / venue 79 / agent 48 / traffic 48 / payout 48 无破坏；`import main` 验证通过 |

**累计**：93项E2E全通过（P0 63项 + P1 30项）。

## 11. 开发分期

| 阶段 | 内容 |
|---|---|
| P0 | 角色目录+契约引擎+客服服务分润闭环（含chat转人工改造：自动建工单+真实派单）+信用总线桥接（竹信分/权责信用分） | ✅ 已实现（2026-08-29，46项E2E测试通过） |
| P1 | 生产工人分润+生命码联动、统一分润总账、代理/酒店/推广模块总账接入、D-7口径对齐 | ✅ 已实现（2026-08-29，累计93项E2E+四模块回归223项通过） |
| P2 | AI监管大脑（满意度预测/异常检测/动态系数调优）、抢单模式、季度联合结算 | ✅ 已实现（2026-08-29，累计117项E2E；动态系数调优留待运行数据积累后调参） |

---

## 12. 实现交付摘要

| 交付项 | 内容 | 提交 |
|---|---|---|
| P0 主提交 | role模块四文件 + chat/ticket断链修复 + D-9前端4处口径同步（13文件，+3190行） | `eabaae8` |
| 钩子修复 | pre-commit状态机v2（嵌套模板字符串级联误报修复） | `a886da5` |
| P1 提交 | 统一总账三模块接入 + 工人分润 + P0遗留routes import补回（10文件，+795行） | `5ae4564` |
| P2 提交 | AI监管大脑（满意度预测/异常分润/信用异动+预警处置）+ 抢单模式 + 季度联合结算（32端点，117项E2E） | `f285d68` |
| P2 收尾 | 订单-批次自动关联（生命码激活回写 orderId + settle-auto/preview-auto，34端点，126项E2E） | 本次 |

**决策闭环报告**：P0 范围内 8 项决策（D-7/D-8/D-9 及细化子项）的"问题→结论→落地→验证"全过程记录，见 [AI智能管理模块P0决策闭环报告.md](AI智能管理模块P0决策闭环报告.md)。

**实现中形成的两条关键设计修正**（均已落码并留档）：
1. **信用系数即时映射**：分润系数按 `level_from_score(当前竹信分)` 计算，不用 `creditLevel` 字段（v8.0持续天数评估结果有时滞），见 §5.3 实现说明；
2. **工人信用事件按人去重**：同一工人多工段打卡，质量奖惩只计一次（premium +5 / accident -15 均按 workerId 去重），避免多工段者倍数奖惩。

---

## 附：决策项闭环总表（P0 范围 8 项，全部确认并落地）

| 编号 | 内容 | 状态 |
|---|---|---|
| D-7 | 分润基数口径：三轨道——合作商既有协议维持"差价利润60/20/20"（diff_profit），过程服务角色采用"实际销售价格×60%"子池（sale_price），代理商返利以月度进货额为基数（purchase_amount），对账分轨道核算 | ✅ 已确认+P1落地（2026-08-29） |
| D-8 | 客服服务分润参数：基础分润率1%；满意度系数断崖式（5星2.0/4星1.2/≤3星0）；信用系数L5=1.3/L4=1.15/L3=1.0/L2=0.8/L1=0；时效系数SLA内1.2/超时0.5/被升级0；满意度确认后即时结算；单笔封顶¥50、月度封顶¥3000（超出滚入季度考核奖励池）；退款等额追回 | ✅ 已确认（2026-08-29） |
| D-8.1 | 退款追回机制：订单退款→流水置reversed（幂等）；追回额记入负账（role_clawbacks）；下月（及以后）结算先抵扣负账（payable=应得−抵扣）再入钱包；负账累计>¥500冻结接单（派单候选剔除） | ✅ 已实现（2026-08-29细化） |
| D-8.2 | 月度封顶溢出：超月度额度部分记 deferredAmount 滚入季度考核奖励池（风控汇总统计 deferredPoolTotal），不丢弃 | ✅ 已实现（2026-08-29细化） |
| D-8.3 | 同人自导防套利：客服与下单会员同人时拒绝结算（settle 时校验 handlerId≠userId） | ✅ 已实现（2026-08-29细化） |
| D-8.4 | 试用期满自动转正（probation-sweep）：probationEndsAt 已过且近30天无负面信用事件→自动active；有负面→保持试用留人工处置 | ✅ 已实现（2026-08-29细化） |
| D-9 | 代理商返利阶梯：后端/文档v2.1为准（T0-T3，¥20万门槛，15%/25%/30%边际累进）；P0同步改造前端profit-sharing.js，前端改纯展示层 | ✅ 已确认（2026-08-29） |
| D-9.1 | 前端残留清理：agent-upgrade-service.js、main.js、mock-test-upgrade.js 三处 calculateRebate 旧口径（25万门槛/全额15%/20%档）同步为 T0-T3；升级门槛体系（市级¥25万/核心¥50万）属等级升迁逻辑，不在返利口径范围、保持不变 | ✅ 已实现（2026-08-29细化） |
