# 竹香酒官网 · 物流接口管理模块 - 数据库表结构设计

> 适配项目既有「内存 dict + Redis Hash」双模式存储，对齐 wallet/payment 模块风格。
> 设计依据：[物流接口管理模块设计文档.md](file:///d:/网站架构设计/物流接口管理模块设计文档.md)
> 开发分期：第一期 MVP（P0 核心功能）+ 第二期（P1 多物流/AI 增强）

---

## 一、表清单与优先级

| 优先级 | 表名 | 用途 | 锁键 | Redis Key 模式 |
|:---:|------|------|------|------|
| P0 | `logistics_orders` | 物流订单（下单/状态流转/面单） | `logistics:order:{waybillNo}` | `Hash logistics:order:{waybillNo}` |
| P0 | `logistics_tracks` | 物流轨迹（轨迹追踪） | - | `List logistics:track:{waybillNo}` |
| P0 | `logistics_settlement` | 物流结算（月结对账） | `logistics:settle:lock:{period}:{carrier}` | `String(JSON) logistics:settle:{settleNo}` |
| P1 | `logistics_carriers` | 物流商配置（费率/限额/启停） | - | `Hash logistics:carrier:{code}` |
| P1 | `logistics_callbacks` | 回调日志（幂等记录） | - | `String(JSON) logistics:callback:{carrier}:{callbackId}` |
| P2 | `logistics_claims` | 物流理赔 | `logistics:claim:{claimNo}` | `String(JSON) logistics:claim:{claimNo}` |

### 说明
- **P0**（第一期 MVP）：覆盖下单/轨迹/签收/结算闭环，仅对接顺丰单物流商
- **P1**（第二期）：多物流商接入（京东/货拉拉/德邦/圆通）+ 回调幂等记录
- **P2**（第三期）：理赔流程（在线理赔）
- AI 增强表（ai_routing / ai_track / ai_receipt 等）属于能力叠加层，不在数据访问层 MVP 范围内，后续以独立文档补充

---

## 二、核心表结构

### 2.1 logistics_orders（物流订单表，P0）

**业务定位**：记录每一笔物流订单的下单、状态流转、面单、签收信息

**状态机**：
```
pending(待下单) → booked(已下单) → picked(已揽收) → transporting(运输中)
              → delivering(派送中) → signed(已签收)
              → failed(投递失败) → returned(已退回)
```

**字段设计**：

| 字段 | 类型 | 说明 |
|------|------|------|
| waybillNo | str (PK) | 运单号（物流商返回，唯一） |
| orderId | str | 关联竹香酒订单号 |
| orderType | str | 订单类型 retail/groupbuy/return |
| carrier | str | 物流商编码 SF/JD/LLL/DB/YT |
| carrierName | str | 物流商名称 |
| serviceType | str | 服务类型（如 顺丰特快/标准） |
| senderName/Phone/Address | str | 寄件人信息 |
| receiverName/Phone/Address | str | 收件人信息 |
| province/city | str | 省市（用于路由） |
| weight/volume/pieceCount | float/int | 重量kg/体积m³/件数 |
| insuredValue | float | 保价金额 |
| baseFee/insuredFee/packageFee/extraFee | float | 费用构成 |
| discount | float | 折扣率（1.0=无折扣） |
| totalFee | float | 总运费 |
| settleMode | str | 结算模式 monthly/cash/prepaid |
| status | str | 状态（见状态机） |
| labelUrl | str | 电子面单 URL |
| pickupTime | str | 揽收时间 |
| signedTime | str | 签收时间 |
| signerName | str | 签收人 |
| signType | str | 签收方式（本人/代收/驿站） |
| signPhoto | str | 签收照片 URL |
| signLocation | str | 签收 GPS |
| createdAt/updatedAt | str | 时间戳 |

**索引**：
- `logistics:order:index:orderId:{orderId}` → waybillNo（按订单号反查运单）
- `logistics:order:index:status:{status}` → Set（按状态筛选）

**锁**：`logistics:order:{waybillNo}`（状态流转保护）

---

### 2.2 logistics_tracks（物流轨迹表，P0）

**业务定位**：记录每个运单的轨迹流水，支持用户端查询与异常监控

**字段设计**：

| 字段 | 类型 | 说明 |
|------|------|------|
| trackId | str (PK) | 轨迹记录ID（自增） |
| waybillNo | str | 运单号 |
| carrier | str | 物流商 |
| trackStatus | str | 原始状态（物流商返回） |
| unifiedStatus | str | 统一状态（映射到状态机） |
| description | str | 轨迹描述 |
| location | str | 所在城市 |
| operator | str | 操作人 |
| trackTime | str | 轨迹时间 |
| createdAt | str | 记录时间 |

**存储模式**：
- 内存：`_mock_store["logistics_tracks"]` 字典，key=trackId
- Redis：`List logistics:track:{waybillNo}`（LPUSH 新轨迹，保留最新 50 条）

**索引**：
- `logistics:track:index:waybill:{waybillNo}` → List（按运单号查轨迹列表，按时间倒序）

---

### 2.3 logistics_settlement（物流结算表，P0）

**业务定位**：月结对账，按账期+物流商维度生成结算单

**状态机**：
```
pending(待对账) → reconciling(对账中) → confirmed(已确认) → paid(已付款)
                                   → diff(有差异) → investigating(调查中) → resolved(已处理) → confirmed
```

**字段设计**：

| 字段 | 类型 | 说明 |
|------|------|------|
| settleNo | str (PK) | 结算单号 SETTLE+YYYYMM+carrier |
| carrier | str | 物流商 |
| period | str | 账期 YYYY-MM |
| totalOrders | int | 总单数 |
| totalWeight | float | 总重量 |
| baseFeeTotal | float | 基础运费合计 |
| insuredTotal | float | 保价费合计 |
| packageTotal | float | 包装费合计 |
| extraTotal | float | 附加费合计 |
| subtotal | float | 小计 |
| discountAmount | float | 折扣金额 |
| payableAmount | float | 应付金额 |
| status | str | 状态（见状态机） |
| diffDetails | list | 差异明细（金额不符/单据缺失） |
| confirmTime | str | 确认时间 |
| payTime | str | 付款时间 |
| createdAt/updatedAt | str | 时间戳 |

**锁**：`logistics:settle:lock:{period}:{carrier}`（防并发对账同一账期同一物流商）

---

### 2.4 logistics_carriers（物流商配置表，P1）

**业务定位**：多物流商配置（费率/限额/启停），支持智能路由选择

**字段设计**：

| 字段 | 类型 | 说明 |
|------|------|------|
| code | str (PK) | 物流商编码 SF/JD/LLL/DB/YT |
| name | str | 物流商名称 |
| carrierType | str | 类型 direct（直连）/aggregate（聚合） |
| status | str | 状态 active/maintenance/disabled |
| supportedServices | list | 支持的服务类型 |
| supportedScenes | list | 支持的业务场景 retail/groupbuy/return |
| apiEndpoint | str | API 地址 |
| appId/appKey/appSecret | str | 鉴权信息 |
| feeRate | float | 费率（折扣 0.6-1.0） |
| minWeight/maxWeight | float | 重量限制 |
| maxInsured | float | 最大保价 |
| dailyLimit | float | 单日发件限额 |
| monthlyLimit | float | 单月发件限额 |
| dailyCount/monthlyCount | int | 累计发件数 |
| retryMax/timeout | int | 重试次数/超时秒数 |
| createdAt/updatedAt | str | 时间戳 |

---

### 2.5 logistics_callbacks（回调日志表，P1）

**业务定位**：记录物流商回调日志，实现幂等性

**字段设计**：

| 字段 | 类型 | 说明 |
|------|------|------|
| callbackId | str (PK) | 回调ID（物流商返回，唯一） |
| carrier | str | 物流商 |
| callbackType | str | 回调类型 track/sign/exception |
| waybillNo | str | 运单号 |
| payload | dict | 回调原始数据 |
| processedAt | str | 处理时间 |
| status | str | 处理状态 success/duplicate/failed |
| createdAt | str | 记录时间 |

**幂等机制**：`logistics:callback:{carrier}:{callbackId}` SETNX + TTL 7 天

---

### 2.6 logistics_claims（物流理赔表，P2）

**业务定位**：物流理赔流程（破损/丢失/延误/污损）

**状态机**：
```
pending(待审核) → auditing(审核中) → approved(已批准) → compensated(已赔付)
                                 → rejected(已驳回)
```

**字段设计**：

| 字段 | 类型 | 说明 |
|------|------|------|
| claimNo | str (PK) | 理赔单号 CLAIM+YYYYMMDD+seq |
| waybillNo | str | 运单号 |
| orderId | str | 关联订单 |
| carrier | str | 物流商 |
| claimType | str | 理赔类型 broken/lost/delay/dirty |
| claimAmount | float | 理赔金额 |
| evidenceUrls | list | 证据照片 URL |
| description | str | 理赔说明 |
| status | str | 状态（见状态机） |
| compensateAmount | float | 实际赔付金额 |
| compensateTime | str | 赔付时间 |
| auditor | str | 审核人 |
| auditRemark | str | 审核备注 |
| createdAt/updatedAt | str | 时间戳 |

**锁**：`logistics:claim:{claimNo}`（审核流程保护）

---

## 三、编号规则

| 前缀 | 格式 | 示例 | 用途 |
|------|------|------|------|
| SF | 物流商返回 | SF1234567890 | 运单号（由物流商生成） |
| SETTLE | `SETTLE{YYYYMM}{carrier}` | SETTLE202608SF | 结算单号 |
| CLAIM | `CLAIM{YYYYMMDD}{seq:04d}` | CLAIM202608220001 | 理赔单号 |
| TRACK | `TRACK{seq:08d}` | TRACK00000001 | 轨迹记录ID |

---

## 四、与既有模块关联

| 关联模块 | 物流模块提供的能力 |
|------|------|
| 订单管理 | 订单 PAID → 调用物流下单 → SHIPPED（物流签收后触发订单签收） |
| 收款管理 | 物流结算付款关联 `payment_payouts`（payoutType=logistics_settle） |
| 代理商管理 | 代理商发货调用物流下单（关联 agent_purchase 订单） |
| 财务管理 | 物流结算确认后生成凭证 |
| 钱包盈利 | 物流签收触发奖品发货（奖品领取流程的 ship 阶段） |

---

## 五、并发安全设计

### 5.1 锁键清单

| 场景 | 锁键 | 说明 |
|------|------|------|
| 物流下单 | `logistics:order:{waybillNo}` | 防止重复下单（同订单多次调用） |
| 状态流转 | `logistics:order:{waybillNo}` | 状态机保护（防止并发状态变更） |
| 月结对账 | `logistics:settle:lock:{period}:{carrier}` | 防止并发对账同一账期 |
| 理赔审核 | `logistics:claim:{claimNo}` | 审核流程保护 |

### 5.2 幂等性

- **物流下单**：同 orderId 只能创建一个未关闭运单（`find_active_by_order`）
- **轨迹回调**：`callbackId` 唯一索引 + SETNX 锁（防重复回调）
- **签收回调**：已 signed 状态直接返回（锁内重新校验）
- **结算确认**：已 confirmed/paid 状态拒绝重复确认

---

## 六、开发优先级

### 第一期 MVP（P0，本期实现）
1. `logistics_orders` 物流订单表（下单/状态流转/面单/签收）
2. `logistics_tracks` 物流轨迹表（轨迹追踪）
3. `logistics_settlement` 物流结算表（月结对账）

### 第二期（P1，后续迭代）
4. `logistics_carriers` 物流商配置表（多物流商接入）
5. `logistics_callbacks` 回调日志表（幂等记录）

### 第三期（P2，后续迭代）
6. `logistics_claims` 物流理赔表（理赔流程）
