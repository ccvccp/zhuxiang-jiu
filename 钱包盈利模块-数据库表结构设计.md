# 竹香酒官网 · 钱包盈利模块 - 数据库表结构设计

> 版本：v1.0
> 日期：2026-08-21
> 适用：竹香酒官网后端（4层架构 core/repositories/services/routes）
> 关联文档：钱包盈利模块设计文档.md
> 存储模式：双模式（内存 dict + Redis Hash），遵循项目既有 Repository Pattern

---

## 一、设计原则与对齐既有架构

### 1.1 存储模式对齐

本模块严格遵循项目既有的双模式存储架构（与 member/order/agent/finance 模块一致）：

| 模式 | 环境变量 | 内存后端 | Redis 后端 | 适用场景 |
|------|------|------|------|------|
| 内存模式 | `STORE_MODE=asyncio`（默认） | `_mock_store["wallet_*"]` 字典 | 不使用 | 开发/测试/单进程 |
| Redis 模式 | `STORE_MODE=redis` | 不使用 | Redis Hash/String/Set | 生产/多进程 |

### 1.2 4层架构职责

| 层 | 文件 | 职责 |
|------|------|------|
| repositories | `repositories/wallet_repository.py` | 双模式数据访问（内存/Redis 透明切换） |
| services | `services/wallet_service.py` | 业务逻辑（锁/事务/规则校验/收益计算） |
| routes | `routes/wallet_routes.py` | HTTP 路由（参数校验/调用 service/异常转换） |
| core | `core/locks.py`（复用） | 分布式锁（`wallet:{userId}`） |

### 1.3 并发安全（锁键设计）

遵循项目约定 `lock:{original_key}` 格式：

| 锁键 | 保护场景 | 持锁操作 |
|------|------|------|
| `wallet:{userId}` | 钱包余额 RMW | 充值/提现/消费/退款/收益发放/转定期 |
| `wallet:withdraw:{withdrawNo}` | 提现审核 | 大额提现审批（防重复审核） |
| `wallet:deposit:{depositNo}` | 定期结清 | 定期到期取出/提前取出（防重复结清） |

> 注：锁通过 `core.locks.get_lock(key)` 获取，自动适配 asyncio.Lock（单进程）/ Redis Lock（多进程）。

---

## 二、P0 核心表结构（第一期 MVP）

### 2.1 wallet_accounts（钱包账户表）

**用途**：钱包开通 + 活期钱包余额管理（P0 核心）

#### 字段定义

| 字段 | 类型（SQL 参考） | 内存类型 | 说明 |
|------|------|------|------|
| user_id | BIGINT PK | int | 用户ID（关联会员 member.id） |
| status | VARCHAR(20) | str | 状态：`pending`(待开通) / `active`(正常) / `frozen`(冻结) / `closed`(注销) |
| balance | DECIMAL(12,2) DEFAULT 0 | float | 活期余额（可消费/可提现） |
| frozen_amount | DECIMAL(12,2) DEFAULT 0 | float | 冻结金额（提现申请中冻结） |
| total_deposit | DECIMAL(12,2) DEFAULT 0 | float | 累计充值 |
| total_withdraw | DECIMAL(12,2) DEFAULT 0 | float | 累计提现 |
| total_interest | DECIMAL(12,2) DEFAULT 0 | float | 累计余额收益 |
| total_reward | DECIMAL(12,2) DEFAULT 0 | float | 累计奖品价值 |
| total_rebate | DECIMAL(12,2) DEFAULT 0 | float | 累计消费返利 |
| pending_interest | DECIMAL(12,2) DEFAULT 0 | float | 待发放收益（本月累计，月末入账） |
| opened_at | DATETIME | str | 开通时间（ISO 8601） |
| closed_at | DATETIME | str | 注销时间 |
| created_at | DATETIME DEFAULT NOW() | str | 创建时间 |
| updated_at | DATETIME | str | 更新时间 |

#### Redis 存储结构

```
Hash  zhuxiang:wallet:{userId}
    user_id          → "1"
    status           → "active"
    balance          → "2856.00"
    frozen_amount    → "0.00"
    total_deposit    → "5000.00"
    total_withdraw   → "2144.00"
    total_interest   → "186.00"
    total_reward     → "536.00"
    total_rebate     → "13.20"
    pending_interest → "6.90"
    opened_at        → "2026-08-21T00:00:00+00:00"
    closed_at        → ""
    created_at       → "2026-08-21T00:00:00+00:00"
    updated_at       → "2026-08-21T08:00:00+00:00"

Set   zhuxiang:wallet:index        # 全部钱包用户ID集合（list_all 用）
```

#### 内存模式结构（`_mock_store["wallets"]`）

```python
_mock_store["wallets"] = {
    1: {
        "userId": 1,
        "status": "active",
        "balance": 2856.00,
        "frozenAmount": 0.00,
        "totalDeposit": 5000.00,
        "totalWithdraw": 2144.00,
        "totalInterest": 186.00,
        "totalReward": 536.00,
        "totalRebate": 13.20,
        "pendingInterest": 6.90,
        "openedAt": "2026-08-21T00:00:00+00:00",
        "closedAt": "",
        "createdAt": "2026-08-21T00:00:00+00:00",
        "updatedAt": "2026-08-21T08:00:00+00:00",
    },
}
```

---

### 2.2 wallet_transactions（钱包交易记录表）

**用途**：记录所有资金流水（充值/提现/消费/退款/余额收益/返利），P0 核心

#### 字段定义

| 字段 | 类型（SQL 参考） | 内存类型 | 说明 |
|------|------|------|------|
| tx_no | VARCHAR(32) PK UNIQUE | str | 交易编号（格式：`WT{YYYYMMDD}{seq:04d}`） |
| user_id | BIGINT NOT NULL | int | 用户ID |
| type | VARCHAR(20) NOT NULL | str | 类型：`deposit`(充值) / `withdraw`(提现) / `consume`(消费) / `refund`(退款) / `interest`(余额收益) / `rebate`(消费返利) / `transfer_regular`(转定期) |
| direction | VARCHAR(10) NOT NULL | str | 方向：`IN`(入账) / `OUT`(出账) |
| amount | DECIMAL(12,2) NOT NULL | float | 金额（正数） |
| balance_after | DECIMAL(12,2) | float | 交易后活期余额（便于审计对账） |
| pay_channel | VARCHAR(20) | str | 支付渠道（充值时）：`alipay` / `wechat` / `bank` |
| order_id | VARCHAR(32) | str | 关联订单号（消费/退款时） |
| deposit_no | VARCHAR(32) | str | 关联定期编号（转定期/定期余额收益时） |
| withdraw_no | VARCHAR(32) | str | 关联提现单号（提现时） |
| status | VARCHAR(20) NOT NULL | str | 状态：`processing`(处理中) / `success`(成功) / `failed`(失败) |
| description | VARCHAR(500) | str | 描述（用户可见） |
| created_at | DATETIME DEFAULT NOW() | str | 创建时间 |

#### Redis 存储结构

```
String(JSON)  zhuxiang:wallet:tx:{txNo}           # 交易记录详情
Set            zhuxiang:wallet:tx:index:{userId}    # 用户交易编号集合（按用户查询）
String         zhuxiang:wallet:tx:seq               # 交易编号自增序列（INCR）
```

#### 内存模式结构（`_mock_store["wallet_transactions"]`）

```python
_mock_store["wallet_transactions"] = {
    "WT202608210001": {
        "txNo": "WT202608210001",
        "userId": 1,
        "type": "deposit",
        "direction": "IN",
        "amount": 5000.00,
        "balanceAfter": 5000.00,
        "payChannel": "alipay",
        "orderId": "",
        "depositNo": "",
        "withdrawNo": "",
        "status": "success",
        "description": "支付宝充值 ¥5000.00",
        "createdAt": "2026-08-21T08:00:00+00:00",
    },
}
_mock_store["_wallet_tx_seq"] = 0  # 交易编号序列
```

#### 交易类型与方向对照

| type | direction | 说明 | balance 影响 |
|------|:---:|------|------|
| `deposit` | IN | 充值 | +amount |
| `withdraw` | OUT | 提现 | -amount |
| `consume` | OUT | 消费支付 | -amount |
| `refund` | IN | 订单退款 | +amount |
| `interest` | IN | 余额收益发放 | +amount |
| `rebate` | IN | 消费返利 | +amount |
| `transfer_regular` | OUT | 活期转定期 | -amount |

---

### 2.3 wallet_withdrawals（提现记录表）

**用途**：提现申请 + 审核流程（大额需人工审批，P0 核心）

#### 字段定义

| 字段 | 类型（SQL 参考） | 内存类型 | 说明 |
|------|------|------|------|
| withdraw_no | VARCHAR(32) PK UNIQUE | str | 提现编号（格式：`WD{YYYYMMDD}{seq:04d}`） |
| user_id | BIGINT NOT NULL | int | 用户ID |
| amount | DECIMAL(12,2) NOT NULL | float | 提现金额 |
| fee | DECIMAL(10,2) DEFAULT 0 | float | 手续费（活期免费，定期提前取出 1%） |
| actual_amount | DECIMAL(12,2) NOT NULL | float | 实际到账金额（= amount - fee） |
| source | VARCHAR(20) NOT NULL | str | 来源：`current`(活期) / `regular_early`(定期提前取出) |
| deposit_no | VARCHAR(32) | str | 关联定期编号（提前取出时） |
| loss_interest | DECIMAL(10,2) DEFAULT 0 | float | 损失余额收益（定期提前取出时） |
| loss_reward | BOOLEAN DEFAULT FALSE | bool | 是否损失奖品（定期提前取出时） |
| pay_channel | VARCHAR(20) | str | 提现渠道：`bank` / `alipay` / `wechat` |
| bank_account | VARCHAR(50) | str | 银行账号（脱敏存储） |
| status | VARCHAR(20) NOT NULL | str | 状态：`pending`(待审核) / `approving`(审核中) / `approved`(已批准) / `paid`(已打款) / `rejected`(已拒绝) / `failed`(失败) |
| auditor | VARCHAR(50) | str | 审核人 |
| audit_remark | VARCHAR(200) | str | 审核备注 |
| pay_time | DATETIME | str | 打款时间 |
| created_at | DATETIME DEFAULT NOW() | str | 创建时间 |
| updated_at | DATETIME | str | 更新时间 |

#### Redis 存储结构

```
String(JSON)  zhuxiang:wallet:withdraw:{withdrawNo}        # 提现记录详情
Set            zhuxiang:wallet:withdraw:index:{userId}      # 用户提现编号集合
Set            zhuxiang:wallet:withdraw:pending             # 待审核提现集合（管理端用）
String         zhuxiang:wallet:withdraw:seq                 # 提现编号自增序列
```

#### 内存模式结构（`_mock_store["wallet_withdrawals"]`）

```python
_mock_store["wallet_withdrawals"] = {
    "WD202608210001": {
        "withdrawNo": "WD202608210001",
        "userId": 1,
        "amount": 1000.00,
        "fee": 0.00,
        "actualAmount": 1000.00,
        "source": "current",
        "depositNo": "",
        "lossInterest": 0.00,
        "lossReward": False,
        "payChannel": "bank",
        "bankAccount": "****1234",
        "status": "pending",
        "auditor": "",
        "auditRemark": "",
        "payTime": "",
        "createdAt": "2026-08-21T08:00:00+00:00",
        "updatedAt": "2026-08-21T08:00:00+00:00",
    },
}
_mock_store["_wallet_withdraw_seq"] = 0  # 提现编号序列
```

#### 提现审核规则

| 提现金额 | 审核方式 | 到账时间 | 手续费 |
|:---:|------|:---:|:---:|
| < ¥5,000 | 自动通过（AI 风控） | T+0（2小时内） | 免费 |
| ¥5,000 - ¥50,000 | 人工审核（一级） | T+1 | 免费 |
| > ¥50,000 | 人工审核（二级） | T+1 | 免费 |
| 定期提前取出 | 人工审核 | T+1 | 1%（损失余额收益+奖品） |

---

## 三、P1 扩展表结构（第二期）

### 3.1 wallet_deposits（定期预付表）

**用途**：定期钱包存入/到期/提前取出管理（P1 核心）

#### 字段定义

| 字段 | 类型（SQL 参考） | 内存类型 | 说明 |
|------|------|------|------|
| deposit_no | VARCHAR(32) PK UNIQUE | str | 预付款编号（格式：`DP{YYYYMMDD}{seq:04d}`） |
| user_id | BIGINT NOT NULL | int | 用户ID |
| amount | DECIMAL(12,2) NOT NULL | float | 预付款金额 |
| period | INT NOT NULL | int | 存期（月）：3 / 6 / 12 / 24 |
| annual_rate | DECIMAL(4,2) NOT NULL | float | 年化收益率：3.0 / 5.0 / 3.0 / 3.5 |
| expected_interest | DECIMAL(12,2) | float | 预计余额收益（= amount × annual_rate） |
| reward_type | VARCHAR(50) | str | 奖品类型（存期达标才有） |
| reward_value | DECIMAL(10,2) | float | 奖品价值 |
| start_date | DATE NOT NULL | str | 存入日期 |
| end_date | DATE NOT NULL | str | 到期日期 |
| status | VARCHAR(20) NOT NULL | str | 状态：`active`(存入中) / `matured`(已到期) / `settled`(已取出) / `early_settled`(提前取出) |
| auto_renew | BOOLEAN DEFAULT FALSE | bool | 是否自动续存 |
| settled_at | DATETIME | str | 结清时间 |
| settled_interest | DECIMAL(12,2) DEFAULT 0 | float | 实际发放余额收益（提前取出时按比例） |
| created_at | DATETIME DEFAULT NOW() | str | 创建时间 |
| updated_at | DATETIME | str | 更新时间 |

#### Redis 存储结构

```
String(JSON)  zhuxiang:wallet:deposit:{depositNo}          # 定期记录详情
Set            zhuxiang:wallet:deposit:index:{userId}       # 用户定期编号集合
String         zhuxiang:wallet:deposit:seq                 # 定期编号自增序列
```

#### 内存模式结构（`_mock_store["wallet_deposits"]`）

```python
_mock_store["wallet_deposits"] = {
    "DP202608210001": {
        "depositNo": "DP202608210001",
        "userId": 1,
        "amount": 5000.00,
        "period": 12,
        "annualRate": 3.0,
        "expectedInterest": 150.00,
        "rewardType": "竹香经典 45° 500ml × 2",
        "rewardValue": 536.00,
        "startDate": "2026-08-21",
        "endDate": "2027-08-21",
        "status": "active",
        "autoRenew": False,
        "settledAt": "",
        "settledInterest": 0.00,
        "createdAt": "2026-08-21T08:00:00+00:00",
        "updatedAt": "2026-08-21T08:00:00+00:00",
    },
}
_mock_store["_wallet_deposit_seq"] = 0  # 定期编号序列
```

#### 定期档位规则（常量定义在 service 层）

| 存期（月） | 最低存入 | 年化收益率 | 奖品资格 | 提前取出 |
|:---:|:---:|:---:|:---:|:---:|
| 3 | ¥1,000 | 3% | ❌ 无 | 损失余额收益 |
| 6 | ¥2,000 | 5% | ✅ 翠竹小酌×1 | 损失余额收益 |
| 12 | ¥5,000 | 3% | ✅ 竹香经典×2 | 损失余额收益+奖品 |
| 24 | ¥10,000 | 3.5% | ✅ 竹香典藏×2 | 损失余额收益+奖品 |

---

### 3.2 wallet_rewards（奖品领取表）

**用途**：定期达标奖品领取/发货/签收管理（P1）

#### 字段定义

| 字段 | 类型（SQL 参考） | 内存类型 | 说明 |
|------|------|------|------|
| reward_no | VARCHAR(32) PK UNIQUE | str | 奖品编号（格式：`RW{YYYYMMDD}{seq:04d}`） |
| user_id | BIGINT NOT NULL | int | 用户ID |
| deposit_no | VARCHAR(32) | str | 关联定期预付编号 |
| reward_name | VARCHAR(200) NOT NULL | str | 奖品名称 |
| reward_items | TEXT | str(JSON) | 奖品明细（JSON 数组） |
| reward_value | DECIMAL(10,2) | float | 奖品价值 |
| address_id | BIGINT | int | 收货地址ID（关联 member_addresses） |
| waybill_no | VARCHAR(50) | str | 运单号 |
| status | VARCHAR(20) NOT NULL | str | 状态：`claimable`(可领取) / `claimed`(已领取) / `shipped`(已发货) / `signed`(已签收) / `expired`(已过期) |
| claim_deadline | DATETIME | str | 领取截止时间（到期后 30 天） |
| claimed_at | DATETIME | str | 领取时间 |
| shipped_at | DATETIME | str | 发货时间 |
| signed_at | DATETIME | str | 签收时间 |
| created_at | DATETIME DEFAULT NOW() | str | 创建时间 |
| updated_at | DATETIME | str | 更新时间 |

#### Redis 存储结构

```
String(JSON)  zhuxiang:wallet:reward:{rewardNo}            # 奖品记录详情
Set            zhuxiang:wallet:reward:index:{userId}       # 用户奖品编号集合
Set            zhuxiang:wallet:reward:claimable            # 可领取奖品集合（通知用）
String         zhuxiang:wallet:reward:seq                 # 奖品编号自增序列
```

#### 内存模式结构（`_mock_store["wallet_rewards"]`）

```python
_mock_store["wallet_rewards"] = {
    "RW202608210001": {
        "rewardNo": "RW202608210001",
        "userId": 1,
        "depositNo": "DP202608210001",
        "rewardName": "竹香经典 45° 500ml × 2 瓶",
        "rewardItems": [
            {"name": "竹香经典 45° 500ml", "quantity": 2, "unitPrice": 268.00}
        ],
        "rewardValue": 536.00,
        "addressId": 0,
        "waybillNo": "",
        "status": "claimable",
        "claimDeadline": "2027-09-20T00:00:00+00:00",
        "claimedAt": "",
        "shippedAt": "",
        "signedAt": "",
        "createdAt": "2026-08-21T08:00:00+00:00",
        "updatedAt": "2026-08-21T08:00:00+00:00",
    },
}
_mock_store["_wallet_reward_seq"] = 0  # 奖品编号序列
```

---

## 四、_mock_store 完整结构汇总

在 `repositories/store.py` 中扩展 `_mock_store`：

```python
_mock_store: dict = {
    # ... 既有字段（agents/members/orders/products/...）保持不变 ...

    # ===== 钱包盈利模块（新增） =====
    # P0 核心
    "wallets": {},                    # user_id -> 钱包账户 dict
    "wallet_transactions": {},        # tx_no -> 交易记录 dict
    "wallet_withdrawals": {},         # withdraw_no -> 提现记录 dict
    # P1 扩展
    "wallet_deposits": {},            # deposit_no -> 定期记录 dict
    "wallet_rewards": {},             # reward_no -> 奖品记录 dict
    # 自增序列
    "_wallet_tx_seq": 0,              # 交易编号序列
    "_wallet_withdraw_seq": 0,        # 提现编号序列
    "_wallet_deposit_seq": 0,         # 定期编号序列
    "_wallet_reward_seq": 0,          # 奖品编号序列
}
```

---

## 五、Redis Key 结构汇总

遵循项目前缀约定 `zhuxiang:`：

### 5.1 P0 核心 Keys

| Redis Key 模式 | 类型 | 说明 |
|------|:---:|------|
| `zhuxiang:wallet:{userId}` | Hash | 钱包账户主信息 |
| `zhuxiang:wallet:index` | Set | 全部钱包用户ID集合（list_all 用） |
| `zhuxiang:wallet:tx:{txNo}` | String(JSON) | 交易记录详情 |
| `zhuxiang:wallet:tx:index:{userId}` | Set | 用户交易编号集合 |
| `zhuxiang:wallet:tx:seq` | String | 交易编号自增序列（INCR） |
| `zhuxiang:wallet:withdraw:{withdrawNo}` | String(JSON) | 提现记录详情 |
| `zhuxiang:wallet:withdraw:index:{userId}` | Set | 用户提现编号集合 |
| `zhuxiang:wallet:withdraw:pending` | Set | 待审核提现集合（管理端用） |
| `zhuxiang:wallet:withdraw:seq` | String | 提现编号自增序列 |

### 5.2 P1 扩展 Keys

| Redis Key 模式 | 类型 | 说明 |
|------|:---:|------|
| `zhuxiang:wallet:deposit:{depositNo}` | String(JSON) | 定期记录详情 |
| `zhuxiang:wallet:deposit:index:{userId}` | Set | 用户定期编号集合 |
| `zhuxiang:wallet:deposit:seq` | String | 定期编号自增序列 |
| `zhuxiang:wallet:reward:{rewardNo}` | String(JSON) | 奖品记录详情 |
| `zhuxiang:wallet:reward:index:{userId}` | Set | 用户奖品编号集合 |
| `zhuxiang:wallet:reward:claimable` | Set | 可领取奖品集合 |
| `zhuxiang:wallet:reward:seq` | String | 奖品编号自增序列 |

---

## 六、编号生成规则

| 业务 | 前缀 | 格式 | 示例 |
|------|:---:|------|------|
| 交易编号 | `WT` | `WT{YYYYMMDD}{seq:04d}` | `WT202608210001` |
| 提现编号 | `WD` | `WD{YYYYMMDD}{seq:04d}` | `WD202608210001` |
| 定期编号 | `DP` | `DP{YYYYMMDD}{seq:04d}` | `DP202608210001` |
| 奖品编号 | `RW` | `RW{YYYYMMDD}{seq:04d}` | `RW202608210001` |

> 序列通过 Redis `INCR` 或内存 `_wallet_*_seq` 自增，保证唯一性。

---

## 七、收益计算规则（常量，定义在 service 层）

### 7.1 活期收益

| 参数 | 值 | 说明 |
|------|:---:|------|
| 年化收益率 | 3% | 基础收益 |
| 计息方式 | 日计收益 | `daily_interest = balance × 3% / 365` |
| 发放方式 | 按月发放 | 月末将 `pending_interest` 入账到 `balance` |
| 最小计息单位 | ¥0.01 | 向下取整到分 |

### 7.2 定期收益

| 存期 | 年化收益率 | 计算公式 |
|:---:|:---:|------|
| 3 个月 | 3% | `interest = amount × 3% × (3/12)` |
| 6 个月 | 5% | `interest = amount × 5% × (6/12)` |
| 12 个月 | 3% | `interest = amount × 3% × 1` |
| 24 个月 | 3.5% | `interest = amount × 3.5% × 2` |

### 7.3 LPR 合规校验

| 参数 | 值 | 说明 |
|------|:---:|------|
| LPR 利率 | 3.45%（当前） | 1 年期 LPR |
| 法定上限 | 13.8%（LPR × 4） | 民间借贷利率上限 |
| 综合收益率校验 | `(余额收益 + 奖品价值) / 本金 ≤ 13.8%` | 超限自动降档 |

### 7.4 消费返利

| 参数 | 值 | 说明 |
|------|:---:|------|
| 返利率 | 1% | 钱包支付额外返现 |
| 返利上限 | ¥100/单 | 单笔返利上限 |
| 发放方式 | 即时入账 | 返利金额即时加到 `balance` |

---

## 八、状态机定义

### 8.1 钱包账户状态

```
pending(待开通) ──开通──→ active(正常) ──冻结──→ frozen(冻结)
                              ↑                   │
                              └──解冻──────────────┘
                              │
                              └──注销──→ closed(注销)
```

### 8.2 提现状态

```
pending(待审核) ──提交──→ approving(审核中)
                              │
                   ┌──────────┴──────────┐
                   │                     │
              approved(已批准)        rejected(已拒绝)
                   │
              paid(已打款)

< ¥5000: pending ──自动通过──→ paid（跳过人工审核）
```

### 8.3 定期状态

```
active(存入中) ──到期──→ matured(已到期) ──取出──→ settled(已取出)
      │
      └──提前取出──→ early_settled(提前取出)
```

### 8.4 奖品状态

```
claimable(可领取) ──领取──→ claimed(已领取) ──发货──→ shipped(已发货) ──签收──→ signed(已签收)
      │
      └──逾期──→ expired(已过期)
```

---

## 九、与既有模块关联

### 9.1 关联会员模块（member）

- `wallet_accounts.user_id` → `members.id`
- 钱包开通前置条件：会员等级 ≥ L2，消费 ≥ ¥500
- 收货地址复用 `member_addresses`（奖品发货用）

### 9.2 关联订单模块（order）

- `wallet_transactions.order_id` → `orders.order_no`
- 钱包消费支付时，调用 `order_service` 创建/支付订单
- 订单退款时，资金退回钱包余额

### 9.3 关联财务模块（finance）

- 钱包充值/提现/消费产生财务凭证（`finance_vouchers`）
- 钱包余额收益计入财务报表（`finance_reports`）
- 钱包资金对账纳入财务对账（`finance_reconciliations`）

---

## 十、开发优先级与实施顺序

### 第一阶段：P0 核心表（本次实现）

| 顺序 | 表 | Repository 方法（核心） | 锁键 |
|:---:|------|------|------|
| 1 | wallet_accounts | `open` / `get` / `add_balance` / `deduct_balance` / `freeze` / `unfreeze` | `wallet:{userId}` |
| 2 | wallet_transactions | `create` / `get` / `list_by_user` | - |
| 3 | wallet_withdrawals | `create` / `get` / `list_by_user` / `list_pending` / `approve` / `reject` / `mark_paid` | `wallet:{userId}` + `wallet:withdraw:{no}` |

### 第二阶段：P1 扩展表

| 顺序 | 表 | Repository 方法（核心） | 锁键 |
|:---:|------|------|------|
| 4 | wallet_deposits | `create` / `get` / `list_by_user` / `mark_matured` / `settle` / `early_settle` | `wallet:{userId}` + `wallet:deposit:{no}` |
| 5 | wallet_rewards | `create` / `get` / `list_by_user` / `claim` / `ship` / `sign` / `expire` | `wallet:reward:{no}` |

---

**设计结束**
