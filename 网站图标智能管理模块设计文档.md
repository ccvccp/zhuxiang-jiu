# 竹香酒官网 · 网站图标智能管理模块设计文档（32号模块）

> 版本：v1.0
> 日期：2026-08-27
> 适用项目：竹香酒官网（zhuxiangjiu）+ 微信小程序（taro-app）
> 关联模块：后台管理（admin）、AI 语义评分层

## 1. 模块定位

**网站图标智能管理**：将网站的颜色管理、图标管理统一为**权限管控**的智能配置中心——管理员创建/修改主题方案（配色+图标组合）→ **AI 智能评估**（无障碍对比度/色彩和谐/品牌一致/图标完整）→ 审核激活 → **运行时即时生效**（小程序导航栏/tabBar 不需重新发版）→ 全程**审计留痕 + 一键回滚**。

核心理念：**「权限管控变更 → AI 评估把关 → 运行时换肤 → 审计可回滚」**，对标 WordPress 主题管理 + 蚂蚁 Ant Design 换肤方案。

## 2. 创新与优化设计

| 创新点 | 说明 |
|---|---|
| **运行时换肤** | 基于 `Taro.setNavigationBarColor`/`setTabBarStyle`/`setTabBarItem` 原生 API，主题激活后 C 端**免发版即时生效**（突破小程序 SCSS 编译期硬编码限制） |
| **AI 健康度评估** | 激活前强制 AI 体检：WCAG 无障碍对比度计算 + 色彩和谐度（HSL 色相环）+ 品牌关联度（竹绿基因）+ 图标完整性，评分 <60 拒绝激活 |
| **AI 季节智能推荐** | 按月份/节日智能推荐主题（春节红金/中秋金棕/夏日竹青/默认经典），多因子评分排序 |
| **审计链 + 一键回滚** | 每次激活记录 before/after 快照，支持任意历史版本回滚（误操作秒恢复） |
| **三层权限** | JWT 令牌强校验（`require_admin` 依赖, 401/403）+ 会员库角色实时核验 + 小程序管理页 `role=admin` 会员门控 |
| **图标库集中管理** | tabBar 图标/金刚区 Emoji 图标注册为资源库，主题方案引用图标 ID，更换解耦 |

## 3. 数据模型

### 3.1 theme_themes（主题方案表）

| 字段 | 类型 | 说明 |
|---|---|---|
| themeId | int PK | 主题编号 |
| name | str | 方案名（如"竹绿经典"/"新春红金"） |
| description | str | 方案描述 |
| colors | dict | 配色组：primary/primaryLight/navBar/tabSelected/tabBg/textOnPrimary 等 |
| icons | dict | 图标组：tabHome/tabProducts/tabMine(图标库ID) + quickGrid(金刚区emoji映射) |
| aiScoreLatest | int | 最近 AI 健康度评分 |
| status | str | draft(草稿)/active(激活)/archived(归档) |
| createdAdminId | int | 创建管理员 |
| activatedAt | str | 最近激活时间 |

### 3.2 theme_icons（图标资源库表）

| 字段 | 类型 | 说明 |
|---|---|---|
| iconId | int PK | 图标编号 |
| name | str | 图标名 |
| type | str | emoji / url |
| content | str | Emoji 字符或图片 URL |
| category | str | tab(底栏)/grid(金刚区)/misc |

### 3.3 theme_change_logs（变更审计表）

| 字段 | 类型 | 说明 |
|---|---|---|
| logId | int PK | 日志编号 |
| themeId | int | 变更主题 |
| adminId | int | 操作管理员 |
| action | str | create/update/activate/rollback/delete |
| beforeSnapshot | dict | 变更快照(前) |
| afterSnapshot | dict | 变更快照(后) |
| createdAt | str | 时间 |

## 4. AI 智能评估（B级规则引擎）

### 4.1 健康度评估（激活把关，0-100）

| 因子 | 权重 | 规则 |
|---|---|---|
| 无障碍对比度 | 35 | 主色 vs 主色上白字 WCAG 对比度 ≥4.5:1 满分，线性衰减（色弱/老年用户可读性） |
| 前景背景区分度 | 20 | 主色 vs 页面背景色 HSL 明度差 ≥40 满分 |
| 色彩和谐度 | 15 | 主色与警示/错误功能色色相距离 30°-180° 得分（避免撞色） |
| 图标完整性 | 15 | tabBar 3 图标 + 金刚区关键图标齐全 |
| 品牌关联度 | 15 | 主色落在绿色系(色相 60°-180°) 竹香品牌基因加分 |

- **score ≥ 60**：允许激活（AI 通过）
- **score < 60**：拒绝激活，返回因子明细引导修正
- 拓展：接入图像识别 AI（图标风格一致性/视觉疲劳预测）升级 A 级

### 4.2 季节智能推荐

按当前月份多因子评分推荐：节日匹配（春节/中秋/国庆）+ 季节色相匹配 + 品牌基因保持度，返回排序方案列表。

## 5. 权限设计（三层防护, v8.1 升级 JWT 强校验）

**管理端 HTTP 层**（复用 `auth_routes.require_admin` 依赖）：
- `Authorization: Bearer <accessToken>` JWT 令牌验证 → 无 Token/无效/过期/被吊销 401
- Token 角色 + 会员库实时角色双重核验 → 非 admin 403（角色降权即时生效）
- 操作人 ID 从 Token 载荷提取（不可伪造）→ 全部写操作落审计日志
- ⚠️ 旧 `X-Role`/`X-Admin-Id` 头方案已废弃：compat 模式下伪造头可绕过校验，v8.1 已堵死

**小程序管理页**：
- 通过会员 profile 的 `role` 字段判断，非 admin 显示"无权限"占位
- 管理端请求自动携带登录令牌（`siteTheme.ts` 的 `adminHeaders`）

**变更权限矩阵**：

| 操作 | draft | active | archived |
|---|---|---|---|
| 编辑 | ✅ | ❌（先复制新草稿） | ❌ |
| AI 评估 | ✅ | ✅ | ✅ |
| 激活 | ✅ | — | ✅（重新激活） |
| 回滚至 | ✅ | ✅ | ✅ |

## 6. API 设计（11 端点）

### 管理端（JWT Bearer + admin 角色, 9 接口）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/site-theme/themes | 创建主题（自动落审计） |
| PUT | /api/site-theme/themes/{id} | 编辑（仅 draft，落审计） |
| POST | /api/site-theme/themes/{id}/ai-check | AI 健康度评估 |
| POST | /api/site-theme/themes/{id}/activate | 激活（AI<60 拒绝；落审计） |
| GET | /api/site-theme/themes | 方案列表 |
| POST | /api/site-theme/themes/{id}/archive | 归档 |
| GET | /api/site-theme/admin/logs | 审计日志列表 |
| POST | /api/site-theme/admin/logs/{logId}/rollback | 一键回滚（落审计） |
| GET | /api/site-theme/admin/recommend | AI 季节智能推荐 |

### 公开（C 端运行时拉取）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/site-theme/active | 当前激活主题（导航栏/tabBar/图标配置） |
| GET | /api/site-theme/icons | 图标资源库（公开只读） |

## 7. 小程序运行时主题引擎

`src/services/theme-service.ts`：
1. App 启动拉取 `/api/site-theme/active`（失败静默用默认竹绿）
2. `Taro.setNavigationBarColor` 应用导航栏配色
3. `Taro.setTabBarStyle` 应用底栏配色（文字选中/未选中/背景）
4. 金刚区图标：首页读取激活主题的 `quickGrid` 映射覆盖默认 Emoji

`pages/theme-admin/index` 管理页（权限门控）：
- 主题卡片列表（AI 评分徽章/状态标签/激活中标记）
- 操作：AI 体检 / 激活 / 回滚（历史日志）
- 新建主题：预设模板（竹绿经典/新春红金/中秋金棕/夏日竹青）+ 颜色选择 + 图标选择

## 8. 防误操作与风控

| 风险 | 对策 |
|---|---|
| 非授权换肤 | 三层防护（前端角色门控 + JWT 令牌验证 + 角色实时核验），全审计 |
| 低可读性配色上线 | AI 健康度 <60 拒绝激活（WCAG 对比度硬性把关） |
| 误激活难恢复 | 审计快照 + 一键回滚任意历史版本 |
| 激活方案被误改 | active 状态锁定编辑，只能复制新草稿 |
| C 端拉取失败 | 静默降级默认竹绿主题，不影响使用 |

## 9. 验收标准

- 后端端到端测试全通过（CRUD/AI 评估拦截/激活/回滚/审计/推荐/权限）
- HTTP 权限专项测试全通过（`test_site_theme_auth.py`: 伪造头 401/会员 403/篡改 Token 401/审计操作人溯源）
- 小程序主题管理页权限门控生效（非 admin 不可见操作）
- 运行时换肤：激活新主题后导航栏/tabBar 即时变色
- 编译通过、预览二维码可访问
