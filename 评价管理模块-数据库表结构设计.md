# 竹香酒官网 · 评价管理模块 - 数据库表结构设计

> 适配项目既有「内存 dict + Redis Hash」双模式存储，对齐 wallet/payment/logistics 模块风格。
> 开发方式：扩展现有 product 模块的评价功能（不新建独立模块）
> 开发分期：第一期 MVP（P0 核心扩展）+ 第二期（P1 互动与审核）

---

## 一、表清单与优先级

| 优先级 | 表名 | 用途 | 锁键 | Redis Key 模式 |
|:---:|------|------|------|------|
| P0 | `product_reviews`（扩展） | 评价主表（新增订单关联/图片/状态/匿名字段） | `product:review:{reviewId}` | `List product:reviews:{productId}`（扩展元素 JSON） |
| P0 | `review_replies` | 评价回复（商家/管理员回复用户评价） | `product:review:{reviewId}` | `List product:review:replies:{reviewId}` |
| P1 | `review_likes` | 评价点赞（用户点赞记录） | `product:review:like:{reviewId}` | `Set product:review:likes:{reviewId}` |
| P1 | `review_reports` | 评价举报（用户举报 + 管理员审核） | `product:review:report:{reportId}` | `String(JSON) product:review:report:{reportId}` |

### 说明
- **P0**（第一期 MVP）：扩展现有评价（订单关联 + 图片附件 + 状态机）+ 评价回复闭环
- **P1**（第二期）：点赞互动 + 举报审核流程
- 现有 `product_reviews` 表保持向后兼容，新增字段使用默认值兜底

---

## 二、核心表结构

### 2.1 product_reviews（评价主表扩展，P0）

**业务定位**：在现有评价基础上，新增订单关联（购买后才能评价）、图片附件、状态机（审核流程）、匿名评价

**现有字段（保持兼容）**：

| 字段 | 类型 | 说明 |
|------|------|------|
| review_id | str (PK) | 评价ID（现有格式 rv_{timestamp}_{random}） |
| product_id | str | 关联产品ID |
| member_id | str | 评价人会员ID |
| member_nickname | str | 评价人昵称（脱敏后存储） |
| rating | int | 评分 1-5 |
| content | str | 评价内容（≤500字） |
| created_at | str | 创建时间 |

**新增字段（P0 扩展）**：

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| order_id | str | "" | 关联订单号（购买后评价） |
| images | list[str] | [] | 评价图片URL列表（≤9张） |
| status | str | "published" | 状态（见状态机） |
| is_anonymous | bool | False | 是否匿名评价 |
| reply_count | int | 0 | 回复数量（冗余字段，加速列表展示） |
| like_count | int | 0 | 点赞数量（P1，冗余字段） |
| updated_at | str | "" | 最后修改时间 |

**评价状态机**：
```
pending_review(待审核) → published(已发布) → hidden(隐藏)
                                      ↘ rejected(审核拒绝)
published → hidden（管理员隐藏）
hidden → published（管理员恢复）
```

> 注：默认状态为 `published`（直接发布），仅触发风控或举报时进入 `pending_review`

**索引**：
- `product:review:index:orderId:{orderId}` → reviewId（按订单反查评价，防重复评价）
- `product:review:index:member:{memberId}` → Set（按会员查询评价历史）

**锁**：`product:review:{reviewId}`（修改/删除/回复保护）

---

### 2.2 review_replies（评价回复表，P0）

**业务定位**：记录商家/管理员对用户评价的回复，支持多轮对话

**字段设计**：

| 字段 | 类型 | 说明 |
|------|------|------|
| reply_id | str (PK) | 回复ID（格式 rp_{timestamp}_{random}） |
| review_id | str | 关联评价ID（FK → product_reviews） |
| replier_id | str | 回复人ID（商家/管理员） |
| replier_role | str | 回复人角色 merchant/admin |
| replier_name | str | 回复人名称 |
| content | str | 回复内容（≤500字） |
| parent_reply_id | str | 父回复ID（多轮对话，""表示一级回复） |
| created_at | str | 创建时间 |

**Redis Key**：`List product:review:replies:{reviewId}`（JSON 字符串，按时间正序）

**索引**：
- `product:review:replies:index:reviewId:{reviewId}` → replyId 列表

**锁**：复用 `product:review:{reviewId}`（回复操作与评价修改互斥）

---

### 2.3 review_likes（评价点赞表，P1）

**业务定位**：记录用户对评价的点赞，限制同一用户对同一评价只能点赞一次

**字段设计**：

| 字段 | 类型 | 说明 |
|------|------|------|
| review_id | str | 关联评价ID |
| member_id | str | 点赞人会员ID |
| created_at | str | 点赞时间 |

**Redis Key**：`Set product:review:likes:{reviewId}`（元素为 member_id）

**唯一约束**：(review_id, member_id) 联合唯一

**锁**：`product:review:like:{reviewId}`（防并发重复点赞）

---

### 2.4 review_reports（评价举报表，P1）

**业务定位**：用户举报不当评价，管理员审核处理

**字段设计**：

| 字段 | 类型 | 说明 |
|------|------|------|
| report_id | str (PK) | 举报ID（格式 rpt_{timestamp}_{random}） |
| review_id | str | 关联评价ID |
| reporter_id | str | 举报人会员ID |
| reason | str | 举报原因（广告/辱骂/色情/其他） |
| description | str | 举报描述（≤500字） |
| status | str | 状态（见状态机） |
| handler_id | str | 处理人ID（管理员） |
| handle_remark | str | 处理备注 |
| created_at | str | 举报时间 |
| handled_at | str | 处理时间 |

**举报状态机**：
```
pending(待处理) → confirmed(举报成立) → resolved(已处理:评价已隐藏)
              ↘ rejected(举报驳回)
```

**Redis Key**：`String(JSON) product:review:report:{reportId}`

**索引**：
- `product:review:report:index:status:{status}` → Set（按状态筛选待处理举报）

**锁**：`product:review:report:{reportId}`（审核处理保护）

---

## 三、双模式存储映射

| 表 | 内存模式 | Redis 模式 |
|------|----------|------------|
| product_reviews | `_mock_store["product_reviews"][product_id]` = list[dict] | `List product:reviews:{product_id}`（JSON 元素） |
| review_replies | `_mock_store["review_replies"][review_id]` = list[dict] | `List product:review:replies:{review_id}`（JSON 元素） |
| review_likes | `_mock_store["review_likes"][review_id]` = set[str] | `Set product:review:likes:{review_id}`（member_id 元素） |
| review_reports | `_mock_store["review_reports"][report_id]` = dict | `String(JSON) product:review:report:{report_id}` |

---

## 四、并发安全设计

### 4.1 锁键清单

| 场景 | 锁键格式 | 保护范围 |
|------|----------|----------|
| 提交评价 | `product:reviews:{productId}` | 防并发提交导致 rating_avg 计算错误 |
| 修改/删除评价 | `product:review:{reviewId}` | 评价字段更新的 RMW 保护 |
| 评价回复 | `product:review:{reviewId}` | 回复与评价修改互斥 |
| 评价点赞 | `product:review:like:{reviewId}` | 防并发重复点赞 |
| 举报处理 | `product:review:report:{reportId}` | 审核流程状态机保护 |

### 4.2 幂等性设计

| 场景 | 幂等键 | 实现 |
|------|--------|------|
| 订单评价 | (order_id, product_id) | 同一订单同一商品只能评价一次 |
| 评价回复 | (review_id, replier_id, content_hash) | 短时间内防重复回复（可选） |
| 评价点赞 | (review_id, member_id) | Set 去重 |
| 评价举报 | (review_id, reporter_id) | 同一用户对同一评价只能举报一次 |

---

## 五、接口规划

### 5.1 P0 接口（核心扩展）

| # | 接口 | 方法 | 路径 | 鉴权 |
|---|------|------|------|------|
| 1 | 提交评价（扩展） | POST | `/api/product/{productId}/reviews` | X-Member-Id |
| 2 | 评价列表（扩展） | GET | `/api/product/{productId}/reviews` | 公开 |
| 3 | 评价详情 | GET | `/api/product/{productId}/reviews/{reviewId}` | 公开 |
| 4 | 修改评价 | PUT | `/api/product/{productId}/reviews/{reviewId}` | X-Member-Id（本人） |
| 5 | 删除评价 | DELETE | `/api/product/{productId}/reviews/{reviewId}` | X-Member-Id（本人/admin） |
| 6 | 提交回复 | POST | `/api/product/{productId}/reviews/{reviewId}/replies` | X-Role: admin/merchant |
| 7 | 回复列表 | GET | `/api/product/{productId}/reviews/{reviewId}/replies` | 公开 |
| 8 | 按订单查询评价 | GET | `/api/product/reviews/by-order/{orderId}` | X-Member-Id |

### 5.2 P1 接口（互动与审核）

| # | 接口 | 方法 | 路径 | 鉴权 |
|---|------|------|------|------|
| 9 | 点赞评价 | POST | `/api/product/{productId}/reviews/{reviewId}/like` | X-Member-Id |
| 10 | 取消点赞 | DELETE | `/api/product/{productId}/reviews/{reviewId}/like` | X-Member-Id |
| 11 | 举报评价 | POST | `/api/product/{productId}/reviews/{reviewId}/report` | X-Member-Id |
| 12 | 举报列表 | GET | `/api/product/reviews/reports` | X-Role: admin |
| 13 | 处理举报 | POST | `/api/product/reviews/reports/{reportId}/handle` | X-Role: admin |
| 14 | 隐藏评价 | PUT | `/api/product/{productId}/reviews/{reviewId}/hide` | X-Role: admin |

---

## 六、向后兼容设计

### 6.1 现有接口兼容
- `GET /api/product/{productId}/reviews`：保持原有响应结构，新增字段使用默认值兜底
- `POST /api/product/{productId}/reviews`：新增字段（orderId/images/isAnonymous）为可选参数

### 6.2 数据迁移
- 现有评价记录自动补全新字段默认值：`status="published"`, `images=[]`, `reply_count=0`, `like_count=0`, `is_anonymous=False`
- 无需数据迁移脚本，读取时使用 `.get(field, default)` 兜底
