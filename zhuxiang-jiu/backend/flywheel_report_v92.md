# 小竹语音 v92 数据飞轮运行报告

生成时间：2026-09-26（v91 挖掘闭环 + v92 评分 V1 上线后）
数据源：生产 failures 表 377 条（v91 上线后积累）+ 今日全量对话日志

---

## 一、Reward 基线

**评分流 n=0**——v92 部署后暂无新对话轮（需复测积累）。

基于今日日志行为推演基线预期（复测一轮后即可校准）：

| 轮类型 | 今日占比(抽样) | 预期 Reward |
|--------|--------------|------------|
| rule 轨正常执行（选酒/查订单） | ~40% | 0.9+（eff 高） |
| LLM 轨闲聊（超时前 3s 版） | ~20% | 0.55~0.65 |
| 碎片/兜底轮 | ~35% | 0.15~0.30 |
| **推演基线 rewardAvg** | | **~0.55** |

v89 提速 + v91-B 瘦身后新数据预期上移至 **0.7+**（LLM 轨 2s 内 + 播报流畅性改善）。

## 二、失败挖掘 Top5（HITL 审核清单）

| # | 短语 | 次数 | 诊断 | 审核建议 |
|---|------|------|------|---------|
| 1 | `#`（空占位） | 213 | **空文本轮污染**（空转写/not_woken 占位被记 fallback） | ⚠️ **数据质量项**：record_score 前置过滤——classify_turn 已有 `if not text: return None` 但 "#" 非空——建议 failures_view 过滤 `#` + 溯源空轮写入路径 |
| 2 | 「小猪」 | 10 | 唤醒词错识（面板轨无拼音弱匹配） | 🔵 观察项：widget 轨有弱匹配兜底；面板轨"小猪"段可加客户端拼音提示重说 |
| 3 | 「请我查看」 | 7 | **跳转意图落兜底**（page.goto pattern 缺此变体） | ✅ **建议新增 pattern**：page.goto patterns += "请我查看"、"让我看看"（7+2=9 次/日，高频刚需） |
| 4 | 「换一款」 | 5 | v91-C 前面板关窗丢指令存量 | ✅ **已修**（长度下限 4→3）——复测验证后该项应归零，归零即闭环证据 |
| 5 | 「嗯」 | 3 | filler 存量（v86 前数据） | ✅ 已修（v86 前置拦截）——新数据应归零 |

repeat 14 条：连续重说（弱音频场景）——关联 v90 残化流自愈，观察自愈生效后是否下降。

## 三、HITL 决策汇总（按优先级）

1. **P0 数据清洗**：`#` 占位 213 条占 57%——过滤后 Top 才真实（建议 failures_view 加 `if p in ("", "#"): continue`）
2. **P1 pattern 新增**：「请我查看/让我看看」入 page.goto（9 次/日）
3. **观察项**：换一款/嗯的归零验证（v91-C/v86 修复闭环证据）；小猪错识面板轨兜底
4. **评分流启动**：任意真机对话后 smoothness 端点出首份基线，跑一周后评估 Bandit 数据燃料

## 四、飞轮健康度

```
挖掘闭环:    ✅ 运转中（377 条/日, 363 fallback 有效信号）
评分闭环:    ⏳ 待首批数据（部署后无流量）
聚合端点:    ✅ failures(377) / smoothness(0)
HITL 通道:   ✅ 本报告（机器提议→人做选择题范式首次运转）
```
