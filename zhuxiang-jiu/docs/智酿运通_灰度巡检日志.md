# 智酿运通 灰度观察期巡检日志

> 计划: 14 天（2026-09-12 起）· 7 检查点 · 见《智酿运通_部署验收报告摘要.md》§六
> 基线: Day 0 快照（同文件 §六末尾）

---

## Day 0 首轮巡检（2026-09-12 02:45 CST）

**结论: 7/7 检查点通过，无异常。**

| # | 检查点 | 判定 | 实测 | 基线对比 |
|---|---|---|---|---|
| 1 | 生产日志零错误 | ✓ | ERROR/Traceback=0；五个 zw 服务无异常输出行 | 持平（0→0） |
| 2 | 渠道熔断误报 | ✓ | open=0；SF=closed(样本4) DB=closed(样本3) JD·LLL·YT=unknown(样本不足诚实标注)；无 half_open | 持平（无漂移） |
| 3 | 提醒信噪比 | ✓ | total=5 全 pending（验收扫描出稿: consumer 3/operations 1/management 1）；驳回率 0%（<50%） | 新增 5 条属验收动作留痕 |
| 4 | 碳排激活 | ✓（待激活） | totalKg=0.0 / 9 单——演示单缺 weight 诚实行为 | 持平（待正式订单） |
| 5 | 协同采纳率 | ✓（无样本） | suggestions=0（与 patterns=0 一致，分母为 0 不计采纳率） | 持平 |
| 6 | 一代智运回归 | ✓ | orders=9 · signRate=0.89 · avgH=62.75 · etaWeight=0.7 | 与基线逐项一致，零漂移 |
| 7 | 公开端点滥用 | ✓ | verify 总访问 1 次（即验收测试自身 404，UA 为 PowerShell 巡检客户端）；无扫描迹象 | 无滥用 |

**附加观察**：
- 容器 healthy，内存 139.6MiB/1GiB（限内 13.6%）
- tri-codes=0（待正式绑定）；reverses=1（验收演示留痕）
- 验收期产生的 5 条 pending 提醒与 1 条逆向协议为演示数据，不影响灰度判读；如需清理可在第 7 天评审时随 ack 处理

**下轮**: Day 1 常规巡检（同口径）。

---

## Day 1 常规巡检（2026-09-12 19:30 CST）

**结论: 7/7 检查点通过，与 Day 0 基线零漂移，无异常。**

| # | 检查点 | 判定 | 实测 | 对比 Day 0 |
|---|---|---|---|---|
| 1 | 生产日志零错误 | ✓ | ERROR/Traceback=0；zw 五服务 0 异常输出行；**容器 restarts=0**（自部署 02:20 无重启） | 持平 |
| 2 | 渠道熔断误报 | ✓ | open=0；SF=closed(4) DB=closed(3) JD·LLL·YT=unknown；无 half_open | 完全一致 |
| 3 | 提醒信噪比 | ✓ | total=5 全 pending（验收留痕未变）；驳回率 0% | 持平 |
| 4 | 碳排激活 | ✓（待激活） | totalKg=0.0 / 9 单 | 持平（待正式订单） |
| 5 | 协同采纳率 | ✓（无样本） | suggestions=0 | 持平 |
| 6 | 一代智运回归 | ✓ | orders=9 · signRate=0.89 · avgH=62.75 · etaW=0.7 | 逐项一致，零漂移 |
| 7 | 公开端点滥用 | ✓ | verify 累计访问仍 1 次（无新增），无扫描迹象 | 持平 |

**附加观察**：
- 容器 healthy，内存 139.6MiB/1GiB（与 Day 0 完全一致，无泄漏迹象）
- nginx 累计 API 请求 6840 次——正常业务流量，无异常峰值
- 最新订单承运商 DB（演示种子数据，未新增正式订单）

**待办跟踪**：验收演示留痕（5 条 pending 提醒 + 1 条逆向协议）维持原状，按计划第 7 天评审时随 ack 一并处理。

**下轮**: Day 2 常规巡检（同口径）。

---

## Day 2 常规巡检（2026-09-13 02:45 CST）

**结论: 7/7 检查点通过，连续两天与基线零漂移，无异常。**

| # | 检查点 | 判定 | 实测 | 对比 Day 1 |
|---|---|---|---|---|
| 1 | 生产日志零错误 | ✓ | ERROR/Traceback=0；zw 五服务 0 异常行；restarts=0（持续无重启） | 持平 |
| 2 | 渠道熔断误报 | ✓ | open=0；SF=closed(4) DB=closed(3) 其余 unknown | 完全一致 |
| 3 | 提醒信噪比 | ✓ | total=5 全 pending（验收留痕未变）；驳回率 0% | 持平 |
| 4 | 碳排激活 | ✓（待激活） | totalKg=0.0 / 9 单 | 持平 |
| 5 | 协同采纳率 | ✓（无样本） | suggestions=0 | 持平 |
| 6 | 一代智运回归 | ✓ | orders=9 · signRate=0.89 · avgH=62.75 · etaW=0.7 | 逐项一致 |
| 7 | 公开端点滥用 | ✓ | verify 累计仍 1 次（无新增） | 持平 |

**附加观察**：
- 容器 healthy，内存 140.1MiB/1GiB（Day1 139.6 → +0.5MiB，微幅波动正常，无泄漏趋势）
- nginx 累计 API 请求 6846 次（Day1 6840 → +6，静默期正常低流量）
- 业务面静默：无新增正式订单（数据面仍待激活，与灰度预期一致）

**待办跟踪**：不变——验收演示留痕（5 pending + 1 逆向）第 7 天评审时处理。

**下轮**: Day 3 常规巡检（同口径）。

---

## Day 3 常规巡检（2026-09-13 19:40 CST）

**结论: 7/7 检查点通过，连续三天与基线零漂移，无异常。**

| # | 检查点 | 判定 | 实测 | 对比 Day 2 |
|---|---|---|---|---|
| 1 | 生产日志零错误 | ✓ | ERROR/Traceback=0；zw 五服务 0 异常行；restarts=0 | 持平 |
| 2 | 渠道熔断误报 | ✓ | open=0；SF=closed(4) DB=closed(3) 其余 unknown | 完全一致 |
| 3 | 提醒信噪比 | ✓ | total=5 全 pending（验收留痕未变） | 持平 |
| 4 | 碳排激活 | ✓（待激活） | totalKg=0.0 / 9 单 | 持平 |
| 5 | 协同采纳率 | ✓（无样本） | suggestions=0 | 持平 |
| 6 | 一代智运回归 | ✓ | orders=9 · signRate=0.89 · avgH=62.75 · etaW=0.7 | 逐项一致 |
| 7 | 公开端点滥用 | ✓ | verify 累计仍 1 次（无新增） | 持平 |

**附加观察**：
- 容器 healthy，内存 140.1MiB/1GiB（与 Day 2 完全持平，无泄漏趋势）
- nginx 累计 API 请求 6853 次（Day2 6846 → +7，静默期正常）
- 业务面持续静默：无新增正式订单（数据面激活仍待首笔正式下单）

**待办跟踪**：不变——验收演示留痕（5 pending + 1 逆向）第 7 天评审时处理。

**下轮**: Day 4 常规巡检（同口径）。

---

## Day 4 常规巡检（2026-09-14 02:50 CST）

**结论: 7/7 检查点通过，连续四天与基线零漂移，无异常。**

| # | 检查点 | 判定 | 实测 | 对比 Day 3 |
|---|---|---|---|---|
| 1 | 生产日志零错误 | ✓ | ERROR/Traceback=0；zw 五服务 0 异常行；restarts=0 | 持平 |
| 2 | 渠道熔断误报 | ✓ | open=0；SF=closed(4) DB=closed(3) 其余 unknown | 完全一致 |
| 3 | 提醒信噪比 | ✓ | total=5 全 pending（验收留痕未变） | 持平 |
| 4 | 碳排激活 | ✓（待激活） | totalKg=0.0 / 9 单 | 持平 |
| 5 | 协同采纳率 | ✓（无样本） | suggestions=0 | 持平 |
| 6 | 一代智运回归 | ✓ | orders=9 · signRate=0.89 · avgH=62.75 · etaW=0.7 | 逐项一致 |
| 7 | 公开端点滥用 | ✓ | verify 累计仍 1 次（无新增） | 持平 |

**附加观察**：
- 容器 healthy，内存 140.1MiB/1GiB（连续两天完全持平；四日走势 139.6→140.1→140.1→140.1，稳态无泄漏）
- nginx 累计 API 请求 6858 次（Day3 6853 → +5，静默期正常）
- 业务面持续静默：无新增正式订单

**待办跟踪**：不变——验收演示留痕（5 pending + 1 逆向）第 7 天评审时处理。

**下轮**: Day 5 常规巡检（同口径）。

---

## Day 5 巡检（2026-09-14 19:35 CST · 含两次后续部署后零漂移验证）

**结论: 7/7 检查点通过，zw 域零漂移；本轮起同时承担智图/智单智客两次计划内部署后的回归验证，全绿。**

| # | 检查点 | 判定 | 实测 | 对比 Day 4 |
|---|---|---|---|---|
| 1 | 生产日志零错误 | ✓* | zw 五服务 0 异常行；容器日志发现**域外 1 起**未捕获异常（/api/pocket/my/stats，`round(str)`）→ 已定位修复（见下） | zw 域持平 |
| 2 | 渠道熔断误报 | ✓ | open=0；SF=closed(4) DB=closed(3) JD·LLL=unknown；**YT 样本 0→2**（智启元 ZY-DEMO 演示单携带圆通渠道，仍 unknown<3 不评级） | 状态零漂移 |
| 3 | 提醒信噪比 | ✓ | total=5 全 pending（验收留痕未变）；驳回率 0% | 持平 |
| 4 | 碳排激活 | ✓（待激活） | totalKg=0.0 / 9 单 | 持平 |
| 5 | 协同采纳率 | ✓（无样本） | suggestions=0 | 持平 |
| 6 | 一代智运回归 | ✓ | orders=9 · signRate=0.89 · avgH=62.75 · etaW=0.7 | 逐项一致，零漂移 |
| 7 | 公开端点滥用 | ✓ | verify 累计仍 1 次（无新增） | 持平 |

**两次计划内部署后回归验证**（Day-4 后后端容器两次重建：智图 ~07:00、智单+智客 ~08:20）：
- 部署后冒烟：logistics-ai / order-ai / member-ai / map-ai / zy 全部 200（本机+公网双口径）
- 重建后 restarts=0、healthy；日志（除下述域外缺陷）无新增 ERROR
- 判读：**智图与智单智客部署对智酿运通零影响**，叠加式升级实证成立

**域外缺陷闭环（顺手赚钱模块，非 zw 域）**：
- 现象: 08:39 `/api/pocket/my/stats` 未捕获异常 `type str doesn't define __round__`
- 根因: `pocket_repository._deserialize` 类型白名单未覆盖 settings 数值键——Redis 模式下 `monthRewardSticker` 等以 str 返回，`ready_month×str` 字符串重复后 `round()` 崩溃；且 `0×"30.0"=""` 使**任何会员**调用必崩（首次读初始化返回内存浮点，第二次起读 Redis 即触发）
- 修复: `_INT_FIELDS`/`_FLOAT_FIELDS` 补齐 settings 键（checkinReward/monthRewardPoster/monthRewardSticker 浮点，maxActiveSites/aiScoreThreshold/durationDays/minAddressLen 整数）；`enabled` 布尔已有处理无需改
- 验证: 本地类型恢复断言 + pocket 回归 26/26 + ruff 全绿 → 部署重建 → 服务器 Redis 二次读路径 settings 类型全恢复 + `my_stats(3)`（即原崩溃场景）正常返回 0.0 → 日志零 ERROR → 公网五端点冒烟 200
- 注: 该缺陷为 Redis 模式潜伏缺陷，与灰度模块及两次部署无关（触发时点在智单智客浏览器验收窗口）

**附加观察**：
- 容器 healthy（pocket 修复重建后），内存 147.5MiB/1GiB（Day4 140.1 → +7.4MiB，为智图+智单+智客三模块服务加载所致，预期增长，限内 14.4%）
- nginx 累计 API 请求 7194 次（Day4 6858 → +336，部署验证与浏览器实测流量，正常）
- 顺手赚钱修复文件: `repositories/pocket_repository.py`（伴随本次巡检提交）

**下轮**: Day 6 常规巡检（同口径）。

---

## 数据激活注记（2026-09-12 09:25 CST · Day 5 与 Day 6 之间）

> 依据《AI大模型升级_管理层价值总结》后续建议第 1 条（数据喂养）执行的**计划内演示数据播种**。
> 脚本: `backend/scripts/seed_observation_data.py`（幂等 v1，双标志：Redis `zhuxiang:seed:obs:v1` + ZD-SEED/WT-CARBON 前缀检查）。
> **Day 6 巡检判读注意**：以下基线变动为本次播种的**预期结果**，非漂移、非异常。

| 观测面 | 播种前 | 播种后 | 说明 |
|---|---|---|---|
| 一代 orders | 9 | **12** | +3 笔 WT-CARBON 物流单（正式 API，weight 12/45/8kg，同城/跨省/偏远） |
| 一代 signRate | 0.89 | **0.92** | 新增 3 单全 signed（11/12） |
| 碳排 totalKg | 0.0（待激活） | **48.8** | 同城 0.3 + 跨省 40.5 + 偏远 8.0（排放因子法） |
| 渠道样本 | SF=4 / DB=3 / YT=2(unknown) | SF=**5** / DB=**4** / YT=**3(closed 首评)** | YT 样本过 3 阈值毕业 unknown→closed |
| 智单履约 ETA | None（无样本） | **45.15h**（7 样本） | 8 笔 ZD-SEED 回溯单（42-88h 分布）+ 1 笔正式全链单 |
| 智单体检总分 | 50（C 关注） | 履约维 100 | 终态占比/履约样本激活 |
| 智单异常留痕 | 0 | 16/次扫描 | 囤货画像（ZD-SEED-004 单品×12 + ZY 既有万元单） |
| 智客流失预警 | 无信号分布 | 红 1 / 黄 2 / 绿 2 | member 2 消费衰减（近30天 0 vs 前30天 ¥704）红色 0.8 分 |
| 智单/智启元 GMV | ¥591,000 | **¥596,200** | +8 笔回溯 + 1 笔正式 |

**播种构成**（全部清晰前缀留痕）：
- A 类·仓储直写 ×8：ZD-SEED-001~008（四态 COMPLETED/RECEIVED/SHIPPED/CANCELLED，会员 1/2/3 分布，回溯 2-46 天，不触发 hooks——对齐 ZY-DEMO 先例）
- B 类·物流正式 API ×3：WT-CARBON-001~003（POST /api/logistics/order 强制 weight>0，推进至 signed）
- C 类·电商正式 API 全链 ×1：RT202609120922378633（create→pay→ship→confirm→review 全 200，44号 hooks/41号券/42号开票/积分成长真实生效——首次生产全链管线验证）

**验证**: 容器内 7/7 检查通过（ETA/体检/预测 12 期+24 天序列/碳排/异常扫描/留痕/流失）；公网复检同口径一致。

**下轮**: Day 6 常规巡检（按上表新基线判读）。
