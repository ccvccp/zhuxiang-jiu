# 54号·小竹AI智能登录引擎大模型 模块验收报告

> 验收日期：2026-09-06
> 验收环境：Windows 本地（asyncio 内存态专项复跑 + HTTP 全端点冒烟）+ Docker 实机（Redis 态 ×2 轮幂等——镜像 dc8841081c12，容器 healthy）
> 结论：**通过**——六期专项 200 断言 + 实机 44 项次（22 项×2 轮幂等）+ 红队 RT-01~04 全拒（含 Redis 态复验）+ 回归 12 套件零新增失败 + 宪法断言四模块每期零改动

---

## 一、验收总账

| 期 | 主题 | 专项断言 | 关键结论 |
|----|------|---------|---------|
| P0 | 模型注册+评分器+双模型接入 | 39 | 第29档案八因子信任分+四级响应 75/50/25 切档+max 合成三态（dual_max/auth_risk_dominant/login54_only）+auth_risk 零侵入 |
| P1 | 决策回流管道 | 42 | 七类信号源真值标注（驻留+1.0/弱驻留+0.3/失败切换-1.0/申诉误拦-1.0/拦截正确+1.0/对抗-1.0/高危-0.5）+幂等 eventId 1:1+44号池双写+T+1 延迟态转正 |
| P2 | 自主学习进化 | 29 | Hedge 学习轮次（min_feedback=10 门槛）+护栏 [0.5,2.0] 数学断言+归一化和=1.0+影子对比双轨档位语义+replay 防护+冻结守卫联动 |
| P3 | 自主升级+回滚 | 31 | 手动晋升+版本回滚（指定/缺省）+权重逐因子还原+版本链可溯+滑动窗口回归检测→自动回滚+46号冻结+学习拦截 |
| P4 | 漂移监控+健康治理 | 30 | EMA 漂移 high 档→drift_alert 告警留痕+46号三检测器单档案评估+LLM 归因 mock/real 三态+冻结观测 |
| P5 | 看板+红队收官 | 29 | 四区看板（版本/因子/回流/漂移）+防御区双层投毒检测+RT-01~04 红队全拒+宪法断言收官 |

**累计：专项 200 断言（39+42+29+31+30+29，验收复跑全过）/ 实机 44 项次（22 项×2 轮幂等）/ 红队 4 向量全拒 / 回归 12 套件零新增失败 / HTTP 13 端点冒烟全过**

## 二、验收方式

### 2.1 专项复跑（验收日全量重跑）

```
python test_login54_p0.py  → 39/39
python test_login54_p1.py  → 42/42
python test_login54_p2.py  → 29/29
python test_login54_p3.py  → 31/31
python test_login54_p4.py  → 30/30
python test_login54_p5.py  → 29/29
```

覆盖：注册表/八因子数学/max 合成/七信号 reward+expected/幂等重扫/延迟态转正/池双写溯源/护栏+归一化/影子对比/晋升-回滚闭环/回归自动回滚+冻结/漂移告警/三检测器/归因三态/看板五区/RT 投毒防御/HTTP 全端点鉴权/宪法断言。

### 2.2 回归复跑（验收日全量重跑）

| 关联模块 | 套件 | 结果 |
|---------|------|------|
| 44号 学习中枢 | test_ai_learning_routes | 40/0 |
| 46号 治理中枢 | p0/p1/p2/p3/p4/p5/scheduler | 47+60+58+44+41+42+34 = 326/0 |
| 43号 认证风控 | test_ai_scoring_auth_routes | 25/0 |
| 53号 智能登录 | p0/p1/p2/p3/p5 | 49+39+32+27+40 = 187/0 |

零新增失败（53号 p4 见 §四验收发现——存量时区缺陷，与 54号 零改动无关）。

### 2.3 HTTP 全端点冒烟（TestClient 直连 main.app）

```
观测面: registry/status/dashboard/stats/health/history → 全 200
受控面: attribution(无事件) 409 / learn(门槛不足) 409 / preview(off) 409
鉴权:   无 X-Role → 403
```

13 端点行为与设计口径完全一致（P1-P5 套件内另覆盖 collect/promote/rollback/shadow-compare 全链）。

### 2.4 Docker 实机验收（Redis 态 ×2 轮幂等）

```
docker compose -p zhuxiang-jiu -f docker-compose.yml build backend   # 镜像 dc8841081c12
docker compose -p zhuxiang-jiu -f docker-compose.yml up -d backend   # 容器 healthy
python verify_login54_live.py  → 44/44（22 项×2 轮）
```

覆盖（[verify_login54_live.py](../backend/verify_login54_live.py)，每轮清理种子重造）：
- **零影响**：健康检查+35号 Hub 面板回归
- **off 铁律+观测面**：preview 409 / registry/status/dashboard/stats off 可访问
- **容器内全链（Redis 态）**：10 会员 53号事件+驻留种子→collect 回流标注（retention_dwell 10 条）+44号池双写 10 条→Hedge 学习（learnedFrom≥10+权重归一化=1.0）→手动晋升→版本回滚（权重还原）→看板五区齐备+回流区呈现+防御区护栏健康
- **红队 Redis 态复验**：RT-01/02 洪流护栏约束（50 条投毒反馈权重不越界）+RT-04 集中度告警（attacker_flood 占比 0.833>0.8 命中）
- **HTTP**：dashboard 无 Role 403 / learn 语义正确 / collect 幂等（已标注跳过）

## 三、红队验收（RT-01~04 权重投毒防御）

| # | 攻击向量 | 防御层 | 结果 |
|---|---------|--------|------|
| RT-01 | 正反馈洪流 30 条（伪造+1 反馈推高权重） | 护栏 [0.5,2.0] 倍上界 | 拒绝越界（权重全在护栏内） |
| RT-02 | 负反馈洪流 30 条（压低权重） | 护栏下界 0.5 倍 | 拒绝越界 |
| RT-03 | 投毒后归一化破坏 | 归一化铁律 | 和恒=1.0 |
| RT-04 | 单源洪流（attacker_flood 占比>80%） | 标注源集中度检测 | 告警命中（对照混合源不告警） |

54号自身新增攻击面评估：回流面为**纯读取式**接入（53号 events/43号 appeals 只读扫描），无新增写入既有表的路径；模型面默认 off（53号编排走 auth_risk 原轨）。

## 四、验收发现

### 4.1 存量缺陷（非 54号 引入——53号 时区混用）

**现象**：验收复跑（本地时区 00:00-08:00 窗口）`test_login53_p4.py` 在"连续累计（昨日2→今日3）"断言处崩溃（KeyError: 'unlocked'）。

**根因**：53号 `retention_claim` 混用两种日期口径——幂等键 `day_key = ts()[:10]`（**UTC**）与连击回溯 `yesterday = datetime.now()-1d`（**本地**）。本地 00:00-08:00（+08:00）窗口内 UTC 日期仍为本地"昨日"→ 幂等检查误中昨日种子记录 → 返回 already_claimed（无 unlocked 字段）。

**影响**：53号驻留连击在每日 00:00-08:00 窗口内的计算口径缺陷；54号 全线未触碰 53号代码（宪法断言每期验证），此前各轮回归均在窗口外通过。已登记为 53号存量债，建议后续以 UTC 统一口径修复（54号 P1 的 `_ts_utc` 同范式）。

### 4.2 交付期缺陷（54号 自身，4 项全部闭环）

| # | 缺陷 | 根因 | 修复 | 复验 |
|---|------|------|------|------|
| 1 | P1 申诉信号未命中 | 本地+08:00 与 UTC ISO 字符串不可字典序比较 | `_ts_utc` 统一 UTC 解析后比较+dayKey 归一 | 专项 42/42 |
| 2 | P3 手动晋升无回归基线 | promote 事件缺 challengerMetrics | 从新冠军版本记录 stats 补充 | 专项 31/31 |
| 3 | P3 回归冻结失效 | 46号注册中心冷态未入册被 fail-soft 吞 | 冻结前幂等 sync_registry | 专项 31/31 |
| 4 | P4/P5 端点计数硬编码 | ==12/==10 精确断言被后续阶段破坏 | ≥基线语义（演进不破坏） | 专项 30/30+29/29 |

## 五、六期核心数字

- **13 端点**（全 admin：观测面 7+回流面 2+学习面 2+升级面 2）/ **2 表** + 复用 44号双池 / **1 调度器**（T+1 补标+学习步+回归检测步三段式）
- **专项 200 断言 + 实机 44 项次（22 项×2 轮幂等）+ RT-01~04 红队全拒（含 Redis 态复验）+ 回归 12 套件零新增失败**
- **第29档案**：44号 SCORER_REGISTRY batch13（八因子权重模型）
- **核心代码约 2780 行**：8 服务/仓储/路由文件 + 1 实机验收脚本
- **零改动宪法**（每期断言验证）：44号学习五接口+档案结构 / 43号 auth_risk 八因子 / 46号治理服务 / 53号路由 20 端点
- **双开关默认 off**：LOGIN54_MODE（模型面）/ LOGIN54_LEARN_MODE（学习调度面）——默认零影响交付
- **Docker 部署态**：镜像 dc8841081c12 容器 healthy（观测面开放/模型面 off 零影响）

## 六、结论

54号六期（P0-P5）全线交付并通过验收：**登录引擎从"规则配置"进化为"自进化大模型"**——八因子信任分让档位有据可依，七类真值信号让模型知对错，Hedge 护栏让进化收敛于安全区间，滑动窗口回归让翻车秒级自愈，双层投毒防御让对抗者无从下手，四区看板+LLM 归因让每一次权重变化都可解释可审计。默认零影响态交付（双开关 off），与 43号 auth_risk 构成"规则底线+模型进化"双保险——**每一次登录都在让下一次登录更懂你，而治理确保这份"懂"永远值得托付**。
