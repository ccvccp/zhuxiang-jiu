# 订单运费规则统一与透明化 · 变更摘要

| 项 | 值 |
|---|---|
| 变更日期 | 2026-09-18 |
| 相关提交 | d775cc5（透明化）、811e45e（三链统一） |
| 涉及模块 | 订单（order）/ 结算（checkout）/ 心智（xinzhi）/ H5 前端 |
| 生产部署 | 后端容器重建 healthy；H5 app.c3f36b73.js → app.eebb43ae.js |
| 回滚备份 | dist.bak-shipfee（H5） |

## 一、变更背景

2026-09-17 订单全流程 E2E 测试（下单→签收 17/17 通过）提出"运费规则"观察项。排查发现三层问题：

1. **结算页金额与实付不一致**：checkout 结算页价格明细硬编码 L5 85 折且漏运费——88 元 1 瓶页面显示 74.8，实际实付 80.4（后端 L5 为 8 折 + 运费 10 元）。
2. **用户协议文案与真实链路不符**：协议写"购买两瓶免运费，否则 ¥12 运费"——该口径描述的是前端降级 mock 链（且金额与 checkout 后端实际的 ¥15 也不符），而真实用户链是"满 ¥99 免/¥10"。
3. **三条下单链运费规则不一致**（见下表）。

## 二、规则变更对照

### 变更前（三链并存，口径分裂）

| 链路 | 端点 | 运费规则 | 前端消费 |
|---|---|---|---|
| order | /api/order/* | 折后满 ¥99 免 / ¥10 | **真实用户链**（结算页首选） |
| checkout | /api/checkout/submit | 满 **2 瓶**免 / **¥15**（按数量） | 后端存在、前端不首选（降级 mock 用） |
| xinzhi | /api/xinzhi | 折后满 ¥99 免 / ¥10 | 心智页（此前已对齐） |

### 变更后（三链统一）

> **统一规则：折后金额（会员折扣 + 满减 + 优惠券 + 积分抵扣后）满 ¥99 免运费，未满收取 ¥10 运费。**

三链全部对齐上述口径，计算语义与 `order_service._calc_price` 的 `after_points >= 99` 一致。

## 三、改动清单

### 提交 d775cc5（运费透明化）

| 文件 | 改动 |
|---|---|
| taro-app/src/api/order.ts | 新增 `OrderAPI.pricePreview()`（对接 POST /api/order/price/preview） |
| taro-app/src/pages/checkout/index.tsx | 价格明细改后端实时试算：展示真实会员等级折扣（如 L5 -17.6）+ 运费行（满 ¥99 免）+ 实付；试算失败回落本地估算并补运费；修复 memberLevel 取值层级（响应顶层非 priceDetail 内） |
| taro-app/src/pages/agreement/index.tsx | 协议运费文案对齐真实链路："订单实付满 ¥99 免运费，未满收取 ¥10 运费" |
| taro-app/src/pages/product-detail/index.tsx + scss | 详情页价格区新增运费提示行"🚚 满 ¥99 免运费 · 未满加 ¥10 运费" |
| zhuxiang-jiu/backend/test_order_routes.py | 顺带固化库存口径回归测试（另一观察项证伪结论） |

### 提交 811e45e（三链统一）

| 文件 | 改动 |
|---|---|
| zhuxiang-jiu/backend/services/checkout_service.py | `SHIPPING_FREE_QTY=2 / SHIPPING_BASE=15` → `SHIPPING_FREE_THRESHOLD=99 / SHIPPING_BASE=10`；计算从 `total_qty >= 2` 改为 `discounted >= 99`（折后金额门槛） |
| taro-app/src/services/checkout-service.ts | 前端降级 mock 同步对齐（原两瓶免/¥12） |
| zhuxiang-jiu/backend/test_business_routes.py | 新增低价单运费专项：50 元 1 瓶 → shipping 10 / 实付 60 |
| zhuxiang-jiu/backend/prod_verify_checkout_shipping.py | 生产三单验证脚本（含自动清理，留痕） |

## 四、验证结果

### 本地测试（102 + 195 全绿）

- 新增专项：checkout 低价单（折后 50 < 99 → 运费 10、实付 60）；订单套件库存口径回归。
- 存量断言零变化：既有测试均用高价商品（599×2 = 1198 等），统一前后均为满额免运费，`finalAmount` 断言值不变（注释同步更新）。

### 生产实证（zxjiu.com）

| 验证点 | 结果 |
|---|---|
| 详情页运费提示 | "🚚 满 ¥99 免运费 · 未满加 ¥10 运费" ✓ |
| 协议页新文案 | "订单实付满 ¥99 免运费，未满收取 ¥10 运费" ✓ |
| 结算页实时试算（1 瓶 ¥88，L5） | 原价 88 / 折扣(L5) -17.6 / 运费 ¥10 / 实付 80.4 ✓ |
| 结算页满额免（2 瓶 ¥176，L5） | 折后 140.8 ≥ 99 → 免运费、实付 140.8 ✓ |
| checkout 低价单（50） | shipping 10 / 实付 60.0 ✓ |
| checkout 满额单（599） | 免运费，实付 569.0（满 500 减 30 满减正常）✓ |
| checkout 临界单（99） | 恰满门槛 → 免运费、实付 99.0 ✓ |

### 数据清理（零残留）

- checkout 生产落库结构：`zhuxiang:sc:checkout_orders` + `zhuxiang:sc:profit_records`（List）+ `zhuxiang:sc:checkout_points`（Hash 按等级）+ inventory 扣减。
- 验证后清理：订单/分润 6 条记录 LREM、积分回补（1071→1000）、库存回补（496→499）。

## 五、影响面与兼容性

- **真实用户链（order）零行为变化**：统一以 order 为基准，order 链未改动。
- **checkout 端点行为变化**：低折后金额（< ¥99）订单运费从 ¥15 降为 ¥10；单瓶高价（≥ ¥99）从收 ¥15 变为免运费；两瓶免运费规则由金额门槛取代。前端结算页不首选该端点（仅降级 mock 路径），实际影响面为 API 直连调用方。
- **协议文案已同步**，与统一后规则一致。
- 顺带修复：工作区 71 个历史双 BOM 污染文件（PowerShell 重定向遗留，SyntaxError 风险）经 `git diff --numstat` 全量识别后用 Python 二进制层无损去重修复。

## 六、调用方迁移指引

### 6.1 影响矩阵（按调用方分类）

| 调用方 | 调用路径 | 影响 | 需要的动作 |
|---|---|---|---|
| H5 结算页（真实用户链） | 优先 `POST /api/order/create` | **零变化**（order 为统一基准） | 无 |
| H5 降级 mock | `CheckoutService.submit()`（本地 ts） | 已随 811e45e 同步对齐 | 无 |
| xinzhi 心智链 | `/api/xinzhi` 系 | 零变化（此前已对齐） | 无 |
| **checkout 端点直连调用方** | `POST /api/checkout/submit` | **运费计算行为变化** | 见 6.2 迁移三步 |
| 内部测试/脚本 | 断言 shipping/finalAmount | 存量高价断言不变 | 低价新增断言已固化 |

### 6.2 checkout 直连调用方迁移三步

**第一步 · 识别依赖**：检索调用代码中对以下内容的依赖——
- 运费金额常量（15 / 12）或"两瓶免运费"数量规则；
- `details.shipping`、`details.finalAmount` 字段（字段名与语义不变，仅计算口径变化，正常无需改动）。

**第二步 · 对照行为变化明细**：

| 场景（折后金额 / 数量） | 旧规则（2 瓶免/¥15） | 新规则（满 ¥99 免/¥10） | 变化 |
|---|---|---|---|
| 1 瓶低价（50） | ¥15 | ¥10 | 运费减少 ¥5 |
| 1 瓶高价（599） | ¥15 | ¥0 | 变为免运费 |
| 2 瓶低价（2×40=80） | ¥0（数量免） | ¥10（金额未达门槛） | **由免转收 ¥10，唯一劣化场景** |
| 2 瓶高价（2×599=1198） | ¥0 | ¥0 | 不变 |

**需特别注意**：多瓶低价订单由"数量免运费"变为收取 ¥10。若业务上需保留低价多瓶免运费优惠，应在营销/优惠券层实现（如满件券），不再由基础运费规则承载。

**第三步 · 验证**：迁移后以三档金额（< 99 / = 99 / > 99）各调一次 `/api/checkout/submit`（或试算链 `/api/order/price/preview`），核对 `details.shipping` 分别为 10 / 0 / 0。

### 6.3 判定门槛口径说明

"折后金额"指会员折扣、满减（满 500 减 30 等）、优惠券、积分抵扣**全部完成之后**的金额（order 链 `after_points`、checkout 链 `discounted`，两链同语义）。前端展示可调 `POST /api/order/price/preview` 获取实时 `priceDetail.shippingFee`，勿在调用方本地复算规则（避免规则再漂移——本轮结算页硬编码漂移即前车之鉴）。

## 七、回滚方案

### 一级（后端轻量，~3 分钟）

```bash
# 旧版 checkout_service.py 从 git 历史取回（811e45e^ 的版本）
git show 811e45e^:zhuxiang-jiu/backend/services/checkout_service.py > checkout_service.py
scp checkout_service.py root@47.236.61.117:/opt/zhuxiang/zhuxiang-jiu/backend/services/
ssh root@47.236.61.117 "cd /opt/zhuxiang/zhuxiang-jiu && docker compose up -d --build backend"
```

仅回退 checkout 链运费口径（恢复 2 瓶免/¥15），order 链与前端不受影响。

### 二级（H5 回退，~5 分钟）

应急恢复 `dist.bak-shipfee` 可整体回退透明化+统一的全部前端改动（注意该快照为透明化**之前**版本，会连丢结算页实时试算）；精确回退须本地 `git checkout d775cc5^ -- taro-app/src` 后重新构建。

### 三级（完全代码回滚，~15 分钟）

```bash
git revert 811e45e d775cc5
# 后端 scp checkout_service.py；前端重新构建部署 H5
```

无 Redis 数据回滚项（运费规则为纯代码常量，存量订单明细中的 shippingFee 为历史快照不受影响）。

## 八、结论

三链运费规则终态统一为"折后满 ¥99 免运费，未满 ¥10"，规则对用户全链路透明（详情页提示 / 结算页实时试算 / 协议文案 / 订单明细四可见）。本地 297 项断言全绿，生产 7 项验证全过、测试数据零残留。变更可按三级路径回滚，无必办遗留项。
