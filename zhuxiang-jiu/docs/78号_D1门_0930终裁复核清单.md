# 78号·竹语 D1 门 09-30 终裁复核清单

> 版本 v1.0 · 2026-09-23
> 用途：09-30 09:00 crontab 自动出正式周样本后，按本清单逐项复核，完成 D1 门最终收口
> 依据：《78号_竹语TTS_G1完整版与D1门终裁报告》（三裁决框架 + 三档判定表）、《78号P2_H2治本_完整验收报告》
> 性质：裁决一（GA/B 不立项）已终结——本清单只做防翻案确认；**裁决二（P90 慢档占比）与裁决三（观察开关）为本次收口主体**
> 生产命令均在服务器侧执行（ssh root@47.236.61.117 后）

---

## 〇、数据包获取（09:00 后第一步）

- [ ] 自动出数确认：`/opt/zhuxiang/g1_final_0930.txt` 已生成且非空（crontab `0 9 30 9 *`）
- [ ] 失败兜底：手动补跑（路径已预检确认）
  `python3 /opt/zhuxiang/zhuxiang-jiu/backend/g1_final.py > /opt/zhuxiang/g1_final_0930.txt`
- [ ] 看板终态留档：`/lat/stats?days=7`（或 h2-watch.html）周样本数字抄录/截图，关开关前留档

## 一、数据完整性与清洗（先洗数，再判定）

- [ ] 双源均有数：[A] 服务端窗口流式 n>0；[B] 客户端周样本 n（观察窗 09-23~09-29 累计轮数）
- [ ] s2>5s 异常逐条定性（[B] 异常明细）：curl 人为样本 / headless 回录伪影（12s+ 长音频、asr_ms 佐证）→ 剔除；真机真实异常 → 单独归因记录
- [ ] 清洗后 s2 P50 仍 <500ms（H2 收益保持的基本盘）
- [ ] **慢档占比计算（裁决二核心输入；g1_final 不直接给，需服务端补算）**：

```bash
docker logs zhuxiang-backend-1 2>&1 | grep -oP 'first_chunk_ms=\K\d+' \
  | awk '{if($1>500)s++;n++} END{printf "n=%d slow=%d pct=%.1f%%\n",n,s,(n?100*s/n:0)}'
```

- [ ] 窗口口径注明：docker logs 仅覆盖当前容器窗（09-23 keep-alive 重建后）；期间若有重启，占比以实际覆盖窗为准并在收口记录注明；样本含少量 curl 测试轮，可接受噪声

## 二、裁决一防翻案确认（已终裁：GA/B 均不立项）

- [ ] 周样本 s2 P50 无大幅恶化（维持 274~393ms 量级或更优）
- [ ] B 路线 ROI 触发器仍不触发（月字符账本 48~108 元量级 vs GPU 月租 500~1500 元）
- 判定：两项均稳 → 裁决一维持终结；任一翻案（如 s2 P50 持续 >800ms）→ 停下重评 GA

## 三、裁决二三档判定（本次主体）

判定输入 = 第一节算出的慢档占比（first_chunk>500ms 比例）：

| 档位 | 判定 | 收口动作 |
|---|---|---|
| **<10%** | P90 达标 | 裁决二以"P1 验收线达成"收口 |
| **10~30%** | 慢档时段性存在 | 验收口径转**体感线**收口（O4 垫场后体感 ~300ms），记录注明口径切换 |
| **>30%** | 慢档常态 | 不直接收口——评估流式降级权重/缓存优先度上调（新决策项，回开发） |

辅助确认：

- [ ] P50 验收线复核（真实轮 s3 86~104ms 已通过，确认周样本无恶化）
- [ ] h2 协议占比维持 100%（nextHopProtocol）

## 四、裁决三：关观察开关（裁决二收口后执行）

- [ ] compose 置 off：`/opt/zhuxiang/docker-compose.yml` 中
  `- XIAOZHU_LAT_REPORT=${XIAOZHU_LAT_REPORT:-on}` 默认值 `:-on` → `:-off`
- [ ] 生效：`docker compose up -d backend`（env 变更触发重建；若同批还有代码变更，按两步式 `build --no-cache` + `up -d`）
- [ ] 验证三连：
  1. `GET /api/xiaozhu/voices` → `latReport=false`
  2. 浏览器语音页 console 无新增 `[LAT-embed]` 上报
  3. Redis 日键 `voice78:lat:{day}` 停止增长
- [ ] Redis 观察键不手动清（TTL 9 天，~10-09 自然过期）；如需留档，关开关前导出 /lat/stats JSON

## 五、收口动作

- [ ] 交付总结升版：《78号_悦声灵犀JoyVoice_交付总结.md》→ 终版（附三裁决终态 + 周样本核心数字 + 慢档占比结论）
- [ ] 78号转运营态（观察期结束，无待办开发项）
- [ ] git 提交推送（总结升版 + 若 compose 变更同步回库）

## 六、外部依赖顺手同步（均非 D1 门阻断项）

- [ ] 真机微信 X5 流式听感终验（采样率 24kHz 已三重定案，听感为最终确认）
- [ ] 智谱控制台 cogtts 台账 1 分钟核对（见 G3 遗留 #2 核对指引；公示价 2 元/万字符已定案）
- [ ] ICP 备案决策（H2 后 GA 剩余收益收窄至 ~150-250ms，可从容办；办妥如需 ~0.3s 再评估）

---

## 预检记录（2026-09-23 已执行）

- [x] crontab 已登记：`0 9 30 9 * python3 /opt/zhuxiang/zhuxiang-jiu/backend/g1_final.py > /opt/zhuxiang/g1_final_0930.txt 2>&1`
- [x] dry-run 全链通过：宿主机 python3 可用，登录→/lat/stats 聚合→双源输出正常（期中快照已验）
- [x] [B] Redis 数据重启不丢：容器重建后 n=9 样本完整（观察链与容器生命周期解耦）
- ⚠️ 窗口起点提示：容器 09-23 晚因 keep-alive 部署 `--no-cache` 重建，docker logs 已清零重计——09-30 出数时 [A] 覆盖 ~6.5/7 天；G1 首日 [A] 数据（n=34+9）已归档于 G1 报告文档，不依赖 docker logs

---

**一句话结论**：出数 → 洗数 → 算慢档占比 → 三档判定收口裁决二 → 关 XIAOZHU_LAT_REPORT → 总结升版转运营态——五步走完，78号·悦声灵犀全项目正式收口。
