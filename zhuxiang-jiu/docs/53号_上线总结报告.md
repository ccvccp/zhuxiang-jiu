# 53号·小竹智能登录引擎 上线总结报告

> 上线日期：2026-09-05
> 版本基线：`bb975f6`（部署终态）| 功能代码：`3063aa7`（P5）| 部署前基线：`7a1ff40`（53号零代码态）
> 运行镜像：`sha256:9a7b4fa4b263`（docker compose 正式构建——取代早期验收期 docker cp 临时态）
> 上线结论：**成功**——正式镜像部署 healthy + 冒烟 7/7 + 实机全链 30/30 + 存量登录零接管实测 + 默认零影响态交付

---

## 一、上线范围与形态

### 1.1 本次上线交付物

| 类别 | 内容 | 规模 |
|------|------|------|
| 功能代码 | 六期 P0-P5（注册表+态势感知 → 多模态编排 → 语音融合 → 角色门户 → 驻留激励 → 效果评估+红队收官） | 20 端点 / 4 表 / 话术 17 场景+五通道+四态+六指标 |
| 测试资产 | 专项 6 套（220 断言）+实机验收 1 套（15×2 幂等）+**冒烟脚本 1 套（7 项自动化）** | 全过 |
| 文档四件套 | 实施计划 / 验收报告 / 交付总结 / 运维手册 / **部署清单与回滚预案** | 5 份 |

### 1.2 上线形态

- **运行模式**：`LOGIN53_MODE` 未设置（默认 off）——**编排面关闭，直通存量 39号 entry 登录**（零接管铁律）
- **观测面开放**：registry/事件历史/快照/看板等管理端查询已可用（不受开关影响）
- **无新增依赖**：无必填环境变量、无 LLM 依赖、无外部 SDK（声纹/刷脸均为 mock/proxy 口径——国标 SDK 外部待办）

## 二、部署执行记录

### 2.1 部署流程（标准五步骤——全部实测通过）

| 步骤 | 命令 | 结果 |
|------|------|------|
| 1 镜像构建 | `docker compose -p zhuxiang-jiu build backend` | `sha256:9a7b4fa4b263` ✅ |
| 2 容器重建 | `docker compose -p zhuxiang-jiu up -d backend` | Started（redis 依赖 healthy 后启动）✅ |
| 3 健康等待 | healthcheck（/api/decision/health）循环 | healthy ✅ |
| 4 开关核验 | `docker exec ... printenv LOGIN53_MODE` | 未设置（默认 off）✅ |
| 5 部署验证 | 冒烟+实机全链（见 2.2/2.3） | 7/7 + 30/30 ✅ |

### 2.2 冒烟清单（smoke_login53_deploy.py——7 项自动化）

| # | 验证项 | 结果 |
|---|--------|------|
| ① | 健康检查 | 200 ✅ |
| ② | 35号 Hub 面板（既有业务零影响） | 200 ✅ |
| ③ | **存量登录零接管**（注册临时会员+39号 entry 密码登录） | 200 → authenticated ✅（off 直通铁律实测） |
| ④ | 观测面 registry 自描述 | 五通道+四态+六指标+话术 17 ✅ |
| ⑤ | 编排面 off 409 铁律 | 409 `"LOGIN53_MODE=off..."` ✅ |
| ⑥ | 快照/看板可达 | 200（空态合法）✅ |
| ⑦ | 鉴权（编排/管理面无头） | 403/403 ✅ |

### 2.3 实机全链（verify_login53_p5_live.py——镜像态复验）

15 项 ×2 轮幂等 = **30/30 全过**（与验收期 docker cp 态结果一致——镜像构建无回归）：健康检查/面板回归/off 铁律/容器内编排 silent 档+语音唤醒+驻留领取+六指标 Redis 态/看板通道占比+四态分布/鉴权/红队 RT-02 跨会员凭证拒绝。

### 2.4 部署期发现并处置（1 项）

| # | 问题 | 处置 |
|---|------|------|
| 1 | 冒烟脚本初版断言键名用 `detail`，实际全局异常处理器返回 `{"error": ...}`——⑤项误报 | 修正脚本（detail/error 双键兼容）+回写部署清单 §四口径说明 |

## 三、上线验证总账

| 层 | 规模 | 结果 |
|----|------|------|
| 六期专项测试 | 220 断言（49/39/32/27/33/40） | 全过 |
| 回归测试 | 718+auth 58 项 | 零新增失败（entry_p1 1 失败为存量债，stash 复验与 53号无关） |
| 实机验收（验收期+镜像态两轮） | 15×2×2 | 全过 |
| 冒烟（正式镜像） | 7 项自动化 | 全过 |
| 红队 RT-01~06 | 伪造令牌/跨会员凭证/通道伪造/预算绕过/挑战伪造/令牌重放 | 全拒绝 |
| 宪法断言（五模块零改动） | 39号 24 路由/43号风控/48号唤醒词表/49号 17 工具/50号 14 行为+声纹 proxy 默认态 | 全过 |

## 四、当前线上状态

| 项 | 状态 |
|----|------|
| 容器 | `zhuxiang-jiu-backend-1 Up healthy`（镜像 9a7b4fa4b263）|
| Redis | `zhuxiang-jiu-redis-1 Up healthy`（18h——login53 键空间就绪） |
| 53号编排面 | off（409——直通存量 39号登录） |
| 53号观测面 | 开放（registry/dashboard/events/metrics 管理端可查） |
| 存量业务 | 全部正常（39号登录实测零接管） |
| 回滚就绪 | L1 开关秒级 / L2 版本 `7a1ff40` 分钟级 / 不可回退项：无 |

## 五、灰度开启建议（LOGIN53_MODE=on——按需执行）

按部署清单 §五流程开启（`.env` 追加 `LOGIN53_MODE=on` → 容器重建）：

**前置条件**：39号 bio enroll 流程实测 + 43号风控误判率复核 + 运营话术人工复核 + 预算口径确认（face 0.05/voice 0.03/fingerprint 0.02 每次单位）。

**开启后 24h 观察**：dashboard byDecision 分布（enhanced/step_up 占比）+voice 通道占比爬升（30% 基线）+49号预算消耗分布。

**红线内建**（无需配置）：声纹 proxy 不作凭证（双因子）/预算耗尽不签发/令牌一次性/退出挽留非拦截/高危去污名化。

## 六、提交与资产清单

| 提交 | 内容 |
|------|------|
| `327c92d` | P0 注册表+态势感知（专项 49） |
| `baf48f0` | P1 多模态编排（专项 39+回归 486） |
| `dcfab63` | P2 语音融合登录（专项 32+回归 428） |
| `2fd15ea` | P3 角色门户+价值钩子（专项 27+回归 375） |
| `5f444bb` | P4 驻留激励（专项 33+回归 458） |
| `3063aa7` | P5 效果评估+看板+红队收官（专项 40+回归 718+实机 15×2） |
| `0b36223` | 三件套文档（验收报告+交付总结+运维手册） |
| `a70e1bf` | 部署清单与回滚预案 |
| `bb975f6` | **部署终态**（正式镜像部署+冒烟脚本+实测记录） |

**文档四件套+计划**：[实施计划](53号_小竹智能登录引擎实施计划.md) / [验收报告](53号_模块验收报告.md) / [交付总结](53号_完整交付总结文档.md) / [运维手册](53号_小竹智能登录引擎运维手册.md) / [部署清单](53号_上线部署清单与回滚预案.md)

## 七、结论

53号按标准流程完成上线：**正式镜像 healthy、冒烟七项全过、实机全链 30/30、存量登录零接管实测、默认零影响态交付**。上线即"信任的首次交付"——编排层就绪但克制（off 直通存量），观测面已开放供管理端巡检，灰度开启路径与回滚预案齐备。技术只是骨架，对人的敬畏才是灵魂——53号已让入口具备"拥抱"的能力，何时张开双臂由运营按需决定。
