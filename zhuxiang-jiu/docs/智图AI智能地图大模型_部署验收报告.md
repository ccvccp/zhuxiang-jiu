# 智图·AI智能地图大模型 生产部署验收报告

> 部署日期: 2026-09-12 · 代码基线: `c816fc1`（功能）+ `1fe638d`（沙盘归一化修复）
> 部署范围: 位置地图模块升级——智图·全域角色时空服务中枢（P0 意图 / P1 资源 / P2 调度 / P3 进化）
> 结论: **验收通过**——后端 22 端点（24 路由）、前端智图工作台四页签全链上线 zxjiu.com，既有 location 13 端点与智运/物流模块零影响

---

## 一、部署动作

| 项 | 内容 | 结果 |
|---|---|---|
| 后端 | git archive `c816fc1` 9 文件落盘 tar → scp → `/opt/zhuxiang` 解压覆盖（被覆盖文件先行备份）→ `docker compose -p zhuxiang up -d --build backend` | 2×10s healthy；4 业务服务+路由容器内导入 OK；**routes: 24** |
| 修复重部署 | 抽检发现沙盘分超上限（见 §四）→ `1fe638d` 2 文件重传 → 重建 backend | 20s healthy |
| 前端 | H5 dist（webpack 5.78 构建，含 zt 页）→ scp → `/var/www/zxjiu/dist` | index.html 指向新构建（`app.cfe4c945.js`）；zt 页块 `2975.e2db6991.js`（21.8KB）生产 200 |
| 部署后快速回归 | 智运 status + 容器健康 | `{"status":"healthy","lockMode":"redis"}`；智运 9 单/89% 签收无漂移 |

## 二、生产验收明细（zxjiu.com 实测）

### 2.1 后端 22 端点抽检（P0-P3 全阶段，X-Role: admin）

| 阶段 | 端点/链路 | 实测 |
|---|---|---|
| P0 | 复合意图解析 | "吃饭+买酒"→ `dining∧retail` 复合能力向量 ✓ |
| P0 | 复合时空搜索 | 命中 3 POI，top=竹香酒·锦江体验馆（93.0 分，四因子降序）✓ |
| P0 | 六角色画像 | count=6（消费者/供货商/采购商/内部运营/代理商/管理层）✓ |
| P1 | POI 附近 | total=11（全业务底座）✓ |
| P1 | 月台预约 | 最短排队分配，响应含建议书（不自动执行）✓ |
| P1 | B 端多仓匹配 | 推荐仓 W-001，运费 ¥16.76（8+0.6/km 测算）✓ |
| P2 | 态势一张图 | layers=6，物流在途 1 单 ✓ |
| P2 | 异常扫描 | 出稿 4 预案工单（queue_surge/seats_full/stock_low）✓ |
| P2 | 工单闭环 | ticket#4 ack 处置 ✓ |
| P2 | 风险雷达 | 供应链域 [high]（库存低于 50%）✓ |
| P3 | 选址沙盘 | 修复前 183.03（缺陷）→ 修复后 **72.63 / 72.46**（两次复核均 ∈ [0,100]），formula+disposition 齐备 ✓ |
| P3 | 时空绩效 | 6 类型效能画像 ✓ |
| P3 | 行为留痕 | "搜索"阶段留痕 ✓ |

### 2.2 鉴权与宪法守卫

- **403 守卫**: 不带 `X-Role: admin` 访问任意智图端点 → `403 需要管理员权限`（时空调度敏感域全端点管理权限，实测 ✓）
- **建议书模式**: 月台预约/工单处置/沙盘响应体均含 `disposition` 字段，永不自动执行 ✓
- **确定性**: 沙盘响应带 `formula` 字段（人口×0.3+竞争×0.3+密度×0.25+成本×0.15）✓

### 2.3 前端浏览器实测（zxjiu.com/#/pages/zt/index，四页签）

| 页签 | 实测 |
|---|---|
| 意图 | 复合意图时空搜索（dining∧retail）、3 POI 命中列表（距离/评分）、六角色清单 ✓ |
| 资源 | 地图 AK 未配置**诚实降级横幅**；20km 圈扫描渲染 11 POI 降级列表（含距离+类型）；月台预约、B 端多仓匹配区块 ✓ |
| 调度 | 态势一张图（6 类网点+异常计数+在途 1 单）、异常派单预案工单、风险雷达（供应链 high）✓ |
| 进化 | 时空战略沙盘（low 风险 72.45 分，收益测算）、时空绩效画像、反馈闭环（有效/拒绝回流）✓ |

控制台零错误、零失败网络请求；截图存档: `C:\Users\ccvcc\AppData\Local\Temp\trae\screenshots\zt_tab{1-4}_*.png`。

## 三、质量证据链（部署前置）

| 项 | 结果 |
|---|---|
| 智图专项（test_zt_map_ai.py） | **54/54 全绿**（服务+HTTP+宪法铁律+location 回归） |
| 既有 location 回归 | 58/58 全绿（零改动实证） |
| 智运/物流回归 | 85/85 + 22/22 全绿（跨模块无影响） |
| ruff lint | 全部通过 |
| 前端 H5 构建 | webpack 5.78 编译成功（104s，app 523KB 入口） |

## 四、过程中发现并修复的问题

**沙盘人口因子未归一化**（唯一缺陷，已闭环）:
- 抽检发现: 春熙路商圈沙盘分 **183.03 > 100 上限**（人口密度代理 = 周边 POI 评分均值×100 未截断）
- 修复: `zt_evolution_service.py` populationProxy 归一化 clamp → 提交 `1fe638d` → 重部署
- 复核: 服务器端 72.63、公网 API 72.46，两次均 ∈ (0,100]，沙盘分位语义恢复正确

## 五、回滚与残留

- **回滚路径**: 后端备份文件还原+重建容器；前端重传上一 dist 包
- **验收残留**（建议书数据，无业务影响）: 抽检产生的 4 条沙盘方案记录（sandboxId 1-4）与少量行为留痕留在生产 Redis；均为分析建议类数据，不进入任何决策链
- **已知边界**（沿用交付总结）: 百度地图 AK 未配置走诚实降级（设 `TARO_APP_BAIDU_MAP_AK` 即恢复）；沙盘收益为线性代理口径；POI 实时状态为种子静态（接业务系统后仅换数据源）

---

**验收结论**: 后端 22 端点 + 前端四页签工作台全链生产可用；唯一缺陷（沙盘归一化）当日发现当日闭环；既有模块零影响；宪法三铁律（LLM 禁入/建议书/进化可控）全程贯彻。**建议: 通过验收，转入常规运行。**
