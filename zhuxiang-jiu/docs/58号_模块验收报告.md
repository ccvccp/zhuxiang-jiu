# 58号·AI智能优化意图识别算法模块 模块验收报告

> 验收日期：2026-09-06
> 验收环境：Windows 本地（asyncio 内存态专项复跑 + HTTP 全端点冒烟）+ Docker 实机（Redis 态 ×2 轮幂等——镜像 zhuxiang-jiu-backend Built，容器 healthy）
> 验收人：＿＿＿＿＿＿ ｜ 评审人：＿＿＿＿＿＿
> 结论：**通过**——六期专项 286 断言 + 实机 256 项次（6 脚本×2 轮幂等）+ 红队 RT-01~07 全防御 + 回归 16 套件零失败 + 宪法断言每期零改动

---

## 一、验收总账

| 期 | 主题 | 专项断言 | 关键结论 | 复跑结果 |
|----|------|---------|---------|---------|
| P0 | 意图注册表+置信度引擎+识别底座 | 55 | INTENT_REGISTRY 12 项四侧三位一体封闭白名单（七字段+三对混淆对双向对称+启动自检 RuntimeError 宪法级）+第33档案 intent_orchestration 八因子（batch17）+L1 语料匹配（FULL/PARTIAL/AMBIGUOUS+共识加成+对抗否决）+三态响应+47号 tier 联动动态阈值+7 表底座 | 55/55 |
| P1 | 语料采集流水线+语料库 | 56 | 正样本挖掘（48号 turns 纯读取 active 直通——executed 规范化判定修复）+负样本增强（failures 三 kind）+对抗/越界样本构造（confusableWith 注册校验）+合成建议（LLM 仅建议不入库）+语料版本化+人工终审（active 唯一出口） | 56/56 |
| P2 | 业务耦合+动态信任校准 | 60 | 识别即合规前置校验（ROLE_RANK 四级+沙箱五级裁决+越界拦截改判+归因保留原始意图）+三态纯度铁律+槽位上下文预填（指代消解+页面状态——48号纯读取零写入）+10 合规模板（valueTags 联动）+阈值配置域（46号审批留痕+镜像终审轨） | 60/60 |
| P3 | 反馈闭环+主动学习 | 47 | 显式反馈（会员面 assist——短期表单+属主校验+PII 脱敏）+隐式反馈转化（failures 三 kind caseId 幂等）+主动学习（0.4-0.7 自动入队——入队≠生效）+decide 人工终审语料回流（四类归类+ingest terminal 轨+review 联动） | 47/47 |
| P4 | 决策回流+信值联动+T+1 调度 | 37 | 六类真值信号（七信号源）44号池双写（第33档案八因子快照+连续 reward）+幂等铁律 evaluationId 1:1（pooledFeedbackId 终态跳过）+高置信错误校准预警（≥3→upper+0.02 收紧建议 pending——人工终审唯一生效出口）+T+1 调度器（默认 off） | 37/37 |
| P5 | 看板+红队+宪法断言收官 | 31 | 四区看板（度量：任务完成率/纠错率/澄清接受率/信值增益+意图+语料+防御——含宪法断言）+红队七向量 allDefended（确定性离线可复现）+decide 回流目标意图在册校验补齐 | 31/31 |

**累计：专项 286 断言（55+56+60+47+37+31，验收复跑 286/286）/ 实机 256 项次（6 脚本×2 轮幂等）/ 红队 7 向量全防御 / 回归 16 套件零失败 / HTTP 19 端点行为与设计口径一致**

## 二、验收方式

### 2.1 专项复跑（验收日全量重跑）

```
python test_ii58_p0.py  → 55/55    # 注册表自检/八因子切档/off 铁律/三态判定/对抗否决/归因链
python test_ii58_p1.py  → 56/56    # 正样本挖掘幂等/负样本三 kind/对抗越界校验/终审铁律
python test_ii58_p2.py  → 60/60    # 越界拦截改判/sensitive 二次确认/槽位预填/阈值终审轨
python test_ii58_p3.py  → 47/47    # 显式反馈属主/隐式转化/主动学习区间/decide 回流
python test_ii58_p4.py  → 37/37    # 七信号判定/池双写幂等/校准预警/调度器
python test_ii58_p5.py  → 31/31    # 四区看板/红队七向量/宪法断言/19 端点
```

覆盖：意图注册表封闭白名单与启动自检；L1 语料匹配数学（FULL/PARTIAL/AMBIGUOUS+共识加成+对抗否决降权）；三态响应与澄清铁律；识别即合规（角色四级+沙箱五级+越界改判归因保留+三态纯度）；槽位上下文预填与 48号零写入；阈值配置域（申请→46号留痕→镜像 pending→人工终审唯一生效出口→驳回轨）；双通道反馈与主动学习入队；decide 语料回流四类归类；七信号终态判定序与 44号池双写幂等；高置信错误预警与队列纪律；调度器开关/周期下限/手动触发轨；四区看板四区指标；红队七向量全防御；宪法断言（33 档案在册+48/55号零改动）。

### 2.2 回归复跑（验收日全量重跑）

| 关联模块 | 套件 | 结果 |
|---------|------|------|
| 58号 六期 | test_ii58_p0~p5 | 286/0 |
| 44号 学习中枢 | test_ai_learning_routes | 40/0（池双写零改动） |
| 46号 治理中枢 | p0/p1 | 47/0、60/0（审批总线零改动） |
| 48号 小竹 | p0/p1/p2/p3/p4/p49_0/p50_0 | 50/40/31/42/34/38/50 全 0（turns 纯读取零写入） |

16 套件零失败（宪法断言每期验证：44号 33 档案在册+48号 COMMAND_ACTIONS≥15 零改动+55号 SERVICE_REGISTRY 在册）。

### 2.3 HTTP 全端点冒烟（TestClient 直连 main.app）

```
观测面: registry/evaluations×2/model/status/corpus/confusables/thresholds/labels/dashboard → 全 200（off 亦可用）
受控面: evaluate/ingest/mine×2/calibrate（off）409 / redteam（off）409 / feedback（off+shadow）409
终审面: corpus review+calibrate 终审模+decide（off 亦可用）200 语义正确
回流面: feedback/collect（off 亦可用）200 幂等
鉴权:   无 X-Role → 403 ｜ 无 X-Member-Id（会员面）→ 403
```

19 端点行为与设计口径完全一致（P0-P5 套件内另覆盖 evaluate 越界样例/预填/终审/驳回/collect 全链）。

### 2.4 Docker 实机验收（Redis 态 ×2 轮幂等）

```
docker compose -p zhuxiang-jiu build backend   # 镜像 zhuxiang-jiu-backend Built
docker compose -p zhuxiang-jiu up -d backend    # 容器 healthy
python verify_ii58_p0_live.py  → 36/36（18 项×2 轮）
python verify_ii58_p1_live.py  → 60/60（30 项×2 轮）
python verify_ii58_p2_live.py  → 48/48（24 项×2 轮）
python verify_ii58_p3_live.py  → 36/36（18 项×2 轮）
python verify_ii58_p4_live.py  → 40/40（20 项×2 轮）
python verify_ii58_p5_live.py  → 36/36（18 项×2 轮）
```

覆盖（每轮清理种子重造——清理键含 `zhuxiang:ii58:*`+`zhuxiang:ai46:*`+`zhuxiang:voice48:*`+`zhuxiang:ai_learning:*`）：
- **零影响**：健康检查+观测面 off 可访问
- **off 铁律+终审铁律**：决策面 409 / review-decide-calibrate 终审模 off 亦可用
- **容器内全链（Redis 态）**：语料→评估三态→归因读回（dict 结构五清单生效）→越界拦截改判→sensitive 确认→槽位预填（48号 turns 纯读取）→采集流水线（正负样本+终审）→阈值闭环（申请→46号留痕→人工终审→生效→驳回轨）→反馈闭环（显式+隐式+主动学习+decide 回流）→六类信号池双写+幂等+校准预警→看板先红队后（向量自产生评估不污染度量）→红队七向量
- **Redis 序列化读回**：executed 规范化判定（字符串"0"真值陷阱——内存态掩蔽防线）+attribution/slots dict 结构+compliance 块完整
- **HTTP**：dashboard 四区 / collect 幂等 / 鉴权 403 / 路由计数 19

## 三、红队验收（RT-01~07——确定性七向量全防御）

| # | 攻击向量 | 防御层 | 结果 |
|---|---------|--------|------|
| RT-01 | 语料投毒（不在册意图/非法样本类型/对抗混淆方伪造三路） | INTENT_REGISTRY 封闭白名单+样本类型四类封闭+confusableWith 注册校验 | 三路全拒 |
| RT-02 | 意图越界（guest 权限外执行） | minRole 前置校验+沙箱五级裁决+越界改判 boundary.unauthorized+归因保留 originalIntentId | guest 拦截+admin 正常意图放行+归因完整 |
| RT-03 | 对抗混淆（易混淆对攻击——positive+adversarial 同文本） | 对抗样本 FULL 命中否决×0.5 降权——混淆域强制澄清（澄清优于错误执行） | adversarialPenalty 标记+state≠resolved |
| RT-04 | 阈值操纵（伪造 tier 注入） | 47号 get_profile 纯读取不可写+fail-soft standard 基线+基线变更 46号审批 | tier=standard+伪造字符串兜底 standard |
| RT-05 | 反馈污染（虚假正反馈灌入） | 反馈→标注队列 pending 人工 decide——语料回流永不自动生效（人工铁律） | label pending+语料库计数不变 |
| RT-06 | 标注注入（非法回流类型/不在册目标意图/重复裁决三路） | 回流类型四类封闭+回流目标意图在册校验+状态机不可重复裁决 | 三路全拒 |
| RT-07 | LLM 越白名单（合成建议逃逸） | II58_LLM_MODE 三态默认 off+建议仅返回不入库（共创 pending 范式） | off 拒绝+on 态语料计数不变 |

58号自身新增攻击面评估：决策面为封闭注册表+终审轨接入（语料/标注/阈值全部人工终审唯一出口——无自动生效路径）；会员面（feedback）仅在 assist 开放（off/shadow 409）；与 48号 turns/failures 纯读取零写入路径；红队向量语料种子 source=redteam 隔离+retired 清理不留 active 污染。

## 四、验收发现

### 4.1 存量缺陷（非 58号 引入——无）

无。

### 4.2 交付期缺陷（58号 自身，7 项全部闭环）

| # | 期 | 缺陷 | 根因 | 修复 | 复验 |
|---|-----|------|------|------|------|
| 1 | P0 | 混淆对引用不在册意图——启动自检失败 | confusableWith 指向未注册意图 | 修正为在册意图间双向对称 | 专项 55/55 |
| 2 | P0 | 对抗否决 PARTIAL 阈值过激误伤正常文本 | 正常输入与对抗文本字窗重叠即降权 | 改 FULL_MATCH 全含触发 | 专项 55/55 |
| 3 | P1 | Redis 态 executed 回读为字符串"0"（真值）——未执行轮次误挖掘 | 仓储序列化 bool→int 后 hgetall 回读字符串；本地 mock 原生 bool 掩盖 | 规范化 `str().lower() in ("true","1","1.0")` 判定 | 专项+实机 56/56、60/60 |
| 4 | P1 | 测试断言"建议不入库"逻辑反写 | 断言误期望语料数≥4（建议应入库） | 修正为语料数保持 3 不变 | 专项 56/56 |
| 5 | P3 | 仓储 `_list` 首参 kind 与 feedback 记录字段 kind 撞名（TypeError multiple values） | 表名参数与过滤字段同名（P0 未演练 kind 过滤路径） | 重命名 table_kind 位置参数 | 专项 47/47 |
| 6 | P4 | evaluate 归因链缺 adversarialPenalty（回流信号依赖） | match 中间结果未入 attribution | 归因链补齐留痕 | 专项+实机 37/37、40/40 |
| 7 | P5 | decide 缺回流目标意图在册校验（恶意标注注入面） | P3 交付遗漏防御项 | INTENT_REGISTRY 在册校验补齐 | 专项+红队 31/31、RT-06 防御 |

**教训沉淀**：①仓储序列化读回陷阱两处复发（executed 字符串/pooled 漏清单同族）——已固化为"Redis 态必须实机验证 bool/int 字段判定"+"新增落库字段同步五清单"范式；②共享方法参数与记录字段撞名——通用基元参数命名需避开业务字段域；③中间结果（match/adversarialPenalty）入归因链的完整性意识——回流信号依赖字段须在产生处即留痕。

## 五、六期核心数字

- **19 端点**（观测 9+管理 7+终审 3）/ **7 表** / **11 服务文件**（registry/scorer/repository/service/compliance/corpus_service/feedback_service/learn_service/scheduler/dashboard_service/redteam_service）/ **1 调度器**（T+1 六类真值信号补标+高置信错误预警）
- **专项 286 断言 + 实机 256 项次（6 脚本×2 轮幂等）+ RT-01~07 红队全防御 + 回归 16 套件零失败**
- **第33档案**：44号 SCORER_REGISTRY batch17（八因子 intent_orchestration——33 档案口径）
- **七类回流信号**：correct_executed +1.0 / clarify_completed +0.8 / boundary_correct +0.6 / weak_negative +0.3 / coverage_gap -0.5 / adversarial_confusion -0.6 / high_conf_error -0.8
- **核心代码约 9,300 行**：11 服务/仓储/路由文件 + 6 专项测试 + 6 实机验收脚本
- **零改动宪法**（每期断言验证）：44号学习接口 / 48号意图轨四级路由+执行器+沙箱 / 55号意图引擎 / 47号信任画像 / 46号治理 / 49号预算 / 57号种子 valueTags
- **三开关默认 off**：II58_MODE（三级灰度 off/shadow/assist）/ II58_LLM_MODE（L2 路由）/ II58_LEARN_MODE（调度面）——默认零影响交付
- **Docker 部署态**：镜像 zhuxiang-jiu-backend Built 容器 healthy（观测面开放/决策会员面 off 零影响）——compose 四变量已入册（`176c539`）
- **8 提交**：e5f7ab0 计划 → 50ab02e P0 → d8fcf5c P1 → df7e28b P2 → 285539f P3 → 176c539 P4 → bc29531 P5 → c28e2fc 总结文档

## 六、宪法一致性核验（零改动红线）

| 红线 | 断言位置 | 结果 |
|------|---------|------|
| 44号 SCORER_REGISTRY=33 档案（intent_orchestration 在册、学习接口零改动） | 各期专项+实机+P5 宪法断言 | ☑ 保持 |
| 48号 turns/failures/custom_cmds 纯读取（四级路由/执行器/沙箱不动——COMMAND_ACTIONS≥15） | p1/p2/p3/p5 专项+实机 | ☑ 保持 |
| 55号意图引擎零改动（SERVICE_REGISTRY 在册、意图三态统计纯读取） | p0/p5 专项 | ☑ 保持 |
| 47号信任画像零改动（get_profile 纯调用、tier 不可写） | p0/p2 专项+红队 RT-04 | ☑ 保持 |
| 46号治理零改动（审批总线 submit_change/review_change 调用） | 回归套件+P2/P4 实机 | ☑ 保持 |
| 49号预算纯调用 / 57号种子 valueTags 只读关联 | 专项+实机 | ☑ 保持 |

## 七、上线建议

1. **发布形态**：默认 off 上线（零影响）——compose 四变量已入册（II58_MODE/II58_LLM_MODE/II58_LEARN_MODE/II58_LEARN_INTERVAL）
2. **灰度顺序**：off→shadow（观察 1-3 天——仅评估留痕+采集流试探）→assist（会员反馈面试点 3-7 天——语料+标注+反馈开放）→II58_LEARN_MODE=on（T+1 回流调度）
3. **放量前置**：§四验收发现全部闭环+红队复验 allDefended+三铁律生效抽查（off 决策面 409/终审通道不关闭/pending 幂等）
4. **回退保障**：L1 开关秒级（review/decide/calibrate 终审通道+collect 回流通道不关闭）/L2 阈值驳回轨（镜像 rejected 基线不变）/L3 代码回退分钟级
5. **运维口径**：参照 57号运维手册范式——每日 dashboard 四区巡检（≤5 分钟）+labels pending 清队+thresholds mirrorPendingAlert 处置

## 八、结论

58号六期（P0-P5）全线交付并通过验收：**意图识别从"分类器"进化为"业务理解引擎"**——12 项三位一体注册表让每个意图都懂权限知边界，置信度分级让引擎在无法确信时选择追问而非猜测，识别即合规让越权请求在交付前被拦截且归因完整，四类语料库让业务语义有据可锚，双通道反馈与主动学习让每一次纠错都成为下一次准确的养料，六类真值信号让第33档案知对错，七向量红队让攻击者无从下手。三开关默认 off 零影响交付，优化永不自动生效铁律贯穿六期（语料/标注/阈值全部人工终审唯一出口），与 48号指令轨/55号意图引擎并行不替换——**懂业务、知边界、有温度，而三铁律确保这份"懂"永远值得托付**。

---

**签字栏**

| 角色 | 姓名 | 签字 | 日期 |
|------|------|------|------|
| 验收负责人 | ＿＿＿ | ＿＿＿ | ＿＿＿ |
| 评审人 | ＿＿＿ | ＿＿＿ | ＿＿＿ |
| 运维代表 | ＿＿＿ | ＿＿＿ | ＿＿＿ |
