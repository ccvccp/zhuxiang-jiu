# 36号·AI智能推广模块 上线检查清单 v1.0

> 适用版本：P0+P1+P2 全量（代码至 `aac410f`，关键修复含 `b459c34`）
> 用途：生产上线前的逐项核对，全部为可执行动作与可观测判据
> 配套：[AI智能推广模块36_技术文档与部署指南.md](AI智能推广模块36_技术文档与部署指南.md)（§6 环境变量 / §7 部署 / §8 运维）

---

## 一、代码与镜像基线（上线前）

- [ ] 代码为最新 master（`git log --oneline -1` 确认，关键提交链：`e32681b → d8e151a → 7d663e6 → 8edd5b0 → e90accc → b459c34 → 776276b → aac410f`）
- [ ] **必须包含 attract 存量 bug 修复（b459c34）**——旧版本 Redis 模式下点击流/漏斗报表 500，并连累 36号归因聚合与 Hedge 反馈误判
- [ ] 本地全量测试通过：`python -m pytest -q -m "not redis"`（基线 1179 通过 / 9 存量失败，失败集须与本清单附录一致，无新增）
- [ ] 36号专项 7 文件 239 项全绿（test_promo_*.py，内存模式）
- [ ] 前端看板文件就位：`ai-promo-radar.html` / `ai-promo-studio.html` / `js/promo-radar.js` / `js/promo-studio.js`

## 二、环境变量配置（上线前，`.env`）

### 2.1 基础必配

- [ ] `LLM_API_KEY` 已配置（智谱开放平台密钥；未配置则 Agent 全程走规则轨模板）
- [ ] `LLM_ENABLED=on`
- [ ] `LLM_MODEL_PROMO=glm-5.3`（Agent 专用档位；降级链 glm-5.3→glm-4-flash→规则轨）

### 2.2 调度开关（按运营节奏逐项开启）

- [ ] `PROMO_RADAR_AUTO`：on（15 分钟自动扫描）/ off（手动触发）——**首次上线建议 off，人工验证一轮后再开**
- [ ] `PROMO_PUBLISH_AUTO`：on（5 分钟自动出队发布）/ off
- [ ] `AI_LEARNING_AUTO`：on / off（通用自学习调度自动纳入 promo_hotspot）

### 2.3 真实通道（资质就绪后才配，否则全部走 mock 轨）

- [ ] `PROMO_CHANNEL_MODE`：mock（默认，模拟轨）/ real
- [ ] real 模式下逐平台核对 `PROMO_CHANNEL_DOUYIN_KEY` / `PROMO_CHANNEL_XHS_KEY` / `PROMO_CHANNEL_WEIBO_KEY` / `PROMO_CHANNEL_MOMENTS_KEY` / `PROMO_CHANNEL_CHANNELS_KEY`（未配平台将 mock_fallback 可观测降级）
- [x] `BAIDU_PUSH_SITE` + `BAIDU_PUSH_TOKEN`（**2026-09-12 已配置生产**：站点验证通过→API 推送真实轨全通，success 4/4；注意 site 须纯域名不带协议，否则 400 site init fail）
- [x] **SEO 推送方案决策（2026-09-12）**：无 ICP 备案号，采用 API 推送单一通道——sitemap 平台通道配额 0 被禁用，待日后办理备案填入站点属性解锁；API 通道不受影响（新站日配额约 10 条，随收录量自动提升，发布后自动推送+当日幂等）

### 2.4 参数调优（可选）

- [ ] `PROMO_DAILY_CAP`（默认 20，单日发布上限）
- [ ] `PROMO_HOTSPOT_COOLDOWN_HOURS`（默认 48，同热点上限 2 条）

### 2.5 安全核对

- [ ] `.env` 不入 git（`.gitignore` 已含）；平台凭证/百度 token 最小权限分发
- [ ] 生产 `AUTH_MODE=strict`（JWT 保护；36号 29 端点全部要求 `X-Role: admin`）
- [ ] CORS `allow_origins` 限定具体域名（勿用 `*`）

## 三、部署与启动（上线时）

- [ ] `docker compose -p zhuxiang-jiu up -d --build backend`（中文目录必须显式 `-p zhuxiang-jiu`）
- [ ] 容器健康：`docker ps` → backend/redis 均 `(healthy)`（健康探测 `/api/decision/health`，10s/5/5/30s）
- [ ] `GET /api/health` 返回 `"lockMode":"redis"`（确认 Redis 模式生效，非内存模式）
- [ ] backend 内存占用 <1GiB（实机基线约 100MiB；限额 mem_limit 1g / memswap 2g）
- [ ] 启动日志无 ERROR/Traceback（`docker logs zhuxiang-jiu-backend-1`）
- [ ] **知悉已知行为**：容器每次启动幂等 seed 重建种子库（清空业务数据）——上线重启窗口选在业务低峰，重启后需重跑扫描/生成链或调整启动命令的 seed 步骤

## 四、36号功能逐项验收（上线后，全链路）

按序执行，每步核对判据：

- [x] **通道状态**：`GET /api/promo/channels/status` → 五平台 effectiveMode 与配置一致（mock 或 real；real+未配 KEY=mock）（**2026-09-12 生产验证**：五平台 effectiveMode=mock，keyConfigured=false，与生产未配平台 KEY 一致）
- [x] **雷达扫描**：`POST /api/promo/radar/scan` → scanned=25 / new=20 / discarded=5 / decisions=20（模拟源口径；风险热点 5 条被否决）（**2026-09-12 生产验证**：复扫幂等 scanned=25/new=0/skipped=25——首轮 09-11 已扫全量，符合指纹去重设计；风险否决 5 条在热点表可见）
- [x] **决策三档**：`GET /api/promo/decisions?pendingOnly=true` → 5 条待人工裁决（50-70 分区间）；抽查 reason 可解释（**2026-09-12 生产验证**：待裁决 4 条（其一已人工裁决）分数 56-62 均处 50-70；reason「评分62处于50-70区间, 转人工确认」可解释）
- [x] **种子初始化**：`GET /api/promo/audience/profiles` → 5 平台；`GET /api/promo/authority/sources` → 4 条国标/协会信源（**2026-09-12 生产验证**：profiles=5 平台/sources=4 信源）
- [x] **三维匹配**（抽查）：`POST /api/promo/audience/match` `{platform:"xiaohongshu", angle:"婚宴", productTone:"礼盒"}` → matched=true；换 douyin → matched=false（**2026-09-12 生产验证**：xhs matched=true / douyin matched=false）
- [x] **Agent 生成**：`POST /api/promo/contents/generate` → 合规分=100、状态 pending、短码 `A-` 开头、`authorityRefs` 非空；内容详情 `agentTrace` 四步走轨（配 LLM_KEY 为 glm-5.3/glm-4-flash，未配为 rule——均为正常）（**2026-09-12 生产验证**：内容#7/#9/#11 均 score=100/pending/A-短码；产品类热点 authorityRefs=[1,2] 非空，文化类热点 refs 合法为空；agentTrace 四步 rule 轨）
- [x] **溯源报告**：内容详情 `provenanceViolations` 为空（规则轨模板无数字声明）（**2026-09-12 生产验证**：内容详情 provenanceViolations=[]）
- [x] **人工审核**：`POST /api/promo/contents/{id}/review` `{"approved":true}` → 状态 approved；再调一次 → 409（状态非法）（**2026-09-12 生产验证**：首次 approved、重复 409）
- [x] **入队发布**：`POST /api/promo/contents/{id}/publish` `{"publishAt":""}` → queued + 黄金时段 scheduledAt（**2026-09-12 生产验证**：#9 queued scheduledAt=当日黄金窗口起点）
- [x] **出队回执**：`POST /api/promo/publish/process` → published + `receipt.mode`（mock/real）+ `publishId` + `exposureEstimate`（**2026-09-12 生产验证**：#7 published，receipt mode=mock/publishId=PUB-douyin-7/exposureEstimate=912000；未到期条目正确跳过）
- [x] **自动 SEO**：`GET /api/promo/seo/pushes` → 最新记录 status=ok，urls 含 sitemap + 新落地页（**2026-09-12 生产验证**：pushId 6 真实推送 4/4 成功+remain 6；pushId 7 当日幂等重推 mode=skipped/status=ok，无假失败记录；pushId 8 发布后自动推送新落地页 1/1 成功）
- [x] **归因闭环**：浏览器访问 `/r/{短码}?utm_source=douyin` → 302 落地页带 clickId；`POST /api/attract/attach` + `attach-order` 后 `GET /api/promo/report/overview` → attribution.clicks/registered/ordered/GMV 计数准确（**此处为零则 attract 修复未包含，回退第一节检查 b459c34**）（**2026-09-12 生产验证**：`/r/A-LNE0JJ?utm_source=douyin` → 302 `/pages/activity/index?clickId=2`；attach+attach-order 后 attribution clicks=2/registered=1/ordered=1/gmv=599.0 准确。**修复记录**：验收发现生产 nginx 缺 `location /r/` 反代，/r/* 被 SPA 回退返回 index.html 致归因断裂——已在服务器 /etc/nginx/conf.d/zxjiu.conf 增补并 reload，仓库模板 deploy/nginx-zxjiu.conf 同步）
- [x] **Hedge 回流**：`POST /api/promo/learning/feedback` `{"contentId":N}` → 有点击内容 correct=true；重复提交 → 409（幂等）（**2026-09-12 生产验证**：内容#7（有点击）correct=true；重复 409）
- [x] **attract 点击流回归**（旁证）：`GET /api/attract/clicks` 与 `GET /api/attract/report/funnel` 返回 200 非 500（**2026-09-12 生产验证**：clicks 200 count=2 / funnel 200）

## 五、前端看板验收

- [x] `ai-promo-radar.html` 打开：统计卡/扫描按钮/热点表（评分进度条）/待裁决跟进-放弃操作正常（**2026-09-12 生产验证**：统计卡 25 热点/12 跟进/5 放弃/5 风险否决/3 待裁决实时更新；25 行热点表含评分进度条与风险词标记；「跟进」点击后 #8 状态翻转+统计卡 12→13/3→2+提示语正常）
- [x] `ai-promo-studio.html` 打开：生成表单（五平台多选）/内容列表（agentTrace 徽章）/审核/入队/发布队列操作正常（**2026-09-12 生产验证**：表单生成内容组 #10（已跟进热点下拉 13 项+五平台多选渲染）；agentTrace 徽章「①分析 rule ②受众 rule ③生成 rule ④自查 rule」；#11 审核通过→入发布队列→已入队全链点击成功；队列表显示黄金窗口「未到窗口」状态）
- [x] P2 区块：通道状态卡（三色徽章）与百度 SEO 推送卡（推送/强制重推/记录表）渲染正常（**2026-09-12 生产验证**：通道卡五平台 mock 徽章+图例说明；SEO 卡含立即推送/强制重推按钮+全部 8 条推送记录表（含 pushId 5/4 历史失败与 6/7/8 修复后成功记录））
- [x] 页面 API 基址指向生产域名（页面内可改，localStorage `promoDash.apiBase`）（**2026-09-12 生产验证**：基址输入框保存 https://zxjiu.com 后 localStorage 持久化，两页共享）
- [x] 30 秒自动刷新不报错、不吞表单输入（**2026-09-12 生产验证**：勾选自动刷新后多轮触发（最后刷新时间持续更新）；热点下拉选择跨刷新保留；浏览器控制台零错误）

## 六、上线后观察项（首 48 小时）

- [ ] `docker logs` 无 `promo_agent_chat_error` / `promo_channel_real_failed` / `promo_baidu_push_failed` 高频刷屏
- [x] **黄金窗口时区对齐（2026-09-12 验收发现并当日修复）**：容器本地时区原为 UTC，`next_publish_time` 按容器本地时计算黄金窗口 → 抖音 (18,22) 窗口实际落在北京时间 02:00-06:00。**已处置**：docker-compose 为 backend 增设 `TZ=Asia/Shanghai`（服务器 `/opt/zhuxiang/docker-compose.yml` 与仓库根 compose 同步，`.bak-tz` 备份），容器重建后验证：`date` 显示 CST、抖音下一窗口落在北京 12:00 午休窗（`04:00 UTC`）、`/api/health` lockMode=redis、业务数据零影响（热点 25/推送记录 8/归因计数/队列全在）。**影响评估**：裸 `datetime.now()` 逻辑（黄金窗口/消息静默时段[message_service 注释明示用户视角本地时间]/groupbuy 日界/博主发布窗口）统一对齐北京时间，与开发测试环境（本机 Asia/Shanghai）口径一致；显式 `datetime.now(UTC)` 调用（SEO dateKey/`_now_iso`）不受影响；存量队列排期不重算（#9/#11 按旧 UTC 排期出队后新入队即走北京窗口）
- [ ] 降级观测（§8.2）：agentTrace 中 rule 占比、receipt.mock_fallback 占比、SEO error 字段
- [ ] 单日发布计数 `report/overview → dailyCap.used` 接近 20 时是否正常 409 拦截
- [ ] 收官脚本复跑：`scripts/docker-prelaunch-check.ps1` 16/16（注意：3.8 节会重启容器并重置 seed 数据）

## 七、回滚预案

- [ ] 回滚触发条件：attract 点击流 500 复现 / 归因恒为 0 / Agent 生成全 rejected / 发布回执异常
- [ ] 回滚动作：`git checkout <上一稳定commit> && docker compose -p zhuxiang-jiu up -d --build backend`（36号零新增依赖，回滚不影响其他模块）
- [ ] 紧急止血（不回滚）：`PROMO_PUBLISH_AUTO=off` + `PROMO_RADAR_AUTO=off` 停自动调度，保留人工操作通道

---

## 附录：已知存量问题（非 36号 引入，上线不阻断）

| 项 | 说明 |
|----|------|
| 全量回归 9 个存量失败 | test_decision_endpoints（决策编排 8）/ test_order_flow（支付回调 1），stash 验证与 36号无关 |
| Redis 集成测试 | 走独立 CI（`@pytest.mark.redis`），本地默认 deselect |
| 主机内存偏紧 | 可用 <4.5GB 时收官脚本告警（非阻断）；实测 vmmem 2GB 下可运行 |

## 附录：36号专属环境变量速查

| 变量 | 默认 | 上线必配 |
|------|------|---------|
| LLM_MODEL_PROMO | glm-5.3 | 建议（未配仍走默认档） |
| PROMO_RADAR_AUTO / PROMO_PUBLISH_AUTO | off | 按节奏 |
| PROMO_CHANNEL_MODE + 5 平台 KEY | mock/- | 资质就绪后 |
| BAIDU_PUSH_SITE / BAIDU_PUSH_TOKEN | 空 | **已配置**（生产 .env；SITE_BASE_URL 同步修正为 https://zxjiu.com） |
| PROMO_DAILY_CAP / PROMO_HOTSPOT_COOLDOWN_HOURS | 20 / 48 | 可选 |
| HOTSPOT_{平台}_API_KEY ×5 | 空 | 可选（未配走模拟源） |
| HOTSPOT_{平台}_URL ×5 | 空 | **真实抓取必配**（KEY+URL 成对，KEY 单配不生效；经第三方聚合服务如聚合数据/天行数据；失败自动回退模拟源） |

> 热点源真实抓取说明（P2 落地实现）：五大平台热榜均不直接对外开放，须经聚合 API；响应解析兼容聚合数据（result.data）/ 天行（newslist）/ 顶层列表等格式，标题/热度字段多别名兼容；KEY+URL 均配置才走真实抓取，网络失败/解析异常/空结果一律回退确定性模拟源（mock-first 铁律）。
