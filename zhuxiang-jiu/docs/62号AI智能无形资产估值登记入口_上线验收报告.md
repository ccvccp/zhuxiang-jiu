# 62号 AI智能无形资产估值登记入口 上线验收报告

> 日期: 2026-09-16 | 版本: **终版** | 验收范围: 信任要素登记入口开通(前端登记页签 + registry 要素明细扩展 + POST /assets 接线), 后端决策面/门控/护栏零改动
> 验收基线: 后端 P0 存量 59 断言 + 前端单测 25 断言(18 存量 + 7 登记) = 84 断言
> 验收结论: **通过**——登记链路生产实机两轮全过, 测试数据清理归零, 必办遗留项清零

---

## 一、验收对象与提交链

| 提交 | 内容 | 状态 |
|---|---|---|
| (存量) | 62号大模型二代转段(7346657): AV62_MODE 三态灰度 + 7 决策门控 + 护栏三指标 + 62号同步桥接 | ✅ 生产 assist 档 |
| bf9dd5d | 前端决策面 409 友好降级 errMsg 补齐 + 单测 18 项 | ✅ 已推送 |
| **57539a2** | **信任要素登记入口开通**(本轮验收对象) | ✅ 已推送 |

生产部署物:
- 后端 1 文件(services/av62_registry.py)scp + 容器重建;
- 前端 H5 两轮(app.149521f4.js 初版 → app.bcf13751.js 结果卡修正版,
  旧版备份 `dist.bak-av62reg`)。

## 二、升级背景与检查结论(前置于实施)

### 2.1 检查发现(2026-09-16)

| 层 | 现状 | 结论 |
|---|---|---|
| 后端模式层 | AV62_MODE=assist(env 来源, 未暂停) | ✅ |
| 后端观测面 | registry 200(三角色×九域+13 要素)/assets 200/mode 200 | ✅ |
| 后端登记端点 | POST /assets 鉴权 403 有效; assist 档门控放行实证(探针载荷穿透至业务校验) | ✅ 能力完备 |
| 前端消费面 | 四页签(状态/看板/资产/评估)纯观测; API 客户端仅 8 观测方法, **无登记方法无表单**; 页脚定位"决策面经管理 API 调用" | ❌ 登记入口缺失 |

### 2.2 实施方案(用户确认)

1. `api/av62.ts` 新增 `registerAsset()`(POST /assets, off 态 409 友好降级);
2. av62 页新增"登记"页签, 表单字典运行时取 registry 接口(不前端硬编码);
3. 后端 registry 观测面补 `elementDetails` 要素明细(动态字典源)。

## 三、功能验收清单

### 3.1 后端(registry 观测面扩展, 门控零改动)

| 验收项 | 验收标准 | 结果 |
|---|---|---|
| elementDetails | 13 要素明细(label/weight/evidenceSchema/note/negative), 前端按 (role, domain) 取证据字段 | ✅ |
| 动态字典源 | 登记表单角色→域→证据字段全部来自后端 registry——对齐钱包利率教训, 杜绝前端硬编码漂移 | ✅ |
| 观测面口径 | registry 扩展为增量键, 不受 AV62_MODE 影响; 决策门控/护栏/控制面零改动 | ✅ |

### 3.2 前端(登记页签)

| 验收项 | 验收标准 | 结果 |
|---|---|---|
| 五页签结构 | 状态/看板/**登记**/资产/评估(原四页签扩展) | ✅ |
| 要素字典加载 | 点"加载要素字典"拉 registry, 失败 errMsg 降级 | ✅ |
| 角色三选 | 企业/组织/个人(enterprise 默认) | ✅ |
| **角色→域联动** | 资产域按 registry.meta.byRole 封闭过滤(企业 4 域/组织 4 域/个人 5 域); risk 负域"(负)"标注; 切角色清空域与证据 | ✅ |
| **证据字段动态渲染** | 选中 (role, domain) 后按 elementDetails evidenceSchema 渲染输入框(如企业合规 3 字段: licenseCount/auditResults/esgDisclosure) + 要素 label/权重展示 + 负资产"证据必填·不可洗白"提示 | ✅ |
| 登记提交 | subjectId 必填>0 / 域必选 / 至少一项证据; POST /api/av62/assets(数值化 evidence) | ✅ |
| off 态降级 | 409 经 errMsg → "估值决策功能暂未开放(决策面关闭), 敬请期待" | ✅ |
| 成功回显 | 登记 #assetId · 状态 registered · 要素/主体/角色/域(表单本地值) · 模式标记 av62Mode | ✅ |

### 3.3 后端契约注意点(工程沉淀)

后端登记返回为**最小化结构**(assetId/status/negative/label/weight/
evidenceSchema/fingerprint/note/createdAt), **不含 subjectId/role/domain**——
前端结果卡须用表单本地值回显, 不可读响应字段(实机首轮曾因此出现主体槽位
插值为空, 当日发现当日修正)。

## 四、质量验收记录

| 测试套 | 结果 |
|---|---|
| 后端 test_av62_p0.py 存量(registry 扩展后复跑) | **59/59** |
| 前端 test-av62.js(18 存量 + 7 登记新增) | **25/25** |
| **合计** | **84/84** |
| ruff(av62_registry.py) | All checks passed |

### 4.1 登记专项断言分布(7 项)

- API 层 3: 方法 9 个(含 registerAsset) / 登记 POST 载荷(X-Role +
  subjectId/role/domain/evidence 数值化 + av62Mode 解包) /
  注册表要素明细解包(elementDetails×byRole);
- 页面层 4: 五页签结构 / 登记字典加载(registry 请求) / 登记表单渲染
  (角色域联动 4 域) / 证据字段动态渲染(placeholder 断言) / 提交 POST
  载荷 / 成功回显(登记 #9 + 模式标记 assist)。

### 4.2 断言工程记录

首轮 24/25——"证据字段动态渲染"失败: 证据字段名在 Input 的
placeholder(props)不在 children, textOf 不覆盖 props——改用
placeholder 断言后全绿。

## 五、生产实机验收(zxjiu.com)

### 5.1 部署实证

| 项 | 结果 |
|---|---|
| 容器健康 | Up (healthy) |
| registry 新结构 | 公网 200, elementDetails 13 要素(enterprise.compliance 明细三字段实证) |
| H5 上线 | app.bcf13751.js(两轮: 初版 + 结果卡修正) |

### 5.2 浏览器登记链路(两轮全过)

| # | 步骤 | 结果 |
|---|---|---|
| 1 | 登录(13800000002) → 首页金刚区"资产估值" → 62号页 | 五页签齐全 |
| 2 | 登记页签 → 加载要素字典 | GET /api/av62/registry 成功, 表单完整渲染 |
| 3 | 角色"企业" | 4 域 chips(behavior/compliance/knowledge/risk(负)), 默认选中态正确 |
| 4 | 选 compliance 域 | "企业合规资产(权重 0.25)" + 3 证据输入框 |
| 5 | 填主体 + licenseCount → 提交登记 | POST /api/av62/assets 成功, **登记 #1 · 状态 registered · 模式标记 assist** |
| 6 | 资产页签刷新 | 回显"共 1 项 · #1 · 主体 99999 · 企业 · compliance" |
| 7 | (修正版复验)主体 99998 二次登记 | 登记 #2, 结果卡**四槽位全有值**(主体/角色/域无空槽), 新 chunk 加载确认 |
| 8 | 控制台 | 两轮均无错误、无 409 |

截图: 登记结果卡 ×2 + 资产列表回显(trae 截图目录)。

### 5.3 测试数据清理(零残留)

- 清理内容: 2 条测试资产(av62_assets:1/2) + 2 条事件留痕
  (av62_events:1/2) + 列表索引(asset_all/event_all LREM);
- 清理后公网复验: GET /api/av62/assets → **total 0**;
- 序列键(asset:seq/event:seq)**保留**——防止资产 id 复用;
- 模式键(mode_state)保留(运行态)。

## 六、工程沉淀

| 主题 | 记录 |
|---|---|
| 动态字典源范式 | 表单字典(角色→域→证据字段)运行时取后端 registry, 常量不落前端——对齐钱包利率"硬编码漂移"教训 |
| 后端最小化返回契约 | 登记返回不含 subjectId/role/domain; 前端回显须用表单本地值——跨端字段核对须在实机首验覆盖 |
| 62号 Redis 测试数据清理路径 | DEL zhuxiang:av62:av62_assets:{id} + av62_events:{id}, LREM asset_all/event_all; seq 保留防 id 复用 |
| 单测 placeholder 断言 | Taro Input 的提示文案在 props.placeholder 不在 children, textOf 断言须覆盖 props |
| 决策面接线口径 | 登记入口经前端 UI 直接调用决策面(区别于该页其余决策面"经管理 API"定位), off 态 409 友好降级复用既有 errMsg 机制 |

## 七、回滚方案

- **代码**: `git revert 57539a2`(后端 1 文件 + 前端 4 文件);
- **后端**: scp av62_registry.py 旧版 + 容器重建(elementDetails 为
  增量观测键, 回滚无数据影响);
- **前端**: dist.bak-av62reg 备份恢复或重构建部署;
- **影响面**: 登记端点 POST /assets 后端本就存在, 回滚仅撤前端入口与
  字典明细, 既有资产数据与估值链路零影响。

## 八、验收结论

| 维度 | 结论 |
|---|---|
| 功能完备性 | ✅ 登记页签全链路(字典/联动/证据/提交/回显)落地, 动态字典源接入 |
| 质量达标 | ✅ 84/84 断言全绿(后端 59 存量零破坏 + 前端 25) |
| 生产实证 | ✅ 容器/公网/浏览器两层实机两轮全过, 结果卡缺陷当日闭环 |
| 数据卫生 | ✅ 测试资产/事件清理归零, seq 保留防复用 |
| 遗留项 | **零必办遗留项**(估值 assess/压测/校准仍按原定位经管理 API 调用) |

**验收通过。** 62号信任要素登记入口开通, 前端消费面从纯观测升级为
"观测 + 登记"双层触达, 表单字典全动态化, 生产实机零残留收官。
