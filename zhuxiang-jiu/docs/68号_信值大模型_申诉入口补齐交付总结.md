# 68号·信值大模型 申诉入口前端补齐交付总结

> 交付日期：2026-09-15 | 提交：`db19125`（后端+测试）+ `9e4bdb7`（前端）+ `f1c4507`（P4 基线同步） | 环境：生产 zxjiu.com（47.236.61.117）+ 本地开发
> 背景：上一会话核查发现申诉通道"后端完备、前端零入口"断层——`POST /api/xx64/appeals` 生产可用，但前端无申诉 API 方法与 UI。本次补齐闭环。

---

## 一、交付内容

### 1.1 后端（`db19125`）

| 项 | 内容 |
|----|------|
| 新端点 | `GET /api/xx64/my-orders?buyer_id=&status=&limit=`（member/admin，观测面不受 XX64_MODE 影响） |
| 联查语义 | 每单附最新申诉进度（同订单多申诉取 appealId 最大）；返回 appealId/status/decision/submittedAt/reviewedAt/expiresAt |
| 填补缺口 | 原 `GET /orders` 为 admin 专属，会员无渠道列出自己的兑换订单——申诉入口无数据源 |

### 1.2 前端（`9e4bdb7`）

| 层 | 内容 |
|----|------|
| API（xx64.ts） | `myOrders()` + `APPEAL_STATUS_NAME` 四态字典 + `Xx64MyOrderVO`/`Xx64AppealVO`/`Xx64AppealResultVO` |
| trust 页 | 「订单·申诉」区块：订单卡（#orderId+品名+九态徽章+金额+时间）+ 申诉按钮 + 理由弹层（Textarea 1-500 字实时计数）+ 受理结果回显（申诉编号/状态/终审截止/宪法口径提示） |
| 申诉判定 | 非 initiated 且无进行中申诉可诉；终审态（approved/rejected/expired）后可再次申诉 |
| 规则说明 | 补申诉条款：通道永不关闭/确定性重算仅展示/终审人工 48h/翻转补偿 |

### 1.3 附带修复

- **68 个 Python 文件多 BOM 叠加污染**（2-6 个 BOM）：导致 xx64 P0-P5 测试、anchor/redteam 服务本地 `SyntaxError` 长期无法运行（存量静默故障）——批量清理为单 BOM
- **P4 宪法断言基线同步**（`f1c4507`）：44号档案在册 55→64（全站评分器注册演进），断言名与检查值同步并注明演进规则

## 二、验证矩阵

| 验证项 | 结果 |
|--------|------|
| 后端专项 test_xx64_my_orders.py | **12/12**（鉴权 403/隔离/联查/终审翻转/最新语义/缺参 422） |
| P4 申诉全链回归 | **42/42**（BOM 修复后首次恢复可跑；基线同步后全绿） |
| 前端单测 test-trust-appeal.js | **12/12**（API 层 5 + 页面层 7） |
| tsc --noEmit | trust/xx64 零类型错误 |
| H5 构建 | webpack 通过（app.4253e9eb.js） |
| ruff lint | 新增/修改文件全绿 |

## 三、生产部署与自验

1. 后端：xx64_routes.py 同步 → 容器重建 healthy → 本地/公网 `my-orders` 200（member 鉴权语义正常，无 Role 403）
2. 前端：dist 打包 → scp → 原子替换（`/var/www/zxjiu/dist`，旧版留 `dist.old` 备份）→ index.html 已引用 `app.4253e9eb.js`
3. 服务器临时文件清理完毕

## 四、68号三类入口终态

| 入口 | 后端 | 前端 | 状态 |
|------|------|------|------|
| 主入口（信值臻选频道） | ✅ | ✅ | 完整 |
| 修复入口（反馈四步闭环） | ✅ | ✅ | 完整 |
| **申诉入口（trust 页订单·申诉）** | ✅ | ✅ | **本次补齐** |

用户链路：信值兑换页 → 订单·申诉卡 → 申诉按钮 → 理由提交 → 确定性重算（precheck 四查+explain+P3 五防）→ 48h 人工终审 → approve 翻转（disputed→paid 冻结解除）/reject 维持 → 翻转留痕供第38档案。
