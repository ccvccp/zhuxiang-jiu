# 43号·AI智能安全管理模块 交付总结（P0-P3 全量）

> 定位：**每个请求进来，AI 都先看一眼**——威胁评分低放行、可疑挑战、高危封禁；每个角色的每个动作都建行为基线，偏离即预警；登录序列建模识破跳步攻击；攻击确认与误报裁决自动回流学习，防线随攻防态势**自动升降级**。把安全从"出事后人工排查"变成"AI 常态化值守、越打越准的免疫系统"。

---

## 一、模块概览

| 维度 | 数据 |
|------|------|
| 分期 | P0 安全网关+威胁评分 → P1 挑战验证+申诉双通道 → P2a UEBA 行为基线 → P2b 态势三态+学习回流 → 管理面板 → P3-1 响应钩子 → P3-2 真实验证码三态 → P3-3 GeoIP+设备指纹 → P3-4 登录序列建模 |
| 端点 | 23 个（会员4：状态/挑战应答/申诉提交/申诉列表；管理19：态势总览/事件流水/事件裁决/IP列表/IP封禁/IP解封/IP钉住/封禁列表/申诉队列/申诉裁决/行为重建/基线查询/偏离记录/态势查询/态势切换/态势钉住/学习collect/学习run/学习status） |
| AI 评分器档案 | 第26档案 `security_threat_gate`（batch 10，六因子可学习权重） |
| 检测器 | 5 个（UEBA 四检测器 D1-D4 + 登录序列 D5，外加撞库预警、设备指纹、异地跳变、确定性攻击硬规则） |
| 专项测试 | 293 项（P0 68 + P1 52 + P2a 39 + P2b 34 + P3-1 27 + P3-2 24 + P3-3 23 + P3-4 26） |
| 实机验收 | 100 项（P0 7 + P1 21 + P2a 13 + P2b 19 + P3-1 8 + P3-2 10 + P3-3 9 + P3-4 11，含真实登录 E2E/Redis 落地实测/geo 静默关闭路径） |
| 全量回归 | 1189/0，与基线完全一致，全程零新增失败 |
| 前端 | 安全管理面板（六区块，浏览器实测全绿）+ 会员挑战页 |
| Commit | P0 `a73ba5b` → P1 `dc57928` → P2a `8a39955` → P2b `2e65f7d` → 面板 `b21264c` → P3-1 `c3ba47a` → P3-2 `4e7f9b4` → P3-3 `e350dcf` → P3-4 `6845b0d` |

## 二、九期核心交付

### P0：安全网关中间件 + 威胁评分（第26档案）
- **ASGI 网关**（JWTAuthMiddleware 范式平移）：挂载次序 CORS(外) → **SecurityGateway(中)** → JWT(内)——攻击请求在进入鉴权层之前被评分拦截；白名单快道；BodyPeeker 请求体预读回放
- **ThreatGateScorer 六因子**：IP信誉 0.20 / 请求频次 0.20 / 注入特征 0.25 / 路径异常 0.10 / 身份风险 0.15 / 时空异常 0.10 → 四档处置（≥70 allow / 50-70 throttle 渐进延迟 / 25-50 challenge / <25 block）
- **确定性攻击硬规则**：任一注入特征/探针路径命中 → 至少 challenge；多类特征叠加 → 直通 block（防单信号被加权合议淹没）
- **攻击特征库**：SQLi/XSS/路径遍历/命令注入/扫描器 UA 五类正则 + 探针路径 + query URL 解码（防编码绕过）
- **IP 信誉库三态**：冷启动 80 / 扣分 -10~-60 / 冷却恢复 / 钉住；封禁 TTL 自动解封
- **防误杀三保险**：observe/shadow/enforce 三级灰度 / fail-open / `SECURITY_GATEWAY_MODE=off` 一键回退

### P1：挑战验证 + 申诉双通道 + enforce 四档处置
- **挑战通行证**：应答验证 → IP 通行证 TTL 900s 豁免挑战档（豁免仍留痕 `challenge_exempt` 可申诉）
- **误报申诉双通道**（42号范式平移）：会员申诉（一事件一申诉幂等）→ 管理裁决 `approve` → 事件置 false_positive + **IP 信誉返还 + 自动解封**
- **事件直接裁决**：管理端确认攻击/误报——**学习真值源**

### P2a：UEBA 行为基线（四层流水线）
- **采集层（零侵入）**：网关顺带三维计数直方图（memberId × hour × module）+ 管理端审计日志只读聚合
- **基线层**：双层基线（个人 → 同角色全局兜底 → 双缺完全豁免，新会员零误报）
- **检测层**：D1 时段偏离 / D2 频率偏离（P95×3 防养号）/ D3 敏感功能首次触碰 / D4 试探堆积
- **联动层**：偏离分注入 identity_risk（只降不升）+ behavior_alert 入事件流水复用裁决链

### P2b：态势三态自动升降级 + 学习回流（智能自主升级）
- **三态**（对标 Cloudflare Under Attack Mode）：peace ×1.5 / alert ×1.0 / wartime ×0.3——防线随攻击密度 EMA 自动升降级（滞后防抖：连续 2 窗口越线才转换）；pinned 钉住优先
- **学习回流三连**：collect（裁决真值批量回流，eventFed 幂等）→ run（Hedge 一轮学习）→ 六因子权重自动调优

### 管理面板：六区块前端
- [ai-security-dashboard.html](../ai-security-dashboard.html)：态势卡（手动切换/钉住）/ 攻击事件（一键裁决+因子展开）/ 申诉队列 / IP 处置 / UEBA 基线（直方图可视化）/ 学习回流三连

### P3-1：网关响应钩子（D4 完整收口）
- **_SendWrapper** 响应观测层：捕获状态码其余透传（响应语义零改写），全部放行路径统一经 `_finish`（fail-open）
- **403/401 加权堆积**（24h 滚动，401 计 0.5）：网关管线实时查询 → D4 完整落地（替换参数化口径）

### P3-2：真实验证码三态通道（极验 v4 / hCaptcha）
- [captcha_client.py](../backend/services/captcha_client.py)：mock/real/mock_fallback 三态（41号范式平移），HMAC 签名头 + httpx 重试
- **票据一次性消费**（SHA256+SETNX 防重放）；**real 态旧口径拒绝**（无票据应答一律 409，堵死脚本绕过）
- 会员挑战页 [challenge.html](../challenge.html)（验证通过自动返回原页面）
- **real 轨待极验凭证**（.env 配置即启用，mock 轨全链路已实机验证）

### P3-3：GeoIP + 设备指纹（geo 精细化）
- [geoip_service.py](../backend/services/geoip_service.py)：GeoLite2 离线库**纯 stdlib** mmdb 二分检索（零外部依赖，缺失静默关闭）
- **设备指纹**（复用 39号信任设备表）：陌生设备打敏感端点降 60 分；信任设备数 ≥3 撞库信号降 40 分
- **异地跳变**：2h 滚动窗口地理历史（mmdb 放入 `./data/` 即启用）；**XFF 反代头**第一跳
- 顺带修复 39号 `entry_repository.save_device` memberId 未落记录体的存量缺陷

### P3-4：登录序列建模（D5 跳步检测）
- **auth_event 留痕钩子**：登录成功/失败入 security_events（火后不管）；**撞库预警**（24h 失败堆积 ≥5 自动生成 behavior_alert）
- **会话序列**：登录标记 `__login__` 起点 + 最近 5 module 环形缓冲（TTL 对齐 JWT 2h）
- **D5 跳步检测器**：真会话 + 登录后 3 请求内直奔敏感端点 + 无常规浏览 → identity_risk 降 20（仅降分不处置，observe 口径）
- 真会话校验（`__login__` 起点防未登录流量误检）

## 三、数据模型（前缀 security43，双模式内存+Redis）

| 表/键 | 用途 |
|-------|------|
| security_ip_reputation | IP 信誉库（三态/攻击计数/冷却/pinned） |
| security_events | 统一事件流水（攻击/行为预警/验证/auth_event/撞库——五档复用一表） |
| security_blocks / security_appeals | 封禁表（TTL 懒清理）/ 误报申诉 |
| security_baselines | UEBA 双层基线（member:{id} + role:{name}_global） |
| security_posture | 态势全局单例（三态/EMA/防抖/转换历史） |
| behavior:{mid} / session:{mid} | 三维计数直方图 / 会话序列环形缓冲 |
| rate:* / geo:{mid} / challpass:* | 频次+堆积计数（forbidden/authfail）/ 地理历史 / 通行证+票据消费 |

## 四、验证体系

| 层级 | 规模 | 结果 |
|------|------|------|
| 宿主机专项（8 套） | 293 项 | 全绿 |
| 实机 Docker 验收（8 套） | 100 项 | 全绿（可重复执行） |
| 面板浏览器实测 | 六区块 | 全绿 |
| 全量回归 `pytest -m "not redis"` | 1189 项 | 0 失败 |

**实机验证亮点**：SQLi 攻击（URL 编码载荷）真实容器识别留痕 / 误报裁决自动恢复信誉+解封 / 态势切换全链路 / Hedge 学习真实触发 / 403 堆积 Redis 落地 / 票据重放 409 / geo 静默关闭路径 / **真实注册→登录→D5 会话→撞库预警 E2E**。

## 五、交付过程中的关键修复（11 项）

| 问题 | 修复 |
|------|------|
| 单次 SQLi 被加权合议淹没（86 分仍放行） | 确定性攻击硬规则：单特征至少 challenge、叠加直通 block |
| URL 编码攻击绕过 | 扫描前 `unquote_plus` |
| 非 /api 探针路径绕过（`/.env`） | 放开快道前缀限制 |
| 通行证豁免后审计断链 | 豁免留痕 `challenge_exempt` 且可申诉 |
| 申诉/基线/态势表 KeyError | `_ensure_store` 补注册（三次） |
| manual 态势模式不生效 | 实时读环境变量（记录值仅留痕） |
| verify_challenge await 缺失 | D2 查询补 await |
| finance 仓储 Redis WRONGTYPE（42号遗留） | seq 键过滤 `":X:seq" not in k`（4 处） |
| 根目录 pytest 18 项假失败 | 根级 pytest.ini（目录陷阱根治） |
| 39号 save_device memberId 未落记录体 | 修复（P3-3 依赖暴露） |
| D5 误检未登录流量 | 真会话校验（`__login__` 起点标记） |

## 六、配置与运维

**环境变量**（docker-compose 已全部暴露）：

| 变量 | 默认 | 说明 |
|------|------|------|
| `SECURITY_GATEWAY_MODE` | on | 总开关（off 一键回退） |
| `SECURITY_ENFORCE_LEVEL` | observe | observe/shadow/enforce |
| `SECURITY_RATE_WINDOW / RATE_LIMIT` | 60/120 | 频次（态势自动缩放） |
| `SECURITY_BAN_TTL` | 900 | 封禁秒数 |
| `SECURITY_UEBA_MODE / BURST_FACTOR / HOUR_WEIGHT` | on/3/0.05 | UEBA 三参 |
| `SECURITY_POSTURE_MODE` | auto | auto/manual |
| `SECURITY_CAPTCHA_MODE / CAPTCHA_PROVIDER / CAPTCHA_ID / KEY` | mock/geetest | 验证码三态（real 待凭证） |
| `GEOIP_DB_PATH`（挂载 ./data:ro） | — | GeoLite2 离线库 |
| `SECURITY_D5_MODE / D5_JUMP_WINDOW` | on/3 | 跳步检测 |

**enforce 上线检查单**：observe ≥1 周 + 误报率 <10% + 积压清零 + 申诉通道畅通 + 切换后 30 分钟盯封禁数与 200 率。

## 七、文档与测试资产

| 资产 | 路径 |
|------|------|
| 设计文档 | [AI智能安全管理模块43_设计文档.md](AI智能安全管理模块43_设计文档.md) |
| UEBA 实现方案 | [43号P2_UEBA行为基线实现方案.md](43号P2_UEBA行为基线实现方案.md) |
| P3 实施计划 | [43号P3_纵深增强实施计划.md](43号P3_纵深增强实施计划.md) |
| 管理端操作指南 | [安全管理端操作指南.md](安全管理端操作指南.md) |
| 管理面板 / 挑战页 | ai-security-dashboard.html / challenge.html |
| 专项+实机（8 套） | backend/test_security_p*.py + verify_security_p*_live.py |

## 八、当前运行状态与后续方向

**当前状态**：`observe` 灰度运行——全部检测器（D1-D5 + 硬规则 + 设备/地理信号）留痕不处置，enforce 待误报率验证达标后切换。

**待办（外部凭证）**：
- 极验商户注册（captcha_id/key）→ real 验证码轨冒烟
- MaxMind 账号 → GeoLite2 mmdb 放 `./data/` 启用异地跳变

**可选增强（P4 展望）**：
- D5 上线 observe 两周后按误报率数据决定是否参与强制联动
- GeoLite2 城市名/经纬度完整解码（当前为命中子集）
- 威胁情报源接入（外部 IP 黑名单订阅）
- UEBA 基线接入日度调度器（当前手动 rebuild）

---

*43号模块 P0→P3 全部交付（2026-09-03）；九期演进形成完整安全免疫系统：请求级（网关六因子+硬规则）→ 身份级（设备指纹/异地跳变/撞库）→ 行为级（UEBA 基线+D5 序列）→ 态势级（三态自适应）→ 学习级（裁决真值 Hedge 回流），全部 observe 灰度可平稳切换 enforce。*
