# 78号·竹语 TTS G1 完整版（首日窗口）与 D1 门终裁报告

> 版本 v1.0 · 2026-09-23 18:30
> 窗口如实说明：G1 采样窗（09-23~09-29）现仅过首日——本报告为**首日窗口版**；09-30 09:00 crontab 自动出**正式周样本版**（g1_final_0930.txt）作最终复核。D1 门三项裁决中，可凭现有证据链终结的项在本报告终裁，数据不足项挂起至正式版。
> 取数：g1_final.py 双源（服务端埋点 + 客户端 [LAT] 聚合）

---

## 一、G1 首日全量数据

### （一）服务端埋点（容器窗口 n=34+9）

| 指标 | P50 | P90 | max | 判读 |
|---|---|---|---|---|
| 整句 conn | 26ms | 71ms | 103ms | 健康 |
| 整句 acoustic | 994ms | 1770ms | 3608ms | 唯一瓶颈（已知结论） |
| **流式 first_chunk** | **903ms** | 1003ms | 1214ms | **今日为慢档主导时段**（15:49 快档 32~83ms 为唯一快档观测窗） |
| 流式 total | 1815ms | 2515ms | 2827ms | 仍优于整句冷合成 |
| 轮次执行 | 43ms | 91ms | — | 健康 |
| nginx /tts 日量 | 20日88 → 21日233 → 22日449 → 23日276 | | | 样本量无忧 |

### （二）客户端 [LAT] 聚合（n=9）与数据清洗（关键）

原始聚合 s2 P50=7990ms、异常 5/9——**不可直接引用**，逐条溯源清洗：

| 异常样本 | 溯源 | 定性 |
|---|---|---|
| 17:36 s2=21000ms | curl 验收脚本人为注入的测试样本 | **剔除**（非真实轮） |
| 18:04~18:07 s2=7990/12618/15128/22027ms ×4 | headless 虚拟麦克风回录自循环：回录 12s+ 长音频 → 整段上传轨 → 服务端 ASR 转写 7s+（asr_ms P90=6904 佐证） | **环境伪影**——生产真机 AEC 无此模式，剔除 |

**清洗后真实轮分布**（首日各验收轮）：
- s2（submit→resp）：H2 后 **274~393ms**（验收实测）
- s3（resp→play）：**86~104ms**（缓存命中）/~150ms（流式快档）
- tt（端到端首声）：最优 **1030ms**
- h2 协议占比 100%

## 二、D1 门三项裁决

### 裁决一：GA（国内动态加速）立项 → **终裁：不立项** ✅

依据链已完备，与周样本无关，可终结：
1. H2 治本已拿走 85%（s2 2.3s→274~393ms，端到端 1030ms 达 1 秒时代）；
2. GA 剩余收益收窄至 ~150-250ms（245ms 本征 RTT → GA 内网 ~70-80ms）；
3. B 路线 ROI 触发器不触发：月账本 48~108 元（2 元/万字符公示价）vs GPU 月租 500~1500 元；
4. 结论：**维持现状（H2 + 流式双层），GA/B 均不立项**；唯一遗留通道为 O1 备案（行政流程可从容办，备案后如需 ~0.3s 再评估）。

### 裁决二：P1 流式验收线 → **P50 通过，P90 挂起至 09-30 正式版** ⏸

- **P50 ≤500ms：通过**（真实轮 s3 86~104ms，远优）
- **P90 ≤800ms：首日数据不支持终裁**——今日 first_chunk P50=903ms 为慢档主导时段（快档 32~83ms 时段亦实测存在），**慢档占比的分时段分布只有周样本能回答**（首日 9 样本全部 >500ms，但时段单一）
- 体感侧：O4 垫场音已把慢档体感压至 ~300ms（首片未到先播"好的"垫场）
- **09-30 判定表**：周样本 first_chunk>500ms 占比 <10% → P90 达标收口；10~30% → 验收口径转"体感线"（垫场后 300ms）收口；>30% → 慢档为常态，评估流式降级权重（缓存优先度上调）

### 裁决三：观察开关（XIAOZHU_LAT_REPORT）→ **保留 on 至 09-30 正式版出数后关闭** ⏸

首日样本不足以替代周样本——观察继续积累；09-30 crontab 出 g1_final_0930.txt 后：正式版复核裁决二 → 关闭上报（compose 置 off）→ 本模型转运营态。

## 三、首日新发现归档

1. **s2 异常根因定案**：headless 回录自循环长音频段（整段轨 ASR 7s+）——环境伪影非生产缺陷，21.1s 快验异常同机理闭环；
2. **智谱双档波动确认**：首日慢档主导（903ms P50）与快档（32~83ms）时段并存——流式 vs 缓存的双层结构价值因此上升（缓存命中永远优先，慢档只影响冷路径）；
3. 生产真实购物流量持续（日 /tts 276 次、含高敏确认轮）。

## 四、终裁结论

**D1 门提前终裁（部分终结）**：GA/B 立项裁决**终结**（均不立项，依据链完备）；P1 验收 P50 **通过**、P90 挂起；观察开关保留。**09-30 09:00 crontab 正式周样本版为最终复核节点**——出数后完成裁决二收口、关闭观察开关、78号·悦声灵犀整体转运营态。

---

**一句话结论**：首日数据 + 三条腿证据链足以终结"路线之争"（GA/B 不立项，双层结构即终态架构）；P90 慢档占比之问交给 09-30 正式版——那是最后一块拼图。
