# 县（区）网店保证金子系统——上线总结报告

> 文档版本：v1.0 · 2026-09-19
> 上线性质：县（区）网店改造的核心子模块——保证金体系（预存·锁定·到期结算·驳回退还）
> 对应实现：后端提交 7736395（随县区网店改造一并交付）· 配套设计：《县区网店保证金子系统_详细设计文档》（436cce0）
> 生产实证：2026-09-19 两轮容器内闭环测试（真实申请→锁定→驳回→退款，账实相符）
> 验证链：本地 15/15（margin 专项）+ routes 59/59（联动路径）+ Ruff 零告警 + 生产实证全通

---

## 一、上线总览

| 维度 | 数据 |
|---|---|
| 业务规则 | 预存钱包 ¥1000 一年期保证金；不可提前取现；到期按年任务完成度退还；未完成按比例扣除；驳回全额退 |
| 实体形态 | 独立保证金实体 `city_store_margins`（locked → settled 状态机） |
| 资金路径 | 钱包仓储原语（add_balance + save_transaction），双流水 type |
| 模块边界 | wallet 三件（service/repository/routes）**零改动**——天然落资金红线豁免面 |
| 端点 | 2 个：GET /{code}/margin（用户查进度）+ POST /{code}/margin/settle（管理端兜底） |
| 自动运营 | 调度器小时级到期结算轮（到期日全年分布，不限月初窗口） |
| 并发安全 | 锁序单向嵌套 + 结算双重检查幂等（防双退） |
| 生产状态 | zxjiu.com 运行中——真实闭环实证（申请锁定/流水/驳回退款/账实相符） |

## 二、业务规则与口径确认

| 规则 | 口径 | 落地 |
|---|---|---|
| 金额/期限 | ¥1000 × 一年 | MARGIN_AMOUNT=1000 / MARGIN_TERM_DAYS=365（自确认开业日起算） |
| 不可提前取现 | 无 early 接口（负向设计） | 不提供入口即不可调用（测试含负测） |
| 到期结算 | 按年任务完成度 | rate=min(1, 年进货额/50000)，refund=round(1000×rate,2) |
| 比例扣除 | 完成 80% 扣 200 退 800 | 实证用例（15/15 之一） |
| 中途取消 | 按已运营期折算目标 | rate=min(1, 进货/(50000×elapsed/365)) |
| 驳回 | 未开业全额退 | reason=rejected，refund=1000 |
| 年任务基准 | 年进货额 ¥5 万 | ANNUAL_PURCHASE_TARGET=50000，与月度考核同源同口径 |

## 三、方案裁定（为什么是独立实体）

| 候选 | 裁定 | 理由 |
|---|---|---|
| 复用 wallet deposit（定期） | ❌ | DEPOSIT_TIERS 收益/LPR/奖品语义污染；early_settle 全局放行与"不可提前取"直接矛盾 |
| 进 frozenAmount | ❌ | 提现在途语义，会被提现链路误伤 |
| **独立 margin 实体** | ✅ | 沿用 citystore 双模式范式；资金只借 wallet repo 原语——wallet 三件零改动 |

**红线豁免面论证**：保证金端点全部挂 citystore_routes（无 @_decision 门控），WALLET_MODE=off 时驳回退/到期退/中途退照常可用——符合"存量资金退出零影响"铁律（设计文档 §8.3 三档行为表）。

## 四、代码变更明细（随 7736395 交付）

| 文件 | 保证金相关变更 |
|---|---|
| repositories/citystore_repository.py | margin 常量（MARGIN_AMOUNT/MARGIN_STATUS_*/MARGIN_SETTLE_REASONS）+ 实体 CRUD（内存/Redis 双模式三键）+ `sum_purchase_between` 日期范围进货统计 |
| services/citystore_service.py | apply 保证金前置校验与锁定（步骤 5/8）/ audit 确认开业补 365 天与驳回联动 / `settle_margin` 幂等结算 / `get_margin` 实时进度 / `run_margin_settlement` 批量轮 |
| routes/citystore_routes.py | 2 新端点（静态路径注册先于动态路由——防遮蔽铁律） |
| services/citystore_assessment_scheduler.py | `run_margin_settlement_round` + 小时级循环追加 |
| test_citystore_margin.py | 15 用例专项（新增） |

## 五、测试矩阵（15/15 + 联动 59/59）

| 类别 | 用例 | 结果 |
|---|---|---|
| 锁定 | 扣款 1000 / locked 无起止 | ✓ |
| 开业起算 | endDate = startDate+365 | ✓ |
| 实时进度 | 年进货 4 万 / 完成率 0.8（不落库计算字段） | ✓ |
| 到期 80% | 退 800 扣 200 / 钱包 +800 | ✓ |
| 满额封顶 | 6 万进货 rate=1.0 全退扣 0 | ✓ |
| 中途干满 | 182 天进 2.5 万 rate>1 封顶全退 | ✓ |
| 中途躺平 | 100 天进 5 千退 ≈365 扣 ≈635 | ✓ |
| 幂等 | 重复结算 refund 不变 / 钱包不变（不双退） | ✓ |
| 驳回 | 全额退 + settled/rejected | ✓ |
| 非法原因 | settle_margin(code,"hack") 409 | ✓ |
| 联动路径 | routes 59 用例（update_status 取消联动 / 考核取消联动 / 驳回联动） | ✓ |

## 六、生产实证验证（两轮容器内闭环，2026-09-19）

### 6.1 真实闭环流程

```
① 仓储层注入测试余额: member3 ¥800 → ¥2800
② HTTP 申请漠河店(232701): CS-232701-002 创建
   保证金 CD202609190001 locked + 钱包扣款 ¥1000(2800→1800)
③ 平台驳回: 店 pending→cancelled, margin settled/rejected
   钱包退款 ¥1000(1800→2800)
④ 收尾: 测试余额收回(2800→800), 店/margin 均终态, 区县释放
```

### 6.2 流水实证（钱包账单原样）

| txNo | type | 方向 | 金额 | balanceAfter | 描述 |
|---|---|---|---|---|---|
| WT202609190001 | citystore_margin_lock | OUT | ¥1000.00 | 1800 | 县（区）网店保证金预存锁定 ¥1000.00(一年期, 不可提前取现) |
| WT202609190002 | citystore_margin_settle | IN | ¥1000.00 | 2800 | 县（区）网店保证金驳回全额退还 ¥1000.00 |

余额链全对：800→2800(注入)→1800(锁定)→2800(退款)→800(收回)——**账实相符**。

### 6.3 运行状态

- 调度器 `citystore_assessment_scheduler started interval=3600s`（含保证金到期结算轮，当前无到期记录静默正常）
- 验证脚本两次 FAIL 均为脚本字段解析问题（`currentBalance`/顶层 `transactions`），修正后数据全部在案——系统零缺陷

## 七、前端联动（zjcity6 一并交付）

- 申请弹层：身份证号/姓名输入 + **保证金协议签名勾选行**（"预存保证金¥1000(一年期不可提前取现, 到期按年任务完成度退还)"）——提交前强制勾选
- 提交校验链：缺签名 toast 拦截 → 完整提交发 POST → 后端 409 钱包校验消息原样回显（浏览器实测）
- 规则文案：保证金条款 + 分店备案 + 年任务 5 万

## 八、运营机制与回滚

**运营节奏**：申请（身份证+签名+保证金锁 1000）→ 确认开业（保证金起算一年）→ 月度考核 → 年度结算（调度器每小时轮询到期 margin；管理端 POST /{code}/margin/settle?reason={expired|cancelled|rejected} 手动兜底，幂等安全）。

**运维速查**：
```bash
# 查店保证金(含年任务实时进度)
GET /api/citystore/{code}/margin          # Bearer JWT
# 手动补结算(已 settled 直接返回, 不双退)
POST /api/citystore/{code}/margin/settle?reason=expired   # Bearer + X-Role: admin
# 调度日志
docker logs zhuxiang-backend-1 | grep margin
```

**回滚**：git revert 7736395（margin 为新增独立实体，不影响钱包存量）；生产 locked 记录可经管理端 settle 手动结算退出（豁免面保证任何档位可退）。

## 九、终态

| 项 | 终态 |
|---|---|
| 生产运行 | 保证金子系统随县区网店全链上线（申请锁定/确认开业起算/三条取消联动/到期自动结算） |
| 实证等级 | 本地 15/15 + 联动 59/59 + 生产真实闭环（申请→流水→驳回退款→账实相符）+ 调度器运行确认 |
| 模块边界 | wallet 三件零改动（资金原语复用 + 豁免面外挂） |
| 文档 | 详细设计文档 v1.0（436cce0，十一章）+ 本上线总结 |
| 测试环境收尾 | 干净（店/区县/保证金均终态释放，member3 余额恢复初始） |

**结论**：保证金子系统完成设计（独立实体+幂等结算+豁免面）、实现（随 7736395）、测试（15/59 双矩阵）、生产实证（真实闭环账实相符）的全周期交付——设计与运行三层一致，可正式运营。
