# 智酿运通（智运二代 P4-P7）生产部署验收报告摘要

> 部署日期: 2026-09-12 · 代码基线: `e817ee4`（部署记录落档: `249308d`）
> 部署范围: 智酿运通·自适应酒水供应链物流中枢（P4 语义调配+熔断 / P5 深度绑定 / P6 角色提醒 / P7 进化2.0）
> 结论: **验收通过**——后端 37 端点、前端八页签工作台全链上线 zxjiu.com，一代智运与既有物流 18 端点零改动回归通过

---

## 一、部署动作

| 项 | 内容 | 结果 |
|---|---|---|
| 后端 | git archive 8 文件落盘 tar → scp → `/opt/zhuxiang` 解压覆盖（`.bak-wt-e817ee4` 备份）→ `docker compose -p zhuxiang up -d --build backend` | 30 秒 healthy；5 二代服务 + 路由 42 条容器内导入 OK |
| 前端 | `npm run package:deploy` 545KB → scp → `/var/www/zxjiu/dist` | index.html 时间戳更新；zw 页块（`4701.eee2208b.js`）含全部新端点字符串 |
| 过程异常 | 首次解压 `-C zhuxiang-jiu` 造成目录嵌套（tar 内含仓库前缀） | 即时回滚修正为 `-C /opt/zhuxiang`，无残留影响 |

## 二、生产验收明细（zxjiu.com 实测）

### 2.1 GET 抽检（7/7 通过）

| 端点 | 判据 | 实测 |
|---|---|---|
| circuit/status | 五渠道状态+阈值 | 5 渠道（0 open）+ thresholds ✓ |
| semantic/carriers | 注册表返回 | 5 内置渠道（SF/JD/LLL/DB/YT）✓ |
| binding/capacity-plan | 预约建议书结构 | 窗口 3 天、3 渠道分摊 ✓ |
| evolution2/carbon | 碳总览结构 | 总排/单均/渠道占比字段 ✓ |
| evolution2/suggestions | 列表 200 | count=0（诚实，见 2.4）✓ |
| alerts | 列表 200 | count=0（未扫描前）✓ |
| logistics-ai/status | 一代回归 | 9 单、89% 签收、62.75h 时效 ✓ |

### 2.2 POST 链路（6/6 通过）

| 链路 | 实测结果 |
|---|---|
| 四维画像 | 零售 ¥12,800 → `premium_gift` / 风险 high ✓ |
| 画像→特征路由 | 优先 SF + 备选 JD + 进化反馈信号 ✓ |
| 语义归一化 | SF 方言 `bill_no/parcel_weight/state"运输中"` → 平台模型 `waybillNo/weight/status=transporting` ✓ |
| 逆向物流 | damaged → 就地销毁 + 《退货处置协议》✓ |
| 五角色扫描 | 出稿 5 条（consumer:3 / operations:1 / management:1）✓ |
| 策略建议 | 生成 0 条（诚实，见 2.4）✓ |

### 2.3 公开端点与浏览器验收

- **binding/verify/{code}**（全模块唯一公开端点）：未绑定码返回 404「谨防假冒」✓
- **浏览器实测**（zxjiu.com/#/pages/zw/index）：八页签全部渲染——
  - 调配页：渠道注册表（五内置+条件标签）、四维画像/特征路由、熔断卡（顺丰 healthy 样本 4 / 德邦 healthy 样本 3 / 京东·货拉拉·圆通样本不足诚实标注）✓
  - 协同页：舱位预约 / 三码合一+扫码验真 / 逆向三路径 / 五角色提醒 / 碳足迹 / 人机协同裁决六区块齐备 ✓
  - 截图存档: `winetunnel-prod-nexus.png`

### 2.4 零值一致性核对（确定性诚实行为）

`suggest=0` / `anomaly-patterns=0` / `carbon=0` 三者逻辑自洽：
生产现有 9 单为演示种子（ZW-DEMO-*，缺 weight/province 字段）→ 碳核算按 0 诚实处理、异常无跨渠道重复聚类。经正式下单接口（create_order 强制 weight>0）产生的订单字段完整，届时碳排/模式/建议自然激活。**非缺陷，不阻断验收。**

## 三、质量证据链（部署前置）

| 项 | 结果 |
|---|---|
| 智酿运通专项（test_zw_winetunnel.py） | 85/85 全绿（服务+HTTP+宪法铁律回归） |
| 一代智运回归（smart_route） | 22/22 全绿（零改动实证） |
| 物流模块回归（routes/service/repo） | 52+39+79 全绿 |
| ruff lint | 全部通过 |
| 前端 H5 构建 | webpack 5.78 编译成功 |

## 四、宪法对齐核验

- **LLM 禁入决策链**: 路由/熔断/路径/碳核算全确定性规则，实测输出均带 explain/formula ✓
- **建议书模式**: 容量预约/运力异常报告/退货处置协议/整改建议书响应体均含 disposition 字段，永不自动执行 ✓
- **进化可控**: 提醒确认/驳回、策略采纳/拒绝负样本回流端点全链可用 ✓

## 五、回滚与残留

- 回滚路径: 后端 `cp *.bak-wt-e817ee4` 还原 + 重建容器；前端重传上一 dist 包
- 临时部署文件（本地 tar/脚本 + 服务器 /tmp）已全部清理
- 遗留观察项: 渠道熔断阈值（揽收 90%/中转 96h/异常 30%）灰度期观察后可调；IoT 温控与真实碳因子接入为后续增强（公式不变，仅换数据源）

---

**验收结论**: 后端 23 新端点（37 总量）+ 前端八页签全链生产可用；一代与既有模块零影响；宪法三铁律全程贯彻。**建议: 通过验收，转入灰度观察期。**
