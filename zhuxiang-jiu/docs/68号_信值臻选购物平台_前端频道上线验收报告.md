# 68号·信值·臻选购物平台——前端频道上线验收报告

> 范围: 前端「臻选」频道(API 客户端 + 频道页 + 首页入口)从开发、测试到生产上线的全链验收
> 环境: 本地构建(Windows + webpack5/Taro 4.1.9 H5) → 生产 zxjiu.com(Docker + Nginx)
> 验收时间: 2026-09-11(提交 7df2729, 基于 affef62 后端生产就绪之上)
> 结论: **验收通过**(单测 21/21 · 公网观测面 200 · 决策面 409 灰度默认 · 前端产物就位)

---

## 一、验收范围与结论总览

| 验收项 | 结果 | 证据 |
|--------|------|------|
| API 客户端(xinzhi.ts) | ✅ 通过 | 17 方法/12 VO/4 字典, X-Member-Id 自动注入, 单测 14 项映射断言全绿 |
| 频道页(pages/xinzhi) | ✅ 通过 | 四区结构(雷达/货架/导购/邻里), 单测 7 项渲染断言全绿 |
| 前端单元测试 | ✅ 21/21 | 2026-09-11 验收轮复跑(node test/test-xinzhi.js) |
| H5 构建产物 | ✅ 通过 | webpack 5.78 全过, app 主包 293,639B + 臻选页独立 chunk |
| 生产静态资源 | ✅ 就位 | app.88e62cf7.js + 206/738 chunk 公网 200, 中文串 UTF-8 解码命中 |
| 公网观测面联调 | ✅ 200 | mode/radar/prime 三端点实测(见 §4.2) |
| 决策面灰度 | ✅ 409 | XINZHI_MODE=off 默认, guide POST 实测 409 |
| 登录门(会员头) | ✅ 生效 | 无 X-Member-Id 请求 prime → 拒绝"需要登录(X-Member-Id)" |
| 提交入库 | ✅ 已推送 | 7df2729 → origin/master(7 文件, +2023/-1) |

**验收口径**: 观测面可用 + 决策面灰度可控 + 无个人数据红线, 三者同时满足即通过。

---

## 二、交付资产清单(提交 7df2729)

| 文件 | 角色 | 要点 |
|------|------|------|
| `taro-app/src/api/xinzhi.ts` | API 客户端 | 17 方法(观测 10 + 决策 6 + 白皮书 1)、12 个 VO 类型、4 套字典(等级五档/商品四级/五维/求购状态); 所有需身份请求自动注入 `X-Member-Id`; 求购发布人字段仅取 `publisherMasked` 脱敏值; 货架 fit/grade 等字符串数字数值化 |
| `taro-app/src/pages/xinzhi/index.tsx` | 频道页 | 五区结构: 雷达卡(总分+等级徽章+五维条) / 臻选货架(契合×安全×转化) / 价格构成拆解(原价−信值抵扣=实付) / 导购人格(SOP 问答) / 邻里社区(求购大厅+碳档案); 灰度脚注展示当前档位与宪法口径 |
| `taro-app/src/pages/xinzhi/index.module.scss` | 样式 | 棕金主题渐变(对齐 68 号模块视觉), 雷达卡渐变底+卡片阴影+标签栏高亮态 |
| `taro-app/test/test-xinzhi.js` | 单测 | 纯 Node 范式(对齐 test-navbar.js): TS 内存编译 + Module._load 拦截 mock, 零外部测试框架 |
| `taro-app/src/app.config.ts` | 路由 | 注册 `pages/xinzhi/index` |
| `taro-app/src/pages/index/index.tsx` | 入口 | 首页金刚区新增「🏅 信值臻选」入口 → navigateTo 臻选页 |
| `taro-app/package.json` | 脚本 | 新增 `test:xinzhi` |

**BOM 噪声甄别**: 工作区另有 73 个"已修改"文件经逐一 diff 核验均为 BOM 字符重复叠加的编码噪声(无内容变化), 按既有惯例未混入提交, 保持提交纯净。

---

## 三、质量门验证

### 3.1 前端单元测试(21/21, 验收轮复跑)

```
============================================================
信值·臻选购物平台(68号) 前端单元测试
============================================================
  ✓ 字典-等级五档          ✓ 字典-商品四级
  ✓ 字典-五维              ✓ 字典-求购状态
  ✓ 映射-等级名            ✓ 映射-维名
  ✓ API-雷达映射           ✓ API-货架数值化
  ✓ API-价格映射           ✓ API-求购脱敏
  ✓ API-碳档案            ✓ API-邻里聚合
  ✓ API-灰度态             ✓ API-会员头注入
  ✓ 页面-四区结构          ✓ 页面-雷达五维条
  ✓ 页面-等级徽章          ✓ 页面-碳档案行
  ✓ 页面-灰度脚注          ✓ 页面-反馈入口
  ✓ 页面-渲染确定性
------------------------------------------------------------
通过: 21 / 21
```

**断言分组口径**:

| 层 | 数量 | 覆盖 |
|----|------|------|
| 字典/映射层 | 6 | 等级 S-A-B-C-D 五档、商品 L1-L4 四级、五维中文名、求购状态、未知值回落 |
| API 层 | 8 | 雷达五维数组+熔断/冷启动布尔、货架字符串数值化、价格 α/抵扣/审计标志、求购脱敏(仅 publisherMasked 无个人明细)、碳档案(不可交易口径)、邻里匿名聚合、灰度三态、请求头自动注入 |
| 页面层 | 7 | 四区结构渲染、五维条分数、等级徽章类名(gradeS/A/B/D)、碳档案行、灰度脚注(off 态提示)、反馈面板开合、同 props 多次渲染结构一致(确定性) |

### 3.2 构建产物

| 项 | 实测 |
|----|------|
| 构建器 | webpack 5.78(Taro 4.1.9 H5 模式) |
| 主包 app.88e62cf7.js | 293,639 B |
| 首页 chunk 206.4b0a9244.js | 18,719 B(含信值臻选金刚区入口) |
| 臻选页 chunk 738.85d6cfee.js | 26,354 B(页面代码分割, 按需加载) |
| 路由注册 | `pages/xinzhi/index` 已入 app 主包路由表 |
| 体积护栏 | 符合 config/index.ts 既有 splitChunks 优化口径 |

---

## 四、生产部署验收(zxjiu.com 公网实测)

### 4.1 静态资源就位

| 资源 | HTTP | 关键内容命中(UTF-8 解码实测) |
|------|------|------------------------------|
| `/js/app.88e62cf7.js` | 200 | `pages/xinzhi/index` 路由 ✓ · `X-Member-Id` 注入逻辑 ✓ |
| `/js/206.4b0a9244.js`(首页) | 200 | `信值臻选` 金刚区标签 ✓ · `pages/xinzhi/index` navigateTo ✓ |
| `/js/738.85d6cfee.js`(臻选页) | 200 | `臻选货架` ✓ · `邻里社区` ✓ · `小竹臻选导购` ✓ · `灰度开放中` ✓ · `gradeA` ✓ |

### 4.2 后端观测面联调(频道页数据链路)

| 端点 | 实测 | 说明 |
|------|------|------|
| `GET /api/xinzhi/mode` | **200** | `mode=off, source=env, paused=false`; 响应携带"观测与纠错永不关停(宪法口径)"与决策面 6 项清单 |
| `GET /api/xinzhi/radar`(X-Member-Id=1) | **200** | 五维实测: 诚信度 100(权重 0.2)/互助值 50(权重 0.25)/专业度 7.6/…, 因子归因文本完整 |
| `GET /api/xinzhi/prime?limit=3`(X-Member-Id=1) | **200** | 真实货架: ZX52-2026X01 竹香小享 52°(fit 64.0/safety 1.0/conversion 0.5671)等 |
| `GET /api/xinzhi/prime`(无会员头) | **401 拒绝** | 响应"需要登录(X-Member-Id)"——登录门生效, 观测面身份口径符合设计 |
| `POST /api/xinzhi/guide`(X-Member-Id=1) | **409** | 决策面关闭态正确拒绝(off 默认灰度) |

> 注: 生产 Redis 业务数据(857 keys)在部署轮已确认零影响(见 affef62 部署记录), 本轮验收未再触碰。

---

## 五、红线核查(前端承载口径)

| 红线 | 前端实现 | 核查结果 |
|------|----------|---------|
| 观测面永不关停 | 频道页观测区数据加载不受 mode 影响; 灰度脚注如实展示当前档位 | ✅ mode=off 时雷达/货架/价格/邻里/碳档案照常渲染 |
| 决策面 off=409 | 决策操作(导购问答/求购发布响应关闭/反馈提交)走决策 API, 409 时页面呈现灰度提示而非静默失败 | ✅ 公网实测 409 |
| 零个人数据 | 求购卡片仅渲染 `publisherMasked`(如"邻里12**"); 邻里频道为品类聚合+匿名门槛 K=5 | ✅ API 层无个人明细字段透传 |
| 价格透明 | 价格构成拆解区公示「原价−信值抵扣=实付」链路, 地板价/审计标志如实展示 | ✅ 单测断言 α/抵扣/审计标志映射 |
| LLM 禁入展示层 | 导购话术为后端确定性模板拼接, 前端仅做数据插值渲染, 不在前端拼接数字 | ✅ 渲染确定性单测(同 props 多次渲染一致) |
| 会员头注入 | `getMemberId()` 统一注入 `X-Member-Id` | ✅ 单测断言请求头存在; 公网无头 401 佐证 |

---

## 六、遗留事项与运维口径

1. **weapp 产物已重建、待人工上传微信平台**: 验收补充轮发现原 `dist-weapp` 为过期产物(2026-09-11 12:45 构建, 早于臻选页源码定稿, 未含该页), 已于同日 17:44 重建(`TARO_OUTPUT_DIR=dist-weapp`, webpack 48.75s 编译通过)。产物核验: app.json 路由含 `pages/xinzhi/index` ✓ · 页面四件套(js/json/wxml/wxss, wxss 10,927B)就位 ✓ · 臻选货架/邻里社区/首页入口接线等关键串(unicode 转义态)命中 ✓。上传发布仍需人工经微信开发者工具操作(既定可选项, 非本次验收范围)。H5(zxjiu.com)为本次验收主口径。
2. **灰度切换**: 切 shadow/assist 档走后端运行时 override(见运行报告 §2.1), 前端无需发版——灰度脚注自动跟随 mode 响应。
3. **回滚口径**: 前端回滚即回退 nginx web 根产物至上一部署包; 后端 XINZHI_MODE 已默认 off, 天然零影响。
4. **监控**: A/B 护栏三指标(退款/客诉/卸载)由后端 P5 护栏自动监控, 前端无独立监控面。

---

## 七、验收结论

前端「臻选」频道已通过全链上线验收:

- **功能完整性**: 17 API 方法 + 五区页面 + 首页入口, 与 68 号后端 P0-P5 全部观测/决策端点一一对接;
- **质量门**: 单测 21/21(验收轮复跑), 构建零错误, 产物体积合规;
- **生产就绪**: zxjiu.com 静态资源就位, 观测面联调 200, 决策面 409 灰度默认, 登录门生效;
- **红线合规**: 零个人数据、观测面永不关停、决策面可灰度、渲染确定性;
- **可追溯**: 提交 7df2729 已推送 origin/master, 与后端提交链(951e83d→…→affef62)形成完整闭环。

**用户访问路径**: https://zxjiu.com → 首页金刚区「🏅 信值臻选」→ 频道页(雷达卡/臻选货架/价格构成/导购/邻里社区/碳档案)。

---

*报告生成: 2026-09-11 · 验收执行: 前端臻选频道收官轮 · 证据基准: 提交 7df2729*
