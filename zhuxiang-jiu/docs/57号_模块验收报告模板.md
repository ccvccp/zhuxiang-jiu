# 57号·AI智能知识库模块 模块验收报告（模板）

> 验收日期：＿＿＿＿-＿＿-＿＿
> 验收环境：Windows 本地（asyncio 内存态专项复跑 + HTTP 全端点冒烟）+ Docker 实机（Redis 态 ×2 轮幂等——镜像＿＿＿＿，容器 healthy）
> 验收人：＿＿＿＿＿＿ ｜ 评审人：＿＿＿＿＿＿
> 结论：**［通过 / 有条件通过 / 不通过］**——六期专项 283 断言 + 实机 198 项次（6 脚本×2 轮幂等）+ 红队 RT-01~07 全防御 + 回归 8 套件零新增失败 + 宪法断言七模块每期零改动

---

## 一、验收总账

| 期 | 主题 | 专项断言 | 关键结论 | 复跑结果 |
|----|------|---------|---------|---------|
| P0 | 缺口信号注册表+趋势诊断引擎+种子底座 | 55 | GAP_SIGNAL_REGISTRY 10 项五侧封闭白名单（权重和=1.0+启动自检 RuntimeError 宪法级）+SOURCE_REGISTRY 6 源+第32档案八因子+三级决策 defer/collect/urgent+9 表底座 | ＿/55 |
| P1 | 定向采集+三重合规鉴别中心 | 50 | 采集运行器（quarantined 沙箱隔离态唯一铁律入口）+版权/隐私/内容安全三关串行+合规指纹（无指纹不入库）+PII lookaround 三型渐进脱敏+49号预算计量（halted 不降级） | ＿/50 |
| P2 | 知识种子工坊+人类终审 | 54 | 多模态 content 四形态（text/qa_pair/image/video）+KNOWLEDGE_REASON 注释即证据+版本化 A/B 旧版降权不删除+发布终审（published 唯一出口+off 亦可用铁律）+召回+有效期降权 | ＿/54 |
| P3 | 角色植入+情境触发+会员面种子入口 | 56 | 角色×场景确定性推荐（仅 published/boosted 入池）+view 合规指纹校验+49号浏览计量+反馈召回预警（≥3 反馈且负≥50%）+学习路径微课程+情境触发埋点 | ＿/56 |
| P4 | 决策回流+信值联动+T+1 调度 | 31 | 六类真值信号+44号池双写（种子 pooledFeedbackId+缺口 gapRecurrence 双域幂等）+误导召回 45号 L2 补偿联动+调度器（collect+freshness 双任务） | ＿/31 |
| P5 | 看板+红队+三件套收官 | 37 | 四区看板（度量五指标/种子库/合规/防御）+红队七向量全防御+宪法断言收官 | ＿/37 |

**累计：专项 283 断言（55+50+54+56+31+37，验收复跑＿＿）/ 实机 198 项次（6 脚本×2 轮幂等）/ 红队 7 向量全防御 / 回归 8 套件零新增失败 / HTTP 25 端点行为与设计口径一致**

## 二、验收方式

### 2.1 专项复跑（验收日全量重跑）

```
python test_kb57_p0.py  → 55/55    # 注册表自检/八因子切档/off 铁律/强信号缺口/计数更新
python test_kb57_p1.py  → 50/50    # 采集隔离态/三关四态 verdict/PII 零泄漏/预算熔断
python test_kb57_p2.py  → 54/54    # 锻造四形态/指纹继承/发布降权 A/B/召回/有效期
python test_kb57_p3.py  → 56/56    # 角色置顶/浏览指纹校验/路径全完成/越权拒绝
python test_kb57_p4.py  → 31/31    # 六类信号池双写/双域幂等/补偿 L2 存证/调度器
python test_kb57_p5.py  → 37/37    # 看板五指标/红队七向量/宪法断言/25 端点
```

覆盖：＿＿＿＿（按实际复跑补充覆盖说明）。

### 2.2 回归复跑（验收日全量重跑）

| 关联模块 | 套件 | 结果 |
|---------|------|------|
| 44号 学习中枢 | test_ai_learning_routes | ＿/0 |
| 46号 治理中枢 | p0/p1/p4/p5/scheduler（计数含 57号入册 32 档案） | ＿ |
| 48号 小竹 | test_xiaozhu_p4（discovered 32） | ＿/0 |
| 45号 信值 | test_trust_value_p3（补偿链路） | ＿/0 |
| 55号 二维码 | test_qr55_p0（信号源只读） | ＿/0 |
| 56号 升级管理 | test_aiup56_p0/p5（≥31 基线语义） | ＿ |

零新增失败（＿＿＿＿）。

### 2.3 HTTP 全端点冒烟（TestClient 直连 main.app）

```
观测面: registry/sources/gaps/model/seeds×2/compliance/feedback-stats/dashboard → 全 200
受控面: gaps/scan+collect/run+compliance+craft（off） 409 / feed+view（off+shadow） 409
终审面: review/recall（off 亦可用） 200 语义正确
鉴权:   无 X-Role → 403 ｜ 无 X-Member-Id（会员面）→ 403
```

25 端点行为与设计口径完全一致（P0-P5 套件内另覆盖 craft/review/recall/feed/view/feedback/paths/trigger/collect 全链）。

### 2.4 Docker 实机验收（Redis 态 ×2 轮幂等）

```
docker compose -p zhuxiang-jiu -f docker-compose.yml build backend   # 镜像＿＿＿＿
docker compose -p zhuxiang-jiu -f docker-compose.yml up -d backend   # 容器 healthy
python verify_kb57_p0_live.py  → 40/40（20 项×2 轮）
python verify_kb57_p1_live.py  → 34/34（17 项×2 轮）
python verify_kb57_p2_live.py  → 40/40（20 项×2 轮）
python verify_kb57_p3_live.py  → 28/28（14 项×2 轮）
python verify_kb57_p4_live.py  → 30/30（15 项×2 轮）
python verify_kb57_p5_live.py  → 26/26（13 项×2 轮）
```

覆盖（每轮清理种子重造——清理键含 `zhuxiang:kb57:*`+第32档案反馈池+信号种子+会员预算）：
- **零影响**：健康检查+观测面 off 可访问
- **off 铁律+终审铁律**：决策面 409 / review-recall off 亦可用
- **容器内全链（Redis 态）**：诊断→采集 quarantined→三关鉴别四态 verdict→锻造→终审 published+旧版降权 A/B→召回补偿 L2 存证→回流双域池双写→调度器双任务→看板五指标→红队七向量
- **Redis 序列化读回**：种子 content/abTest 结构 dict（五清单生效——内存态掩蔽陷阱防线）
- **HTTP**：dashboard 四区 / collect 幂等（已标注跳过）/ 鉴权 403

## 三、红队验收（RT-01~07——确定性七向量全防御）

| # | 攻击向量 | 防御层 | 结果 |
|---|---------|--------|------|
| RT-01 | 知识投毒（伪造缺口信号灌入——未知 eventType+伪造必要性字段） | GAP_SIGNAL_REGISTRY 封闭白名单+来源纯读取 | 命中全白名单（注册表外通道不入命中） |
| RT-02 | 白名单逃逸（未注册源采集/伪造指纹/注册对照三路） | 版权关第一道阻断+admin 动态注册域封闭 | 采集跳过 0 资源+鉴别 blocked+注册后对照通过 |
| RT-03 | PII 泄漏（未脱敏内容入库/隔离态伪造浏览三路） | 隐私关 lookaround 扫描+渐进脱敏+隔离态永不暴露铁律 | maskedFields≥3+存储零明文+隔离态 view 拒绝 |
| RT-04 | 种子污染（quarantined/无指纹/rejected 资源锻造三路） | 无指纹不入库铁律（compliant 态+指纹前缀双重校验） | 三路全拒 |
| RT-05 | 预算耗尽（缺口超支硬测/会员账户耗尽两路） | 预算关 halted 熔断（不降级放行无指纹）+49号浏览计量 | halted+无指纹+浏览拒绝 |
| RT-06 | 入口越权（他人路径推进/学习记录/反馈伪造三路） | advance_path 属主校验+学习记录会员域隔离+反馈落账自域 | 推进拒+隔离+自域一致 |
| RT-07 | 过期种子误导（有效期绕过三路） | freshness 自动降权+推荐池 FEEDABLE 过滤+入径状态机 | downgraded+出池+入径拒 |

57号自身新增攻击面评估：＿＿＿＿（按实际评估填写——建议口径：采集面为**白名单+隔离态**接入（原始资源永存 quarantined 无暴露路径）；会员面仅在 assist 开放（off/shadow 409）；与既有 knowledge_* 纯读取零写入路径）。

## 四、验收发现

### 4.1 存量缺陷（非 57号 引入——＿＿＿＿）

＿＿＿＿（如无则填"无"）。

### 4.2 交付期缺陷（57号 自身，9 项全部闭环）

| # | 期 | 缺陷 | 根因 | 修复 | 复验 |
|---|-----|------|------|------|------|
| 1 | P0 | 仓储五清单类属性访问崩溃 | 清单误定义在模块级而 `_deserialize` 以 `cls.` 访问（内存态掩蔽） | 移入类内 | 专项 55/55 |
| 2 | P0 | budgetCap 读回字符串+budgetSpent 截断 | 漏 FLOAT 清单+跨清单冲突 | 补清单+去冲突 | 专项 55/55 |
| 3 | P1 | compliance/feedback 表名断裂 | 通用 `f"{kind}s"` 推导不规则复数词 | `_TABLE_BY_KIND` 显式映射 | 专项 50/50 |
| 4 | P1 | PII 自然语言漏检 | `\b` 在数字与汉字间不成立（汉字属 `\w`） | lookaround `(?<!\d)(?!\d)` 升级 | 专项 50/50 |
| 5 | P1 | budgetHalted 读回字符串 | 漏 BOOL 清单 | 补清单 | 专项 50/50 |
| 6 | P2 | seed 服务两处语法错误 | f-string 嵌套引号跨行断裂（56号 P1 同款） | 局部变量提取 | 专项 54/54 |
| 7 | P2 | 验收管道拼接断行 | 容器脚本 sha256 链式调用跨行 | 局部变量+调试定位 | 实机 40/40 |
| 8 | P3 | pooled 漏清单 | P4 回流标记前瞻 | 补清单 | 专项 56/56 |
| 9 | P4 | gapRecurrence/results 漏清单 | 回流+补偿新字段 | 补清单 | 专项 31/31 |

**教训沉淀**：五清单纪律五处复发（类属性作用域/清单遗漏×4/跨清单冲突/不规则复数）——已固化为"新增落库字段必须同步五清单且不得跨清单冲突+仓储读回结构断言"范式。

## 五、六期核心数字

- **25 端点**（观测 9+决策 5+终审召回 2+会员 7+回流 1+红队 1）/ **9 表** / **13 服务文件** / **1 调度器**（T+1 补标+有效期检查双任务）
- **专项 283 断言 + 实机 198 项次（6 脚本×2 轮幂等）+ RT-01~07 红队全防御 + 回归 8 套件零新增失败**
- **第32档案**：44号 SCORER_REGISTRY batch16（八因子 knowledge_orchestration——32 档案口径）
- **六类回流信号**：seed_effective +1.0 / seed_high_value +0.8 / seed_weak +0.3 / gap_reject_recurrence -0.6 / seed_recalled -0.8 / compliance_overturned -0.5 / budget_halt_frequent -0.4
- **核心代码约＿＿行**：13 服务/仓储/路由文件 + 6 专项测试 + 6 实机验收脚本
- **零改动宪法**（每期断言验证）：44号学习接口 / 既有 knowledge_* 全模块 / 51号图谱 / 46号治理 / 49号预算 / 45号信值 / 52/55号观测面
- **双开关默认 off**：KB57_MODE（三级灰度 off/shadow/assist）/ KB57_LEARN_MODE（调度面）——默认零影响交付
- **Docker 部署态**：镜像＿＿＿＿ 容器 healthy（观测面开放/决策会员面 off 零影响）——compose 三变量已入册（`d1d4afd`）

## 六、宪法一致性核验（零改动红线）

| 红线 | 断言位置 | 结果 |
|------|---------|------|
| 44号 SCORER_REGISTRY=32 档案（knowledge_orchestration 在册、接口零改动） | 各期专项+实机 | ☐ 保持 |
| 既有 knowledge_* 路由/服务/仓储零改动（缺口队列/统计纯读取） | p0/p5 专项 | ☐ 保持 |
| 51号知识图谱零触碰（KG_MODE 在册） | p0/p5 专项 | ☐ 保持 |
| 46号治理台账 32 档案只读消费 | 回归套件 | ☐ 保持 |
| 49号预算接口纯调用（check_and_spend） | p1/p3 专项 | ☐ 保持 |
| 45号信值 deposit 补偿范式调用 | p4 专项+实机 | ☐ 保持 |
| 52/55号观测面纯读取信号源 | p0 专项 | ☐ 保持 |

## 七、上线建议（与部署手册衔接）

1. **发布形态**：默认 off 上线（零影响）——compose 三变量已入册（[57号_上线部署操作手册](57号_上线部署操作手册.md) §2.1）
2. **灰度顺序**：off→shadow（观察 1-3 天）→assist（种子暴露面试点 3-7 天）→KB57_LEARN_MODE=on
3. **放量前置**：§四验收发现全部闭环+红队复验 allDefended+三铁律生效抽查（quarantined view 409/无指纹 craft 409/cap 满 halted）
4. **回退保障**：L1 开关秒级（终审/召回通道不关闭）/L2 种子召回单粒度+45号补偿/L3 代码回退分钟级

## 八、结论

57号六期（P0-P5）全线交付并通过验收：**知识库从"被动存储"进化为"主动认知引擎"**——五侧缺口信号让知识缺口有据可诊，三重合规鉴别让每一份入库知识携带合规指纹，多模态种子工坊让知识结构化可版本化可 A/B，人类终审让 published 永远是唯一出口，角色植入与情境触发让正确的知识在正确的时刻抵达正确的人，六类真值回流让第32档案知对错，七向量红队让攻击者无从下手。默认零影响态交付（双开关 off），与既有 knowledge_* 构成"存量训练+信值认知基座"双引擎——**每一颗种子都懂规矩、有温度、知进退，而三铁律确保这份"懂"永远值得托付**。

---

**签字栏**

| 角色 | 姓名 | 签字 | 日期 |
|------|------|------|------|
| 验收负责人 | ＿＿＿ | ＿＿＿ | ＿＿＿ |
| 评审人 | ＿＿＿ | ＿＿＿ | ＿＿＿ |
| 运维代表 | ＿＿＿ | ＿＿＿ | ＿＿＿ |
