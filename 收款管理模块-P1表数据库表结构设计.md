# 收款管理模块 · P1 表数据库表结构设计

> P1 表补充文档,适配项目双模式存储(内存 dict + Redis Hash),对齐 P0 表风格。
> 重点设计:**对账流程**(日终对账 + 差异处理)与**渠道配置**(多渠道接入 + 费率管理)。

## 一、设计原则

### 1.1 双模式存储对齐
- **内存模式**(`STORE_MODE=asyncio`)：dict 主信息 + 索引 Set + 计数器
- **Redis 模式**(`STORE_MODE=redis`)：Hash/String(JSON) + Set(索引) + INCR(计数器)
- 与 P0 表(payment_orders/refunds/payouts)风格一致

### 1.2 对账状态机(对齐 P0 文档)

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   pending ──→ matched(一致)                                  │
│      │                                                      │
│      └──→ diff(差异) ──→ investigating(调查中) ──→ resolved │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**状态流转规则**:
- `pending` → `matched`: 平台流水 = 渠道流水, 金额一致
- `pending` → `diff`: 金额不一致 / 流水缺失
- `diff` → `investigating`: 管理员介入调查
- `investigating` → `resolved`: 人工调整完成(补单/退款/标记误差)

### 1.3 渠道配置管理
- 支持多渠道接入: 微信/支付宝/银联/银行直连/聚合支付
- 费率配置: 按渠道 + 业务场景差异化定价
- 渠道状态: 启用/停用/维护中
- 限额控制: 单笔/单日/单月交易限额

## 二、P1 表清单(2 张表)

| 优先级 | 表名 | 用途 | 主键 | Redis Key 模式 |
|:---:|------|------|------|------|
| P1 | `payment_reconciliation` | 对账记录 | `recon_no` | String(JSON) `payment:recon:{recon_no}` |
| P1 | `payment_channels` | 渠道配置 | `channel_code` | Hash `payment:channel:{code}` |

### 编号规则
| 实体 | 前缀 | 格式 | 示例 |
|------|:---:|------|------|
| 对账批次号 | `RECON` | `RECON{YYYYMMDD}{channel}` | `RECON20260821WX` |
| 差异单号 | `DIFF` | `DIFF{YYYYMMDD}{seq:04d}` | `DIFF202608210001` |
| 渠道编码 | - | 大写字母, ≤10 字符 | `WECHAT` / `ALIPAY` |

## 三、表结构详细设计

### 3.1 payment_reconciliation(对账记录表 · P1)

**业务定位**：记录每日对账批次,对平平台流水与渠道流水,处理差异

**内存键**:
- `_mock_store["payment_reconciliation"]` — 主信息 dict（key=recon_no）
- `_mock_store["_payment_recon_seq"]` — 序号计数器
- `_mock_store["_payment_recon_index:{date}"]` — 日期索引 Set(按日查询)
- `_mock_store["_payment_recon_index:{channel}"]` — 渠道索引 Set
- `_mock_store["_payment_recon_diff_pending"]` — 待处理差异 Set(管理端查询)

**Redis Key**:
- `payment:recon:{recon_no}` — String(JSON)(主信息)
- `payment:recon:seq` — String(INCR 序号)
- `payment:recon:index:date:{date}` — Set(日期索引)
- `payment:recon:index:channel:{channel}` — Set(渠道索引)
- `payment:recon:diff:pending` — Set(待处理差异集合)
- `payment:recon:lock:{date}:{channel}` — String(对账锁, TTL 1h, 防并发对账)

**字段设计**:

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| reconNo | str | 对账批次号(主键) | `RECON20260821WX` |
| reconDate | str | 对账日期(YYYY-MM-DD) | `2026-08-21` |
| channel | str | 渠道编码: WECHAT/ALIPAY/UNIONPAY/BANK | `WECHAT` |
| status | str | 状态: pending/matched/diff/investigating/resolved | `matched` |
| statusName | str | 状态中文名 | `已对平` |
| platformCount | int | 平台流水笔数 | `156` |
| platformAmount | float | 平台流水金额(元) | `48500.00` |
| channelCount | int | 渠道流水笔数 | `156` |
| channelAmount | float | 渠道流水金额(元) | `48500.00` |
| matchType | str | 对平类型: full/partial/mismatch | `full` |
| diffCount | int | 差异笔数 | `0` |
| diffAmount | float | 差异金额(元, 正数平台多/负数渠道多) | `0.00` |
| diffDetails | list | 差异明细(JSON 数组) | `[{"payNo": "...", "type": "..."}]` |
| reconFile | str | 渠道对账文件路径 | `/recon/wechat/20260821.csv` |
| startedAt | str | 对账开始时间 | `2026-08-22 02:00:00` |
| finishedAt | str | 对账完成时间 | `2026-08-22 02:05:23` |
| operator | str | 操作人(admin) | `system` / `admin01` |
| remark | str | 备注 | `日终自动对账` |
| createdAt | str | 创建时间 | `2026-08-22 02:00:00` |
| updatedAt | str | 更新时间 | `2026-08-22 02:05:23` |

**差异明细结构(diffDetails 数组元素)**:

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| payNo | str | 平台支付单号 | `PAY202608210001` |
| channelTradeNo | str | 渠道交易号 | `420000222021...` |
| type | str | 差异类型: amount_mismatch/platform_only/channel_only | `amount_mismatch` |
| platformAmount | float | 平台金额 | `850.00` |
| channelAmount | float | 渠道金额 | `860.00` |
| diffAmount | float | 差异金额 | `10.00` |
| handleSuggestion | str | 处理建议: refund/supplement/ignore | `refund` |

**锁键设计**:
- 对账锁: `payment:recon:lock:{date}:{channel}`(防并发对账同一日同一渠道)

**状态常量定义**:

```python
RECON_STATUS_PENDING = "pending"           # 待对账
RECON_STATUS_MATCHED = "matched"            # 已对平
RECON_STATUS_DIFF = "diff"                  # 存在差异
RECON_STATUS_INVESTIGATING = "investigating" # 调查中
RECON_STATUS_RESOLVED = "resolved"          # 已处理

RECON_STATUS_NAMES = {
    RECON_STATUS_PENDING: "待对账",
    RECON_STATUS_MATCHED: "已对平",
    RECON_STATUS_DIFF: "存在差异",
    RECON_STATUS_INVESTIGATING: "调查中",
    RECON_STATUS_RESOLVED: "已处理",
}

MATCH_TYPE_FULL = "full"           # 完全对平
MATCH_TYPE_PARTIAL = "partial"     # 部分对平(有差异但已处理)
MATCH_TYPE_MISMATCH = "mismatch"   # 完全不匹配

DIFF_TYPE_AMOUNT_MISMATCH = "amount_mismatch"  # 金额不一致
DIFF_TYPE_PLATFORM_ONLY = "platform_only"      # 平台有/渠道无
DIFF_TYPE_CHANNEL_ONLY = "channel_only"        # 渠道有/平台无

HANDLE_SUGGEST_REFUND = "refund"        # 退款(平台多收)
HANDLE_SUGGEST_SUPPLEMENT = "supplement" # 补单(平台少收)
HANDLE_SUGGEST_IGNORE = "ignore"        # 忽略(误差范围内)
```

### 3.2 payment_channels(渠道配置表 · P1)

**业务定位**：管理各支付渠道的接入配置、费率、限额,支持动态启停

**内存键**:
- `_mock_store["payment_channels"]` — 主信息 dict（key=channel_code）
- `_mock_store["_payment_channel_index:status"]` — 按状态索引 Set(active/maintenance/disabled)

**Redis Key**:
- `payment:channel:{code}` — Hash(主信息,支持 HINCRBYFLOAT 累计交易额)
- `payment:channel:index:status:{status}` — Set(状态索引)
- `payment:channel:config:active` — Set(当前启用的渠道列表, 缓存)

**字段设计**:

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| channelCode | str | 渠道编码(主键, 大写) | `WECHAT` |
| channelName | str | 渠道名称 | `微信支付` |
| channelType | str | 渠道类型: third_party/bank/aggregate | `third_party` |
| status | str | 状态: active/maintenance/disabled | `active` |
| statusName | str | 状态中文名 | `启用` |
| supportedMethods | list | 支持的支付方式 | `["native", "jsapi", "h5"]` |
| supportedScenes | list | 支持的业务场景 | `["order_pay", "wallet_deposit"]` |
| merchantId | str | 商户号 | `1900000101` |
| appId | str | 应用 ID | `wx6789...` |
| apiEndpoint | str | API 接入地址 | `https://api.mch.weixin.qq.com/` |
| notifyUrl | str | 回调通知地址 | `https://zhuxiang.com/api/payment/callback/pay` |
| signType | str | 签名类型: MD5/RSA2/SM2 | `RSA2` |
| signKey | str | 签名密钥(加密存储) | `******` |
| certPath | str | 证书路径 | `/cert/wechat/apiclient_cert.pem` |
| feeRate | float | 手续费率(0-1) | `0.006` (0.6%) |
| feeType | str | 费率类型: fixed/ratio/mixed | `ratio` |
| fixedFee | float | 固定手续费(feeType=fixed/mixed 时) | `0.00` |
| settleCycle | str | 结算周期: T+0/T+1/D+1 | `T+1` |
| minAmount | float | 单笔最小金额 | `0.01` |
| maxAmount | float | 单笔最大金额 | `50000.00` |
| dailyLimit | float | 单日累计限额 | `500000.00` |
| monthlyLimit | float | 单月累计限额 | `5000000.00` |
| dailyAmount | float | 当日累计交易额(动态) | `125000.00` |
| dailyCount | int | 当日累计交易笔数(动态) | `45` |
| monthlyAmount | float | 当月累计交易额(动态) | `1500000.00` |
| retryMax | int | 回调重试最大次数 | `8` |
| timeout | int | 支付超时(秒) | `1800` |
| remark | str | 备注 | `主渠道` |
| createdAt | str | 创建时间 | `2026-08-01 10:00:00` |
| updatedAt | str | 更新时间 | `2026-08-21 19:00:00` |

**嵌套结构(supportedMethods 数组元素)**:

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| method | str | 支付方式 | `jsapi` |
| methodName | str | 中文名 | `JSAPI 支付` |
| enabled | bool | 是否启用 | `true` |

**锁键设计**:
- 配置更新锁: `payment:channel:lock:{code}`(防并发修改配置)
- 限额累计锁: `payment:channel:limit:{code}:{date}`(防并发累计交易额)

**状态常量定义**:

```python
CHANNEL_STATUS_ACTIVE = "active"             # 启用
CHANNEL_STATUS_MAINTENANCE = "maintenance"   # 维护中
CHANNEL_STATUS_DISABLED = "disabled"          # 停用

CHANNEL_STATUS_NAMES = {
    CHANNEL_STATUS_ACTIVE: "启用",
    CHANNEL_STATUS_MAINTENANCE: "维护中",
    CHANNEL_STATUS_DISABLED: "停用",
}

CHANNEL_TYPE_THIRD_PARTY = "third_party"   # 第三方支付(微信/支付宝)
CHANNEL_TYPE_BANK = "bank"                 # 银行直连
CHANNEL_TYPE_AGGREGATE = "aggregate"       # 聚合支付

FEE_TYPE_FIXED = "fixed"     # 固定手续费
FEE_TYPE_RATIO = "ratio"     # 比例手续费
FEE_TYPE_MIXED = "mixed"     # 混合(比例 + 固定)

SETTLE_T0 = "T+0"   # 当日结算
SETTLE_T1 = "T+1"   # 次日结算
SETTLE_D1 = "D+1"   # 延迟 1 天
```

## 四、对账流程设计

### 4.1 日终自动对账流程

```
┌─────────────────────────────────────────────────────────────┐
│  1. 触发(每日 02:00 定时任务)                                 │
│     ↓                                                       │
│  2. 获取对账锁(payment:recon:lock:{date}:{channel})           │
│     ↓                                                       │
│  3. 拉取渠道对账文件(reconFile)                              │
│     ↓                                                       │
│  4. 查询平台当日流水(status=paid 的 payment_orders)          │
│     ↓                                                       │
│  5. 逐笔比对(按 channelTradeNo 匹配)                         │
│     ↓                                                       │
│  6. 生成差异明细(diffDetails)                                │
│     ↓                                                       │
│  7. 更新对账状态(matched/diff)                               │
│     ↓                                                       │
│  8. 差异进入待处理集合(diff_pending)                         │
│     ↓                                                       │
│  9. 通知管理员(差异 > 阈值时)                                │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 差异处理流程

| 差异类型 | 处理建议 | 操作 |
|------|------|------|
| `amount_mismatch`(金额不一致) | refund/supplement | 平台多收→退款, 平台少收→补单 |
| `platform_only`(平台有/渠道无) | supplement/ignore | 渠道延迟→等待, 确认缺失→补单 |
| `channel_only`(渠道有/平台无) | refund/investigating | 异常交易→调查, 确认→退款 |

### 4.3 限额校验流程

```
┌─────────────────────────────────────────────────────────────┐
│  创建支付订单时:                                             │
│     ↓                                                       │
│  1. 查询渠道配置(payment_channels)                           │
│     ↓                                                       │
│  2. 校验状态(status=active)                                 │
│     ↓                                                       │
│  3. 校验单笔限额(minAmount ≤ amount ≤ maxAmount)             │
│     ↓                                                       │
│  4. 校验单日累计(dailyAmount + amount ≤ dailyLimit)          │
│     ↓                                                       │
│  5. 校验单月累计(monthlyAmount + amount ≤ monthlyLimit)     │
│     ↓                                                       │
│  6. 校验支付方式(method ∈ supportedMethods)                  │
│     ↓                                                       │
│  7. 通过 → 创建订单 + 累计交易额                             │
└─────────────────────────────────────────────────────────────┘
```

## 五、Repository 方法清单

### 5.1 payment_reconciliation 方法

| 方法 | 用途 | 锁 |
|------|------|------|
| `create_recon(recon_no, date, channel, ...)` | 创建对账批次 | `payment:recon:lock:{date}:{channel}` |
| `get_recon(recon_no)` | 查询对账详情 | - |
| `update_recon_fields(recon_no, fields)` | 更新字段 | - |
| `update_recon_status(recon_no, status, ...)` | 更新状态(状态机) | `payment:recon:lock:{date}:{channel}` |
| `list_recons(date=None, channel=None, status=None, limit=50)` | 列表查询 | - |
| `list_pending_diffs(limit=100)` | 待处理差异列表 | - |
| `add_diff_detail(recon_no, diff_detail)` | 添加差异明细 | - |
| `acquire_recon_lock(date, channel)` | 获取对账锁(SETNX + TTL 1h) | - |

### 5.2 payment_channels 方法

| 方法 | 用途 | 锁 |
|------|------|------|
| `create_channel(code, name, ...)` | 创建渠道配置 | `payment:channel:lock:{code}` |
| `get_channel(code)` | 查询渠道配置 | - |
| `get_channel_by_method(method)` | 按支付方式反查渠道 | - |
| `update_channel_fields(code, fields)` | 更新字段 | `payment:channel:lock:{code}` |
| `update_channel_status(code, status)` | 更新状态(启停) | `payment:channel:lock:{code}` |
| `list_channels(status=None, type=None, limit=50)` | 列表查询 | - |
| `list_active_channels()` | 启用的渠道列表(高频查询缓存) | - |
| `check_limit(code, amount, daily=True, monthly=True)` | 限额校验 | `payment:channel:limit:{code}:{date}` |
| `add_transaction_amount(code, amount)` | 累计交易额(HINCRBYFLOAT) | `payment:channel:limit:{code}:{date}` |
| `reset_daily_stats()` | 重置日累计(每日 00:00 定时任务) | - |
| `reset_monthly_stats()` | 重置月累计(每月 1 日定时任务) | - |

## 六、与既有模块的关联

### 6.1 与 P0 表的关联

| 关联表 | 关联字段 | 关联说明 |
|------|------|------|
| `payment_orders` | `channelTradeNo` | 对账时按渠道交易号匹配平台支付单 |
| `payment_orders` | `payChannel` | 创建支付时校验渠道配置与限额 |
| `payment_refunds` | `channelRefundNo` | 对账时按渠道退款号匹配平台退款单 |
| `payment_payouts` | `payChannel` | 创建付款时校验付款渠道配置 |

### 6.2 与其他模块的关联

| 关联模块 | 关联说明 |
|------|------|
| 订单管理 | 支付成功时联动订单状态 PENDING → PAID |
| 钱包盈利 | 充值支付成功时入账; 提现打款时校验付款渠道 |
| 财务管理 | 对账完成生成对账凭证; 渠道手续费生成费用凭证 |
| 代理商管理 | 进货付款校验付款渠道限额 |

## 七、设计要点总结

### 7.1 对账记录设计要点
1. **对账粒度**: 按日 + 渠道维度(每日每渠道一个对账批次)
2. **差异追踪**: diffDetails 结构化记录每笔差异, 支持人工处理
3. **状态机**: pending → matched/diff → investigating → resolved
4. **并发控制**: 对账锁防止并发对账同一日同一渠道
5. **自动对账**: 日终定时任务自动触发, 差异超阈值通知管理员

### 7.2 渠道配置设计要点
1. **多渠道支持**: 微信/支付宝/银联/银行直连/聚合支付
2. **动态启停**: 运行时切换渠道状态, 无需重启
3. **限额控制**: 单笔/单日/单月三级限额, 动态累计
4. **费率管理**: 支持固定/比例/混合三种费率模式
5. **缓存优化**: 启用渠道列表缓存, 减少高频查询压力
6. **统计重置**: 日累计每日重置, 月累计每月重置(定时任务)

### 7.3 双模式存储一致性
- 内存模式: dict + Set + 计数器
- Redis 模式: Hash + Set + INCR
- 两种模式下业务逻辑透明切换, `is_redis_mode()` 统一判断
