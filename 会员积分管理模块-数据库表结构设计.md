# 竹香酒官网 · 会员积分管理模块-数据库表结构设计

> 版本：v1.0
> 日期：2026-08-22
> 适用模块：会员积分管理模块（03）
> 核心理念：积分账户+流水+签到+过期 4 表支撑 P0 功能

---

## 一、表清单与优先级

| 优先级 | 表名 | 说明 | 状态 |
|--------|------|------|------|
| P0 | points_account | 积分账户表（当前可用/冻结/累计） | 待开发 |
| P0 | points_logs | 积分流水表（获取/消耗明细） | 待开发 |
| P0 | points_signin | 签到记录表（每日签到+连续签到） | 待开发 |
| P0 | points_expire | 积分过期记录表（24个月滚动过期） | 待开发 |
| P1 | points_referral | 推荐关系表（2级推荐） | 后续 |
| P1 | credit_score | 信用分表（竹信分0-1000） | 后续 |

---

## 二、P0 核心表结构

### 2.1 points_account（积分账户表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| user_id | bigint | PK | 用户ID |
| total_points | int | NOT NULL DEFAULT 0 | 当前可用积分 |
| frozen_points | int | NOT NULL DEFAULT 0 | 冻结积分（预扣） |
| total_earned | bigint | NOT NULL DEFAULT 0 | 累计获取积分 |
| total_spent | bigint | NOT NULL DEFAULT 0 | 累计消耗积分 |
| expiring_points | int | NOT NULL DEFAULT 0 | 30天内将过期积分 |
| version | int | NOT NULL DEFAULT 0 | 乐观锁版本号 |
| updated_at | datetime | NOT NULL | 更新时间 |

**索引：**
- PK: user_id

---

### 2.2 points_logs（积分流水表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | bigint | PK AUTO_INCREMENT | 流水ID |
| user_id | bigint | NOT NULL | 用户ID |
| type | varchar(10) | NOT NULL | earn（获取）/ spend（消耗）/ freeze（冻结）/ unfreeze（解冻） |
| source | varchar(20) | NOT NULL | 来源：checkin/order/referral/credit/review/transaction/promo/refund/expire/deduct |
| points | int | NOT NULL | 积分数量（正数获取，负数消耗） |
| balance | int | NOT NULL | 变动后余额 |
| ref_id | varchar(64) | NULL | 关联ID（订单号/签到日期等） |
| ref_desc | varchar(200) | NULL | 描述 |
| expire_at | datetime | NULL | 过期时间（仅获取类流水） |
| status | tinyint | NOT NULL DEFAULT 1 | 0冻结 1可用 2已过期 3已消耗 |
| created_at | datetime | NOT NULL | 创建时间 |

**索引：**
- PK: id
- INDEX idx_user_created: (user_id, created_at) — 查询用户流水
- INDEX idx_user_source: (user_id, source) — 按来源统计
- INDEX idx_expire: (expire_at, status) — 过期扫描

---

### 2.3 points_signin（签到记录表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | bigint | PK AUTO_INCREMENT | ID |
| user_id | bigint | NOT NULL | 用户ID |
| sign_date | date | NOT NULL | 签到日期（YYYY-MM-DD） |
| continuous_days | int | NOT NULL DEFAULT 1 | 连续签到天数 |
| points_earned | int | NOT NULL | 获得积分 |
| is_bonus | tinyint | NOT NULL DEFAULT 0 | 是否宝箱奖励 0/1 |
| created_at | datetime | NOT NULL | 签到时间 |

**索引：**
- PK: id
- UNIQUE INDEX uk_user_date: (user_id, sign_date) — 防止重复签到
- INDEX idx_user_date: (user_id, sign_date) — 查询签到记录

---

### 2.4 points_expire（积分过期记录表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | bigint | PK AUTO_INCREMENT | ID |
| user_id | bigint | NOT NULL | 用户ID |
| batch_id | bigint | NOT NULL | 批次ID（关联 points_logs.id） |
| points | int | NOT NULL | 积分数量 |
| consumed_points | int | NOT NULL DEFAULT 0 | 已消耗数量 |
| earned_at | datetime | NOT NULL | 获取时间 |
| expire_at | datetime | NOT NULL | 过期时间 |
| status | tinyint | NOT NULL DEFAULT 0 | 0未过期 1已过期 2已消耗 |
| created_at | datetime | NOT NULL | 创建时间 |

**索引：**
- PK: id
- INDEX idx_user_expire: (user_id, expire_at, status) — 过期提醒查询
- INDEX idx_batch: (batch_id) — 批次关联

---

## 三、积分规则常量

| 规则 | 值 | 说明 |
|------|------|------|
| 积分单位 | 竹叶 | 品牌化命名 |
| 积分价值 | 100竹叶 = ¥1 | 抵现汇率 1% |
| 积分有效期 | 24个月 | 滚动过期 |
| 最低抵现 | 100竹叶 | 最小抵扣单位 |
| 抵扣上限 | 30% | 单笔订单最多抵扣商品金额的30% |
| 签到基础积分 | 10 | 每日签到基础 |
| 连续签到奖励 | 5×day | 第N天奖励=基础+5×min(day,7) |
| 签到宝箱 | 50 | 第7/14/21/28天额外宝箱 |
| 消费返分比例 | 1.5% | 每消费¥1返1.5竹叶 |
| 等级加成 | L1:1.0×/L2:1.2×/L3:1.5×/L4:2.0×/L5:3.0× | 会员等级返分倍数 |
| 每日获取上限 | 500 | 防刷单 |
| 每月获取上限 | 5000 | 防薅羊毛 |

---

## 四、状态机设计

### 4.1 积分流水状态机

```
                ┌──────────┐
                │  earn    │
                │  (获取)   │
                └────┬─────┘
                     │
            ┌────────┴────────┐
            ▼                 ▼
     ┌──────────┐      ┌──────────┐
     │ status=1 │      │ status=0 │
     │  可用    │      │  冻结    │
     └────┬─────┘      └────┬─────┘
          │                 │
          ▼                 ▼
   ┌────────────┐    ┌────────────┐
   │ status=3   │    │ status=1   │
   │  已消耗    │    │  解冻可用  │
   └────────────┘    └────────────┘
          │
          ▼
   ┌────────────┐
   │ status=2   │
   │  已过期    │
   └────────────┘
```

### 4.2 签到状态流转

```
首次签到 → continuous_days=1
    │
    ▼
连续签到 → continuous_days=N（基于昨日+1）
    │
    ▼
断签 → continuous_days=1（重新计算）
    │
    ▼
第7/14/21/28天 → is_bonus=1（宝箱奖励）
```

---

## 五、并发安全设计

### 5.1 积分账户并发更新

**问题：** 多个请求同时修改同一用户的积分账户

**方案：** 使用 Redis 分布式锁 + 乐观锁版本号

```
锁键格式: lock:points:account:{user_id}

流程:
1. 获取锁 lock:points:account:{user_id}
2. 读取账户（含 version）
3. 修改积分
4. version+1 写回（CAS: WHERE version=旧值）
5. 写入流水
6. 释放锁
```

### 5.2 签到幂等性

**问题：** 同一用户同一天重复签到

**方案：** 唯一索引 (user_id, sign_date) + 锁

```
锁键格式: lock:points:signin:{user_id}:{date}

流程:
1. 获取锁
2. 查询今日是否已签到
3. 若已签到 → 返回错误（409 Conflict）
4. 计算连续天数
5. 写入签到记录
6. 发放积分
7. 释放锁
```

### 5.3 积分抵扣 FIFO 过期

**问题：** 抵扣时优先消耗即将过期的积分

**方案：** 按 expire_at ASC 排序消耗

```
流程:
1. 查询用户未过期积分批次（status=0, expire_at ASC）
2. 按批次顺序消耗，更新 consumed_points
3. 全部消耗完 → status=2（已消耗）
4. 写入消耗流水
```

---

## 六、P0 接口清单

| 编号 | 接口 | 方法 | 说明 |
|------|------|------|------|
| 1 | /api/points/signin | POST | 每日签到 |
| 2 | /api/points/signin/{user_id} | GET | 查询签到记录 |
| 3 | /api/points/earn/order | POST | 消费返分 |
| 4 | /api/points/deduct | POST | 积分抵现 |
| 5 | /api/points/account/{user_id} | GET | 查询积分账户 |
| 6 | /api/points/logs/{user_id} | GET | 查询积分流水 |
| 7 | /api/points/refund | POST | 退款返还积分 |
| 8 | /api/points/expire/run | POST | 执行过期处理 |
| 9 | /api/points/expiring/{user_id} | GET | 查询将过期积分 |
| 10 | /api/points/stats/{user_id} | GET | 积分统计 |

---

## 七、与其他模块的关联

| 关联模块 | 关联点 | 说明 |
|---------|--------|------|
| 会员管理(02) | 等级加成 | 根据 member_level 计算返分倍数 |
| 订单管理(04) | 消费返分 | 订单完成后触发返分 |
| 收款管理(05) | 积分抵现 | 支付时使用积分抵扣 |
| 钱包盈利(12) | 积分价值 | 积分与钱包余额的汇率 |
| 信用管理(23) | 竹信分联动 | P1 期实现 |
