# 竹香酒官网 · 产品溯源管理模块设计文档（34号模块）

> 版本：v2.1（2026-08-29 接口表按代码实际重录 TD-4：14→16 端点+补鉴权列，详见 6）
> 原版：v1.0，2026-08-27
> 修订记录：| 2026-08-29 | v2.1 | 接口表按代码实际重录(TD-4) |
> 适用项目：竹香酒官网（zhuxiangjiu）+ 微信小程序（taro-app）
> 关联模块：权限AI智能管理（33）、竹奕酒生命码（trace 流通码）、代理商箱码、合规合法智能监控（24）
> 参考平台：茅台/五粮液一物一码溯源、蚂蚁链溯源（区块链存证）、京东智臻链、顺丰溯源、EPCIS 事件标准、HACCP 食品安全体系

## 1. 模块定位

**产品溯源管理**：以「工段二维码」为锚点，将生产全流程（工艺酿酒 → 原酒储藏 → 产品调配 → 灌装 → 包装 → 仓库 → 出库）逐工段上链留痕——每个工段张贴专属二维码，**责任人扫码打卡**记录批次流转（时间/责任人/工艺参数/质检结论），批次号贯穿全链，最终与流通码（瓶码/箱码）绑定，实现**「一根鲜竹（笋）· 来自泰山姊妹山徂徕山、国家森林公园一滴富硒水——鲜竹酝香成美酒」全生命周期可追溯**。

与 33 号权限模块深度联动：**责任人 = 持有对应生产环节权限且已签责任书的员工**，无权限扫码即拦截（越权记录落 AI 监控），打卡数据回流权责信用分考核。

核心理念：**「工段码锚点 → 责任人扫码 → 批次流转链 → 瓶码贯通 → AI 把关」**。

## 2. 创新与优化设计

| 创新点 | 参照平台 | 优化创新 |
|---|---|---|
| **工段二维码锚点** | 茅台车间码 + EPCIS 事件 | 7 工段各一个固定码（STG-xxx），张贴于工段现场，扫码即打卡，免录入 |
| **权限即责任** | SAP 生产工单 | 责任人必须持有 33 号模块对应环节权限+已签责任书，扫码自动校验，越权即拦截并落 AI 监控 |
| **批次贯穿全链** | 京东智臻链 | batchNo 贯穿 7 工段 + 关联瓶码/箱码，消费者扫瓶码看完整生产时间线 |
| **AI 流转异常检测** | 蚂蚁风控 | 跳工段（未走完前置工段）/ 时间倒流 / 超时滞留（工段停留超阈值）/ 非本人异地打卡 四类异常自动检出 |
| **打卡回流考核** | OKR 绩效闭环 | 工段打卡记录（及时率/异常率）作为权责信用分考核输入（33 号 P1 引擎扩展因子） |
| **质检关卡** | HACCP CCP 关键控制点 | 产品工段（调配检测）与包装工段为质检关卡，须上传质检结论才能放行下一工段 |

## 3. 工段体系与权限映射

| 序号 | 工段 | 工段码前缀 | 权限模块环节 | 责任人要求 | 质检关卡 |
|:---:|---|---|---|---|:---:|
| 1 | 工艺酿酒 | STG-BREW | production | 持酿造环节权限+已签责任书 | - |
| 2 | 原酒储藏 | STG-STOR | storage | 持仓储环节权限 | - |
| 3 | 产品（调配/检测） | STG-BLEND | production | 持酿造环节权限 | ✅ 须质检结论 |
| 4 | 灌装 | STG-FILL | storage | 持仓储环节权限 | - |
| 5 | 包装 | STG-PACK | storage | 持仓储环节权限 | ✅ 须质检结论 |
| 6 | 仓库 | STG-WARE | storage | 持仓储环节权限 | - |
| 7 | 出库 | STG-OUT | logistics | 持物流环节权限 | - |

流转规则（AI 检测依据）：
1. **顺序流转**：批次必须按 1→2→3→4→5→6→7 顺序打卡，跳工段/时间倒流标记异常
2. **质检关卡**：3（产品检测）与 5（包装质检）未通过则阻断后续工段打卡
3. **超时滞留**：单工段停留 > 阈值（默认 72h，储藏工段 30 天）标记预警
4. **责任人签名**：每次打卡 = 责任人对本工段质量的背书，永久留痕

## 4. 数据模型

### 4.1 trace_stages（工段定义表，种子内置 7 工段）

| 字段 | 类型 | 说明 |
|---|---|---|
| stageId | int PK | 工段编号(1-7) |
| code | str | 工段码（STG-BREW 等，印制二维码内容） |
| name | str | 工段名（工艺酿酒等） |
| seq | int | 流转顺序(1-7) |
| permStage | str | 权限模块环节（production/storage/logistics） |
| permLevel | str | 责任人所需操作级（默认 operate） |
| isQcGate | bool | 是否质检关卡 |
| maxDwellHours | int | 滞留预警阈值(小时) |

### 4.2 trace_batches（生产批次表）

| 字段 | 类型 | 说明 |
|---|---|---|
| batchId | int PK | 批次编号 |
| batchNo | str | 批次号（如 ZX52-2026L08，与库存/订单批次对齐） |
| productId | int | 产品ID |
| plannedQty | int | 计划产量(瓶) |
| currentStageSeq | int | 当前流转到的最大工段序号 |
| status | str | producing(生产中)/released(已出库)/blocked(质检阻断) |
| lifeCodes | list | 关联瓶码（出库前绑定） |
| createdAt | str | 创建时间 |

### 4.3 trace_stage_punches（工段打卡记录表，核心）

| 字段 | 类型 | 说明 |
|---|---|---|
| punchId | int PK | 打卡编号 |
| batchNo | str | 批次号 |
| stageCode | str | 工段码 |
| memberId | int | 责任人（权限校验通过者） |
| result | str | pass(通过)/block(阻断) |
| qcConclusion | str | 质检结论（质检关卡必填） |
| params | dict | 工艺参数快照（温度/度数/数量等） |
| anomalies | list | AI 检出的异常（skip_stage/time_backflow/dwell_overdue） |
| blockHash | str | 简易链式哈希（前条哈希+本条内容 → 不可篡改） |
| punchedAt | str | 打卡时间 |

### 4.4 trace_prod_logs（AI 审计日志）

权限模块审计日志复用 + 独立留痕（action: stage_punch/batch_create/batch_block）。

## 5. 核心流程

### 5.1 责任人扫码打卡（生产端）

```
责任人在工段现场扫工段二维码(STG-xxx)
    ↓ 解析工段码 → 工段定义(permStage/permLevel)
    ↓
权限校验(调 33 号模块 check_permission)
    ├─ 无权限 → 拦截 + 越权记录落 AI 监控(1h≥3次冻结)
    └─ 通过 → 选择/输入批次号
    ↓
AI 流转校验(4 类异常检测)
    ├─ 跳工段/时间倒流 → 标记异常(可继续, 留痕追责)
    ├─ 质检关卡未通过/缺质检结论 → 阻断(block)
    └─ 超时滞留 → 预警标记
    ↓
打卡落库(责任人签名+工艺参数+链式哈希)
    ↓
批次 currentStageSeq 推进 → 下一工段
```

### 5.2 消费者溯源查询（C 端）

```
消费者扫瓶码(lifeCode) / 输入批次号
    ↓
批次 + 打卡记录时间线:
  2026-08-01 工艺酿酒 ✅ 责任人:张师傅 (52°/窖池3号)
  2026-08-30 原酒储藏 ✅ 责任人:李库管 (陶坛T-102/30天)
  2026-09-29 产品检测 ✅ 质检员:王工 (合格/酒度52.1)
  2026-09-30 灌装 ✅ / 10-01 包装质检 ✅
  2026-10-02 入库 ✅ / 10-15 出库 ✅
    ↓
AI 溯源健康度评分(异常数/完整度/时效)
```

## 6. API 设计（16 端点）

> **v2.1 修订（2026-08-29，TD-4）**：接口表按 `routes/trace_prod_routes.py` 代码实际重录，原三列表升级为「方法/路径/鉴权/说明」四列。原 14 端点全部已实现，另新增 2 个已实现端点（stages/qr、public/code）；路径参数改为代码实际的 snake_case（{batch_no}/{stage_id}/{code}）。
> 鉴权方式（按代码）：生产端/管理端复用 `auth_routes` 依赖（`Depends(get_current_member)` / `Depends(require_admin)`，JWT Bearer Token，401/403）；C 端公开无鉴权。异常映射：KeyError→404、ValueError→409、PermissionError→403。

### 生产端（8 接口，JWT 登录，环节权限在 service 层校验）

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | /api/trace-prod/stages | JWT | 工段定义列表（7 工段，附责任人候选=环节权限持有者+参数模板） |
| GET | /api/trace-prod/stages/qr | JWT | 工段二维码印刷载荷（P2：格式 ZXBJ-TRACE:{code}:v1，供印刷张贴）【v2.1 新增】 |
| POST | /api/trace-prod/batches | JWT（生产环节权限） | 创建生产批次 |
| GET | /api/trace-prod/batches | JWT | 批次列表（status 过滤：producing/released/blocked） |
| POST | /api/trace-prod/punch | JWT（对应工段环节权限） | 工段扫码打卡（权限即责任+AI 流转异常检测+链式哈希） |
| GET | /api/trace-prod/batches/{batch_no}/chain | JWT | 批次完整生产溯源链（含链完整性校验） |
| POST | /api/trace-prod/batches/{batch_no}/bind-codes | JWT（仓储环节权限） | 出库前绑定瓶码（P4 逐码强校验） |
| POST | /api/trace-prod/batches/{batch_no}/release | JWT（物流环节权限） | 出库放行（须 7 工段全完成+瓶码已绑定） |

### C 端（3 接口，公开）

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | /api/trace-prod/public/{batch_no} | 公开 | 批次生产溯源时间线（责任人脱敏） |
| GET | /api/trace-prod/public/health/{batch_no} | 公开 | AI 溯源健康度评分（P3 独立卡片） |
| GET | /api/trace-prod/public/code/{code} | 公开 | 扫瓶码/箱码查生产溯源（P4 流通贯通，一扫见全链）【v2.1 新增】 |

### 管理端（5 接口，超管）

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | /api/trace-prod/admin/anomalies | 超管（JWT+role=admin） | AI 异常打卡事件列表（limit 可选） |
| POST | /api/trace-prod/admin/batches/{batch_no}/unblock | 超管（JWT+role=admin） | 质检阻断解除（附解除原因） |
| POST | /api/trace-prod/admin/stages/{stage_id} | 超管（JWT+role=admin） | 编辑工段（滞留阈值/质检关卡/名称/描述） |
| GET | /api/trace-prod/admin/stats | 超管（JWT+role=admin） | 溯源统计（批次/打卡/异常/健康度） |
| GET | /api/trace-prod/admin/punch-logs | 超管（仅超级管理员） | 全部打卡审计日志（batch_no 过滤，路由层再校验超管） |

> 注：开发分期 P4 提及的激活端点 `/api/trace/activate` 属 22 号双码追溯模块路由（`routes/trace_routes.py`），不在本模块 16 端点范围内。本文档 v1.0 所列 14 端点均在代码中实现，无移入待实现的端点。

## 7. AI 异常检测规则（B 级规则引擎）

| 异常类型 | 规则 | 处置 |
|---|---|---|
| skip_stage 跳工段 | 打卡工段 seq > currentStageSeq+1 | 标记异常留痕，跳过工段计数 |
| time_backflow 时间倒流 | 打卡工段 seq ≤ 已完成最大 seq | 标记异常（补卡/返工） |
| dwell_overdue 超时滞留 | 距上一工段打卡超 maxDwellHours | 预警标记 |
| qc_blocked 质检阻断 | 质检关卡 block 后尝试后续工段 | 硬阻断（超管解锁） |

溯源健康度（0-100）：链完整度 40 + 无异常 30 + 时效达标 20 + 质检齐全 10。

## 8. 前端设计（小程序）

### 8.1 trace-punch 页（生产责任人）

- 扫码按钮（wx.scanCode）→ 解析工段码 → 权限自动校验
- 批次选择 + 工艺参数录入 + 质检结论（关卡工段）
- 打卡结果反馈（含 AI 异常提示）+ 我的打卡历史

### 8.2 trace-view 页（消费者/公开）

- 输入批次号/扫瓶码 → 生产溯源时间线（7 工段进度条 + 责任人脱敏姓氏 + 参数展示）
- AI 健康度评分卡

### 8.3 管理端（嵌入 perm-admin 或独立）

- 异常事件流 + 阻断解锁 + 溯源统计大屏

## 9. 测试与验收

| 套件 | 覆盖 |
|---|---|
| test_trace_prod_e2e.py | 工段种子/建批次/7工段顺序打卡/质检关卡阻断与放行/跳工段异常/时间倒流异常/超时滞留/瓶码绑定/出库放行/链式哈希校验/公开溯源脱敏 |
| test_trace_prod_auth.py | 无权限扫码拦截/越权升级冻结(联动33号)/未签责任书拦截/物流权限出库校验 |
| test_trace_prod_p2.py | 工段码印刷载荷/参数模板必填校验/AI质检语义审核(模糊词/缺指标/不合格阻断/复检豁免) |
| test_trace_prod_p3.py | 公开健康度结构/公开溯源脱敏/质检阻断-强闯-解锁-复检闭环/管理驾驶舱统计与异常事件流/非超管拦截 |
| test_trace_prod_p4.py | 流通贯通: 绑码逐码强校验(未生成/批次不匹配/非pending拦截+回写)/出库放行回写 prodReleased/批瓶双向可查/扫瓶码箱码串联生产溯源/未知码404 |

验收标准：全量测试通过 + 预览演示（责任人扫码打卡全流程 → 消费者扫码看溯源链）。

## 10. 开发分期

| 期 | 内容 | 状态 |
|---|---|---|
| P0 | 工段定义+批次管理+扫码打卡(权限联动)+顺序/质检校验+溯源链查询+公开时间线 | 已完成 |
| P1 | AI 异常检测全量+健康度评分+打卡回流信用分考核+阻断解锁管理 | 已完成 |
| P2 | 工段码印刷格式优化/工艺参数模板化/大模型质检结论语义审核 | 已完成 |
| P3 | 消费者溯源页 trace-view(免登录/扫瓶码/7工段进度条/AI健康度卡)+管理驾驶舱(统计大屏/异常事件流/一键解锁)+公开健康度端点 | 已完成 |
| P4 | 流通贯通: bind-codes 逐码强校验(流通码系统存在/批次匹配/pending 状态)+出库放行回写瓶码 prodReleased+扫瓶码/箱码串联生产溯源(public/code 端点+trace-view 流通验真卡)+补激活端点 /api/trace/activate | 已完成 |

---

**文档结束**

> 本产品溯源管理模块v1.0参照茅台/五粮液一物一码溯源（工段车间码+批次贯穿）、蚂蚁链/京东智臻链（链式哈希不可篡改+消费者溯源时间线）、EPCIS 事件标准（工段流转事件模型）、HACCP（质检关键控制点）成熟模式创新设计：以 7 工段二维码为锚点，责任人与 33 号权限模块深度绑定（权限即责任，扫码即签名），AI 四类流转异常检测把关，最终通过瓶码贯通让消费者看见「从一粒粮到一瓶酒」的全链路。
