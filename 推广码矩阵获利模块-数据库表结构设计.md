# 推广码矩阵获利模块 - 数据库表结构设计

> 版本: v1.1 | 日期: 2026-08-27
> 模块: promotion（推广码矩阵获利）| 依赖: 会员模块、钱包模块、产品模块
> v1.1 变更: 奖励规则调整（一级每满10人¥20可叠加、二级改为¥15钱包现金可叠加，废弃自动领酒）；settings 新增 level2RewardAmount；rewardType 新增 wallet_l2

## 1. 业务概述

角色领取专有推广码（含竹奕酒品牌标识 `ZXBJ`），前端将推广码渲染为**二维码**供好友扫码识别（小程序「扫码赚钱」入口）。亦可通过微信小程序、抖音、快手、小红书、B站、淘宝等成熟平台分享引流。新用户凭码注册绑定矩阵关系，触发两级奖励：

| 奖励层级 | 达成条件 | 奖励内容 |
|---|---|---|
| 一级（直推奖） | 每直接推广满 `level1Threshold`（默认 10）人 | `level1RewardAmount`（默认 ¥20）钱包奖励余额，**可叠加**（满20人得两轮、30人三轮…） |
| 二级（裂变奖） | 直推下线中每有 `level2SubPromoterCount`（默认 6）人各自完成推广 `level2SubThreshold`（默认 5）人 | `level2RewardAmount`（默认 ¥15）钱包奖励余额，**可叠加** |

> 注: 二级奖励原为"领酒资格"（可领 ¥200+ 竹奕酒），v1.1 起改为钱包现金自动发放（rewardType=`wallet_l2`）。领酒链路保留仅供管理端手动补发 `wine_qualify` 场景兼容，不再自动触发。

**奖励余额规则**: 计入钱包 `rewardBalance` 字段，标注为奖励，**仅可购买本站产品，不可提现、不可转赠**。提现仅操作活期 `balance`，两余额天然隔离。

**参数管理**: 管理端可动态修改全部阈值与奖励参数，修改后对新发生的绑定即时生效。

**新人注册原则**: 仅"新注册会员"（注册后 24 小时内）的绑定计入推广人数并触发两级奖励；老会员绑定可成功但不计业绩（关系 status=invalid）、不触发奖励，防老会员互刷。

## 2. 表清单

| 表名 | 主键 | 说明 |
|---|---|---|
| promotion_codes | code (str) | 专属推广码 |
| promotion_relations | invitee_member_id (int) | 邀请绑定关系（invitee 唯一，防重复绑定） |
| promotion_rewards | reward_id (int) | 奖励发放记录（一级/二级钱包轮次，兼容领酒资格） |
| promotion_wine_claims | claim_id (int) | 领酒记录（**已废弃自动触发**，仅管理端补发资格核销兼容） |
| promotion_settings | 单例 | 模块参数配置（管理端可改） |

## 3. 表结构明细

### 3.1 promotion_codes 专属推广码

| 字段 | 类型 | 说明 |
|---|---|---|
| code | VARCHAR(16) PK | 推广码，格式 `ZXBJ-{6位大写字母数字}`（竹奕标识） |
| ownerMemberId | INT | 归属会员ID（角色：会员/代理/站长/管理员均可领取） |
| channel | VARCHAR(32) | 投放渠道: wechat_miniprogram/douyin/kuaishou/xiaohongshu/bilibili/taobao/direct |
| status | VARCHAR(16) | active / revoked |
| boundCount | INT | 累计绑定人数（冗余计数，便于列表展示） |
| createdAt / updatedAt | DATETIME | 创建/更新时间 |

约束: (ownerMemberId, channel, status=active) 唯一 —— 同一角色同一渠道仅一个生效码；撤销后可重新领取新码。

### 3.2 promotion_relations 邀请绑定关系

| 字段 | 类型 | 说明 |
|---|---|---|
| inviteeMemberId | INT PK | 被邀请人（下线）会员ID，**一人仅可绑定一次** |
| inviterMemberId | INT | 邀请人（上级）会员ID |
| code | VARCHAR(16) | 使用的推广码 |
| channel | VARCHAR(32) | 来源渠道（冗余自推广码） |
| status | VARCHAR(16) | valid / invalid（管理端可作废） |
| createdAt | DATETIME | 绑定时间 |

约束:
- invitee 不可绑定自己（invitee ≠ inviter）
- 防环: 绑定后沿 inviter 向上遍历祖先链，若出现 invitee 则拒绝（禁止 A→B→A 闭环）
- 绑定成功后触发上级奖励检查（`_check_rewards`）

### 3.3 promotion_rewards 奖励发放记录

| 字段 | 类型 | 说明 |
|---|---|---|
| rewardId | INT PK | 自增ID |
| memberId | INT | 获奖会员ID |
| rewardType | VARCHAR(16) | wallet（一级钱包奖励余额）/ wallet_l2（二级钱包奖励余额，v1.1 新增）/ wine_qualify（领酒资格，仅管理端补发） |
| cycle | INT | 第几轮（第 1 轮/第 2 轮...，可循环领取，同类型独立计数） |
| amount | DECIMAL(10,2) | wallet / wallet_l2 类型金额；wine_qualify 为 0 |
| status | VARCHAR(16) | issued（已发放）/ used（领酒资格已核销） |
| detail | VARCHAR(255) | 描述（如"直推满10人第1轮"、"6个下线各推广满5人第1轮"） |
| createdAt | DATETIME | 发放时间 |

### 3.4 promotion_wine_claims 领酒记录（已废弃自动触发）

> v1.1 起二级奖励不再自动发放领酒资格，本表仅服务于管理端手动补发 `wine_qualify` 后的核销流程，属兼容保留。

| 字段 | 类型 | 说明 |
|---|---|---|
| claimId | INT PK | 自增ID |
| memberId | INT | 领取会员ID |
| rewardId | INT | 核销的奖励资格ID |
| productId | VARCHAR(32) | 选择的竹奕酒产品ID（须在活动池且价格 ≥ wineMinPrice） |
| productName | VARCHAR(64) | 产品名快照 |
| address | VARCHAR(255) | 收货地址 |
| status | VARCHAR(16) | pending_shipped / shipped / done |
| createdAt / shippedAt | DATETIME | 领取/发货时间 |

### 3.5 promotion_settings 参数配置（单例）

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| enabled | BOOLEAN | true | 模块总开关 |
| level1Threshold | INT | 10 | 一级奖励人数阈值 |
| level1RewardAmount | DECIMAL | 20.00 | 一级奖励金额(元/轮，可叠加) |
| level2SubPromoterCount | INT | 6 | 二级达标所需下线数 |
| level2SubThreshold | INT | 5 | 每个下线需完成的推广人数 |
| level2RewardAmount | DECIMAL | 15.00 | 二级奖励金额(元/轮，可叠加，v1.1 新增) |
| wineMinPrice | DECIMAL | 200.00 | 奖励酒最低价（仅管理端补发领酒场景使用） |
| eligibleProductIds | JSON | [价格≥200的产品] | 活动酒池（领酒可选产品，仅管理端补发场景使用） |
| updatedAt | DATETIME | - | 最后修改时间 / 修改人 |

## 4. 核心算法: 矩阵奖励检查（绑定触发）

```
绑定成功(invitee → inviter) 后, 对 inviter 加锁执行:

n1 = inviter 的有效下线总数
c1 = inviter 已发放的 wallet(一级) 轮次数
若 n1 >= level1Threshold * (c1 + 1):
    发放第 c1+1 轮 → 钱包 rewardBalance += level1RewardAmount
    （流水 type=reward, 标注"推广奖励,仅限购物不可提现"）
    奖励可叠加: 满 10 人第 1 轮, 满 20 人第 2 轮, 以此类推

n2 = inviter 的下线中"各自有效下线数 >= level2SubThreshold"的人数
c2 = inviter 已发放的 wallet_l2(二级) 轮次数
若 n2 >= level2SubPromoterCount * (c2 + 1):
    发放第 c2+1 轮 → 钱包 rewardBalance += level2RewardAmount
    （rewardType=wallet_l2, 同样计入奖励余额, 仅限购物不可提现）
    奖励可叠加: 每 6 个达标下线发一轮
```

> 注: 上线（inviter 的 inviter）的 n2 可能因子下线达标而增加，故绑定后沿祖先链向上逐级触发检查（最多上溯 2 级即可覆盖两级奖励场景）。

## 5. 防刷与风控

| 风险 | 对策 |
|---|---|
| 重复绑定刷下线 | invitee 全局唯一，一人仅计一次 |
| 自己绑自己 | invitee ≠ inviter 校验 |
| 循环推荐环 | 祖先链遍历防环 |
| 撤销码继续使用 | status=revoked 拒绝绑定 |
| 老会员互刷业绩 | 新人注册原则: 注册超 24h 的会员绑定 status=invalid，不计入 n1/n2 也不触发奖励 |
| 无效关系计入业绩 | status=invalid 的关系不计入 n1/n2 |
| 奖励余额套现 | rewardBalance 与 balance 隔离，withdraw 仅扣 balance；reward 购物专用接口核销 |

## 6. 接口清单

### 6.1 用户端（X-Member-Id）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/promotion/code/claim | 领取专属推广码（指定渠道） |
| GET | /api/promotion/my/codes | 我的推广码列表 |
| GET | /api/promotion/my/stats | 推广统计（下线数/达标数/奖励/奖励余额） |
| GET | /api/promotion/my/team | 我的下线列表 |
| GET | /api/promotion/my/rewards | 我的奖励记录 |
| POST | /api/promotion/bind | 新用户绑定推广码 |
| POST | /api/promotion/wine/claim | 领取奖励酒（已废弃自动触发，仅管理端补发资格后使用） |
| POST | /api/promotion/reward/purchase | 奖励余额购买产品 |
| GET | /api/promotion/products/eligible | 活动酒池产品列表（仅管理端补发领酒场景使用） |

### 6.2 管理端（X-Role: admin）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/promotion/admin/settings | 读取参数配置 |
| PUT | /api/promotion/admin/settings | 修改参数配置 |
| GET | /api/promotion/admin/relations | 关系列表 |
| GET | /api/promotion/admin/rewards | 奖励发放列表 |
| GET | /api/promotion/admin/wine-claims | 领酒记录列表 |
| PUT | /api/promotion/admin/wine-claims/{claim_id}/ship | 领酒发货 |
| POST | /api/promotion/admin/codes/{code}/revoke | 撤销推广码 |
| POST | /api/promotion/admin/relations/{invitee_id}/invalidate | 作废绑定关系 |
| POST | /api/promotion/admin/rewards/grant | 手动补发奖励 |
