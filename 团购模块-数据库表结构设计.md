# 团购模块 · 数据库表结构设计

> 版本：v1.0
> 日期：2026-08-22
> 关联文档：团购模块设计文档.md、会员管理模块设计文档.md、收款管理模块设计文档.md
> 设计原则：对齐项目 4 层架构（core/repositories/services/routes），双模式存储（内存 + Redis）

---

## 一、表清单与优先级

| # | 表名 | 中文名 | 优先级 | 说明 |
|---|------|--------|:---:|------|
| 1 | group_buy_orders | 团购订单表 | P0 | 团购主订单，含阶梯折扣计算结果 |
| 2 | group_buy_items | 团购订单明细表 | P0 | 多产品混合团购明细 |
| 3 | group_buy_audit | 团购审核记录表 | P0 | 审批流水（专员/主管/总监/总经理）|
| 4 | group_buy_custom | 团购定制需求表 | P1 | 刻字/标签/礼盒/包装箱定制 |
| 5 | group_buy_installment | 团购分期付款表 | P1 | ≥¥200,000 分期方案 |

**P0 表（3 张）：** group_buy_orders + group_buy_items + group_buy_audit
**P1 表（2 张）：** group_buy_custom + group_buy_installment

---

## 二、P0 表结构

### 2.1 group_buy_orders（团购订单表）

| 字段 | 类型 | 必填 | 说明 |
|------|------|:---:|------|
| orderNo | VARCHAR(32) | ✓ | 团购订单号 TG+时间戳，唯一索引 |
| userId | BIGINT | ✓ | SVIP 用户ID |
| userLevel | TINYINT | ✓ | 会员等级（固定 5 = SVIP）|
| groupType | VARCHAR(20) | ✓ | 团购类型: enterprise/wedding/festival/custom |
| purpose | VARCHAR(200) | | 用途说明 |
| originalTotal | DECIMAL(12,2) | ✓ | 原价合计 |
| tier | VARCHAR(10) | ✓ | 阶梯: T1/T2/T3/T4 |
| discount | DECIMAL(3,2) | ✓ | 折扣: 0.80/0.75/0.72/0.70 |
| groupPrice | DECIMAL(12,2) | ✓ | 团购价（原价 × 折扣）|
| savedAmount | DECIMAL(12,2) | ✓ | 节省金额（原价 - 团购价）|
| status | VARCHAR(20) | ✓ | 状态: pending/approved/paying/in_production/shipped/completed/cancelled/rejected |
| customNeeds | TEXT | | 定制需求(JSON) |
| invoiceInfo | TEXT | | 发票信息(JSON) |
| addresses | TEXT | | 收货地址(JSON 数组，支持多地址) |
| applyTime | DATETIME | ✓ | 申请时间 |
| auditTime | DATETIME | | 审核时间 |
| auditUser | VARCHAR(50) | | 审核人 |
| payTime | DATETIME | | 付款时间 |
| shipTime | DATETIME | | 发货时间 |
| completeTime | DATETIME | | 完成时间 |
| createdAt | DATETIME | ✓ | 创建时间 |
| updatedAt | DATETIME | ✓ | 更新时间 |

**索引：**
- PRIMARY KEY: orderNo
- INDEX: userId（用户维度查询）
- INDEX: status（按状态筛选）

### 2.2 group_buy_items（团购订单明细表）

| 字段 | 类型 | 必填 | 说明 |
|------|------|:---:|------|
| id | BIGINT | ✓ | 自增主键 |
| orderNo | VARCHAR(32) | ✓ | 关联团购订单号 |
| productId | VARCHAR(32) | ✓ | 产品ID |
| productName | VARCHAR(100) | ✓ | 产品名称 |
| productSpec | VARCHAR(50) | | 规格(度数/容量) |
| quantity | INT | ✓ | 数量 |
| originalPrice | DECIMAL(10,2) | ✓ | 原价 |
| subtotal | DECIMAL(12,2) | ✓ | 小计(原价 × 数量) |
| createdAt | DATETIME | ✓ | 创建时间 |

**索引：**
- PRIMARY KEY: id
- INDEX: orderNo（按订单查询明细）

### 2.3 group_buy_audit（团购审核记录表）

| 字段 | 类型 | 必填 | 说明 |
|------|------|:---:|------|
| id | BIGINT | ✓ | 自增主键 |
| orderNo | VARCHAR(32) | ✓ | 关联团购订单号 |
| auditLevel | VARCHAR(20) | ✓ | 审批层级: staff/supervisor/director/general_manager |
| auditor | VARCHAR(50) | ✓ | 审核人 |
| auditResult | VARCHAR(20) | ✓ | 审核结果: approved/rejected |
| auditRemark | TEXT | | 审核备注 |
| auditTime | DATETIME | ✓ | 审核时间 |
| createdAt | DATETIME | ✓ | 创建时间 |

**索引：**
- PRIMARY KEY: id
- INDEX: orderNo（按订单查询审核流水）

---

## 三、P1 表结构

### 3.1 group_buy_custom（团购定制需求表）

| 字段 | 类型 | 必填 | 说明 |
|------|------|:---:|------|
| id | BIGINT | ✓ | 自增主键 |
| orderNo | VARCHAR(32) | ✓ | 关联团购订单号 |
| customType | VARCHAR(20) | ✓ | 定制类型: engraving/label/giftbox/package |
| customContent | TEXT | | 定制内容 |
| designFile | VARCHAR(500) | | 设计稿URL |
| effectImage | VARCHAR(500) | | 效果图URL |
| extraFee | DECIMAL(10,2) | | 定制费用（默认 0）|
| productionDays | INT | | 增加工期(天) |
| status | VARCHAR(20) | ✓ | 待确认/已确认/生产中/已完成 |
| createdAt | DATETIME | ✓ | 创建时间 |

### 3.2 group_buy_installment（团购分期付款表）

| 字段 | 类型 | 必填 | 说明 |
|------|------|:---:|------|
| id | BIGINT | ✓ | 自增主键 |
| orderNo | VARCHAR(32) | ✓ | 关联团购订单号 |
| totalAmount | DECIMAL(12,2) | ✓ | 总金额 |
| installmentCount | INT | ✓ | 分期数(3/6) |
| paidAmount | DECIMAL(12,2) | | 已付金额（默认 0）|
| schedule | TEXT | | 还款计划(JSON) |
| status | VARCHAR(20) | ✓ | 进行中/已结清/逾期 |
| createdAt | DATETIME | ✓ | 创建时间 |

---

## 四、状态机设计

### 4.1 团购订单状态机（group_buy_orders.status）

```
pending（待审核）
   ├── approved（审核通过）→ paying（待付款）→ in_production（生产中）→ shipped（已发货）→ completed（已完成）
   └── rejected（已驳回）【终态】

cancelled（已取消）【终态】可从 pending/approved/paying 流转到此
```

**合法状态流转：**

| 当前状态 | 可流转到 |
|---------|---------|
| pending | approved, rejected, cancelled |
| approved | paying, cancelled |
| paying | in_production, cancelled |
| in_production | shipped |
| shipped | completed |
| completed | （终态）|
| rejected | （终态）|
| cancelled | （终态）|

### 4.2 审核结果（group_buy_audit.auditResult）

| 结果 | 说明 |
|------|------|
| approved | 审核通过 |
| rejected | 审核驳回 |

### 4.3 定制需求状态（group_buy_custom.status）

```
pending（待确认）→ confirmed（已确认）→ in_production（生产中）→ completed（已完成）
```

---

## 五、阶梯折扣规则

### 5.1 阶梯折扣表

| 阶梯 | 团购金额（按原价） | 折扣 | 折扣率 |
|:---:|:---:|:---:|:---:|
| T1 | ¥50,000 - ¥99,999 | 0.80 | 20% off |
| T2 | ¥100,000 - ¥199,999 | 0.75 | 25% off |
| T3 | ¥200,000 - ¥499,999 | 0.72 | 28% off |
| T4 | ≥ ¥500,000 | 0.70 | 30% off |

### 5.2 团购门槛

| 门槛项 | 要求 |
|--------|------|
| 单笔金额 | ≥ ¥50,000（按原价计算）|
| 最低数量 | ≥ 50 瓶（或等值礼盒/套装）|
| 单次上限 | ≤ ¥500,000（超出需额外审批）|
| 年度限额 | ≤ ¥2,000,000（单个 SVIP 年度团购总额）|
| 频次限制 | ≤ 4 次/月 |

### 5.3 团购类型

| 类型 | groupType | 起订量 | 起订金额 |
|------|-----------|:---:|:---:|
| 企业团购 | enterprise | ≥ 50 瓶 | ≥ ¥50,000 |
| 婚宴团购 | wedding | ≥ 30 瓶 | ≥ ¥30,000（固定 8.5 折）|
| 节日团购 | festival | ≥ 50 瓶 | ≥ ¥50,000 |
| 定制团购 | custom | ≥ 100 瓶 | ≥ ¥100,000 |

---

## 六、并发安全设计

### 6.1 锁键设计

| 场景 | 锁键 | 说明 |
|------|------|------|
| 团购申请提交 | `lock:groupbuy:apply:{userId}` | 防止重复提交 |
| 团购订单状态流转 | `lock:groupbuy:order:{orderNo}` | 防止并发状态变更 |
| 团购审核 | `lock:groupbuy:audit:{orderNo}` | 防止重复审核 |
| 频次限制检查 | `lock:groupbuy:freq:{userId}` | 月度频次计数 |

### 6.2 幂等设计

- **团购申请幂等：** 同一 userId 在 pending/approved/paying 状态下不允许再次申请
- **审核幂等：** 同一 orderNo 已审核后再次审核返回冲突
- **支付幂等：** 复用收款管理模块的支付幂等机制

---

## 七、接口规划（对齐设计文档第 11.2 节）

### 7.1 P0 接口（8 个）

| # | 接口 | 方法 | 鉴权 | 说明 |
|---|------|:---:|------|------|
| 1 | /api/groupbuy/products | GET | X-Member-Id | 获取可团购产品列表 |
| 2 | /api/groupbuy/calculate | POST | X-Member-Id | 计算团购价（输入产品+数量）|
| 3 | /api/groupbuy/apply | POST | X-Member-Id | 提交团购申请 |
| 4 | /api/groupbuy/list | GET | X-Member-Id | 我的团购订单列表 |
| 5 | /api/groupbuy/{orderNo} | GET | X-Member-Id | 团购订单详情 |
| 6 | /api/groupbuy/{orderNo}/audit | POST | X-Role: admin | 审核团购申请 |
| 7 | /api/groupbuy/{orderNo}/cancel | PUT | X-Member-Id | 取消团购申请 |
| 8 | /api/groupbuy/tiers | GET | 公开 | 阶梯折扣表查询 |

### 7.2 P1 接口（6 个，后续扩展）

| # | 接口 | 方法 | 鉴权 | 说明 |
|---|------|:---:|------|------|
| 9 | /api/groupbuy/{orderNo}/custom | POST | X-Member-Id | 提交定制需求 |
| 10 | /api/groupbuy/{orderNo}/custom | GET | X-Member-Id | 查询定制需求 |
| 11 | /api/groupbuy/{orderNo}/installment | POST | X-Member-Id | 申请分期付款 |
| 12 | /api/groupbuy/{orderNo}/installment | GET | X-Member-Id | 查询分期计划 |
| 13 | /api/groupbuy/pending | GET | X-Role: admin | 待审核订单列表 |
| 14 | /api/groupbuy/stats | GET | X-Role: admin | 团购统计 |

---

## 八、与其他模块联动

| 模块 | 联动点 |
|------|--------|
| 会员管理 | 校验 SVIP 等级（L5）+ 实名认证 + 账户状态 |
| 产品展示 | 复用产品列表 + 价格信息 |
| 收款管理 | 复用支付接口（团购订单支付）|
| 物流接口 | 复用物流下单（团购订单发货）|
| 会员积分 | 团购订单按实际支付金额 1:1 获取积分 |
