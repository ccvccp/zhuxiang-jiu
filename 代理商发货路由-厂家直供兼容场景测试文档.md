# 代理商发货路由 · 厂家直供兼容场景测试验证文档

> 版本：v1.0 ｜ 日期：2026-08-18 ｜ 测试集：`js/agent-shipping-regression-test.js` v2.0（8 用例）
> 关联模块：`js/agent-shipping-service.js` / `js/checkout-service.js`
> 关联文档：《代理商管理模块设计文档》§区域发货服务认领

---

## 1. 背景与目标

### 1.1 业务背景

竹香酒官网订单结算系统支持「代理商区域发货服务认领」机制：

- 一个区域同一时间只能被一个代理商认领（一区一代理）
- **认领区域**：网站订单由该代理商发货 + 售后，厂家按订单实付金额 5% 计提同品分润作为服务费
- **未认领区域**：由厂家直供，不计提服务费

厂家直供是系统的「默认兜底路径」，必须保证以下场景均能正确回落：

1. 区域存在但无代理商认领
2. 旧调用方不传 `region` 参数
3. 区域曾被认领但已释放
4. 同一会话内认领区域与未认领区域订单并存

### 1.2 兼容性目标

| 目标 | 验收标准 |
|------|----------|
| 路由正确 | 未认领区域 `shipperType === 'manufacturer'` |
| 不计提服务费 | `manufacturerServiceFee === 0` |
| 无服务费流水 | `service_fees` 表无新增记录 |
| 字段完整 | `shipper_agent_id === null`、`ship_region` 正确反映传入 |
| 路由隔离 | 同会话内不同区域订单互不影响 |
| 状态可恢复 | 释放认领后路由自动恢复为厂家直供 |

### 1.3 风险点

本次新增用例重点防控以下缺陷：

| 风险 | 描述 | 覆盖用例 |
|------|------|----------|
| 路由误判 | 未认领区域被错误路由到代理商 | TC5 / TC6 / TC7 |
| 服务费误计提 | 未认领区域产生 5% 服务费流水 | TC5 / TC6 / TC7 / TC8 |
| 状态残留 | 释放认领后路由未恢复厂家直供 | TC7 |
| 路由污染 | 同会话内不同区域订单相互影响 | TC8 |

---

## 2. 测试范围与用例总览

### 2.1 测试集

- **文件**：[agent-shipping-regression-test.js](file:///d:/网站架构设计/zhuxiang-jiu/js/agent-shipping-regression-test.js)
- **用例总数**：8（原 4 用例 + 新增 4 用例）
- **运行入口**：`window.runAgentShippingRegression()`
- **约定检查器注册**：`service-convention-checker.js` 中 `agent-shipping` 服务的 `runFn`

### 2.2 用例矩阵

| 用例 | 场景 | 验证重点 | 时长 |
|------|------|----------|------|
| TC1 | 正常认领（代理商1认领山东泰安） | 5 阶段事务 + 认领记录 + 异步任务 | 6ms |
| TC2 | 重复认领拒绝（代理2抢同区域） | 阶段3抛错 + 快照回滚 + 原子性 | 8ms |
| TC3 | 释放认领（代理1释放山东泰安） | 状态→已退出 + 区域可重认领 | 13ms |
| TC4 | 发货路由+服务费（认领后下单） | 发货方=代理商 + 5%同品分润计提 | 20ms |
| **TC5** | **未认领区域下单（山东济南）** | **厂家直供 + 无服务费 + 无流水** | 11ms |
| **TC6** | **无 region 下单（兼容旧调用方）** | **默认厂家直供 + ship_region=null** | 13ms |
| **TC7** | **认领→释放→下单（状态转换）** | **路由恢复厂家直供 + 无服务费** | 24ms |
| **TC8** | **混合区域下单（认领+未认领）** | **两单发货方不同 + 仅认领区域计提** | 28ms |

> 加粗为本次新增的厂家直供兼容场景用例。

---

## 3. 厂家直供兼容场景用例详细设计

### 3.1 TC5 · 未认领区域下单 → 厂家直供

**场景**：区域存在（山东济南）但无代理商认领，下单后发货方应为厂家直供，不计提 5% 服务费。

**前置条件**：
- `AgentShippingService.resetMock()`（清空认领记录）
- `CheckoutService.resetMock()`（重置订单/库存/分润）
- 不进行任何认领

**执行步骤**：
1. 只读校验：`resolveShipper('山东济南')` → 期望 manufacturer
2. 下单：`CheckoutService.submit({ region: '山东济南', ... })`
3. 校验 details、订单记录、分润记录、服务费流水、结算汇总

**断言点清单**：

| # | 断言对象 | 期望值 | 说明 |
|---|----------|--------|------|
| 1 | `resolveShipper('山东济南').shipper` | `'manufacturer'` | 路由解析 |
| 2 | `resolveShipper('山东济南').agentId` | `null` | 无代理商 |
| 3 | `resolveShipper('山东济南').agentName` | `'厂家直供'` | 发货方名称 |
| 4 | `resolveShipper('山东济南').claimId` | `null` | 无认领记录 |
| 5 | `r.details.shipperType` | `'manufacturer'` | 订单详情 |
| 6 | `r.details.shipperAgentName` | `'厂家直供'` | 订单详情 |
| 7 | `r.details.region` | `'山东济南'` | 区域回传 |
| 8 | `r.details.manufacturerServiceFee` | `0` | **不计提服务费** |
| 9 | `order.shipper_type` | `'manufacturer'` | 订单表字段 |
| 10 | `order.shipper_agent_id` | `null` | 订单表字段 |
| 11 | `order.ship_region` | `'山东济南'` | 订单表字段 |
| 12 | `profit.manufacturer_service_fee` | `0` | 分润表字段 |
| 13 | `profit.shipper_type` | `'manufacturer'` | 分润表字段 |
| 14 | `cdb.service_fees.length` | `0` | **无服务费流水** |
| 15 | `settlement.pendingCount` | `0` | 结算汇总为空 |
| 16 | `settlement.pendingAmount` | `0` | 结算汇总为空 |

---

### 3.2 TC6 · 无 region 参数下单 → 厂家直供

**场景**：旧调用方不传 `region` 参数（兼容场景），checkout 内部跳过路由解析，默认厂家直供。

**前置条件**：
- `AgentShippingService.resetMock()`
- `CheckoutService.resetMock()`

**执行步骤**：
1. 只读校验：`resolveShipper('')` 和 `resolveShipper(null)` → 期望 manufacturer
2. 下单：`CheckoutService.submit({ /* 无 region */ ... })`
3. 校验 details、订单记录、服务费流水

**断言点清单**：

| # | 断言对象 | 期望值 | 说明 |
|---|----------|--------|------|
| 1 | `resolveShipper('').shipper` | `'manufacturer'` | 空区域路由 |
| 2 | `resolveShipper(null).shipper` | `'manufacturer'` | null 区域路由 |
| 3 | `r.details.shipperType` | `'manufacturer'` | 默认发货方 |
| 4 | `r.details.shipperAgentName` | `'厂家直供'` | 默认名称 |
| 5 | `r.details.region` | `null` | **region 未传** |
| 6 | `r.details.manufacturerServiceFee` | `0` | 不计提服务费 |
| 7 | `order.shipper_type` | `'manufacturer'` | 订单表字段 |
| 8 | `order.shipper_agent_id` | `null` | 订单表字段 |
| 9 | `order.ship_region` | `null` | **ship_region=null** |
| 10 | `cdb.service_fees.length` | `0` | 无服务费流水 |

> 关键：`region` 未传时 `ctx.region` 为 `undefined`，checkout 阶段3的条件 `if (ctx.region && ...)` 不触发，保留默认值 `shipperType='manufacturer'`。

---

### 3.3 TC7 · 认领后释放再下单 → 恢复厂家直供

**场景**：代理商认领区域后释放，该区域订单发货方应从代理商恢复为厂家直供，验证路由状态转换。

**前置条件**：
- `AgentShippingService.resetMock()`
- `CheckoutService.resetMock()`

**执行步骤**：
1. 认领：`claim(1, '山东泰安')`
2. 校验：`resolveShipper('山东泰安')` → agent
3. 释放：`release(1, '山东泰安')`
4. 校验：`resolveShipper('山东泰安')` → manufacturer
5. 下单：`CheckoutService.submit({ region: '山东泰安', ... })`
6. 校验 details、服务费流水、订单记录

**断言点清单**：

| # | 断言对象 | 期望值 | 说明 |
|---|----------|--------|------|
| 1 | 认领后 `resolveShipper('山东泰安').shipper` | `'agent'` | 认领态路由 |
| 2 | 释放后 `resolveShipper('山东泰安').shipper` | `'manufacturer'` | **释放后恢复** |
| 3 | 释放后 `resolveShipper('山东泰安').agentId` | `null` | 无代理商 |
| 4 | 释放后 `resolveShipper('山东泰安').claimId` | `null` | 无认领 |
| 5 | `r.details.shipperType` | `'manufacturer'` | 订单发货方 |
| 6 | `r.details.manufacturerServiceFee` | `0` | 不计提服务费 |
| 7 | `cdb.service_fees.length` | `0` | 无服务费流水 |
| 8 | `order.shipper_type` | `'manufacturer'` | 订单表字段 |
| 9 | `order.shipper_agent_id` | `null` | 订单表字段 |

> 关键：释放认领将 `shipping_claims[].status` 置为 `'已退出'`，`resolveShipper` 只匹配 `status === '已认领'` 的记录，故释放后区域自动回落厂家直供。

---

### 3.4 TC8 · 混合区域下单 → 分别路由

**场景**：同一会话内，认领区域订单走代理商 + 服务费，未认领区域订单走厂家直供，验证路由隔离。

**前置条件**：
- `AgentShippingService.resetMock()`
- `CheckoutService.resetMock()`
- 认领 `'山东泰安'`（仅此区域）

**执行步骤**：
1. 订单1：`submit({ region: '山东泰安' })` → 认领区域
2. 订单2：`submit({ region: '山东济南' })` → 未认领区域
3. 校验两单发货方不同、服务费流水仅 1 条、结算汇总仅 1 笔

**断言点清单**：

| # | 断言对象 | 期望值 | 说明 |
|---|----------|--------|------|
| 1 | `r1.details.shipperType` | `'agent'` | 订单1发货方 |
| 2 | `r1.details.shipperAgentName` | `'张三酒业'` | 订单1代理商 |
| 3 | `r1.details.manufacturerServiceFee` | `> 0` | 订单1计提服务费 |
| 4 | `r2.details.shipperType` | `'manufacturer'` | 订单2发货方 |
| 5 | `r2.details.manufacturerServiceFee` | `0` | 订单2不计提 |
| 6 | `r1.shipperType !== r2.shipperType` | `true` | **两单发货方不同** |
| 7 | `cdb.service_fees.length` | `1` | **仅1条服务费流水** |
| 8 | `fees[0].order_no` | `r1.orderNo` | 流水关联订单1 |
| 9 | `fees[0].agent_id` | `1` | 流水代理商ID |
| 10 | `settlement.pendingCount` | `1` | 结算仅1笔 |

> 关键：同会话内两单各自独立路由，认领区域订单产生服务费，未认领区域订单不产生，互不污染。

---

## 4. 关键兼容性断言汇总

### 4.1 无服务费流水（原子性）

未认领区域、无 region、释放后下单时，`service_fees` 表不应有新增记录。这是厂家直供路径的核心原子性保证——只有认领区域的代理商发货订单才计提服务费。

| 用例 | 断言 | 结果 |
|------|------|------|
| TC5 | `cdb.service_fees.length === 0` | PASS |
| TC6 | `cdb.service_fees.length === 0` | PASS |
| TC7 | `cdb.service_fees.length === 0` | PASS |
| TC8 | `cdb.service_fees.length === 1`（仅订单1） | PASS |

### 4.2 路由隔离

TC8 验证同会话内不同区域订单互不影响：认领区域订单 shipperType=agent，未认领区域订单 shipperType=manufacturer，两单独立路由。

### 4.3 字段完整性

| 字段 | 厂家直供期望值 | 验证用例 |
|------|----------------|----------|
| `shipper_type` | `'manufacturer'` | TC5/TC6/TC7/TC8订单2 |
| `shipper_agent_id` | `null` | TC5/TC6/TC7 |
| `shipper_agent_name` | `'厂家直供'` | TC5/TC6 |
| `ship_region` | 传入值或 `null` | TC5='山东济南'/TC6=null/TC7='山东泰安' |
| `manufacturer_service_fee` | `0` | TC5/TC6/TC7/TC8订单2 |

### 4.4 结算汇总为空

未认领场景下代理商结算汇总应为空：

| 用例 | `pendingCount` | `pendingAmount` |
|------|----------------|-----------------|
| TC5 | 0 | 0 |
| TC8 | 1（仅订单1） | `r1.finalAmount * 5%` |

---

## 5. 沙箱验证结果

### 5.1 验证环境

- 本地静态服务器：`http://localhost:8080`（`zhuxiang-jiu/server.ps1`）
- 测试页面：`module-test.html`
- 浏览器：Chromium 内核，硬刷新（`location.reload(true)`）绕过缓存
- 约定检查器：`ConventionChecker.run({ runtime: true })`

### 5.2 发货认领回归 8/8 PASS

| 用例 | 场景 | 状态 | 时长 | 错误 |
|------|------|------|------|------|
| TC1 | 正常认领 | PASS | 6ms | — |
| TC2 | 重复认领拒绝 | PASS | 8ms | — |
| TC3 | 释放认领 | PASS | 13ms | — |
| TC4 | 发货路由+服务费 | PASS | 20ms | — |
| **TC5** | **未认领区域下单** | **PASS** | **11ms** | — |
| **TC6** | **无 region 下单** | **PASS** | **13ms** | — |
| **TC7** | **释放后恢复直供** | **PASS** | **24ms** | — |
| **TC8** | **混合区域下单** | **PASS** | **28ms** | — |

**汇总**：8/8 PASS，0 FAIL，通过率 100%。

### 5.3 约定检查器结果

```
ConventionChecker.run({ runtime: true })
→ total=32, pass=28, warn=4, fail=0, success=true
→ fails: []（空，无失败项）
```

| 检查项 | 说明 | agent-shipping 结果 |
|--------|------|---------------------|
| C1-preflight-no-business | 预检无业务判断 | PASS |
| C2-stage-uses-throw | 阶段用 throw | PASS |
| C3-shared-core-no-tx | 共享核心无嵌套事务 | PASS |
| C4-uses-toolkit | 基于 TransactionTemplate | PASS |
| C5-test-failedStage | 测试断言 failedStage | PASS |
| C6-test-rollback | 测试断言 ROLLBACK | PASS |
| C7-test-begin-after-rollback | 回滚不断言 BEGIN | PASS |
| C8-runtime | 回归全部通过 | **PASS（8/8）** |

### 5.4 控制台错误检查

- `browser_console_messages`：无 JS 运行时异常、无网络失败条目
- 仅有 info 级别事务日志输出

---

## 6. 测试环境与依赖

### 6.1 文件依赖

| 文件 | 角色 |
|------|------|
| `js/agent-shipping-service.js` | 被测服务（claim/release/resolveShipper/accrueServiceFee） |
| `js/checkout-service.js` | 集成点（阶段3路由 + 阶段8服务费计提） |
| `js/order-pricing.js` | 价格计算（calculateOrderPrice） |
| `js/toolkit/upgrade-logger.js` | 结构化事务日志 |
| `js/toolkit/transaction-template.js` | 事务编排（BEGIN/COMMIT/ROLLBACK） |
| `js/toolkit/service-convention-checker.js` | 约定检查器（C1-C8） |

### 6.2 运行方式

| 方式 | 命令 |
|------|------|
| 浏览器面板 | `module-test.html` → 发货认领面板 → 一键回归按钮 |
| 控制台 | `runAgentShippingRegression()` |
| 约定检查器 | `ConventionChecker.run({ runtime: true })` |
| CI 流水线 | `ci/run-convention-check.cjs`（GitHub Actions 自动触发） |

---

## 7. 维护说明

### 7.1 新增用例

在 `agent-shipping-regression-test.js` 中：

1. 编写 `async function TCn_xxx() { ... }` 函数，开头调用 `await setup()`
2. 在 `cases` 数组追加 `{ name: 'TCn ...', fn: TCn_xxx }`
3. 更新文件头部注释的用例计数

新增用例自动被约定检查器 C8-runtime 和 CI 流水线覆盖，无需额外注册。

### 7.2 约定检查器集成

`service-convention-checker.js` 已注册 `agent-shipping` 服务：

```javascript
.register({
    name: 'agent-shipping',
    servicePath: 'js/agent-shipping-service.js',
    testPath: 'js/agent-shipping-regression-test.js',
    sharedCoreFns: ['accrueServiceFee'],
    standaloneFns: ['claim', 'release'],
    runFn: 'runAgentShippingRegression',
})
```

C8-runtime 会调用 `runAgentShippingRegression()` 并校验 `report.passed === report.total`。

### 7.3 CI 集成

CI 流水线配置文件：[.github/workflows/convention-check.yml](file:///d:/网站架构设计/.github/workflows/convention-check.yml)

#### 7.3.1 触发条件

| 事件 | 分支 | 说明 |
|------|------|------|
| `push` | `main` / `master` | 主干提交自动触发 |
| `pull_request` | 任意 | PR 提交自动触发(含 fork) |
| `workflow_dispatch` | 手动选择 | Actions 页面手动运行指定分支 |

路径过滤（仅以下文件变更才触发，省 CI 配额）：

```
zhuxiang-jiu/js/**          # 所有 JS 服务/工具包/测试
zhuxiang-jiu/*.html         # 所有页面(含 checkout.html/module-test.html)
ci/**                       # CI 脚本自身
.github/workflows/convention-check.yml  # workflow 自身
```

#### 7.3.2 双 Job 结构

```
┌─────────────────────────────────────┐
│  Job 1: static-check (C1-C7)        │  零外部依赖, ~10s
│  · 检出代码                          │
│  · 配置 Node 20                      │
│  · node ci/run-convention-check.cjs  │  静态源码分析
│  · 输出 Job Summary                  │
│  · 上传 convention-static-report     │
└──────────────┬──────────────────────┘
               │ needs: static-check
               ▼ (静态通过后才跑运行时,省配额)
┌─────────────────────────────────────┐
│  Job 2: runtime-regression (C8)      │  puppeteer+chromium, ~30s
│  · 检出代码                          │
│  · 配置 Node 20                      │
│  · 缓存 puppeteer-core              │
│  · 安装 chromium-browser            │  apt 安装,不下载
│  · npm install puppeteer-core       │
│  · node ci/run-regression-headless.cjs │ 加载 module-test.html
│  · 输出 Job Summary (含用例明细)     │
│  · 上传 convention-runtime-report    │
└─────────────────────────────────────┘
```

**Job 依赖**：`runtime-regression` 依赖 `static-check` 通过，静态检查 FAIL 时不跑运行时回归，节省 CI 配额。

#### 7.3.3 静态检查 Job (C1-C7)

| 步骤 | 命令/Action | 说明 |
|------|-------------|------|
| 检出代码 | `actions/checkout@v4` | — |
| 配置 Node | `actions/setup-node@v4` | node-version: 20 |
| 运行检查 | `node ci/run-convention-check.cjs` | 零外部依赖,复用浏览器版检查器静态分析 |
| Job Summary | 输出 Markdown 表格到 `$GITHUB_STEP_SUMMARY` | total/pass/warn/fail 摘要 |
| 上传报告 | `actions/upload-artifact@v4` | `convention-static-report` (convention-report.json) |

脚本：[ci/run-convention-check.cjs](file:///d:/网站架构设计/ci/run-convention-check.cjs)

- 通过 shim `globalThis.fetch` 让浏览器版检查器直接读本地文件，无需 HTTP 服务
- 退出码：0 通过 / 1 FAIL 阻断 / 2 异常
- 支持 `--strict-warn` 参数让 WARN 也阻断

#### 7.3.4 运行时回归 Job (C8)

| 步骤 | 命令/Action | 说明 |
|------|-------------|------|
| 检出代码 | `actions/checkout@v4` | — |
| 配置 Node | `actions/setup-node@v4` | node-version: 20 |
| 缓存 | `actions/cache@v4` | 缓存 `~/.npm` 加速 puppeteer-core 安装 |
| 安装 Chromium | `sudo apt-get install -y chromium-browser` | 系统包,不下载 chromium |
| 安装 puppeteer-core | `npm install puppeteer-core@latest --no-save` | 连接系统 chromium |
| 运行回归 | `node ci/run-regression-headless.cjs` | 加载 module-test.html 执行 C8 |
| Job Summary | 输出 Markdown 表格 + 用例明细 | 含 TC1-TC8 逐条状态 |
| 上传报告 | `actions/upload-artifact@v4` | `convention-runtime-report` (regression-report.json) |

脚本：[ci/run-regression-headless.cjs](file:///d:/网站架构设计/ci/run-regression-headless.cjs)

- Node 内置 `http` 模块启动静态文件服务器（零外部依赖，无端口冲突）
- `puppeteer-core` 连接系统 chromium（apt 安装，不下载，省时间）
- 在页面上下文调用 `ConventionChecker.run({ runtime: true })`
- 报告含发货认领回归逐条结果（`window.__lastAgentShippingRegressionReport`）
- 退出码：0 通过 / 1 FAIL 阻断 / 2 浏览器异常

#### 7.3.5 失败阻断策略

| 场景 | 阻断 | 退出码 |
|------|------|--------|
| 静态检查 FAIL | ✅ 阻断 PR 合并 | 1 |
| 运行时回归 FAIL | ✅ 阻断 PR 合并 | 1 |
| 静态 WARN | ❌ 不阻断（仅提示） | 0 |
| `--strict-warn` 模式 WARN | ✅ 阻断 | 1 |
| 脚本/浏览器异常 | ✅ 阻断 | 2 |

GitHub 分支保护规则需将 `convention-check` 设为必须通过的状态检查。

#### 7.3.6 Job Summary 输出

两个 Job 均将检查摘要写入 `$GITHUB_STEP_SUMMARY`，在 Actions 运行页面的 Summary 标签页直接展示：

**静态检查摘要示例**：

| 指标 | 值 |
|------|----|
| 总检查项 | 32 |
| PASS | 28 |
| WARN | 4 |
| FAIL | 0 |
| 结果 | ✅ 通过 |

**运行时回归摘要示例**（含发货认领逐条明细）：

| 用例 | 状态 | 时长 |
|------|------|------|
| TC1 正常认领 | ✅ | 6ms |
| TC5 未认领区域下单 | ✅ | 11ms |
| TC8 混合区域下单 | ✅ | 28ms |
| ... | ... | ... |

汇总: 8/8 PASS (100.0%)

#### 7.3.7 本地复现

**静态检查**（零依赖，需 Node）：

```bash
node ci/run-convention-check.cjs
```

**运行时回归**（需 Node + 本机 Chrome）：

```bash
# 方式1: 设 CHROME_BIN 环境变量
set CHROME_BIN=C:\Program Files\Google\Chrome\Application\chrome.exe
npm install puppeteer-core
node ci/run-regression-headless.cjs

# 方式2: 脚本自动查找本机 Chrome 路径
npm install puppeteer-core
node ci/run-regression-headless.cjs
```

脚本跨平台支持：Linux（apt chromium）/ macOS（/Applications）/ Windows（Program Files）。

#### 7.3.8 新增用例的维护

新增测试用例**无需修改 CI 配置**：

1. 在 `agent-shipping-regression-test.js` 追加 `TCn_xxx` 函数 + cases 数组注册
2. 约定检查器已注册 `runFn: 'runAgentShippingRegression'`，自动包含新用例
3. CI 流水线路径过滤覆盖 `zhuxiang-jiu/js/**`，提交后自动触发

#### 7.3.9 状态徽章

在 README.md 顶部添加：

```markdown
![convention-check](https://github.com/<owner>/<repo>/actions/workflows/convention-check.yml/badge.svg)
```

徽章实时反映最新主干运行的约定检查状态。

---

## 附录 · 用例与检查项映射

| 用例 | 覆盖检查项 | 覆盖风险点 |
|------|------------|------------|
| TC5 | C8-runtime | 路由误判、服务费误计提 |
| TC6 | C8-runtime | 路由误判（兼容旧调用方） |
| TC7 | C8-runtime | 状态残留 |
| TC8 | C8-runtime | 路由污染 |
| TC5/TC6/TC7 | C3-shared-core-no-tx | accrueServiceFee 未被调用（厂家直供不触发共享核心） |
| TC8 | C3-shared-core-no-tx | accrueServiceFee 仅在认领区域订单触发 |
