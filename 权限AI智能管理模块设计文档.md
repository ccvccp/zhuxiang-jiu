# 竹香酒官网 · 权限AI智能管理模块设计文档（33号模块）

> 版本：v1.0
> 日期：2026-08-27
> 适用项目：竹香酒官网（zhuxiangjiu）+ 微信小程序（taro-app）
> 关联模块：后台管理（admin）、网站图标智能管理（32）、钱包盈利、会员积分、合规合法智能监控（24）
> 参考平台：钉钉审批/飞书审批（申请-审批流）、阿里云RAM/腾讯云CAM（权限策略最小化）、K8s RBAC（角色-权限解耦）、SAP GRC（权责矩阵SoD职责分离）、蚂蚁风控（行为AI监控）、企业微信管理后台（分级管理员）

## 1. 模块定位

**权限AI智能管理**：将全站权限从「单一 admin 角色」升级为**生产流程导向的分级权限体系**——超级管理员分配主要权限 → 员工按流程岗位**申请权限** → **逐级审批**（沿生产流程链）→ 授权即**绑定责任书**（权责共存）→ **AI 智能监控**权限使用（越权/异常即时拦截）→ 依据权责履行情况**自动奖惩**（绩效分/奖金/降权/冻结）→ 权限**限时到期自动回收**。

核心理念：**「超管分配 → 申请审批 → 权责绑定 → AI 监控 → 奖惩闭环 → 到期回收」**六大环节全生命周期管理，对标钉钉审批流 × 阿里云RAM × SAP GRC 权责矩阵的融合创新。

## 2. 创新与优化设计

| 创新点 | 参照平台 | 优化创新 |
|---|---|---|
| **权限树+角色模板** | K8s RBAC / 阿里云RAM | 权限点按「生产流程 7 环节 × 操作 4 级」组织成树，角色=权限集合模板，超管一键分配 |
| **流程逐级审批** | 钉钉/飞书审批流 | 权限申请单沿生产流程链自动路由：岗位 → 环节主管 → 分管总监 → 超管（按权限敏感级决定审批深度 1-3 级） |
| **权责共存责任书** | SAP GRC SoD | 每个权限绑定《责任清单》（数据保密/操作规范/损失赔付条款），授权必签电子责任书，权限与责任一一映射 |
| **权责奖惩引擎** | 绩效OKR + 芝麻信用分 | AI 依据权限使用合规度+责任履行度计算「权责信用分(0-100)」，≥90 发奖金入钱包收益，<60 自动降权/冻结 |
| **AI 智能监控** | 蚂蚁风控/云安全中心 | 行为基线学习：异常时段/异常频率/越权尝试/批量导出 → 风险评分 4 级处置（记录/告警/临时冻结+人工复核/即时吊销） |
| **限时权限沙箱** | 阿里云RAM临时凭证 | 权限带有效期（默认30天），到期自动回收；高危权限默认仅7天+到期续签需再审批 |
| **职责分离矩阵(SoD)** | SAP GRC | 互斥权限矩阵（如「收款录入」与「收款审核」不可同人持有），AI 申请预检+超管分配硬拦截 |

## 3. 核心概念模型

```
超级管理员(Super Admin, 唯一/内置不可删)
    │ 分配主要权限(角色模板 → 环节主管/总监)
    ↓
生产流程权限树(7环节 × 4操作级)
  原料采购 → 酿造生产 → 灌装仓储 → 物流配送 → 市场销售 → 售后服务 → 财务管理
  操作级: 查看(view) / 操作(operate) / 审批(approve) / 管理(manage)
    ↓
员工申请权限(申请单: 岗位+权限+理由+期限)
    ↓
逐级审批流(按敏感级: 一般1级/重要2级/核心3级)
  岗位直属主管 → 环节主管 → 分管总监 →(核心)超管
    ↓
授权生效(限时) + 签署责任书(权责共存)
    ↓
AI 智能监控(行为审计+风险评分+异常拦截)
    ↓
权责考核(信用分) → 奖惩(奖金/积分/降权/冻结/追责)
    ↓
到期自动回收 / 续签再审批
```

## 4. 数据模型

### 4.1 perm_nodes（权限树表）

| 字段 | 类型 | 说明 |
|---|---|---|
| nodeId | int PK | 权限编号 |
| code | str | 权限码（如 `production.brewing.operate`） |
| name | str | 权限名（如「酿造生产·操作」） |
| stage | str | 生产环节：purchase/production/storage/logistics/sales/aftersale/finance |
| level | str | 操作级：view/operate/approve/manage |
| sensitivity | str | 敏感级：normal(一般)/important(重要)/core(核心) |
| duties | list | 责任清单（权责共存条款） |
| conflictWith | list | 互斥权限码（SoD 矩阵） |

### 4.2 perm_roles（角色模板表）

| 字段 | 类型 | 说明 |
|---|---|---|
| roleId | int PK | 角色编号 |
| name | str | 角色名（采购员/酿造主管/物流总监…） |
| stage | str | 所属生产环节 |
| nodeCodes | list | 权限码集合 |
| createdBy | int | 创建人（仅超管可建） |

### 4.3 perm_grants（授权实例表）

| 字段 | 类型 | 说明 |
|---|---|---|
| grantId | int PK | 授权编号 |
| memberId | int | 被授权会员 |
| nodeCode | str | 权限码 |
| source | str | assign(超管直授)/apply(申请审批) |
| status | str | active(生效)/expired(到期)/revoked(吊销)/frozen(冻结) |
| expiresAt | str | 到期时间（到期自动回收） |
| dutySigned | bool | 责任书是否签署 |
| grantedBy | int | 审批人/分配人 |
| createdAt | str | 授权时间 |

### 4.4 perm_requests（权限申请单表）

| 字段 | 类型 | 说明 |
|---|---|---|
| requestId | int PK | 申请单编号 |
| applicantId | int | 申请人 |
| nodeCode | str | 申请权限码 |
| reason | str | 申请理由 |
| durationDays | int | 申请期限（1-90天） |
| status | str | pending/approved/rejected/cancelled/expired |
| approvals | list | 审批链记录（逐级：审批人/结论/意见/时间） |
| currentStep | int | 当前审批级（0=待一级） |
| aiPrecheck | dict | AI 预检结果（SoD冲突/风险提示/建议） |

### 4.5 perm_duty_scores（权责信用分表）

| 字段 | 类型 | 说明 |
|---|---|---|
| scoreId | int PK | 考核编号 |
| memberId | int | 被考核人 |
| period | str | 考核周期（月） |
| creditScore | int | 权责信用分(0-100) |
| complianceRate | int | 权限使用合规率 |
| dutyFulfilment | int | 责任履行度 |
| rewardType | str | 奖惩类型：bonus(奖金)/points(积分)/demote(降权)/freeze(冻结) |
| rewardAmount | int | 奖惩数值（分） |
| aiReport | str | AI 考核报告（事实+依据） |

### 4.6 perm_audit_logs（AI 监控审计表）

| 字段 | 类型 | 说明 |
|---|---|---|
| logId | int PK | 日志编号 |
| memberId | int | 行为人 |
| action | str | 行为（grant/revoke/use/deny/apply/approve…） |
| nodeCode | str | 相关权限 |
| riskLevel | str | low/medium/high/extreme |
| riskScore | int | AI 风险评分(0-100) |
| detail | dict | 行为明细+AI 判定依据 |
| handled | str | 处置：none/alert/freeze/revoke |
| createdAt | str | 时间 |

## 5. 六大子系统设计

### 5.1 超管权限分配（需求1）

- **超级管理员**：内置账号（ memberId=2 现有 admin 升级为 super_admin 角色），唯一拥有：权限树维护/角色模板创建/主要权限直授/审批规则配置。不可被降权/删除（系统最后防线）。
- **主要权限分配**：超管按生产环节将「环节主管级角色」直授各环节负责人（assign 来源，免除申请流，仍需签责任书+限时）。
- 权限树种子（7环节 × 4操作级 = 28 权限点起步，可由超管扩展）：

| 环节 | view | operate | approve | manage |
|---|---|---|---|---|
| 原料采购 | 采购单查看 | 采购录入 | 采购审批 | 供应商管理 |
| 酿造生产 | 批次查看 | 批次操作 | 工艺审批 | 工艺参数管理 |
| 灌装仓储 | 库存查看 | 出入库操作 | 库存盘点审批 | 仓库配置 |
| 物流配送 | 运单查看 | 发货操作 | 运费审批 | 物流商管理 |
| 市场销售 | 订单查看 | 订单操作 | 折扣审批 | 价格管理 |
| 售后服务 | 工单查看 | 工单处理 | 退款审批 | 退换政策管理 |
| 财务管理 | 流水查看 | 收付款操作 | 收款审核 | 账户管理 |

- **SoD 硬拦截**：`finance.operate(收付款操作)` 与 `finance.approve(收款审核)` 互斥；超管分配与员工申请均强制预检。

### 5.2 生产流程逐级审批（需求2）

审批深度由权限敏感级决定（借鉴钉钉审批流引擎）：

| 敏感级 | 审批链 | 示例权限 |
|---|---|---|
| normal 一般 | 直属主管 1 级 | 各环节 view/operate |
| important 重要 | 直属主管 → 环节主管 2 级 | 各环节 approve |
| core 核心 | 直属主管 → 环节主管 → 超管 3 级 | manage 级 + 财务操作 |

- **逐级流转**：currentStep 逐级推进，任一级驳回即终止（全链留痕）。
- **超时升级**：审批人 48h 未处理自动升级至上一级审批人（防流程卡死）。
- **审批人自动路由**：按被申请权限的 stage 查找该环节持有 `stage.manage` 权限的在职主管；无主管时兜底超管。
- **代理审批**：审批人可设定代理人（钉钉模式），代理审批标注「代」。

### 5.3 权责共存（需求3）

- **责任书电子签**：授权生效前置条件——申请人确认《责任清单》（duties 种子示例：数据保密/操作留痕/损失赔付/离职交接），未签署授权不生效（grant.dutySigned=false 阻断权限校验）。
- **权责映射展示**：前端权限详情页「权限 ↔ 责任」双栏对照，明示「享此权，担此责」。
- **责任履行留痕**：每次权限行使自动落审计日志，作为责任履行度考核依据。

### 5.4 权责奖惩引擎（需求4）

月度考核，AI 依据审计数据自动计分（借鉴芝麻信用分多因子模型）：

| 因子 | 权重 | 规则 |
|---|---|---|
| 使用合规率 | 40 | 无越权/无异常操作记录满分，按风险事件扣减 |
| 责任履行度 | 30 | 权责对应操作留痕完整度+按时办结率 |
| 审批尽责度 | 20 | 作为审批人：按时审批率+误批率（批后被AI判违规） |
| 主动报告加分 | 10 | 主动上报漏洞/异常经核实加分 |

奖惩规则（自动执行，重大惩戒需超管确认）：

| 信用分 | 处置 |
|---|---|
| ≥90 卓越 | 奖金 200元 入钱包收益 + 500 竹叶积分 |
| 80-89 优秀 | 奖金 100元 + 200 积分 |
| 60-79 正常 | 无 |
| 40-59 预警 | 高危权限降级（core→important）+ 警告通知 |
| <40 失信 | 全部权限冻结 + 通知超管人工追责 |

### 5.5 AI 智能监控（需求5）

行为风控引擎（规则引擎起步，B级；预留接大模型升级 A 级）：

| 监控维度 | 规则 |
|---|---|
| 越权尝试 | 无权限访问管理端点 → 403 + 记录，1小时内≥3次 → 冻结+告警 |
| 异常时段 | 02:00-06:00 管理操作 → 风险+20（夜班白名单豁免） |
| 异常频率 | 单权限 1h 操作 >50 次（基线3倍）→ 风险+30 |
| 敏感数据 | 批量导出/连续查询会员手机号 >100 条 → 风险+40 |
| SoD 规避 | 短时间先后申请互斥权限 → 申请预检直接驳回 |

风险评分 4 级处置：

| 风险级 | 分值 | 处置 |
|---|---|---|
| low | <40 | 仅记录 |
| medium | 40-69 | 记录+通知本人+直属主管 |
| high | 70-89 | 临时冻结该权限+超管复核工单 |
| extreme | ≥90 | 即时吊销全部权限+超管告警+落合规监控模块(24) |

### 5.6 申请-审批制度（需求6）

全流程（前端小程序「我的→工作台→权限中心」）：

```
员工提交申请(选权限+理由+期限≤90天)
    ↓ AI 预检(SoD冲突/信用分<40拦截/风险提示自动附言)
    ↓
逐级审批(5.2 审批链, 每级: 同意/驳回/转交)
    ↓ 通过
电子签署责任书 → 授权生效(限时) → 通知申请人
    ↓
使用期: AI 监控 + 到期前3天提醒续签
    ↓
到期自动回收(expired) / 续签=重新走审批 / 中途吊销(revoked)
```

## 6. API 设计（16 端点）

### 管理端（JWT Bearer，权限码校验）

| 方法 | 路径 | 所需权限 | 说明 |
|---|---|---|---|
| GET | /api/perm/nodes | 登录 | 权限树（全员可查，申请用） |
| GET | /api/perm/roles | 登录 | 角色模板列表 |
| POST | /api/perm/roles | super_admin | 创建角色模板 |
| POST | /api/perm/grants | super_admin | 超管直授主要权限 |
| DELETE | /api/perm/grants/{id} | super_admin | 吊销授权 |
| POST | /api/perm/requests | 登录 | 提交权限申请（AI 预检） |
| GET | /api/perm/requests | 登录 | 我的申请/待我审批（按身份） |
| POST | /api/perm/requests/{id}/approve | 对应审批级 | 同意/驳回/转交 |
| POST | /api/perm/requests/{id}/cancel | 登录 | 撤回申请 |
| POST | /api/perm/grants/{id}/duty-sign | 登录 | 签署责任书 |
| GET | /api/perm/my/grants | 登录 | 我的权限（含到期倒计时） |
| GET | /api/perm/scores | 登录 | 我的权责信用分+考核报告 |
| POST | /api/perm/admin/scores/run | super_admin | 触发月度考核 |
| GET | /api/perm/admin/logs | super_admin | AI 监控审计日志 |
| POST | /api/perm/admin/review/{logId} | super_admin | 高危冻结复核 |
| POST | /api/perm/admin/recommend | super_admin | AI 角色配置推荐（按岗位） |

权限校验中间件：`require_perm(code)` 依赖——JWT → super_admin 直通 → 查 perm_grants（active+dutySigned+未过期）→ 403 落审计。

## 7. 前端设计（小程序）

### 7.1 perm-center 页面（全员·权限中心）

- **我的权限卡**：生效权限列表（权限名/到期倒计时/责任摘要），到期前3天标红提醒
- **申请入口**：权限树按生产环节分组选择 → 理由+期限 → 提交（展示 AI 预检结果）
- **我的申请**：审批进度时间线（一级✅→二级⏳→三级○）
- **待我审批**（主管）：申请卡（申请人/权限/理由/AI 风险提示）→ 同意/驳回/转交

### 7.2 perm-admin 页面（超管）

- **权限分配**：会员搜索 → 权限树勾选 → 直授（SoD 冲突实时标红拦截）
- **审批链监控**：全部进行中申请单+超时预警
- **AI 监控大屏**：风险事件流（风险级色标）+ 冻结复核操作
- **考核中心**：月度信用分排行+奖惩执行记录+一键触发考核

### 7.3 权责对照组件

权限详情弹层：左栏「权限收益」右栏「责任条款」，签署责任书需勾选全部条款确认。

## 8. 与现有系统集成

| 集成点 | 方案 |
|---|---|
| 现有 role 字段 | 会员表 role 扩展：member/admin/super_admin；admin 视为持有全部 normal+important 权限（向下兼容存量功能），perm_grants 为增量核心管控 |
| require_admin | 保留（32号模块等继续用）；新模块管理端点用 require_perm(code)，super_admin 直通 |
| 钱包收益 | 奖惩引擎奖金发放调用 wallet_service 入账（类型：权责奖励） |
| 会员积分 | 奖励积分调用积分服务（竹叶） |
| 合规监控(24) | extreme 风险事件转发合规模块留证 |
| 审计日志 | 复用 32 号模块审计快照模式（before/after） |

## 9. 测试与验收

| 套件 | 覆盖 |
|---|---|
| test_perm_e2e.py | 权限树种子/角色/直授/申请逐级审批(1-3级)/超时升级/驳回终止/责任书阻断/到期回收/续签 |
| test_perm_sod.py | SoD 互斥（直授拦截+申请预检驳回+规避申请识别） |
| test_perm_ai.py | 风险评分4级处置/越权计数冻结/异常时段/异常频率/批量导出 |
| test_perm_reward.py | 信用分计算/奖惩执行（奖金入钱包/积分/降权/冻结）/40分以下申请拦截 |
| test_perm_auth.py | HTTP 层：伪造头401/无权限403/过期权限403/未签责任书403/审批越级403 |

验收标准：全量测试通过 + 微信开发者工具预览（超管分配→员工申请→逐级审批→责任书→行使权限→AI 监控→考核奖惩→到期回收全链路演示）。

## 10. 开发分期

| 期 | 内容 | 状态 |
|---|---|---|
| P0 | 权限树+角色+超管直授+申请审批流(1-3级)+责任书+限时回收（核心闭环） | ✅ 已完成(34/34测试) |
| P1 | AI 监控引擎（风险评分+4级处置+越权升级冻结+高危复核）+ 权责信用分奖惩（对接钱包收益/竹叶积分+降权/冻结）+ 考核中心 | ✅ 已完成(17/17测试) |
| P2 | 超时升级（48h未批自动追加上一级候选+批量扫描）/ 代理审批（委托设置+代批标注delegatedFrom+防代理环）/ AI 角色配置推荐（岗位关键词→环节匹配+职级定级→权限码组合+SoD自检） | ✅ 已完成(15/15测试) |

---

**文档结束**

> 本权限AI智能管理模块v1.0参照钉钉/飞书审批（申请-逐级审批流）、阿里云RAM/腾讯云CAM（权限策略最小化+限时凭证）、K8s RBAC（角色-权限解耦）、SAP GRC（权责矩阵+职责分离）、蚂蚁风控（行为AI监控）、芝麻信用分（多因子信用评分）等成熟平台模式融合创新：以生产流程为骨架组织权限树，以申请-审批制度实现权限精细化下发，以责任书绑定实现权责共存，以AI监控+信用分奖惩实现权责闭环，最终形成「授权有据、用权有痕、失责必究、履责有奖」的权限治理体系。
