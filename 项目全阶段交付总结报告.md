# 竹香酒全站 — 项目全阶段交付总结报告

> 报告日期：2026-08-30 ｜ 最终提交：`5095e9d` ｜ CI 状态：全绿 ｜ 开发周期：2026-08-20 ~ 2026-08-30

---

## 一、项目概览

**竹香酒（zhuxiang-jiu）** 是一个面向会员电商 + 代理商分销 + 供应链管理的全栈业务站点，本次交付以 **AI 智能知识库训练模块** 为核心增量，同步完成供应链事务、统一鉴权、生产化部署的全链路闭环。

| 维度 | 数据 |
|---|---|
| 总提交数 | 221 次（近 11 天 200 次） |
| 后端规模 | 45 个路由文件 / 58 个服务 / 47 个仓储仓库 |
| 前端规模 | 16 个 HTML 页面 / 28 个 JS 模块（零框架依赖） |
| 测试规模 | 85 个测试文件，pytest **1168 项全过** + 评分器 115 项 + 自学习 40 项 + 反馈挂钩 30 项 |
| 质量门禁 | ruff lint 全过 + 双 CI 工作流全绿 |
| 知识库端点 | 36+ 个（覆盖治理/检索/三源接入/进化） |

---

## 二、阶段交付明细

### P0~P2.5 — 知识底座与智能进化

- **P0 知识底座**：治理流水线（合规筛查/相似去重/人工审核）+ 统一检索 + 缺口队列 + FAQ 迁移，13 端点
- **P1 三源接入**：对话教学 + 文档分块 + 多模态 rule 轨 + 白名单抓取（D-14/D-15），12 端点
- **P2 智能进化**：质量淘汰调度器（命中率/新鲜度/来源可信度）、缺口自动摘要、渐进信任过审（D-16）、跨模块分发建议、6 小时后台调度器
- **P2.5 数据闭环**：chat 检索接线、missCount 埋点、KEYS→SCAN、sweep 增量化、缺口通知飞轮

### P3.x — 检索链全系列（RAG 完全体）

| 子阶段 | 核心成果 |
|---|---|
| P3.1 RAG 核心 | `rag_answer` 置信分级路由（direct≥0.55 / synthesized≥0.25 / unsolved）+ rule 轨融合生成 + 引用溯源 |
| P3.2 chat 接线 | 回复透出 ragMode / aiConfidence 动态置信 / citations 引用列表 |
| P3 检索升级 | 倒排索引突破 2000 条扫描瓶颈 |
| P3.3 LLM 轨 | `llm_client.py` 纯标准库 urllib 调用智谱 GLM，幻觉治理 prompt + 自动回退 rule |
| P3.5 Embedding | 语义向量检索，同义改写命中 conf 0.71-0.92 |
| 多模态视觉 | GLM-4V 图片描述/视频时间轴自动生成 |
| 抓取智能清洗 | LLM 语义级去噪，导航/广告噪声全去除 |
| P3.6 视频抽帧+ASR | ffmpeg 抽帧 + GLM-ASR 转写，本地化"画面/语音"时间轴 |
| P3.7 Rerank 重排 | 智谱 /rerank 精排，召回池放大后取 top_k |

### P4 — 生产化四阶段

- **P4.1 部署加固**：docker-compose LLM 全量环境变量 + `.env.example` 模板 + 启动开关状态日志
- **P4.2 监控与缓存**：纯标准库 Prometheus 指标（HTTP/LLM 五方法/RAG 缓存命中）+ `/metrics` 端点；RAG 双缓存（向量 5min TTL + 结果 60s TTL 条目变更失效）
- **P4.3 前端接线**：知识库管理后台两页（运营台+接入台）+ `js/chat-widget.js` 客服聊天窗
- **P4.4 供应链补齐**：tx_utils 事务基础设施 + 18 域双模式仓库；inventory 多行事务（流水/预警/补偿回滚）；**checkout 9 阶段事务**（发货方路由/券核销/积分双向/分润+5% 服务费，失败逆序补偿）；warehouse 10 端点全量；shipping claim 状态机

### P5 — 收尾闭环三阶段

- **P5.1 前端 live 补全**：H5 GET 丢参修复 + inventory/stock、settlement 查询端点 + agent-shipping 前端 live 分支补全（清理死代码）
- **P5.2 统一鉴权登录**：`js/auth.js` 统一 Token 管理（会员 JWT 双令牌 + 管理员两段式登录）+ login.html 双 Tab 登录页（防开放跳转）+ 五管理页接入；compat 10 项 + strict 7 项三角闭环验证
- **P5.3 Redis 生产补齐**：seed_supply_chain 19 域灌注 + Redis 集成测试 16 项 + fakeredis 双单例本地验证；CI 兜底抓出 fixture 漏灌普通域并修复闭环

---

## 三、技术架构亮点

**1. 双轨制贯穿全栈**
- LLM 轨（真实大模型）/ rule 轨（纯标准库模板）贯穿 RAG 合成、多模态理解、抓取清洗——LLM 失败自动降级，全站无硬依赖外部服务
- 五方法统一接入点 `llm_client.py`：chat/vision/embed/transcribe/rerank，一个 key 五项能力

**2. 双模式存储透明切换**
- 内存（开发/测试）/ Redis（生产）由 `STORE_MODE` 控制；`LOCK_MODE` 独立控制锁策略（asyncio/Redis 分布式锁）
- 全部 47 个仓储仓库双模式实现，seed 数据复用同一数据源保证一致性

**3. 纯标准库工程纪律**
- 监控（Counter/Histogram Prometheus 文本格式）、LLM 调用（urllib）、检索（2-gram 稀疏向量兜底）均零第三方依赖
- 前端 28 个 JS 模块零框架，单文件管理页模式

**4. 事务完整性**
- 多锁升序获取防死锁 + TxLog 阶段日志 + 失败逆序补偿全量回滚
- 并发不变量测试：不超卖 / 库存守恒 / 事务零污染 / 一区一代理

---

## 四、质量保障体系

| 层级 | 机制 |
|---|---|
| 单元/集成测试 | pytest 1168 项（内存模式）+ Redis 集成 192 项（真实 Redis，CI 兜底） |
| 专项流水线 | AI Scoring & Flash Sale 独立工作流（评分器 115 + 自学习 40 + 反馈挂钩 30 + 决策阻断 49 + 秒杀 101） |
| Lint 门禁 | ruff check 质量门禁（CI 强制） |
| CI 矩阵 | 主 CI 13 个 job（Ubuntu/Windows 双平台单测 + Docker 构建 + Redis 集成 + 三个并发探针 + -Force 参数测试） |
| 浏览器端到端 | 源码注入真实执行三 JS 模块 live 模式实测（服务费 55.9=1118×5% 精确匹配） |
| 教训沉淀 | 更新日志.md 持续记录根因分析与经验（如本次 CI 抓出 fixture 漏灌的闭环） |

---

## 五、生产部署形态

- **Docker Compose**：backend（1G 内存限制 + healthcheck + uvicorn 单 worker）+ Redis + Prometheus 抓取就绪
- **环境变量体系**：`AUTH_MODE`（compat/strict）、`STORE_MODE`、`LOCK_MODE`、`LLM_*` 五轨开关、`KNOWLEDGE_*` 各功能开关——全部带默认值兜底，`.env` 注入密钥不落盘
- **监控就绪**：`GET /metrics` 可被既有 Prometheus 栈直接抓取

---

## 六、遗留事项与建议

| 事项 | 说明 | 建议 |
|---|---|---|
| SSH 推送不可用 | 本地 SSH key 未注册 GitHub，origin 已固定 HTTPS + PAT | 如需 SSH，重新注册公钥 |
| strict 模式默认关闭 | 生产建议显式设 `AUTH_MODE=strict` 封死旧兼容头 | 上线前切换并回归 7 项 strict 测试 |
| LLM 轨开关默认 off | 生产需配 `LLM_API_KEY` + `LLM_ENABLED=true` 才启用真实合成 | 按 .env.example 模板配置 |
| Docker 本地未验证 | 本地 Docker Desktop 未运行，容器联调由 CI docker job 兜底 | 上线前本地 `docker compose up` 冒烟一次 |

---

**结论**：P0~P5.3 全部 20 个阶段交付完成，最终推送 CI 双工作流全绿，工作区干净、本地与远程同步。项目具备生产部署条件。
