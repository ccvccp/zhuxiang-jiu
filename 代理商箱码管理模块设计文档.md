# 竹香酒官网 · 代理商箱码管理模块设计文档（已合并）

> 版本：v2.0（已归档）  
> 日期：2026-08-16  
> 适用项目：竹香酒官网（zhuxiangjiu）  
> **合并状态：本文档已合并至「双码追溯管理模块设计文档（22号模块）v3.0」**  
> **合并原因：箱码与生命码本就是一套完整追溯体系，合并为双码追溯管理模块（22号模块）实现一体化管理**  
> **最新文档：请参阅 [双码追溯管理模块设计文档](竹奕酒生命码管理模块设计文档.md)**  
> 关联模块：代理商管理模块、双码追溯管理模块(22)、防伪验证、后台管理模块  
> 参考平台：茅台箱码防窜/京东品质溯源/阿里码上溯源/蚂蚁链溯源/阿里风控/高德地图  
> 核心理念：**双码全链路追溯 + AI智能码生成 + AI智能防窜 + AI智能回收判定 + AI智能库存 + 区块链存证**

---

## 一、模块概述

### 1.1 什么是箱码

竹奕酒代理商箱码采用**双码体系**：箱顶码（TBC）+箱底码（BBC），是每一箱竹奕酒的**代理商专属防伪防窜码**，绑定代理商身份，记录箱体从出厂→入库→出库→开箱的全过程。箱顶码**开箱即失效**，失效后箱内产品**不参与3年增值回收**；箱底码**永久有效**，用于库存管理和防窜追踪。

### 1.2 核心规则（AI智能驱动）

| 规则 | 说明 |
|------|------|
| **一箱双码** | 每箱竹奕酒拥有箱顶码+箱底码（AI智能生成） |
| **代理商专属** | 双码绑定代理商身份，出厂即指定（AI智能绑定） |
| **箱顶码开箱失效** | 打开箱顶封口码即失效，不可恢复（AI智能检测） |
| **箱底码永久有效** | 箱底码不随开箱失效，用于库存管理和防窜追踪 |
| **双码完好为回收条件** | **箱顶码+箱底码双码完好是老酒回收的必要条件**（AI智能判定） |
| **不参与增值回收** | 任一码失效（箱顶码开箱/箱底码损坏），不参与3年增值回收（AI智能识别） |
| **防窜货追踪** | 双码记录流转轨迹，检测跨区销售（AI智能防窜） |
| **AI驱动** | 码生成/防窜/回收判定/库存85%自动化 |
| **区块链存证** | 双码全生命周期上链存证，不可篡改 |
| **智能预警** | AI异常流转检测+窜货预警+库存预警 |

### 1.3 AI箱码管理体系架构（升级版）

```
┌─────────────────────────────────────────────────────────────────┐
│              竹奕酒AI智能箱码管理平台                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─ AI智能核心层（85%自动化）──────────────────────────┐      │
│  │                                                       │      │
│  │  ┌─ AI智能码生成 ─┐ ┌─ AI智能开箱 ─┐ ┌─ AI智能防窜 ─┐ │      │
│  │  │ 防伪算法      │ │ 开箱AI识别    │ │ 位置AI分析   │ │      │
│  │  │ 批量生成      │ │ 状态智能变更  │ │ 异常流转检测 │ │      │
│  │  │ 唯一性校验    │ │ 位置智能记录  │ │ 窜货AI预警   │ │      │
│  │  └──────────────┘ └──────────────┘ └──────────────┘ │      │
│  │  ┌─ AI智能库存 ─┐ ┌─ AI智能回收 ─┐ ┌─ AI智能追溯 ─┐ │      │
│  │  │ 智能盘点    │ │ 双码智能判定  │ │ 全链路追溯   │ │      │
│  │  │ 库存预测    │ │ 回收时机预测  │ │ 区块链存证   │ │      │
│  │  │ 补货建议    │ │ 风险评估      │ │ 异常检测     │ │      │
│  │  └──────────────┘ └──────────────┘ └──────────────┘ │      │
│  └───────────────────────────────────────────────────────┘      │
│                                                                 │
│  ┌─ 人工辅助层（15%介入）──────────────────────────────┐      │
│  │  · 重大窜货处罚决策  · 重大回收审核                  │      │
│  │  · 异常流转核实      · 码制战略调整                  │      │
│  └───────────────────────────────────────────────────────┘      │
│                                                                 │
│  ┌─ 执行层 ────────────────────────────────────────────┐      │
│  │  码生成/库存/开箱/防窜/回收/追溯/区块链             │      │
│  └───────────────────────────────────────────────────────┘      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.4 六大AI核心能力

```
┌─ AI智能码生成 ──────────────────────────────────────────────┐
│  · 防伪算法生成（CRC4+加密+唯一性校验）                      │
│  · 批量智能生成（万箱秒级生成+不重复）                       │
│  · 代理智能绑定（出厂赋码自动绑定代理商+区域）              │
│  · 瓶码智能关联（箱码-生命码自动绑定6瓶）                    │
└─────────────────────────────────────────────────────────────┘
┌─ AI智能开箱检测 ────────────────────────────────────────────┐
│  · 开箱AI识别（扫码+CV+物理传感器综合判断）                  │
│  · 状态智能变更（箱顶码失效自动触发+不可逆）                 │
│  · 位置智能记录（GPS+LBS+高德地图智能定位）                  │
│  · 开箱异常检测（异常时间/批量/异地开箱预警）                │
└─────────────────────────────────────────────────────────────┘
┌─ AI智能防窜 ────────────────────────────────────────────────┐
│  · 开箱位置AI分析（GPS+授权区域智能比对）                   │
│  · 生命码激活位置AI分析（激活地+代理区域智能比对）           │
│  · 异常流转AI检测（批量跨区+异常扫码+疑似窜货）             │
│  · 窜货AI预警（实时预警+分级处罚+自动取证+区块链存证）      │
└─────────────────────────────────────────────────────────────┘
┌─ AI智能库存 ────────────────────────────────────────────────┐
│  · 智能盘点（库存自动盘点+差异智能识别）                     │
│  · 库存AI预测（基于销售+季节+趋势的库存需求预测）            │
│  · 补货AI建议（补货时机+补货量+批次建议）                    │
│  · 库存预警（不足/积压/临期AI预警）                          │
└─────────────────────────────────────────────────────────────┘
┌─ AI智能回收判定 ────────────────────────────────────────────┐
│  · 双码完好AI智能判定（箱顶码+箱底码状态自动核验）          │
│  · 回收时机AI预测（满3年库存提前预警+回收建议）             │
│  · 回收风险AI评估（虚报库存/重复回收/异常回收检测）         │
│  · 回收区块链存证（双码状态+回收记录上链）                   │
└─────────────────────────────────────────────────────────────┘
┌─ AI智能追溯 ────────────────────────────────────────────────┐
│  · 全链路AI追溯（出厂→入库→出库→开箱→激活→回收）          │
│  · 区块链AI存证（双码全生命周期上链+不可篡改）              │
│  · 异常AI检测（异常流转+异常扫码+异常激活）                 │
│  · 追溯AI报告（一键生成追溯报告+证据链）                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、箱码编码规则

### 2.0 AI智能码生成引擎（新增）

> **核心理念：防伪算法智能生成+批量生成+唯一性校验+智能绑定**
> 借鉴茅台箱码防伪/阿里码上溯源/蚂蚁链溯源，基于加密算法+AI模型，实现箱码AI智能生成。

```
┌─ AI码生成层 ───────────────────────────────────────────┐
│  · 防伪算法生成（CRC4+AES加密+随机数+唯一性校验）    │
│  · 批量智能生成（万箱秒级生成+并行处理+不重复）      │
│  · 码结构智能组装（类别+代理+产品+批次+箱序+校验）   │
│  · 双码智能配对（箱顶码+箱底码一一绑定）              │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI智能绑定层 ───────────────────────────────────────┐
│  · 代理智能绑定（出厂赋码自动绑定代理商+区域）        │
│  · 瓶码智能关联（箱码自动绑定6个瓶级生命码）           │
│  · 批次智能管理（生产批次+出厂日期+有效期管理）       │
│  · 防伪智能关联（箱码-防伪码-生命码三方绑定）         │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI唯一性校验层 ─────────────────────────────────────┐
│  · 全局唯一性校验（已生成码库比对+防重复）            │
│  · 校验码AI验证（CRC4校验+防伪验证）                 │
│  · 码状态AI初始化（未开箱+未激活+出厂状态）           │
│  · 区块链AI存证（码生成记录上链+不可篡改）            │
└─────────────────────────────────────────────────────┘
```

**AI自动化率：95%**（仅码制战略调整人工）

### 2.1 码结构设计

```
箱码编码格式（24位）:

ABC-AGT-PRD-LOT-BOX-CRC

示例: ABC-JN01-ZX42-2026L07-0150-A3F2

┌─ ABC ──────┐ 类别标识（Agent Box Code）
├─ JN01 ─────┤ 代理商标识（JN01=济南代理01）
├─ ZX42 ─────┤ 产品标识（ZX=竹香型, 42=42度）
├─ 2026L07 ──┤ 批次标识（2026年第07批次）
├─ 0150 ─────┤ 箱序号（该代理该批次第150箱）
└─ A3F2 ─────┘ 校验码（CRC4防伪校验）
```

### 2.2 代理商标识表

| 代理商标识 | 区域 | 代理商名称 |
|------|------|------|
| JN01 | 济南 | 济南总代理01 |
| QD01 | 青岛 | 青岛总代理01 |
| TA01 | 泰安 | 泰安总代理01 |
| ZZ01 | 郑州 | 郑州总代理01 |
| ... | ... | ... |

### 2.3 双码物理载体

> **核心设计：每箱双码 = 箱底码 + 箱顶码**

```
┌──────────────────────────────────────────────────────────────┐
│                    竹奕酒包装箱双码体系                      │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─ 箱顶码（Top Code）────────────────────────────┐        │
│  │  位置: 箱体顶部封口处                          │        │
│  │  作用: 开箱防拆 + 开箱失效 + 回收判定           │        │
│  │  特点: 撕开箱口即破坏，一次性防拆              │        │
│  │  状态: 未开箱→有效 / 开箱→失效（不可逆）      │        │
│  │  用途: 判定开箱状态，失效后不参与3年增值回收   │        │
│  └─────────────────────────────────────────────────┘        │
│                                                              │
│  ┌──────────────────────────────────────┐                   │
│  │                                      │                   │
│  │        箱体（6瓶装）                 │                   │
│  │                                      │                   │
│  │   瓶1  瓶2  瓶3  瓶4  瓶5  瓶6    │                   │
│  │                                      │                   │
│  └──────────────────────────────────────┘                   │
│                                                              │
│  ┌─ 箱底码（Bottom Code）─────────────────────────┐        │
│  │  位置: 箱体底部                                │        │
│  │  作用: 代理身份绑定 + 库存管理 + 防窜追踪       │        │
│  │  特点: 开箱不失效，永久有效（至回收终止）      │        │
│  │  状态: 出厂赋码→入库→出库→回收终止             │        │
│  │  用途: 代理商库存盘点、区域追溯、回收凭证       │        │
│  └─────────────────────────────────────────────────┘        │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 2.4 双码编码规则

```
箱顶码（Top Code）:
  TBC-JN01-ZX42-2026L07-0150T-A3F2
  └ T = Top（顶部）

箱底码（Bottom Code）:
  BBC-JN01-ZX42-2026L07-0150B-A3F2
  └ B = Bottom（底部）

关联: 同一箱的顶码与底码一一绑定
```

| 对比 | 箱顶码（TBC） | 箱底码（BBC） |
|------|------|------|
| **位置** | 箱顶封口处 | 箱体底部 |
| **作用** | 开箱防拆+回收判定 | 代理绑定+库存管理 |
| **开箱** | ✅ 开箱即失效 | ❌ 开箱不失效 |
| **有效期** | 开箱即终止 | 永久有效至回收 |
| **用途** | 判定不参与回收 | 库存盘点+防窜追踪 |
| **扫码** | 开箱时扫 | 入库/出库/盘点扫 |

### 2.5 双码与瓶码关系

```
一箱 = 6瓶（标准箱）

箱顶码: TBC-JN01-ZX42-2026L07-0150T-A3F2（开箱失效）
箱底码: BBC-JN01-ZX42-2026L07-0150B-A3F2（永久有效）
    │
    ├─ 瓶1生命码: BLC-ZX42-2026L07-015001-A3F2
    ├─ 瓶2生命码: BLC-ZX42-2026L07-015002-B4G3
    ├─ 瓶3生命码: BLC-ZX42-2026L07-015003-C5H4
    ├─ 瓶4生命码: BLC-ZX42-2026L07-015004-D6I5
    ├─ 瓶5生命码: BLC-ZX42-2026L07-015005-E7J6
    └─ 瓶6生命码: BLC-ZX42-2026L07-015006-F8K7

双码绑定6个瓶级生命码
开箱后箱顶码失效，箱底码仍有效
箱内瓶级生命码可逐瓶激活
```

---

## 三、开箱失效机制（AI智能开箱检测·双码）

### 3.0 AI智能开箱检测引擎（新增）

> **核心理念：开箱AI识别+状态智能变更+位置智能记录+异常检测**
> 借鉴茅台开箱检测/京东品质溯源，基于扫码+CV+传感器+AI模型，实现开箱全流程AI智能检测。

```
┌─ AI开箱识别层 ───────────────────────────────────────┐
│  · 扫码识别（箱顶码扫码+自动识别开箱动作）           │
│  · CV视觉识别（摄像头识别封口破坏）                  │
│  · 物理传感器（封口传感器触发开箱事件）              │
│  · 综合AI判断（扫码+CV+传感器多模态综合判断）        │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI状态变更层 ───────────────────────────────────────┐
│  · 箱顶码状态自动变更（有效→失效·不可逆）            │
│  · 箱底码状态保持（永久有效）                        │
│  · 瓶级生命码状态更新（待激活）                      │
│  · 状态变更区块链存证（不可篡改）                    │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI位置记录层 ───────────────────────────────────────┐
│  · GPS定位（开箱GPS位置自动记录）                    │
│  · LBS定位（基站辅助定位）                           │
│  · 高德地图智能解析（省/市/区智能解析）              │
│  · 位置与授权区域AI比对（窜货智能检测）              │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI异常检测层 ───────────────────────────────────────┐
│  · 异常时间检测（夜间/凌晨开箱预警）                 │
│  · 批量开箱检测（短时大量开箱预警）                  │
│  · 异地开箱检测（非授权区域开箱预警）                │
│  · 异常操作人检测（非授权人员开箱预警）              │
│  · 异常AI预警（自动标记+人工复核触发）               │
└─────────────────────────────────────────────────────┘
```

**AI自动化率：90%**（仅异常开箱/重大争议人工复核）

### 3.1 开箱流程

```
代理商收到货物
    ↓
扫描箱底码 → 入库登记（箱底码永久有效）
    ↓
库存管理（箱顶码未开箱·有效）
    ↓
需要销售/开箱
    ↓
撕开箱顶封口码（箱顶码）
    ↓
┌─────────────────────────────────────────────────┐
│  ⚠️ 箱顶码已失效                                 │
│                                                 │
│  箱顶码: TBC-JN01-ZX42-2026L07-0150T-A3F2      │
│  状态:   已开箱（箱顶码失效·不可逆）            │
│  箱底码: BBC-JN01-ZX42-2026L07-0150B-A3F2      │
│  状态:   有效（永久有效·库存管理用）            │
│  开箱时间: 2026-07-17 10:30                     │
│  开箱位置: 山东省济南市历下区                     │
│  操作人: 代理商标识JN01                         │
│                                                 │
│  ⚠️ 重要提示:                                    │
│  · 箱顶码失效后，箱体不参与3年增值回收           │
│  · 箱底码仍有效，继续用于库存管理和追溯         │
│  · 箱内瓶级生命码可逐瓶激活                     │
│                                                 │
│  [查看箱内生命码] [批量激活瓶码]                 │
│                                                 │
└─────────────────────────────────────────────────┘
    ↓
箱内瓶级生命码可逐瓶激活
（生命码激活后参与增值回收，不受箱顶码失效影响）
```

### 3.2 双码失效规则

| 规则 | 箱顶码（TBC） | 箱底码（BBC） |
|------|:---:|:---:|
| **开箱** | ✅ 开箱即失效 | ❌ 开箱不失效 |
| **物理防拆** | ✅ 封口破坏 | ❌ 底部不受影响 |
| **数字失效** | ✅ 状态不可逆 | ❌ 永久有效 |
| **有效期** | 开箱即终止 | 至回收终止 |
| **回收判定** | ✅ 必须完好（条件1） | ✅ 必须完好（条件2） |
| **库存管理** | ❌ | ✅ 入库/出库/盘点 |
| **防窜追踪** | ⚠️ 开箱时定位 | ✅ 全程追踪 |

### 3.3 回收判定（双码完好为必要条件）

> **核心规则：箱顶码+箱底码双码完好 = 老酒回收的必要条件**
>
> 老酒回收必须满足双码完好：箱顶码未开箱（有效）+箱底码完好（有效）。任一码失效（箱顶码开箱/箱底码损坏），箱体不参与3年增值回收。

```
═════════════════════════════════════════════════════════
  双码完好 = 老酒回收必要条件
═════════════════════════════════════════════════════════

回收条件判定矩阵:

┌──────────┬──────────┬──────────────────────┐
│ 箱顶码   │ 箱底码   │ 回收资格             │
├──────────┼──────────┼──────────────────────┤
│ ✅ 完好  │ ✅ 完好  │ ✅ 可参与3年增值回收  │
│ ❌ 失效  │ ✅ 完好  │ ❌ 不参与回收         │
│ ✅ 完好  │ ❌ 损坏  │ ❌ 不参与回收         │
│ ❌ 失效  │ ❌ 损坏  │ ❌ 不参与回收         │
└──────────┴──────────┴──────────────────────┘

箱顶码状态:
  ├─ 未开箱（完好）→ ✅ 满足条件1
  ├─ 已开箱（失效）→ ❌ 不满足条件1
  └─ 箱顶码失效即判定：不参与回收

箱底码状态:
  ├─ 完好（有效）→ ✅ 满足条件2
  ├─ 损坏（失效）→ ❌ 不满足条件2
  └─ 箱底码损坏即判定：不参与回收

瓶级生命码状态（独立计算，不受箱码影响）:
  ├─ 未激活 → ❌ 不参与回收
  ├─ 已激活 → ✅ 参与回收（基于第一次激活日期）
  └─ 已回收 → 已终止

═════════════════════════════════════════════════════════
  核心规则:
  
  双码完好 = 老酒回收必要条件
  
  · 箱顶码+箱底码双码完好，箱体方可参与3年增值回收
  · 任一码失效（箱顶码开箱/箱底码损坏），不参与回收
  · 箱内瓶级生命码单独激活后，仍可按瓶级独立参与回收
  
  即:
  · 箱级层面：双码完好→可参与回收（防代理囤积套利）
  · 瓶级层面：生命码激活→可参与回收（保障终端权益）
═════════════════════════════════════════════════════════
```

> 💡 **设计说明**：箱码失效后不参与3年增值回收，是指**箱码作为整体**不参与箱级回收。但箱内每瓶酒的生命码是独立的，用户扫码激活生命码后，该瓶酒仍可按生命码规则参与增值回收。这样可以防止代理商囤积整箱未开箱酒进行箱级套利，同时保障终端用户购买后扫码激活的正常增值权益。

---

## 四、代理商库存管理（AI智能库存）

### 4.0 AI智能库存管理引擎（新增）

> **核心理念：智能盘点+库存AI预测+补货AI建议+库存AI预警**
> 借鉴京东库存管理/SAP库存优化/用友AI云，基于数据中台+AI模型，实现库存全流程AI智能化。

```
┌─ AI智能盘点层 ───────────────────────────────────────┐
│  · 库存自动盘点（箱底码扫码+系统自动盘点）           │
│  · 库存差异AI识别（系统vs实物差异智能识别）          │
│  · 库存状态AI更新（未开箱/已开箱/待激活/已激活）      │
│  · 批次库存AI管理（按批次+日期智能管理）             │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI库存预测层 ───────────────────────────────────────┐
│  · 销售AI预测（基于历史+季节+趋势的销量预测）        │
│  · 库存需求AI预测（未来30/60/90天库存需求预测）       │
│  · 安全库存AI计算（基于销量+补货周期智能计算）       │
│  · 库存周转AI分析（周转率+积压风险分析）             │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI补货建议层 ───────────────────────────────────────┐
│  · 补货时机AI建议（最佳补货时机推荐）                │
│  · 补货量AI建议（最优补货量计算）                    │
│  · 补货批次AI建议（生产批次+到货时间建议）           │
│  · 补货ROI分析（补货成本vs利润分析）                 │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI库存预警层 ───────────────────────────────────────┐
│  · 库存不足AI预警（低于安全库存自动预警）            │
│  · 库存积压AI预警（库存积压+资金占用预警）           │
│  · 库存临期AI预警（满3年回收期临近预警）             │
│  · 异常库存AI预警（异常出入库+异常损耗预警）         │
└─────────────────────────────────────────────────────┘
```

**AI自动化率：85%**（仅重大库存调整/异常库存人工核实）

### 4.1 库存状态

```
代理商库存看板:

┌──────────────────────────────────────────────────────┐
│              代理商标识: JN01 济南总代理01            │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌─ 库存总览 ─────────────────────────────────┐      │
│  │  未开箱: 200箱（1200瓶）                  │      │
│  │  已开箱: 50箱（300瓶，箱码已失效）        │      │
│  │  待激活: 300瓶（已开箱未激活生命码）      │      │
│  │  已激活: 120瓶（终端用户已激活生命码）    │      │
│  └───────────────────────────────────────────┘      │
│                                                      │
│  ┌─ 批次库存 ───────────────────────────────┐      │
│  │  2026L07批次: 150箱（未开箱）             │      │
│  │  2026L06批次: 80箱（未开箱）              │      │
│  │  2026L05批次: 20箱（已开箱）              │      │
│  └───────────────────────────────────────────┘      │
│                                                      │
│  ┌─ 防窜检测 ───────────────────────────────┐      │
│  │  本区域开箱: 48箱 ✅                     │      │
│  │  跨区开箱: 2箱 ⚠️（需核实）               │      │
│  └───────────────────────────────────────────┘      │
│                                                      │
│  [箱底码入库] [出库登记] [箱顶码开箱] [库存盘点]    │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### 4.2 入库管理

```
厂家发货 → 代理商收货
    ↓
扫描箱底码
    ↓
┌─────────────────────────────────────────┐
│  入库登记                                │
│                                         │
│  箱底码: BBC-JN01-ZX42-2026L07-0150B-A3F2│
│  箱顶码: TBC-JN01-ZX42-2026L07-0150T-A3F2│
│  产品: 竹奕·竹香型 42° 500ml × 6瓶     │
│  批次: 2026年第07批                     │
│  出厂日期: 2026-06-15                   │
│  代理商: JN01 济南总代理01              │
│  入库时间: 2026-07-17 09:00            │
│  入库位置: 山东省济南市                  │
│  箱顶码: 未开箱（有效）                 │
│  箱底码: 有效（永久）                   │
│                                         │
│  [确认入库]                             │
│                                         │
└─────────────────────────────────────────┘
    ↓
库存数量+1，状态：未开箱
```

### 4.3 开箱管理（箱顶码）

```
代理商需要销售 → 开箱
    ↓
撕开箱顶封口码（箱顶码物理破坏）
    ↓
系统自动变更箱顶码状态为失效
箱底码保持有效
    ↓
┌─────────────────────────────────────────────────┐
│  ⚠️ 开箱成功 · 箱顶码已失效                     │
│                                                 │
│  箱顶码: TBC-JN01-ZX42-2026L07-0150T-A3F2      │
│  状态:   已开箱（箱顶码失效·不可逆）            │
│  箱底码: BBC-JN01-ZX42-2026L07-0150B-A3F2      │
│  状态:   有效（永久·库存管理用）                │
│  开箱时间: 2026-07-17 10:30                     │
│  开箱位置: 山东省济南市历下区                     │
│                                                 │
│  箱内6瓶生命码:                                 │
│  · BLC-ZX42-2026L07-015001（待激活）           │
│  · BLC-ZX42-2026L07-015002（待激活）           │
│  · BLC-ZX42-2026L07-015003（待激活）           │
│  · BLC-ZX42-2026L07-015004（待激活）           │
│  · BLC-ZX42-2026L07-015005（待激活）           │
│  · BLC-ZX42-2026L07-015006（待激活）           │
│                                                 │
│  [批量激活瓶码] [逐瓶激活]                      │
│                                                 │
│  ⚠️ 提示:                                       │
│  · 箱顶码失效→箱体不参与3年增值回收             │
│  · 箱底码有效→继续用于库存管理和防窜追踪       │
│  · 瓶码激活后可单独参与回收                    │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 五、防窜货管理（AI智能防窜引擎）

### 5.0 AI智能防窜引擎（升级版）

> **核心理念：位置AI分析+异常流转AI检测+窜货AI预警+自动取证+区块链存证**
> 借鉴茅台箱码防窜/阿里风控/京东品质溯源，基于GPS+CV+AI模型，实现防窜全流程AI智能化。

```
┌─ AI位置分析层 ───────────────────────────────────────┐
│  · 开箱位置AI分析（开箱GPS+授权区域智能比对）        │
│  · 生命码激活位置AI分析（激活地+代理区域智能比对）   │
│  · 销售地AI分析（购买地址+收货地址智能比对）         │
│  · 边界区域AI识别（区域边界异常销售智能识别）        │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI异常流转检测层 ───────────────────────────────────┐
│  · 批量跨区AI检测（短时大量异地销售检测）            │
│  · 异常扫码AI检测（异常时间+异常地点扫码检测）       │
│  · 疑似窜货AI检测（开箱地≠授权地+激活地≠授权地）   │
│  · 异常物流AI检测（异地发货+异常物流路线检测）       │
│  · 关联异常AI检测（关联代理商异常流转检测）          │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI窜货预警层 ───────────────────────────────────────┐
│  · 窜货AI实时预警（异常流转实时预警+秒级响应）       │
│  · 窜货AI分级（轻微/一般/严重/极重自动分级）         │
│  · 窜货AI取证（位置+时间+批次+证据链自动生成）      │
│  · 窜货AI区块链存证（证据链上链+不可篡改）           │
│  · 跨区举报AI受理（举报信息NLP分析+智能核实）        │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI处罚建议层 ───────────────────────────────────────┐
│  · 处罚AI建议（警告/罚款/取消资格自动建议）          │
│  · 信用档案AI更新（违规记录+信用分调整）             │
│  · 保证金AI扣罚（按违规级别自动扣罚建议）            │
│  · 返利AI扣减（按违规级别返利扣减建议）              │
└─────────────────────────────────────────────────────┘
```

**AI自动化率：90%**（仅重大窜货处罚人工决策）

### 5.1 区域绑定

```
箱码区域绑定:

出厂赋码时:
  箱码 → 代理商 → 授权区域

示例:
  箱码 ABC-JN01-... → 代理商JN01 → 济南市
  箱码 ABC-QD01-... → 代理商QD01 → 青岛市

开箱检测:
  开箱位置 vs 授权区域
  ├─ 一致 → ✅ 正常
  └─ 不一致 → ⚠️ 窜货嫌疑
```

### 5.2 跨区检测

```
AI防窜检测引擎:

数据输入:
  · 箱码代理区域
  · 开箱GPS位置
  · 开箱操作人

AI分析:
  ├─ 正常: 开箱位置=授权区域
  ├─ 跨区: 开箱位置≠授权区域
  ├─ 边界: 开箱位置在区域边界
  └─ 异常: 批量跨区开箱

输出:
  · 风险评分
  · 预警通知
  · 人工复核标记
```

### 5.3 窜货处罚

| 违规级别 | 情况 | 处罚 |
|:---:|------|------|
| 轻微 | 跨区开箱1-2箱 | 警告+限期整改 |
| 一般 | 跨区开箱3-10箱 | 扣返利10%+保证金20% |
| 严重 | 跨区开箱>10箱 | 扣返利30%+保证金50% |
| 极重 | 恶意窜货 | 取消代理资格+扣除全部保证金 |

---

## 六、数据库设计

```sql
-- 箱码主表（双码）
box_codes (
    id              BIGINT PRIMARY KEY,
    top_code        VARCHAR(32) UNIQUE,           -- 箱顶码（开箱失效）
    bottom_code     VARCHAR(32) UNIQUE,           -- 箱底码（永久有效）
    agent_id        BIGINT,                       -- 代理商ID
    agent_code      VARCHAR(10),                  -- 代理商标识（JN01）
    product_id      BIGINT,                       -- 产品ID
    product_name    VARCHAR(200),
    product_abv    INT,                           -- 度数
    batch_no        VARCHAR(20),                  -- 批次号
    manufacture_date DATE,                        -- 出厂日期
    bottles_per_box  INT DEFAULT 6,               -- 每箱瓶数
    top_status      VARCHAR(20) DEFAULT 'valid',  -- 箱顶码状态: valid(有效)/invalid(失效)
    bottom_status   VARCHAR(20) DEFAULT 'valid',  -- 箱底码状态: valid(有效)/terminated(回收终止)
    is_anti_fake_valid BOOLEAN DEFAULT TRUE,      -- 箱顶防伪是否有效
    opened_at       DATETIME,                     -- 开箱时间（箱顶码失效时间）
    opened_location VARCHAR(200),                 -- 开箱位置
    opened_longitude DECIMAL(10,6),              -- 开箱经度
    opened_latitude  DECIMAL(10,6),               -- 开箱纬度
    crc_code        VARCHAR(8),                   -- 校验码
    created_at      DATETIME DEFAULT NOW(),
    INDEX idx_top (top_code),
    INDEX idx_bottom (bottom_code),
    INDEX idx_agent (agent_id),
    INDEX idx_top_status (top_status)
)

-- 箱码-瓶码关联表
box_bottle_relations (
    id              BIGINT PRIMARY KEY,
    box_id          BIGINT,                       -- 箱码ID（关联box_codes）
    top_code        VARCHAR(32),                  -- 箱顶码
    bottom_code     VARCHAR(32),                  -- 箱底码
    life_code       VARCHAR(32),                  -- 瓶级生命码
    bottle_seq      INT,                          -- 瓶序号(1-6)
    bottle_status   VARCHAR(20),                  -- 待激活/已激活/已回收
    created_at      DATETIME DEFAULT NOW(),
    INDEX idx_box (box_id),
    INDEX idx_top (top_code),
    INDEX idx_bottom (bottom_code),
    INDEX idx_life (life_code)
)

-- 代理商入库记录表
agent_inbound_logs (
    id              BIGINT PRIMARY KEY,
    box_code        VARCHAR(32),
    agent_id        BIGINT,
    inbound_date    DATETIME,                     -- 入库时间
    inbound_location VARCHAR(200),                -- 入库位置
    batch_no        VARCHAR(20),
    created_at      DATETIME DEFAULT NOW(),
    INDEX idx_box (box_code),
    INDEX idx_agent (agent_id)
)

-- 开箱记录表
box_opened_logs (
    id              BIGINT PRIMARY KEY,
    box_code        VARCHAR(32),
    agent_id        BIGINT,
    opened_date     DATETIME,                     -- 开箱时间
    province        VARCHAR(50),                  -- 开箱省
    city            VARCHAR(50),                  -- 开箱市
    longitude       DECIMAL(10,6),
    latitude        DECIMAL(10,6),
    is_cross_region BOOLEAN DEFAULT FALSE,        -- 是否跨区
    created_at      DATETIME DEFAULT NOW(),
    INDEX idx_box (box_code),
    INDEX idx_agent (agent_id)
)

-- 防窜预警表
cross_region_warnings (
    id              BIGINT PRIMARY KEY,
    box_code        VARCHAR(32),
    agent_id        BIGINT,
    authorized_region VARCHAR(100),               -- 授权区域
    actual_region   VARCHAR(100),                 -- 实际开箱区域
    warning_level   VARCHAR(20),                  -- 轻微/一般/严重/极重
    status          VARCHAR(20),                  -- 待处理/已处理
    handled_by      BIGINT,                       -- 处理人
    handled_at      DATETIME,
    penalty         VARCHAR(500),                -- 处罚措施
    created_at      DATETIME DEFAULT NOW(),
    INDEX idx_box (box_code),
    INDEX idx_agent (agent_id)
)

-- ========== AI智能相关表（v2.0新增） ==========

-- AI码生成记录表
box_ai_generations (
    id              BIGINT PRIMARY KEY,
    batch_no        VARCHAR(20),                  -- 批次号
    agent_id        BIGINT,                       -- 代理商ID
    generation_count INT,                         -- 生成数量
    algorithm_version VARCHAR(20),                -- 防伪算法版本
    encryption_method VARCHAR(20),                -- 加密方式（AES）
    uniqueness_check BOOLEAN DEFAULT TRUE,        -- 唯一性校验
    blockchain_hash VARCHAR(128),                 -- 区块链存证哈希
    ai_model_version VARCHAR(20),                 -- AI模型版本
    ai_automation_rate DECIMAL(5,2) DEFAULT 95.00, -- AI自动化率
    generated_at    DATETIME DEFAULT NOW(),
    INDEX idx_batch (batch_no),
    INDEX idx_agent (agent_id)
)

-- AI开箱检测记录表
box_ai_unboxing (
    id              BIGINT PRIMARY KEY,
    box_code        VARCHAR(32),
    top_code        VARCHAR(32),
    agent_id        BIGINT,
    scan_recognized BOOLEAN,                      -- 扫码识别
    cv_recognized   BOOLEAN,                      -- CV视觉识别
    sensor_triggered BOOLEAN,                     -- 物理传感器触发
    multi_modal_score DECIMAL(5,2),                -- 多模态综合得分
    gps_location    VARCHAR(200),                  -- GPS定位
    lbs_location    VARCHAR(200),                  -- LBS定位
    map_parsed      VARCHAR(200),                 -- 高德地图解析地址
    is_anomaly      BOOLEAN DEFAULT FALSE,         -- 是否异常开箱
    anomaly_type    VARCHAR(50),                  -- 异常类型（夜间/批量/异地/非授权人）
    blockchain_hash VARCHAR(128),                  -- 区块链存证哈希
    ai_automation_rate DECIMAL(5,2) DEFAULT 90.00,
    detected_at     DATETIME DEFAULT NOW(),
    INDEX idx_box (box_code),
    INDEX idx_agent (agent_id),
    INDEX idx_anomaly (is_anomaly)
)

-- AI智能库存管理表
box_ai_inventory (
    id              BIGINT PRIMARY KEY,
    agent_id        BIGINT,
    total_unopened  INT,                          -- 未开箱总数
    total_opened    INT,                           -- 已开箱总数
    total_pending   INT,                          -- 待激活瓶数
    total_activated INT,                          -- 已激活瓶数
    sales_forecast_30d INT,                       -- 30天销量预测
    sales_forecast_60d INT,                       -- 60天销量预测
    sales_forecast_90d INT,                       -- 90天销量预测
    safety_stock    INT,                          -- 安全库存
    turnover_rate   DECIMAL(5,2),                 -- 周转率
    replenish_suggested BOOLEAN DEFAULT FALSE,    -- 是否建议补货
    replenish_qty   INT,                          -- 建议补货量
    replenish_timing VARCHAR(100),                -- 建议补货时机
    low_stock_warning BOOLEAN DEFAULT FALSE,     -- 库存不足预警
    overstock_warning BOOLEAN DEFAULT FALSE,      -- 库存积压预警
    near_expiry_warning BOOLEAN DEFAULT FALSE,    -- 临期预警
    ai_automation_rate DECIMAL(5,2) DEFAULT 85.00,
    updated_at      DATETIME DEFAULT NOW(),
    INDEX idx_agent (agent_id)
)

-- AI防窜分析表
box_ai_anti_channel (
    id              BIGINT PRIMARY KEY,
    box_code        VARCHAR(32),
    agent_id        BIGINT,
    authorized_region VARCHAR(100),
    open_location   VARCHAR(200),                 -- 开箱位置
    activate_location VARCHAR(200),               -- 激活位置
    sale_location   VARCHAR(200),                 -- 销售地
    risk_score      DECIMAL(5,2),                 -- 风险评分（0-100）
    anomaly_type    VARCHAR(50),                  -- 异常类型
    warning_level   VARCHAR(20),                  -- 轻微/一般/严重/极重
    evidence_chain  TEXT,                        -- 证据链（JSON）
    penalty_suggested VARCHAR(500),               -- 处罚建议
    credit_adjusted DECIMAL(5,2),                 -- 信用分调整
    blockchain_hash VARCHAR(128),                 -- 区块链存证哈希
    ai_automation_rate DECIMAL(5,2) DEFAULT 90.00,
    detected_at     DATETIME DEFAULT NOW(),
    INDEX idx_box (box_code),
    INDEX idx_agent (agent_id),
    INDEX idx_risk (risk_score)
)

-- AI智能回收判定表
box_ai_recycling (
    id              BIGINT PRIMARY KEY,
    box_code        VARCHAR(32),
    agent_id        BIGINT,
    top_code_status VARCHAR(20),                  -- 箱顶码状态
    bottom_code_status VARCHAR(20),               -- 箱底码状态
    dual_code_valid BOOLEAN,                      -- 双码是否完好
    life_code_activated BOOLEAN,                  -- 生命码是否激活
    liquor_age_years DECIMAL(5,2),                -- 酒龄（年）
    recycling_eligible BOOLEAN DEFAULT FALSE,     -- 是否符合回收条件
    recycling_timing_predicted DATE,             -- 预测回收时机
    risk_assessment VARCHAR(200),                 -- 风险评估
    pricing_suggested DECIMAL(10,2),              -- 智能定价建议
    batch_price     DECIMAL(10,2),                -- 批次原价
    markup_rate     DECIMAL(5,2) DEFAULT 20.00,   -- 加价率
    blockchain_hash VARCHAR(128),                 -- 区块链存证哈希
    ai_automation_rate DECIMAL(5,2) DEFAULT 85.00,
    assessed_at     DATETIME DEFAULT NOW(),
    INDEX idx_box (box_code),
    INDEX idx_agent (agent_id),
    INDEX idx_eligible (recycling_eligible)
)

-- AI智能追溯存证表
box_ai_traceability (
    id              BIGINT PRIMARY KEY,
    box_code        VARCHAR(32),
    top_code        VARCHAR(32),
    bottom_code     VARCHAR(32),
    lifecycle_events TEXT,                       -- 全生命周期事件（JSON）
    anomaly_events  TEXT,                        -- 异常事件（JSON）
    trace_report_url VARCHAR(500),               -- 追溯报告URL
    evidence_chain  TEXT,                        -- 证据链
    blockchain_tx_hash VARCHAR(128),              -- 区块链交易哈希
    blockchain_block BIGINT,                     -- 区块高度
    blockchain_timestamp DATETIME,               -- 上链时间
    is_immutable    BOOLEAN DEFAULT TRUE,         -- 不可篡改
    ai_automation_rate DECIMAL(5,2) DEFAULT 95.00,
    created_at      DATETIME DEFAULT NOW(),
    INDEX idx_box (box_code),
    INDEX idx_top (top_code),
    INDEX idx_bottom (bottom_code),
    INDEX idx_blockchain (blockchain_tx_hash)
)
```

---

## 七、箱码与生命码协同（AI智能协同引擎）

### 7.0 AI智能协同引擎（新增）

> **核心理念：双码协同AI+回收智能判定+全链路AI追溯+区块链存证**
> 基于AI模型+区块链，实现箱码与生命码的智能协同与全链路追溯。

```
┌─ AI双码协同层 ───────────────────────────────────────┐
│  · 箱码-生命码智能绑定（出厂自动绑定6瓶生命码）       │
│  · 双码状态智能联动（箱码失效→瓶码独立运作）         │
│  · 双码回收智能判定（箱顶码+箱底码+生命码综合判定）   │
│  · 双码数据智能同步（状态变更实时同步）               │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI回收智能判定层 ───────────────────────────────────┐
│  · 箱级回收AI判定（双码完好→可参与箱级回收）          │
│  · 瓶级回收AI判定（生命码激活满3年→可参与瓶级回收）   │
│  · 回收时机AI预测（满3年库存提前预警+回收建议）       │
│  · 回收风险AI评估（虚报库存/重复回收/异常回收检测）   │
│  · 回收智能定价（批次+原价+加价20%自动计算）          │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─ AI全链路追溯层 ─────────────────────────────────────┐
│  · 全链路AI追溯（出厂→入库→出库→开箱→激活→回收）   │
│  · 异常AI检测（异常流转+异常扫码+异常激活检测）       │
│  · 追溯AI报告（一键生成追溯报告+证据链）             │
│  · 区块链AI存证（双码全生命周期上链+不可篡改）        │
└─────────────────────────────────────────────────────┘
```

**AI自动化率：85%**（仅重大回收/异常流转人工审核）

### 7.1 协同流程

```
厂家出厂
    ↓
赋箱码 + 赋瓶级生命码 + 绑定关联
    ↓
代理商入库（箱码有效）
    ↓
代理商开箱（箱码失效）
    ↓
┌─ 瓶级生命码独立运作 ──────────────────────┐
│                                          │
│  瓶1 → 终端用户购买 → 扫码激活生命码     │
│        → 激活日期记录 → 满3年可回收      │
│                                          │
│  瓶2 → 终端用户购买 → 扫码激活生命码     │
│        → 激活日期记录 → 满3年可回收      │
│                                          │
│  瓶3-6 → 待销售                          │
│                                          │
└──────────────────────────────────────────┘
```

### 7.2 回收判定规则

```
回收申请时系统自动判定:

┌─ 箱码层面（双码完好为必要条件）──────────┐
│  箱顶码状态:                              │
│  ├─ 未开箱（完好）→ ✅ 满足条件1          │
│  └─ 已开箱（失效）→ ❌ 不满足条件1       │
│                                          │
│  箱底码状态:                              │
│  ├─ 完好（有效）→ ✅ 满足条件2          │
│  └─ 损坏（失效）→ ❌ 不满足条件2       │
│                                          │
│  双码完好 → ✅ 可参与3年增值回收        │
│  任一码失效 → ❌ 不参与回收             │
└──────────────────────────────────────────┘

┌─ 瓶级生命码层面 ──────────────────────────┐
│  生命码状态:                              │
│  ├─ 未激活 → ❌ 不参与回收                │
│  ├─ 已激活 → ✅ 参与回收（独立计算）      │
│  │   · 酒龄 = 当前日期 - 第一次激活日期   │
│  │   · 满3年 → 增值15%起                 │
│  │   · 折现 = 老酒价值 × 80%             │
│  └─ 已回收 → 已终止                      │
└──────────────────────────────────────────┘

结论:
  箱码失效不影响瓶级生命码的回收资格
  回收以瓶级生命码为唯一凭证
```

---

## 八、各平台对比（AI智能升级版）

### 8.1 基础能力对比

| 对比项 | 茅台箱码 | 京东溯源 | 阿里码上 | 蚂蚁链 | **竹奕箱码（v2.0）** |
|------|:---:|:---:|:---:|:---:|:---:|
| 一箱一码 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 代理专属 | ✅ | ⚠️ | ⚠️ | ⚠️ | ✅ |
| 双码体系（顶+底） | ❌ | ❌ | ❌ | ❌ | ✅ |
| 开箱失效 | ✅ | ❌ | ❌ | ⚠️ | ✅ |
| 防拆设计 | ✅ | ❌ | ❌ | ❌ | ✅ |
| 防窜货 | ✅ | ⚠️ | ⚠️ | ⚠️ | ✅ |
| 库存管理 | ✅ | ✅ | ⚠️ | ❌ | ✅ |
| 瓶码关联 | ✅ | ⚠️ | ⚠️ | ⚠️ | ✅ |
| 回收判定 | ❌ | ❌ | ❌ | ❌ | ✅ |
| 区块链存证 | ⚠️ | ⚠️ | ⚠️ | ✅ | ✅ |

### 8.2 AI智能能力对比（v2.0新增）

| AI能力 | 茅台箱码 | 京东溯源 | 阿里码上 | 蚂蚁链 | 高德地图 | **竹奕箱码** |
|------|:---:|:---:|:---:|:---:|:---:|:---:|
| AI智能码生成 | ❌ | ❌ | ⚠️ | ❌ | ❌ | ✅(95%) |
| AI智能开箱检测 | ❌ | ⚠️ | ❌ | ❌ | ❌ | ✅(90%) |
| AI智能防窜 | ⚠️ | ❌ | ❌ | ❌ | ⚠️ | ✅(90%) |
| AI智能库存 | ❌ | ✅ | ❌ | ❌ | ❌ | ✅(85%) |
| AI智能回收判定 | ❌ | ❌ | ❌ | ❌ | ❌ | ✅(85%) |
| AI智能追溯 | ⚠️ | ✅ | ⚠️ | ✅ | ❌ | ✅(95%) |
| AI多模态识别（扫码+CV+传感器） | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| AI位置智能分析 | ⚠️ | ❌ | ❌ | ❌ | ✅ | ✅ |
| AI异常流转检测 | ⚠️ | ❌ | ❌ | ⚠️ | ❌ | ✅ |
| AI窜货预警+取证 | ⚠️ | ❌ | ❌ | ⚠️ | ❌ | ✅ |
| AI库存预测+补货建议 | ❌ | ✅ | ❌ | ❌ | ❌ | ✅ |
| AI回收时机预测 | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| AI追溯报告一键生成 | ❌ | ⚠️ | ❌ | ⚠️ | ❌ | ✅ |
| 区块链全生命周期存证 | ❌ | ⚠️ | ⚠️ | ✅ | ❌ | ✅ |

### 8.3 参考平台与借鉴能力

| 参考平台 | 借鉴能力 | 应用章节 |
|------|------|------|
| 茅台箱码防窜 | 双码防伪、开箱失效、代理专属、防窜货 | 二、三、五 |
| 京东品质溯源 | 库存管理、AI智能盘点、AI库存预测 | 四 |
| 阿里码上溯源 | 码生成、瓶码关联、追溯 | 二、七 |
| 蚂蚁链溯源 | 区块链存证、不可篡改、全链路追溯 | 二、三、五、七 |
| 阿里风控 | 异常流转检测、风险评分、分级处罚 | 五 |
| 高德地图 | GPS定位、LBS定位、区域智能解析 | 三、四、五 |

---

## 九、开发优先级（AI智能升级版）

### 第一期（MVP·核心功能+AI码生成）

| 功能 | 优先级 | 说明 | AI自动化率 |
|------|:---:|------|:---:|
| AI智能码生成 | P0 | 防伪算法+批量生成+唯一性校验 | 95% |
| 代理智能绑定 | P0 | 出厂赋码自动绑定代理商+区域 | 95% |
| 瓶码智能关联 | P0 | 箱码-生命码自动绑定6瓶 | 95% |
| 入库管理 | P0 | 箱底码扫码入库 | 90% |
| AI智能开箱检测 | P0 | 扫码+CV+传感器多模态识别 | 90% |
| 箱顶码失效 | P0 | 状态不可逆变更+区块链存证 | 95% |
| 双码回收判定 | P0 | 箱顶码+箱底码完好为必要条件 | 85% |
| 区块链AI存证 | P0 | 码生成/开箱/回收记录上链 | 95% |

### 第二期（AI防窜+智能库存）

| 功能 | 优先级 | 说明 | AI自动化率 |
|------|:---:|------|:---:|
| 区域智能绑定 | P1 | 代理授权区域智能匹配 | 90% |
| AI位置智能分析 | P1 | GPS+LBS+高德地图智能定位 | 90% |
| AI异常流转检测 | P1 | 批量跨区+异常扫码+疑似窜货检测 | 90% |
| AI窜货预警+取证 | P1 | 实时预警+分级处罚+证据链+区块链存证 | 90% |
| AI智能盘点 | P1 | 库存自动盘点+差异智能识别 | 85% |
| AI库存预测 | P1 | 30/60/90天销量+库存需求预测 | 85% |
| AI补货建议 | P1 | 补货时机+补货量+批次建议 | 85% |
| AI库存预警 | P1 | 不足/积压/临期/异常库存预警 | 85% |

### 第三期（AI协同+追溯+高级）

| 功能 | 优先级 | 说明 | AI自动化率 |
|------|:---:|------|:---:|
| AI双码协同 | P2 | 双码状态智能联动+数据同步 | 85% |
| AI回收时机预测 | P2 | 满3年库存提前预警+回收建议 | 85% |
| AI回收风险评估 | P2 | 虚报库存/重复回收/异常回收检测 | 85% |
| AI回收智能定价 | P2 | 批次+原价+加价20%自动计算 | 85% |
| AI全链路追溯 | P2 | 出厂→入库→出库→开箱→激活→回收 | 95% |
| AI追溯报告生成 | P2 | 一键生成追溯报告+证据链 | 95% |
| AI异常检测 | P2 | 异常流转+异常扫码+异常激活 | 90% |
| NFC箱码 | P2 | NFC碰一碰防伪升级 | 80% |
| 批量智能激活 | P2 | 箱码批量激活瓶码 | 90% |
| AI模型自学习优化 | P2 | 基于历史数据持续优化AI模型 | 80% |

---

## 十、六大AI核心能力总览（v2.0）

| 序号 | AI能力 | 核心功能 | AI自动化率 | 参考平台 | 应用章节 |
|:---:|------|------|:---:|------|:---:|
| 1 | AI智能码生成 | 防伪算法+批量生成+唯一性校验+智能绑定 | 95% | 茅台/阿里码上/蚂蚁链 | 二 |
| 2 | AI智能开箱检测 | 多模态识别+状态变更+位置记录+异常检测 | 90% | 茅台/京东 | 三 |
| 3 | AI智能防窜 | 位置分析+异常流转+窜货预警+取证+处罚 | 90% | 茅台/阿里风控/高德 | 五 |
| 4 | AI智能库存 | 智能盘点+库存预测+补货建议+库存预警 | 85% | 京东/SAP/用友 | 四 |
| 5 | AI智能回收判定 | 双码判定+时机预测+风险评估+智能定价 | 85% | 竹奕自研 | 七 |
| 6 | AI智能追溯 | 全链路追溯+异常检测+报告生成+区块链存证 | 95% | 京东/蚂蚁链 | 七 |

**整体AI自动化率：85%**（人工辅助15%，仅重大决策/异常复核人工介入）

---

**文档结束**

> 本代理商箱码管理模块（v2.0）创新设计"箱码双码"概念，升级为AI智能模块，核心理念为"**双码全链路追溯 + AI智能码生成 + AI智能防窜 + AI智能回收判定 + AI智能库存 + 区块链存证**"，与"生命码"形成互补：箱码管箱（代理商库存+防窜+双码回收条件），生命码管瓶（终端用户+增值回收）。
>
> **核心创新（v2.0升级）**：
> 1. **六大AI核心能力**：AI智能码生成（95%）、AI智能开箱检测（90%）、AI智能防窜（90%）、AI智能库存（85%）、AI智能回收判定（85%）、AI智能追溯（95%），整体AI自动化率85%；
> 2. **双码体系（TBC+BBC）**：箱顶码开箱失效+箱底码永久有效，双码完好为老酒回收必要条件；
> 3. **AI多模态识别**：扫码+CV视觉+物理传感器综合判断开箱动作，准确率提升至90%；
> 4. **AI智能防窜**：GPS+LBS+高德地图智能定位+异常流转AI检测+窜货AI预警+分级处罚+区块链存证；
> 5. **AI智能库存**：智能盘点+30/60/90天销量预测+补货建议+不足/积压/临期/异常库存预警；
> 6. **AI智能回收判定**：双码完好AI智能判定+回收时机AI预测+回收风险AI评估+回收智能定价；
> 7. **AI智能追溯**：全链路AI追溯（出厂→入库→出库→开箱→激活→回收）+异常AI检测+追溯AI报告+区块链全生命周期存证；
> 8. **6张AI相关数据库表**：box_ai_generations/box_ai_unboxing/box_ai_inventory/box_ai_anti_channel/box_ai_recycling/box_ai_traceability。
>
> **核心规则（不变）**：① 每个代理商具有专属箱顶码+箱底码双码（AI智能生成）；② 箱顶码开箱即失效（物理防拆+数字失效+AI智能检测）；③ **双码完好为老酒回收必要条件**——箱顶码+箱底码双码完好方可参与3年增值回收（AI智能判定），任一码失效则不参与回收（防止代理商箱级套利）。箱内瓶级生命码独立激活后仍可参与回收，保障终端用户权益。箱码双码+生命码双层管理体系+AI智能+区块链存证，实现从出厂→代理→开箱→终端→增值→回收的全链路AI智能闭环。
